---
name: sess16-REFUTES-release-drain-gap-blocks-durable-at-release
description: sess16 REFUTES the release-drain-gap (GPT demote-drain) hypothesis: P16-RELEASE-UNDESTAGED shows EVERY dir block durable at EX release (undest=0, in_…
metadata:
  type: project
---

## sess16 IMPORTANT NEGATIVE (build 895603D7, P16-RELEASE-UNDESTAGED + P-EVICT-SKIP with lseq/wseq). crash_consistency probe failed iter14, 3 entries lost.

## REFUTED: the release-drain-gap / GPT-demote-drain-is-primary hypothesis [[sess16-gpt-architecture-demote-drain-by-lock-ownership]] [[sess16-HEAD-status]]. P16-RELEASE-UNDESTAGED (dumps EVERY dir data-fork buffer at the dir EX release fence, just before unlock) shows on BOTH nodes for ino 131: EVERY block undest=0, in_ail=0, lseq==wseq (e.g. daddr=472 lseq=467 wseq=467 undest=0; daddr=1888 lseq=223 wseq=223; daddr=2093320 lseq=191 wseq=191). => the sess97 release fence DOES drain+write all dir data/leaf blocks durably before handoff. The on-disk dir is consistent at release. So the durable loss is NOT caused by releasing an un-drained/in-AIL-undestaged block.

## WHERE wseq=0 ACTUALLY APPEARS: only MID-TENURE at the modify-path evict (P-EVICT-SKIP, mxfs_dir_evict_data_blocks): daddr=472 in_ail=1 pin=0 buf_gen=0/6 lseq=8..78 wseq=0 undest=1 li_empty=1. wseq=0 = the buffer was never write-submitted YET (xfsaild lazy) — normal mid-tenure for freshly-committed blocks. buf_gen=6 (low) vs cur_gen (higher) suggests a low-tenure/early state. This block is the acquirer's OWN current-incarnation committed-unwritten work (daddr 472 is a fresh buffer for the reused ino's new incarnation — wseq resets to 0 on buffer realloc). The evict-skip KEEPS it (correct: don't discard own committed work). li_empty=1 (BLI off the buffer's b_li_list but bip set + in_ail) recurs in all these — a transitional/lingering-BLI state.

## SO THE LOSS IS: a MID-TENURE concurrent-create issue, NOT a release-handoff durability gap. Candidates for next session (RULE 4, the release path is now EXONERATED): (1) the evict-skip keeps a block that is the acquirer's own work BUT the acquirer's view is missing the peer's entries because the acquirer's first read of the block (when it acquired EX) was served STALE (the v0.4.7 read-invalidation / FUA-reread didn't fetch the peer's just-released durable block) — i.e. the ACQUIRE-side reread is the gap, not release. Check: at EX acquire, does the acquirer FUA-reread daddr 472 from the (durable, per above) LUN, or serve a stale cached buffer? (2) li_empty=1 + in_ail + bip-set transitional BLI state — is the block's BLI being mis-tracked so a stale image is flushed? (3) the lingering low-buf_gen (6) reused-daddr buffer ([[sess15-PIVOTAL-loss-requires-inode-daddr-reuse]] original ABA-daddr theory). 

## KEY PIVOT: release is durable+consistent (proven). Focus the next session on the ACQUIRE-side reread coherency: the acquirer must, on getting dir EX, re-read each dir block FRESH from the (durable) LUN before RMW. The read-hook invalidation (xfs_da_btree.c) is gated `!owned_ex` so it's SKIPPED during the owned_ex RMW; the owned_ex path relies on mxfs_dir_evict_data_blocks which SKIPS in-AIL-undestaged (the acquirer's own fresh block) — but if that "own fresh block" was BUILT from a stale base at acquire (first read served stale), it's missing the peer's entries from the start. INSTRUMENT: at the acquirer's FIRST read of daddr 472 in a tenure, FUA-compare in-core vs LUN (the LUN is durable per P16-RELEASE). If they differ, the acquire-reread is the gap. Cluster on 895603D7 (15/16 baseline + diagnostics). Repro tests/cc_blockdir_probe.sh (ino131, foreground timeout 280, <15 iter). [[sess16-DECISIVE-gen-differs-by-one-reuse-aba]]
