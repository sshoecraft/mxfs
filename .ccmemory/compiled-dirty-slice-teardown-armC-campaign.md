---
name: compiled-dirty-slice-teardown-armC-campaign
description: Compiled sess179-194: D-SHUTDOWN-UMOUNT-CLEAN-RELEASE-DIRTY-SLICE full arc — 3 GPT rulings, snlocal/kind-17, admission barrier, Arm C late release, c…
metadata:
  type: project
tags: [compiled, dirty-slice, recovery, kind-17, admission-barrier, arm-c]
---

# Dirty-slice teardown & claim-recovery campaign (sess179–194, 0.11.464→467)

Central topic: D-SHUTDOWN-UMOUNT-CLEAN-RELEASE-DIRTY-SLICE (critical) — umount of a forced-shutdown fs released the heartbeat slot as "clean teardown" over a DIRTY journal slice; remount fresh-claimed (ADOPTED) the sector, skipped the untagged images, and lost fsync'd data. Discovered by the vergate mixed_build durability leg ([[ccloop-c7ee71c6-sess179-B1-LANDED-464-board-PASS-new-defect-dirty-slice-release]], which also landed #14 B1: m_mxfs_proto_admitted asserted before any recovery). CLOSED FIXED+VERIFIED on 0.11.465 ([[ccloop-c7ee71c6-sess191-dirty-slice-CLOSED-465-board-27of27]]); Arm C hardening verified through 0.11.467 ([[ccloop-c7ee71c6-sess193-armC-VERIFIED-467-dmesg-late-visibility-trap]], [[ccloop-c7ee71c6-sess194-armC-COMPLETE-board-27of27-467-P277-unreachable-via-withdraw]]).

## The three arms (sess180 measurement) and governing ruling
[[ccloop-c7ee71c6-sess180-GPT-ruling-dirty-slice-teardown-and-claim-recovery]]:
- Arm A (race): GOINGDOWN queues withdraw work; immediate umount cancel_work_sync'd it before it ran → withdrawn=false → clean release of dirty slice. Fix: depart_clean must include !xfs_is_shutdown + log-clean; put_super synchronously persists WITHDRAWN before DLM teardown; stamp failure fails closed (retain ACTIVE).
- Arm B (claim): disklock treated any non-ACTIVE non-GUARD sector as claimable → WITHDRAWN slice consumed dirty. Ruling: state machine ACTIVE → WITHDRAWN → RECOVERY_GUARD → durably-complete → CONSUMABLE; claimers must REFUSE WITHDRAWN (P274-CLAIM-WITHDRAWN-SKIP); claim-triggered recovery reuses the SAME survivor engine (CAS WITHDRAWN→GUARD, fence, replay, durable completion, zero, separate ordinary claim). GUARD is the mutex between survivor and next-mounter. PR fence + completion is NOT lineage authority for untagged images (inherits #1 status).
- Arm C (ordering): v5_shutdown zeroed the slot BEFORE xfs_unmountfs wrote the unmount record. Ruling: zero only after the unmount record + prior writes are durable (storage ordering); CONSUMABLE = "recovery completed durably OR clean unmount completed durably".
- single-node/no-PR recovery allowed only as HARD exclusivity with an explicit operator flag, never inferred from "PR unsupported". GOINGDOWN NOLOGFLUSH is crash-equivalent for slot disposition.

## Single-node untagged replay: kind-17 + snlocal (Fix 3 chain)
- Fix 3c ([[ccloop-c7ee71c6-sess183-fix3c-LANDED-cert-mints-two-blockers-untagged-and-complete-bail]]): MXFS_FENCE_KIND_SINGLE_NODE_EXCLUSIVE=17 (proves exclusion), minted when single_node_exclusive param + single-node membership and no contradicting fence evidence (P238-FENCE-SINGLENODE). Exposed: single-node mounts emit NO authority trailers (wants_authority false, DLM early-outs) → slices structurally 100% untagged; plus a put_super UAF/lost-publication (P163-COMPLETE-BAIL ctx=0: v5_shutdown freed ctx before cancel_work_sync(foreign_replay_work)).
- [[ccloop-c7ee71c6-sess184-GPT-ruling-snlocal-write-time-provenance]]: OPTION C — kind-17 alone never unlocks P227; synthetic trailers refused. Untagged replay permitted only for an incarnation durably classified SINGLE_NODE_LOCAL_LOG BEFORE writing (MXFS_HB_FEAT_SNLOCAL at claim) under hard exclusivity. Two independent predicates: takeover_is_exclusive_now (kind-17) vs log_was_single_node_authorized_when_written (marker). Separate P227-SNLOCAL-ACCEPT counter; no dirty mode transition; marker rides proto-gen for mixed-version fail-closed.
- Marker landed sess187 ([[ccloop-c7ee71c6-sess187-snlocal-marker-LANDED-P239-recheck-is-new-blocker]]): hb_feature_fill stamps SNLOCAL, VICTIM_SNLOCAL desc flag at both creators, mxfs_v5_dlm_victim_untagged_authority, P227-SNLOCAL-DIVERGE/-TORN. New blocker: v5_exclusion_recheck called PR-based exclusion_holds unconditionally → P239-EXCL-LAPSED on kind-17.
- Kind-17 recheck leg ([[ccloop-c7ee71c6-sess188-GPT-ruling-kind17-exclusion-recheck]] → [[ccloop-c7ee71c6-sess188-kind17-recheck-LANDED-cohort-gap-is-new-blocker]]): validate ONE strict descriptor image against cached recov_auth tuple BEFORE branching on fence_kind (out_fence_kind set only on rc==0); kind-17 "holds now" = param asserted AND our slot is the sole live slot (identity, not count; lowest_live_slot(skip=local)<0); refusals P239-AUTH-STALE / P239-SN-LAPSED; membership growth mid-recovery = terminal fail-closed, never resumable in-place; fence_kind immutable per recovery generation. Outstanding from that ruling: join/admission interlock audit (req 4) — a joining node must be unable to issue FS I/O while a claimed kind-17 recovery descriptor exists.

## Mount admission barrier (blocker 2a → closure)
[[ccloop-c7ee71c6-sess189-GPT-ruling-mount-admission-barrier]]: barrier cohort snapshotted frozen-ACTIVE peers only; monitor consumed the WITHDRAWN stamp (fence cert overwrites the hb sector) before step 6.5 scanned → cohort=0 → loop never ran → async replay after mount live → stale in-core root hid the replayed marker. Ruling: the barrier is an ADMISSION barrier — drain mphase at top of every round including round 0; sweep durable on-disk recovery-pending descriptors (in-memory pending is insufficient — fence_intent atomically replaces WITHDRAWN with a stage>=FENCING descriptor in one CAS, so "dirty slice is WITHDRAWN xor descriptor" at every instant); a mount may not go live while any applicable pending descriptor remains — bounded wait then FAIL -EBUSY. Landed as get_recovery_pending_slots + mount_pending_recovery + the for(;;) barrier with 30s admission wait ([[ccloop-c7ee71c6-sess190-admission-barrier-LANDED-0.11.465-unverified]]). Behavior change: unfenceable descriptor-bearing residue now fails the mount (~47s observed, "already mounted or mount point busy" signature).

## Arm C: deferred HB-slot release (0.11.466/467)
[[ccloop-c7ee71c6-sess192-armC-late-release-LANDED-466-first-verify-FAILED]]: struct mxfs_v5_dlm_slot_release; shutdown hands the stopped disklock + dev-clone ownership out when depart_clean && late (v5_mount.c:4543); put_super/fill_super commit after xfs_unmountfs with unmount_clean=!xfs_is_shutdown(mp); unmount-record iclog is PREFLUSH|FUA and record I/O error forces shutdown, so the gate is sound. disklock BORROWS ctx->dev — ownership must travel. First verify "failed" — [[ccloop-c7ee71c6-sess193-armC-VERIFIED-467-dmesg-late-visibility-trap]] proved it a FALSE NEGATIVE: umount-task final printks become visible to dmesg readers LATE (minutes), with original timestamps. RULE: never judge teardown logging from one immediate dmesg read. ftrace func_stack_trace proved release_slot ← slot_release_commit ← xfs_fs_put_super; P278 rc=0 each clean cycle.
[[ccloop-c7ee71c6-sess194-armC-COMPLETE-board-27of27-467-P277-unreachable-via-withdraw]]: dirty umount (GOINGDOWN before umount) takes the WITHDRAW path (depart_clean=false → no handout → "P278 no deferred slot (disklock=0)", slot retained WITHDRAWN, fence+replay, marker survives). P277-SLOT-RETAINED-UNMOUNT-DIRTY covers only shutdown INSIDE xfs_unmountfs after a clean handout; its action is omitting release_slot. 467 fleet-wide: full board 27/27 + mixed_build PASS.

## Recurring traps (paid in blood)
- `make clean` deletes tools/* binaries; without `make tools` the repro's mkfs fails and (pre-guard) GOINGDOWN through an unmounted mountpoint shut down the node's ROOT fs (sess187; script now aborts on mount_rc!=0).
- dmesg ring keeps prior runs' tags; sed anchored on the FIRST match counted 18 phantom recovery lines — all harness tags must be unique per run ($RUN suffix) and marker checks content-based (sess191).
- Loop-device mounts: no PR, single_node, writes untagged; single_node_exclusive=1 needed for the whole arm, reset after.
- Manual caw mounts use /dev/mapper/mpatha, never /dev/sda (multipathd holds sda).
- 467 joins a 466 cluster fine — srcversion mix is admitted; only proto-gen is gated.

## Still open downstream
Join/admission interlock audit (sess188 req 4); kind-17/PR lapse blocked-state is in-memory only (pre-existing); #14 B2/B3/B4; whole-cluster multi-dirty-slice restart ordering unproven until lineage gen4 (#1).
