You are running inside ccloop, a relay-loop wrapper that hands work between
fresh Claude Code sessions as context fills. This is session 207 of the
current run.

IMPORTANT — this run has explicit success criteria:

<BEGIN CRITERIA>
is it production ready?
<END CRITERIA>

These criteria are your ONLY exit condition.

YOU HAVE UNLIMITED CONTEXT AND UNLIMITED SESSIONS.

ccloop is wrapping your session. When your context fills, ccloop will:
  1. summarize this session's transcript into a resume file
  2. spawn a fresh Claude Code session with that summary as its prompt
  3. pass through the same criteria so the new session continues the work
The new session inherits the project state, the criteria, and a digest
of what you've already learned and tried. You can take as many sessions
as the problem needs. There is no session cap, no turn cap, no time cap.

Because of this, NEVER:

- propose to "kill the wrapper", "end the session", or "exit so a fresh
  session can take over". The wrapper handles relay automatically.
- ask the user "what should I do?" or offer them options. This is
  autonomous — there is no human in the loop.
- claim the work "requires a fresh session", "needs multi-day effort
  outside this session's scope", or "is architectural and out of scope".
  Session scope is irrelevant; the work is in scope by definition.
- write YES dishonestly to escape the loop. The criteria are checked;
  lying wastes downstream work.

The Stop hook is active. Every time you try to end a turn it asks:
HAVE YOU MET THE CRITERIA? If YES, write YES to the marker:

    echo YES > "/src/mxfs/.ccloop/runs/c7ee71c6-d6a4-4316-9b43-cb64295f419d/criteria-met"

Only on cited, third-party-verifiable evidence that EVERY criterion is
met.

If NO, keep working. Pick a new angle: read more of the code, write a
targeted diagnostic, generate a minimal reproducer, consult any external
analysis tools available to you, form a new hypothesis and test it.
Then return to the criteria.

---

## Maintain your handoff file

    /src/mxfs/.ccloop/handoff.md

Keep it current AS YOU WORK — not at the end. A session that fills its context
or dies mid-tool never gets the chance to write a parting summary, and ccloop
can only hand on what is already on disk when the session stops.

Rewrite the whole file (don't append) whenever your understanding changes:
what you are doing, what you have established, what you tried that did NOT
work, and the single next concrete step. Keep it under ~100 lines — this is a
handoff, not a log.

ccloop freshness-checks it. If the file was not written during your session,
the next session is told plainly that it is stale and falls back to scraping
your transcript. An unmaintained file costs the handoff; it does not silently
pass off old intent as current.

---

# Resume — run c7ee71c6-d6a4-4316-9b43-cb64295f419d, after session 206

## Original task

Continue the MXFS production-readiness campaign.

Current project state — build identity, board tally, and the RULE 6 open
defects in severity order with each one's next step — is GENERATED FRESH
into the "## Current project state" section at the END of this prompt.
Start there, not with discovery. It is computed seconds before you read
it and supersedes anything earlier in the prompt that conflicts.

Work the open defects in the order that section lists them unless you
state why not.

Do NOT read or maintain state.md. It is a deprecated artifact of the
pre-ccloop handoff system (user directive, sess35; ccmemory
state-md-deprecated-do-not-maintain) and it is stale.


## Previous session

- session-id: `fca9a6fc-39ce-427d-9e7f-2b3e420777b4`
- transcript: `/home/steve/.claude/projects/-src-mxfs/fca9a6fc-39ce-427d-9e7f-2b3e420777b4.jsonl`
- approx context at last assistant turn: 154967 tokens
- tools used: Bash×14 Read×14 Write×2 ToolSearch×1 mcp__ccmemory__memory_list×1 mcp__ccmemory__memory_write×1

## Files written or edited in the previous session

- /src/mxfs/.ccloop/handoff.md

## Handoff from the previous session

Written by the previous session itself, during that session, at `/src/mxfs/.ccloop/handoff.md`. This is that session's own account of where it got to — prefer it over the scraped sections above where they conflict.

# ccloop handoff — sess206

## Active defect
#18 D-RSYNC-OVERWRITE-LAP-USERSPACE-FAIL — EIO source is
P95-OPEN-PROTECT-FAIL (xfs_mxfs_dlm.c:36884, fail-closed -EIO in
mxfs_dlm_open_protect when i_dlm_mode==NL after the ilock ride).
Fleet sweep DONE this session: exactly ONE firing across all 32
kernlogs in /tmp/run_rsync_paired_20260810T135045Z (test19 13:52:49).
NL-setter still unidentified → instrument next (design ready, below).

## Where the code is
- mxfs_dlm_open_protect: xfs/xfs_mxfs_dlm.c:36801-36889 (C3 hook;
  fast path 36810-36838, ilock ride 36839, CLUSTER-CONVERT 36847-60,
  NL re-read 36861-63, -ESTALE arm 36874, -EIO arm 36882-87).
- mxfs_dlm_ilock_begin: 26819+. Exit paths audited THIS session:
  cannot return with mode==NL for the rsync open path EXCEPT via
  paths ruled out below. Slow path publishes mode at 29082 on rc==0;
  EDEADLK restarts (28917-29007); rc!=0 → force-shutdown (29048) or
  bounded-tries exit (29030, needs i_dlm_tries_owner==current — not
  the open path).
- mxfs_dlm_inode_lock_routed: 26703. IMPORTANT: conv_pi drop of the
  old local grant happens ONLY on rc==0 (26771) — a FAILED
  CLUSTER-CONVERT does NOT leave NL (sess205 hypothesis (a) variant
  REFUTED by code).
- iclus sweep/admit: covered_active 44775, fan_out 44833,
  open_admit 44869 (!busy && disk_mode!=NL), publish_open_bits 44911.
- Transition ring: mxfs_dlmtr_rec 1236 — EVERY i_dlm_mode/state
  assignment in the file transits it WITH __LINE__ (sess98 audit at
  line 1213; re-verified by grep this session: all 26 assignment
  sites in xfs_mxfs_dlm.c only, each paired with dlmtr_rec). But ring
  records only when ip->i_ino==mxfs_watch_ino — useless for an
  unpredictable ino.

## Ruled out (this session, by code read)
- mxfs_ilock_admit_ioend early-returns (28422/28572): require
  xfs_task_in_ioend()/in_writepages() — rsync open is neither.
- CLUSTER-CONVERT drop-then-fail: drop only on success (26771).
- Bounded-tries exit: needs lock_two_inodes context.
- Shutdown-fence/single-node/i_ino==0: FS was up, 32-node.
Remaining live hypotheses: (b) ilock_begin fast-path served a cached
mode that an in-flight iclus release sweep then revoked (mode→NL
despite our pr_holder?), or an unmapped NL-setter. The ring line
number will name it — instrument, don't guess further (RULE 4).

## Next (literal) — instrumentation build 0.11.473
1. xfs_inode.h: add u32 i_dlm_nl_line; pid_t i_dlm_nl_pid;
   u64 i_dlm_nl_ns; char i_dlm_nl_comm[16]; u8 i_dlm_nl_om (mode
   before NL).
2. mxfs_dlmtr_rec (xfs_mxfs_dlm.c:1236): BEFORE the watch_ino gate:
   if (ip->i_dlm_mode==MXFS_LOCK_NL && om!=MXFS_LOCK_NL) stamp the
   5 fields (line,pid,comm,ns,om).
3. P95-OPEN-PROTECT-FAIL arm: snapshot under i_dlm_lock at the
   36861-63 read: mode,state,ex/pr_holders,acq_inflight,
   routed_iclus,unpublished,stale/stale_src,i_mode(octal),i_gen,
   open_n,self_created,reused_create + nl_line/nl_pid/nl_comm/
   nl_age_us; print all in the fail message.
4. VERSION → 0.11.473, make clean (multi-file .h change!) + modules,
   deploy 32/caw, run rsync_paired laps until P95 fires again; the
   nl_line names the setter → then RULE-5 consult on the fix
   (retry-acquire vs -ESTALE vs current -EIO semantics).
5. Then: crash_consistency 90s NO_TERMINAL_RECORD standalone re-run
   with post-mortem; board chunks 3-5 (sess201 chunk cmds in sess204
   handoff); #17 LIVESKEW soak needs AGED fs.

## Traps
- ssh: tools/mxfs_sshpass.sh testN "cmd" (no root@). grep 'P53-' not
  'P53'. run.sh copies fail logs to /tmp/run_<name>_<RUNID>.
- P95 print is AFTER xfs_iunlock — snapshot state at the read, not
  in the print.
- Build 0.11.472 sv 9EF804271D01AA283D14131 currently on fleet.
- EDEADLK rc=-35 kernlog lines are benign adopt traffic — not #18.

## Continue

Continue the original task from where the previous session stopped. The
previous session's transcript is at the path noted above — you may Read
it if you need full detail on what was done. (Loop mechanics and how to
signal DONE are in the wrapper preamble above this summary.)

---

## Current project state

Generated at the start of THIS session by the project's state hook:
`/src/mxfs/.ccloop/state.sh`

Everything above describes what PREVIOUS sessions did. This section
describes the project as it is RIGHT NOW, and supersedes anything above
that conflicts with it.

### Build
tree VERSION **0.11.472**  |  mxfs.ko srcversion `9EF804271D01AA283D14131`  |  last rig run: 32 nodes / caw @ 2026-08-10T13:55:52Z

The srcversion above is what THIS TREE builds, not necessarily what the
fleet is running. Confirm deployment before trusting a measurement.

### Board — last conditions (32/caw)
25 PASS, 3 not-passing, 28 applicable
not passing:
  - rsync_paired [FAIL] nodes_pass=31/32 states:FAIL=1 hostload=24.10 first_fail[test19:checks=6 passed=3 failed=3] rank1[checks=6 pas
  - crash_consistency [FAIL] nodes_pass=0/32 states:NO_TERMINAL_RECORD=32 hostload=15.70
  - open_defects [FAIL] open=27 of=71 ids=[D-FOREIGN-REPLAY-UNGATED-IMAGES(critical),D-MATRIX-UNMEASURED(minor),D-32NODE-SHARED-DIR-CR

`open_defects` is a POLICY cell: red BY DESIGN under RULE 6 while the
ledger below is non-empty. It is not a regression and not a test fault.
`NO_TERMINAL_RECORD` means the harness captured no verdict — that is a
capture failure, NOT a filesystem fault. Do not diagnose the FS from it.

### RULE 6 ledger — open defects, severity order
29 open of 73 ledgered — 18 critical. Work these in order unless you state why not.

1. [critical] D-FOREIGN-REPLAY-UNGATED-IMAGES
   next: PLAN OF RECORD (sess175 RULE-5 ruling, ccmemory ccloop-c7ee71c6-sess175-GPT-ruling-enforcement-gate-design-and-sample3 and ccloop-c7ee71c6-sess175-GPT-ruling-lineage-implementation-shape; fu
2. [critical] D-INODE-CLUSTER-PUBLISH-WITHOUT-AUTHORITY
   next: 1) DONE sess199/sess201: 0.11.469 knob=1 FULL board 27/27 PASS and knob=0 regression board 27/27 PASS, both 32/caw on 2026-08-10 (criteria.json 11:31-11:45Z run); sess201 re-ran the full kno
3. [critical] D-CROSSNODE-OPEN-UNLINK-DATA-LOSS
4. [critical] D-RSYNC-RENAME-DIRTY-CANCEL-MASS-SHUTDOWN-361
   next: 1) Ship containment+telemetry build: full-target pre-dirty revalidate (-ESTALE restart / src -ENOENT), P217-RENAME-DIRTYCANCEL helper-failure probe at out_trans_cancel (error, XFS_TRANS_DIRT
5. [critical] D-MOUNT-WINDOW-PEER-DEATH-IMMEDIATE-PURGE
   next: 1) Finish the sess57 GPT review set that shares this code (items 3, 4, 6C), then a single RULE-5 consult on the whole barrier before any rig cycle. 2) Verification run: 32/caw, mount node A 
6. [critical] D-FOREIGN-REPLAY-FAILURE-PUBLISHED-AS-RECOVERED
   next: 1) Fault-inject: gate a forced failure of mxfs_xlog_recover_foreign_slice behind a module knob (default off), kill a node at 32/caw, and assert the survivor logs the new refusal, leaves the 
7. [critical] D-FOREIGN-SLICE-INTENTS-ABANDONED
   next: 1) DONE (sess164 audit): the versioned GUARD/recovery descriptor + victim-manifest freeze is BUILT AND LIVE — recovery_complete stage machine in dlm/v5_mount.c (~2897-3180), freeze gate in d
8. [critical] D-PR-FENCE-PREEMPT-WITHOUT-ABORT
   next: 1) DONE (sess71). 2) DONE (sess73, rig). 3) DONE (sess93): fence_during_write and crash_consistency re-boarded on 0.11.422 with the certificate live and CONSUMED — both PASS 32/32. 4) OUTSTA
9. [critical] D-PURGE-NONATOMIC-PUBLICATION
   next: 1) Build a test that forces two survivors to purge the SAME victim concurrently and asserts no live slot is zeroed — the CAS MISCOMPARE probes (P235-PURGE-CONTENDED / -HB) are the observable
10. [critical] D-OWN-CRASH-RECLAIM-PATH-UNREACHABLE
   next: 1) RULE-5 consult: is a stable per-node identity the right fix (persisted node_uuid / identity derived from the slot+incarnation pair), or should pass-1 be deleted and the adopted-slice gate
11. [critical] D-RECOV-ADVANCE-UNBOUNDED-RETRY
   next: Per the sess91 RULE-5 ruling: (1) On each failure REREAD the descriptor and CLASSIFY. If owner/gen/term/recovery-id genuinely changed, stop immediately, discard the auth and let the new owne
12. [critical] D-RECOV-ZERO-EPOCH-DESCRIPTOR-AUTHORITY-UNPROVEN
   next: 1) THE CLOSURE STEP: a test that exercises the cause. Inject epoch 0 into a victim's ACTIVE heartbeat record on the backing store, kill it, and assert P238-FENCE-ZEROINC fires and NO descrip
13. [critical] D-FENCED-VICTIM-MAY-REREGISTER
   next: 1) DONE sess93 — the measurement that decides severity: CONFIRMED, see evidence. 2) DONE sess93 — the interim detector shipped and rig-verified (0.11.424, D-FENCE-RESERVATION-HEALTH-UNCHECKE
14. [critical] D-MIXED-VERSION-UNGATED-REPLAY
   next: 1) DONE sess131 — mechanism chosen: MXFS_PROTO_GEN 3->4, RULE-5 APPROVED (see evidence). 2) B1 DONE sess179 (0.11.464 sv 66F51D070BC971F72E39387): sess178 audited all 7 entry-point classes; 
15. [critical] D-SAMENODE-WAITER-CANCEL-COLLISION
   next: sess113 RULE-5 RULING: the sess112 landing (0.11.441) is STOP-SHIP -- 'no-go for the 32-node rig'. Sampling the registry's aggregate counters does NOT establish exclusive ownership of the ad
16. [critical] D-RECONCILE-SLOT-IDENTITY-UNCHECKED
   next: sess112 landed the resource memcmp at the remembered slot on 0.11.441 (the check was previously UNREPRESENTABLE -- the function took no resource argument). That direction is right but sess11
17. [critical] D-IUNL-LIVESKEW-REFUSES-PENDING-WINDOW-GRAFT-471
   next: Fix landed sess203 on 0.11.472 (sv 9EF804271D01AA283D14131) per GPT ruling (option c strengthened + narrow b): pending-transition certificate {old,next,valid} on xfs_inode published in xfs_i
18. [critical] D-RSYNC-OVERWRITE-LAP-USERSPACE-FAIL-ERRNO-UNKNOWN
   next: 1) sess202 added stderr capture to tests/suite/rsync_paired.sh (err text into the check reason) - UNVERIFIED; find where RESULT reason lands in the collect path (criteria.json / showstat -v 
19. [high] D-DIRVIEW-NONCONVERGE-SESS25
   next: Reproduce pressure: aged-mount dirent_durability laps at 32/caw while one node is denied the dir EX handoffs (readdir-only role). If a next occurrence fires: capture the stale node's i_dlm_d
20. [high] D-MONITOR-INCARNATION-DOWNGRADE-TO-ZERO
   next: Per the sess91 RULE-5 ruling: (1) Guard the rebase with inc_valid(): a nonzero cached incarnation must be RETAINED when the sector reads 0, and the slot marked inconsistent/unknown rather th
21. [high] D-FENCE-CAPABILITY-UNVALIDATED-AT-MOUNT
   next: 1) Add the admission check to the mount path, before normal I/O is admitted: issue PERSISTENT RESERVE IN / REPORT CAPABILITIES (assert PTPL_A=1 and that the type mask offers WE-RO), READ RES
22. [high] D-FENCE-CRASH-MATRIX-UNTESTED
   next: 1) Build the crash-matrix harness: inject death/power loss before intent persistence, after intent before P&A, during P&A, after P&A before certify, during certify, after certificate before 
23. [high] D-RECONCILE-EXHAUSTION-SILENT
   next: sess112 landed -EBUSY + P245-RECONCILE-EXHAUST + requeue-as-owed on 0.11.441. sess113 RULE-5: THAT IS NOT ESCALATION and does not satisfy the requirement. 'A warning, counter and owed flag a
24. [high] D-TRACK-PUBLISH-ORDERING
   next: sess112 moved tenure publication into the same critical section that ends the acquiring attempt (0.11.441), closing the observe-to-track window for the publish side. sess113 RULE-5: necessar
25. [major] D-32NODE-SHARED-DIR-CREATE-PACE
   next: 1) RULE-5 CONSULT: DONE (sess130, gpt-5.6-sol; full review in the sess130 transcript). Its closure requirements now MET: queue conservation accounting (closes exactly); zero stale-ticket exp
26. [major] D-READDIR-PEER-CACHED-DIR-PACE
   next: sess127: check this against the BAST-DELIVERY root (single-threaded bast_recv_fn doing a synchronous shared-LUN slot read per packet, hints dropped at RcvbufErrors=8085) before doing any ind
27. [minor] D-MATRIX-UNMEASURED
28. [minor] D-RELOAD-FREED-ADOPT-BOGUS-IMODE
   next: 0.11.357 guards the rewire with i_mode != 0 (freed-shell adopt keeps prior self-consistent ops; a later resurrecting reload still rewires since old_ifmt is captured per-reload). Verify: reru
29. [unset] D-AGI-UNLINKED-CROSSNODE-RECOVERY-SHUTDOWN
   next: Reproduce with AGED fs: at grace=10, run board-chunk (dir_reuse x2 + cache_coherency + crash_consistency + rsync_paired) THEN dirent_durability loops, instr ON only from the dirent phase (av

RULE 6: an entr

_(truncated at 8000 bytes — raise CCLOOP_STATE_HOOK_MAX_BYTES to see the rest)_
