# MXFS build / session history (archived from CLAUDE.md [AWARENESS] head)

This is the append-only per-session build-history blob that used to live as a
single giant bullet in CLAUDE.md. Moved out 2026-06-29 (sess46) so the always-
loaded CLAUDE.md stays small. Current rolling state lives in /src/mxfs/STATE.md;
per-session detail lives in ccmemory (sessNN-* notes). This file is reference only.

- **Current build (sess46 ccloop, 2026-06-29)**: srcversion `B349FD6E` (= A00D8CFE with P46-GROW capped-counter instead of ratelimited, so EVERY dir-grow is logged for complete same-round double-grow correlation) = sess45 keeper FB296422 + PROBE-ONLY changes (NO behavior change → keeper-equivalent; safe on disk). **sess46 REFRAMED the dominant 8/tcp blocker — it is NOT the SINGLE intra-block double-alloc sess45 chased; it is a NODE-ISOLATION/WEDGE.** PROVEN (tests/drc_detail8.sh, non-perturbing P46-GROW probe): the 8/tcp dir_reuse failure is MULTI-MODAL — (1) **MASS/isolation (DOMINANT)**: a node goes UNREACHABLE >40s mid-create-wave → peers self-fence then "did not reconnect within 40000 ms — declaring dead, recovering locks" → its in-flight round work never publishes. Captured: round 24, ALL ~54 of node5's entries gone (all 50 .md5 + tail files f47-f50; f1-f46 present) = node5 isolated partway through the round. Membership HAD fully formed (active_count=8) first. (2) **SINGLE (occasional)**: 1 dirent lost (intra-block/grow). **KEY CORRECTIONS to sess45's model**: (a) **host is NOT oversubscribed** — clyde 56 CPUs (load ~4.5), 70GB free, 8 VMs use 32 vcpu/32GB → the >40s unreachability is a WEDGE (unbounded sync drain / DLM-EX starvation), NOT CPU starvation; sess45's "host-load FLAP" assumption is WEAK; bumping tcp_death_grace MASKS. (b) **release DOES drain the dir inode** — `mxfs_ail_drain_inode_sync(ip)` (xfs_mxfs_dlm.c ~8724) waits `!in_ail && pincount==0` = checkpointed AND written-home → on the WRITE-THROUGH LIO target A's dinode IS durable at release → the double-grow is NOT a release-side inode gap. (c) **LIO target is WRITE-THROUGH and REJECTS FUA passthrough** (kern.c:502-514) — `mxfs_pal_scsi_read_fua_bdev` falls back to a plain bio read into a DISCARDED kmalloc temp → causally INERT except for LATENCY → sess45's `dir_release_fua_write` is likely redundant on this target, and ANY "FUA read fixes it" result is TIMING-MASKING (RULE-0 debt). **P46-GROW masking experiment (RULE-4)**: a FUA/bio read at the dir-grow raised the pass rate (keeper ~4-7/8 → +1 grow read ~6/8 → +2 grow reads ~5/5) — but it is read+discard (no buffer effect) so the ONLY effect is grow-path latency → CONFIRMS the SINGLE is a tight grow-latency-sensitive race; masking ≠ fix. **P13-COLLIDE off=64 hits are FALSE POSITIVES (ourdir=0 = garbage disk bytes, not a real dir block); P46-GROW ourdir=1 is ABA reuse (the daddr held a PRIOR ROUND's dir block — owner=131 because the dir inode is reused), NOT a same-round double-grow.** **NEXT (RULE 4) — attack the DOMINANT isolation/wedge mode**: a node disconnects only on a HARD recv/send error (peer.c:104; transient -EAGAIN loops) → the node's TCP is genuinely not serviced → a kernel-level WEDGE. BASTs ARE queued to a workqueue (not drained inline in the recv thread), so the recv-thread-inline-drain theory is weakened. Need LIVE instrumentation: a per-node watchdog/beacon that captures WHAT a node is blocked in (stack/op) when its peer declares it dead (dead node's dmesg is wiped on reboot). Also suspect: self-fence + memb_settle(20s) + grace(40s) AMPLIFY a brief wedge into a cluster-wide EX-freeze cascade. Map node-id→testN via the active_count beacon (local=<id>). Harnesses: tests/drc_detail8.sh (now captures [FAILROUND]+per-node failing-round P46-GROW grows+[MEMBERSHIP]+[DEATH/FLAP]), tests/drc_batch8.sh. ccmemory `sess46-REFRAME-8tcp-dominant-failure-is-node-isolation-wedge-not-doublegrow`. — Prior **sess45 ccloop, 2026-06-29**: srcversion `FB296422` — BIGGEST advance in project history. **8/tcp dir_reuse 0/5 → 4/8-7/8** (variance), **4/tcp 3/3**, coherency root SOLVED, membership split-brain SOLVED (0 MASS, 0 FLAP in the defaults batch). Criterion NOT yet met (8/tcp not 100%; sole residual = a SINGLE intra-block double-alloc on dirty/data_init blocks, ~50%/run). **sess45 RESURRECTED the platter-lag thread sess44 dropped**: the dir_reuse loss is FUA-PLATTER-LAG (LIO write-cache vs platter), NOT the epoch signal sess44 chased. **KEPT FIXES (all now DEFAULT in FB296422)**: (1) `dir_release_fua_write=1` (xfs_mxfs_dlm.c:4218) — explicit SCSI WRITE16+FUA of released dir blocks before DLM unlock forces the LIO platter current (bio blkdev_flush is LIO-dropped); (2) `dir_addname_coherent=1` (xfs_mxfs_dlm.c:5363) — FUA-reread the target dir block before the first add into a CLEAN block, invalidate+restart if in-core != platter → prevents the PROVEN intra-block free-slot DOUBLE-ALLOC (P13-COLLIDE: node7 placed node7_f8.md5 onto node8's durable node8_f8.md5); REFUTED in sess28 ONLY because the platter lagged then — now complementary with (1); (3) `memb_settle_ms=20000` (was 6000) — outlasts the 1→N formation ramp so master=hash%count never diverges; (4) `tcp_death_grace_ms=40000` (was 15000) + **self-fence** (dlm/v5_mount.c: stamp last_memb_change_ms on peer disconnect AND unconditionally on reconnect → a starved/declared-dead-then-returning node freezes EX until membership reconverges); (5) **membership beacon** (dlm.c "MXFS-MEMBERSHIP active_count") + **run.sh convergence gate** (prep waits all N nodes report active_count==N before workload). **MECHANISM PROVEN (tests/drc_detail8.sh, no instr)**: pure ACQUIRE-SIDE stale-IN-CORE-base double-alloc — platter current (FUA works), in-core stale; NO doublegrant/stalemaster/flap at the loss → DLM serializes. **RESIDUAL (next): the SINGLE double-alloc on blocks addname_coherent SKIPS (dirty/in-AIL)** — two sub-cases: (a) data_init-zeroes-block0 (stale shortform fork vs disk block-format, same di_gen); (b) dirty-block-kept-across-handoff. **GPT-5.5 consult (RULE-5, on file)**: raw FUA makes the PLATTER current but leaves the XFS buffer DIRTY/in-AIL → addname_coherent skips it → stale RMW. FIX = make DLM EX RELEASE a synchronous XFS clean+invalidate boundary (drain→bwrite→retire BLI→invalidate, not just FUA bytes) + ACQUIRE cold-read before first modify (per-tenure tag: dirty_seq==grant_seq→keep else refresh) + sf_to_block di_gen-gated fork revalidation. **`dir_release_retire_bli=1` TESTED = RISKY (iter1 0/8 OTHER, likely shutdown — sess39 corruption risk persists), do NOT enable; implement GPT's per-tenure-tag safe version instead.** Do NOT union-merge or wholesale-adopt-dirty. Harnesses: `tests/drc_batch8.sh <n> [modargs]` (classifies PASS/FLAP/SINGLE/MASS), `tests/drc_detail8.sh` (mechanism: P13-COLLIDE/DOUBLEGRANT/P62), `tests/drc_cap4.sh`. ccmemory `sess45-WINNING-config-and-remaining-work`, `sess45-BREAKTHROUGH-8node-loss-is-intrablock-doublealloc-addname-coherent-fix`, `sess45-GPT-consult-release-must-clean-not-just-fua-platter`, `sess45-RESURRECTED-platter-lag-fix-dir-release-fua-write-default-on`. — Prior **sess44 ccloop, 2026-06-28**: srcversion `EBBC5A82` = keeper + ONE real DLM fix (2/tcp dir_reuse PASS re-verified; all sess44 levers default-OFF; P44 probe gated). 8/tcp dir_reuse still ~33% fail; criterion NOT met. **sess44 KEPT FIX**: `dlm/dlm.c` ~1842 (lock DOWNGRADE→promote dispatch) was the ONLY 1-of-4 promote_waiters dispatch sites NOT calling `dg_grant_ex` → EX grants promoted there got `dir_epoch=0`; now matches the other 3 (stamps dir_epoch + send_grant to remote). **sess44 ROOT-LOCALIZED the 8/tcp loss to a DEAD COHERENCY SIGNAL**: P68-EVDECIDE shows `cur_mep=0` (master dir epoch) at the modifying nodes though the master COMPUTES it (P64-MASTER-HANDOFF epoch→515) → so ALL acquire-side stale-base eviction (newtenure/prior-tenure/grant_evict, gated on epoch/grant_gen != 0) is structurally INERT for the storm dir → it RMWs a stale cached base = intra-block offset double-alloc (4 dirents at the same byte offset, name-level PROVEN). The 1842 fix only partially helped (cur_mep 0×9853 vs 15×2147); **the DOMINANT cur_mep=0 source is the FAST-PATH MHT serve** (cached EX, no DLM round-trip → dir_epoch/grant_gen never refreshed). Also PROVEN this session: DLM serializes (P44-MODGRANT all held=1, but 1 rare P-DOUBLEGRANT), release-completeness is sufficient (whole-AIL push still loses), fresh-base+push together still lose. **NEXT = sess61 step 4b**: add the grant_gen/epoch staleness check IN the dir-EX FAST-PATH serve in mxfs_dlm_ilock_begin (force reload when the live grant_gen/epoch != cached → a peer took the lock since we cached), NOW reliable post-1842-fix. CAVEAT sess35: refresh at tenure START not mid-tenure (else readdir=0). ccmemory `sess44-KEPT-fix-1842-dir-epoch-propagation-partial-next-is-fastpath-serve`, `sess44-ROOT-epoch-and-grantgen-both-zero-for-reused-dir-inode-coherency-signal-dead`, `sess44-FINAL-bug-is-acquire-side-stale-read-dlm-serializes-correctly`. **sess44 FINAL TRIANGULATION (3 legs all proven): the loss is CONCLUSIVELY ACQUIRE-SIDE.** P44-MODGRANT proved the DLM SERIALIZES CORRECTLY (all 38394 modifies of ino=131 at mode=EX held=1, monotonic wall-clock realns, NO double-grant, NO cached-EX-stale-grant → REFUTES sess42 concurrency root); and a maximal whole-AIL release fence STILL loses → release durability is sufficient → REFUTES the 40-session release-drain focus. ∴ a node holding a fresh EX grant reads its OWN STALE CACHED dir buffer as the addname RMW base (xfs_da_read_buf reval gated `!owned_ex` so SKIPPED during addname; `mxfs_dir_newtenure_evict` default-ON under-fires). NEXT: instrument newtenure_evict at first-addname-post-acquire; make EX-acquire cold-read ATOMIC across the WHOLE dir fork (data+leaf+free together — per-block invalidation CORRUPTS via leaf/data desync). ccmemory `sess44-FINAL-bug-is-acquire-side-stale-read-dlm-serializes-correctly`. **sess44 PROVED the root at NAME LEVEL (P13-NADD/LADD offset trace + per-rank DRCph windows — the count/dland ring is BLIND to it): the loss is cross-node INTRA-BLOCK dir-data freespace DOUBLE-ALLOCATION** — round19 daddr=14652648 byte-offset aoff=1600 got FOUR distinct dirents from rank3/6/8 (victim node8_f31.md5 lost the write race; count rises so it's invisible to count discriminators). **PIVOTAL NEGATIVE (redirects ~40 sessions of release-side focus): a MAXIMAL release fence does NOT fix it.** Built `dir_release_ail_all` = whole-AIL `xfs_ail_push_all_sync_bounded` at dir-EX release (after the existing per-AG push which only drains the dir INODE's AG `d_agno`, missing the dir DATA blocks scattered across OTHER AGs) → push-only **0/2, round-1 loss persists** ⇒ **release durability is NOT the gap.** So the bug is **ACQUIRE-side** (acquiring node modifies on its OWN cached stale base despite the LUN being current — xfs_da_read_buf revalidation is gated `!owned_ex` so SKIPPED during addname; newtenure_evict under-fires) **OR DLM mutual-exclusion** (sess42 cached-EX stale-grant: MHT batch `holders>0` window lets a node modify under a grant silently moved to a peer → concurrent divergent RMW). REFUTED this session (all): acquire bypass `dir_tenure_evict=1+dir_tenure_stale_bypass=1` (2 pass then CORRUPTS — xfs_dir2_sf_verify shutdown); broadened acquire scan `dir_coherent_modify=1` (P11 fired 0× — the stale base is the node's own UNDESTAGED buffer the clean-only guard skips; combo 3/4 but the 1 fail CORRUPTS via bmbt_scan side-effects); `dir_wseq_at_completion=1` (insufficient, confirms sess42); whole-AIL release push (insufficient). **GPT-5.5 consult (RULE-5, on file in sess44 ccmemory): "DLM EX unlock is a metadata home-block visibility fence" — but proven insufficient here, so the gap is NOT release.** NEXT (RULE 4): instrument the MODIFY moment on ino=131 — at xfs_dir2_*_addname log (node, wall-realns, does-this-node-ACTUALLY-hold-EX-grant via mxfs_v5_dlm_inode_held, cached_ex_holders); if two nodes' addname realns OVERLAP → cached-EX stale-grant concurrency (fix in DLM layer); if serialized → pure acquire-side stale cache (make EX-acquire cold-read ATOMIC across data+leaf+freeindex — per-block invalidation CORRUPTS via leaf-hash/data desync). Tools: P13-NADD/LADD offset trace + per-rank DRCph windows (DECISIVE, name-level); DRC_STREAM=1 rotation-immune. ccmemory `sess44-DECISIVE-release-completeness-not-the-gap-bug-is-acquire-or-concurrency`, `sess44-PROVEN-offset-collision-double-alloc-aoff1600-four-dirents`, `sess44-REFUTED-tenure-stale-bypass-corrupts-fix-must-be-release-side`. — Prior **sess43 ccloop, 2026-06-28**: srcversion `39C98098` = 445B37FF-equivalent for 1/2/4 (new param `dir_owner_scan` DEFAULT 0; code+P43 probes inert). 8/tcp dir_reuse still ~33% fail; criterion NOT met. **sess43 dland-PROVED the loss is a STALE-BASE RMW CLOBBER** (write-completion ring, global time-order: daddr=16745864 count jumps 143→98 = one node overwrites a peer's durable adds with its stale cached base). **CORRECTED the test model: `tests/suite/dir_reuse_coherency.sh` line 162 rank1 `rm -rf "$D"` EVERY round → ino=131 + dir leaf/data DADDRS are freed+REUSED next round (incarnation-ABA reuse stressor); the loss is during the concurrent create wave under reuse, NOT a stable-entry corruption.** Two map-INDEPENDENT owner-scans (model: mxfs_dir_bmbt_scan) built+tested+REFUTED: (1) **release-flush owner-scan** (`mxfs_dir_data_owner_scan`, wired into mxfs_dir_data_durable + flush_data_blocks[_relsafe]) lands owned dir blocks the for_each_xfs_iext drains MISS — P43-OWNERSCAN cand>0 (gap real) but STILL FAILS ⇒ release durability is not the gap; (2) **acquire retire-evict owner-scan** (`mxfs_dir_evict_owned_data_blocks` at the FAST-PATH handoff P101-FASTEX-EVICT ~14369, with P34-style BLI retire) fired 53-113× but **evicted=0 ALWAYS** ⇒ at the handoff the stale base is the node's OWN pinned/dirty buffer (its un-checkpointed work), NOT a clean cached block → nothing safe to evict → INERT. **KEY NEGATIVE: the stale base is the modifying node's own pinned buffer — cannot fix by evicting at acquire; the fix must rebase/merge the peer's durable image INTO the buffer at MODIFY time, or drop the stale write at submit.** clobber chokepoint (P-DATACLOBBER-SKIP) stays SILENT (in-core ⊇ disk-at-that-moment). NEXT (RULE 4): re-test the union-merge writemerge (dir_choke_merge) against the dland-proven 143→98 case w/ removed-set disambiguation; verify sess40 incarnation-skip (P40-INCARN-ABA-DIRSKIP) covers the data block at the loss given the rm-rf reuse. Tools: `tests/drc_cap8.sh N "<modargs>"`, `mxfs.dirland=1` ring + tests/tcp/drc_cap/dland_*.txt (CAVEAT: leaf-owner field is a probe artifact o=12050306709998375241; real owner always 131). ccmemory `sess43-FIX-twosided-owner-scan-dir-blocks-map-independent`. — Prior **sess42 ccloop, 2026-06-28**: srcversion `7E1EFD90` = keeper FUNCTIONALLY (all new params default-OFF; only added storm-dir-scoped ratelimited probes → NO regression; 8/tcp dir_reuse still ~33% fail; criterion NOT met). **sess42 CORRECTED the sess41 root: the readdir=799 loss is a STALE-BASE RMW CLOBBER, not insert-loss. The sess41 "sum==801 ⇒ insert-loss" discriminator is AMBIGUOUS/WRONG** — PROVEN with the dland write-COMPLETION ring (per-block dirent COUNT): victim's data block was written by its creator up to count 112, then a PEER wrote it up to count 132 WITHOUT the victim (count rose DURING the clobber → fools count-regression AND sum discriminators). Write-merge ON = INERT (dko=0 ⇒ LUN lacked victim at the clobbering write ⇒ a real DURABILITY gap, not recoverable-from-disk). **SMOKING GUN (code site): `xfs/xfs_mxfs_dlm.c` ~13485-13516 — the cached-EX stale-grant demote: when a node holds CACHED EX but `mxfs_v5_dlm_inode_held` returns 0 (grant silently lost to a peer), it sets i_dlm_mode=NL + i_dlm_stale=true WITHOUT flushing its un-checkpointed dir DATA blocks → slow-path re-acquire reloads disk (P63-HANDOFF adopt) and DISCARDS the just-added dirent. The check is SKIPPED while holders>0 (MHT batch) → modifications happen under a stale grant (concurrent divergent RMW with the peer that took the grant).** FIXES TRIED+REFUTED: written_seq-at-submit→completion (`dir_wseq_at_completion`, correct per GPT but INSUFFICIENT, default-OFF for safety, UNVERIFIED on 1/2/4); write-merge ON (inert); `dir_release_flush_all_done=1` (no help); BTREE-vacuous-durable bail (`P42-VACUOUS-DURABLE` fired 0×); prior-tenure-evict override (params default-0, not firing). **GPT-5.5 PLAN (RULE-5 consult): the per-dir EX TENURE (not the physical daddr — daddr ABA defeats daddr-keyed checks) owns the writeback lifetime: EX release/downconvert MUST checkpoint ALL tenure-dirty dir metadata home before any peer can acquire EX (track dirty set via generic xfs_trans_log_buf/log_inode hooks, NOT format-specific drains — the leaf-drain misses the data block); cached EX must be backed by a valid grant token; reload/adopt must NEVER discard undestaged local dirents (checkpoint-first if still EX, else fail-closed). NEXT (RULE 4): Phase-1 = per-modify grant-verify under cached EX (prevent the holders>0 window at 13485) + fail-closed detectors at the demote/reload-adopt paths to name the exact illegal path; find where the TCP grant flips to !held WITHOUT a BAST/drain (lease/membership/peer-steal — the async grant-loss-without-callback is the deeper root).** Probes added: `P13-LADD` (leaf placement), `P42-RELDUR` (per-data-block release durability verdict), `P42-VACUOUS-DURABLE`. Tool: `mxfs.dirland=1` write-completion ring is DECISIVE (count trajectory per daddr distinguishes clobber from insert-loss). ccmemory: `sess42-PROVEN-799-is-stalebase-clobber-not-insert-loss-gpt-tenure-fence-plan`, `sess42-SMOKINGGUN-cached-EX-stalegrant-demote-discards-uncheckpointed-dir-work`. — Prior **sess41 ccloop, 2026-06-28**: srcversion `0B73D70B` = keeper `B17141DA` FUNCTIONALLY (ALL new params default-OFF → NO regression, still 8/tcp dir_reuse ~6/8; criterion NOT met). **sess41 DECISIVELY REDIRECTED the `readdir=799` root: it is NOT a stale-base RMW — it is a POST-SUBMIT writeback/REUSE ORDERING loss (an older/stale write of a REUSED physical daddr wins on media).** PROOF (RULE 4): a removed-set-disambiguated content-merge at the universal dir-data write chokepoint (`mxfs_dir3_data_writemerge`, `dir_choke_merge` default 0) is structurally INERT — probe `P-WMR-REACH` fires (disk≠in-core) but `disamb=0`/`dko=0` ALWAYS → **in-core ⊇ disk at EVERY dir-data write** (the writer never lacks an on-disk name), so the lost entry is never recoverable-from-disk at any write = not a stale-base clobber. GPT-5.5 (consult, RULE-5-justified) concurs: the needed invariant is PHYSICAL-block/owner-incarnation based, not inode/extent. **GPT RANKED FIX PLAN (do next, RULE 4 — prove ordering FIRST):** (1) written_seq at I/O COMPLETION not SUBMIT (+b_mxfs_io_inflight); (2) **release drain by OWNER/INCARNATION incl FREED/converted/leaf blocks, not current extent map — THE KEY GAP**; (3) cluster physical-daddr REVOKE/quarantine before reuse (or local per-inode freed-daddr ring drained at free); (4) remove acquire-side bounded wait-then-keep-stale fallback; (5) per-daddr single-in-flight-write. **TOOL:** `mxfs.dirland=1` → `P-DLAND` logs daddr/owner/incarn/sum/realns at I/O COMPLETION (merge nodes by realns → which content version lands last). CAVEAT: kernel ring ROTATES over 24 rounds (fail snapshot kept ~40 lines/node, rounds 1-4 only; the lost `.md5` sidecar write rotated out) — NEXT use DRC_STREAM (rotation-immune file; run.sh doesn't forward it yet) + reduced DRC_ROUNDS to catch the exact late write, then implement matching fix 2/3. ccmemory: `sess41-PROVEN-799-is-post-submit-writeorder-not-stalebase-merge-dead`. — Prior **sess40 ccloop**: srcversion `A985424B` (2/tcp dir_reuse 3/3, 8/tcp dir_reuse **6/8**). **(A) the `readdir=799` loss [sess40 theory, now SUPERSEDED by sess41] = ASYNC-WRITEBACK OVERLAP ACROSS THE EX HANDOFF (release-drain gap) — PROVEN BY ELIMINATION.** On no-flap failing iters (build `B17141DA` = keeper + detectors), ALL of these are SILENT at the loss: flap events, `MX-DOUBLEGRANT` (dlm.c:601 single-master audit), `P-STALEMASTER-GRANT`, `P-COUNTREGRESS` (NEW per-buffer dir-data count high-water), `P-DATACLOBBER-SKIP`, `P25-RELVERIFY-MISMATCH`. So the DLM lock layer SERIALIZES CORRECTLY (never two EX holders) yet a dirent is durably lost → the WRITEBACK of a dir-data block crosses the handoff: node A releases EX while its dir-data write isn't durable on the LUN; B acquires + cold-reads (misses A's entry) or A's late write interleaves with B's — each write individually consistent with disk-at-its-own-submit (so dataclobber/countregress are blind), net 1 entry lost; cascades to `DABUF_MAP_HOLE`. PRIME SUSPECT: the release fence's durability check `mxfs_dir_buf_is_undestaged` (`b_mxfs_logged_seq != b_mxfs_written_seq`, xfs_mxfs_dlm.c ~15715) stamps `written_seq` at write SUBMIT not COMPLETION → a submitted-but-uncompleted bio reads "destaged"=durable → fence releases before it lands. NEXT FIX (RULE 4): make the EX release fence guarantee ACTUAL per-dir-inode bio completion before unlock — stamp written_seq (or a new `b_mxfs_io_inflight` flag) at I/O COMPLETION, blocking-lock each dir data/leaf/free block to wait out in-flight bios, and unconditionally sync-flush any not-provably-durable block; ALSO fix the acquire-side `mxfs_dir_drain_evict_data_blocks` LOCKED-SKIP (wait out the in-flight write then evict+cold-read). The mount-wide `dir_wr_barrier` was the right idea / wrong granularity (submit-count, mount-wide → 0 at release). **(B) a separate MASS-FAIL membership-flap mode** (flap >15s → declared-dead → P-STALEMASTER split-brain → readdir=0); flap-prevention helps it. Full elimination chain: ccmemory `sess40-PROVEN-799-is-release-drain-gap-async-writeback-overlap`. **FIXES this session**: (1) reverted sess39 v2 `dir_refresh_inplace`→0 (was CATASTROPHIC: leaf_removename corruption + bnobt double-free); (2) `dir_wr_barrier` writeback-barrier built but REFUTED as NO-OP (P40 never fired — publish-before-notify already syncs dir writes), default 0, infra kept; (3) **KEPT: TCP flap-prevention** (dlm/peer.c `mxfs_peer_send`) — a TRANSIENT send timeout no longer tears down the socket+fires disconnect; only HARD errors do (keepalive ~19s + lease ~75s still catch true death). Helps mode (B); does NOT fix mode (A). 8/tcp 3/6→6/8, 2/tcp 3/3. ⚠️ A kern.c `mxfs_pal_tcp_send` return-code tweak (-EAGAIN/-ECONNRESET) REGRESSED 2/tcp to 0/3 (other callers retry-loop on -EAGAIN → hang) and was REVERTED — flap-prevention must live only in peer.c; pal keeps uniform -ETIMEDOUT. **NEXT**: mode (A) = inode/bmbt extent-fork detector → root the concurrent-add lost-update; mode (B) = GPT-5.5 reliable-midcomms roadmap (seq/ack/resend-on-reconnect/dedup + grant-gen cookies). Re-verify 1/2/4 tcp before any criterion claim. Harnesses `tests/drc_loop8.sh`, `tests/drc_cap8.sh` (breaks on fail; pulls RDMISS/CLASS/flap-events/P40). ccmemory: `sess40-CORRECTION-799-is-concurrent-add-not-flap-bmbt-extent-fork`, `sess40-FIX-tcp-flap-prevention-send-no-teardown`, `sess40-REFRAME-799-is-tcp-flap-not-buffer-barrier-noop`. — Prior **sess39 ccloop, 2026-06-28**: KEEPER srcversion `8CC09D97` — **ROOT-CAUSED + FIXED the 8/tcp dir_reuse CATASTROPHIC (0/8) failures = MEMBERSHIP SPLIT-BRAIN** (the layer 37 dir-block-heuristic sessions missed). Two DLM fixes, both DEFAULT-ON, validated (2/tcp dir_reuse 3/3, P-STALEMASTER→0): (1) **deferred-TCP-death** (dlm/v5_mount.c, `tcp_death_grace_ms=15000`) — `v5_peer_disconnect_cb_tcp` no longer immediately purges+unregisters+refreshes on a transient TCP stall (~7s flaps observed); marks SUSPECT, grace-checker thread declares death only if no reconnect; (2) **EX-grant membership-settle gate** (dlm/dlm.c, `memb_settle_ms=6000`) — `dlm_lock_impl` blocks EX acquires while `ctx->last_memb_change_ms` is within the window so mastership converges (formation 1→8 ramp + real death) before exclusive work. **RESIDUAL (criterion BLOCKER, NOT fixed)**: rare steady-state xfsaild ZOMBIE reflush (`readdir=799`, stale=0 disc=0, `daddr=120 buf_cnt=N disk_cnt=N+1 in_ail=1 comm=xfsaild` → cascades to DABUF_MAP_HOLE). Acquire-side eager evict (`mxfs_dir_drain_evict_data_blocks`) SKIPS the block xfsaild is mid-flushing (race). **ALL buffer-layer fixes REFUTED this session** (on top of the membership fix, so unconfounded): skip/`dir_tenure_reflush_skip`→readdir=0; retire `dir_release_retire_bli`+`dir_release_retire_done`→readdir=796 over-lose; `dir_force_evict`→268/lookup_fail; NEW `dir_refresh_inplace` (memcpy disk→buf)→`xfs_dir3_block_verify` corruption (default-0 inert); `dir_ail_defer`→whole-batch loss+starvation (pre-existing). NEXT = architectural: full GPT write-authority-token (release-drain-complete + per-resource EX write-authority gating xfsaild + liveness valve) OR write-through dir blocks. Harness `tests/drc_loop8.sh`. Full detail: ccmemory `sess39-ROOTFIX-membership-splitbrain-formation-and-flap`. — Prior **sess22**: srcversion `D589FA5F` (KEEPER = EFBB9861 + Layer-3 free-slot guard + INERT refuted `dir_stale_incarn_skip` param; functionally == 8948C889, no shutdowns). **sess22 DECISIVELY narrowed `readdir=799`: it's a durable content-divergent count-preserving clobber = an EX-holder destaging a STALE-KEPT prior-tenure dir DATA block (RMW on a stale base). RULED OUT (all): addname free-slot double-alloc (P22-FREESLOT-STALE 0×), prior_tenure, release_fua_write, leaf_rebuild, ALL count-based write guards, AND write-side EX-held suppression (`dir_stale_incarn_skip=1` was CATASTROPHIC — readdir 474/800 + 4-node shutdown; sess23 "suppression is the corruptor" trap). CONCLUSION: the fix MUST be READ-SIDE — on EX (re)acquire/modify, force-evict + FUA-re-read (clear XBF_DONE on a CLEAN DONE block whose b_mxfs_dir_gen < i_dlm_dir_gen, same-incarnation gated) EVERY prior-tenure dir-fork block (data+leaf+freeindex), so the EX holder never RMWs a stale base. The sess41 evict-refresh (mxfs_dirrefresh, default off) is the right SITE but was count-based ("disk strictly MORE", fired 0×) — re-target it to fire on ANY dc_stale prior-tenure dir block, NOT a count/content compare. Full plan: ccmemory `sess22-REFUTED-exheld-stale-write-suppression-harmful`, `sess22-freeslot-guard-0x-loss-is-postadd-stale-destage`, `sess22-GPT-fix-design-freeslot-doublealloc-readdir799`.** Prior in-sess22: EFBB9861 (reorder-remove + leaf holeskip/bail + NODE datascan leaf-hash heal); 8948C889 (+Layer-3 freeslot guard). **sess22 deep-dived the last dir_reuse residual `readdir=799` (durable content-divergent count-preserving data-block loss). RULED OUT: stale-freeindex addname-time double-alloc (P22-FREESLOT-STALE 0×), prior_tenure, release_fua_write, all count-based write guards. PROVEN: the dirent is ADDED correctly to a real free slot then VANISHES durably (sess11 logged-then-vanishes) = a POST-ADD stale-block DESTAGE (xfsaild ABA writeback of a stale copy of the block, missing the just-added entry). NEXT: instrument the dir-DATA-block bio write chokepoint (pal/linux/xfs_buf.c, where ex_write_guard lives) with a CONTENT-superset check (is the in-core block missing any on-disk inumber? — NOT count), same-incarnation gated; fix = suppress xfsaild dir-data writeback by a non-EX-holder, like P126(AG-meta)/P60(bmbt). GPT design + full diagnosis in ccmemory `sess22-freeslot-guard-0x-loss-is-postadd-stale-destage`, `sess22-GPT-fix-design-freeslot-doublealloc-readdir799`, `sess22-readdir799-is-content-divergent-clobber-count-guards-blind`.** Prior in-sess22: EFBB9861 (reorder-remove + leaf holeskip/bail + NODE-format datascan leaf-hash-hole READ heal). **sess22 datascan heal: extended mxfs_dir2_datascan_lookup to BTREE/node-format dirs + wired into xfs_dir2_node_lookup → ELIMINATES dir_reuse `leaf-hash lookup_fail` (the 800-entry dir is node-format, was unhealed). dir_reuse residual NARROWED to a single face: `readdir count exp=800 got=799` = durable single-entry DATA-block lost-update (RMW-on-stale-base create-miss; flaky ~PASS 1/3). NO shutdowns. See ccmemory `sess22-FIX-node-format-datascan-leafhash-heal`.** Prior in-sess22: CRITERIA = `./run.sh {1,2,4,8} tcp` 100% (NOT MET). **sess22 WIN: reordered xfs_dir_remove_child non-dir path (xfs/libxfs/xfs_dir2.c) so xfs_dir_removename runs FIRST while the trans is CLEAN → eliminates the 8/tcp dir_reuse remove-path dirty-cancel SHUTDOWN (was 0/8 cascade). Now NO shutdowns on 8-node dir_reuse, flaky-PASS (~50-60%). leaf_rebuild stays DEFAULT-OFF (=1 is HARMFUL: bnobt double-free `ltbno+ltlen>bno` shutdowns). Holeskip/bail (xfs_dir2_leaf.c mxfs_dir_rebuild_leaf_from_data) are dormant when rebuild off.** REMAINING 8/tcp deep bugs (130-sess core): (1) leaf-block lost-update = durable leaf-hash hole (dominant dir_reuse residual); (2) AGI CRC shutdown in ifree (flaky, in-FULL-suite only — 3× standalone 2-node PASS); (3) bnobt double-free / data-block double-alloc. See ccmemory `sess22-FINAL-keeper-DFDBAAFE-and-remaining-deep-bugs`, `sess22-FIX-remove-reorder-eliminates-dirty-cancel-shutdown`, `sess22-AGI-crc-shutdown-ifree-fua-write-no-crc`. — Prior sess21 KEEPER 7B66691E (union merge + OFFSET FIX). **VERIFIED: 1/tcp=16/16, 2/tcp=17/17, 4/tcp=17/17, tcp_dlm_scaling 4/tcp=4/4.** **CRITICAL: the union merge had an OFFSET-COLLISION corruption bug (grafted sf entries kept stale `offset` → `xfs_dir2_sf_to_block` overlap → dir3_data corruption block 0x3fea88 + shutdown); FIXED in 7B66691E by re-assigning fresh monotonic offsets to grafted entries (xfs_dir2_sf_put_offset, counter from max disk-entry end). DO NOT revert — reintroduces corruption.** **8/tcp BLOCKERS: (1) dir_reuse off-by-one readdir=799 (durable single-entry lost-update, FLAKY across merge configs = the deep classic dir-block lost-update, NOT solely rebase); (2) BROAD 8-node intermittent flakiness — different tests wedge a node or two each run (strong_consistency/posix_multi/mmap/crash_consistency), then cascade. Merge-OFF runs ALSO flaked → pre-existing 8-node reliability issue. NEXT: the durable single-entry dir-block lost-update (readdir=799) is likely the UNIFYING bug (RMW-on-stale-base) that also wedges the other coherency tests at 8 nodes.** See ccmemory `sess21-FIX-union-merge...` (offset fix), `sess21-NEW-blocker-crash_consistency...`, `sess21-dir_reuse-8node-speed...`. — Original earlier-sess21 note: **UNION MERGE in `mxfs_dir_rebase_shortform` (xfs/xfs_mxfs_dlm.c ~3912)** reconciles the last tension: sess20's DCC2B8FD ownskip guard (skip rebase while dir holds un-checkpointed work) FIXED tcp_dlm_scaling rename-miss but REGRESSED dir_reuse (8-node off-by-one readdir=799, A/B PROVEN via param `dir_sf_rebase_ownskip`). Wholesale-adopt loses OUR entry; plain-skip loses PEER's entry; correct = UNION (disk ∪ our in-core-only entries). New params `dir_sf_rebase_merge=1` (the merge), `dir_sf_rebase_ownskip` (legacy). Both tcp_dlm_scaling (4/4×3) AND dir_reuse (8/8 CORRECT) now pass. Also: yield_basted lockless fast-skip + P58-AGSTATE gated behind instr (hot-path, negligible perf). **8/tcp STATUS: ALL coherency+scaling tests PASS 8/8; dir_reuse 8/8 CORRECT but ~332s standalone/~420s in-suite > old 300s blanket.** Per RULE-0 (budgets are workload-derived, not blanket; dir_reuse work is O(N), no native-XFS equiv, cost = NECESSARY coherent FUA I/O not waste — PROVEN exhaustively, near-linear 2.2× for 2× work), added workload-derived per-test budget in run.sh (`dir_reuse_coherency` N>4 → 60×N=480s) + TIMEOUT_BUDGETS.md row. **8/tcp full-suite (run B) = 11/17: crash_consistency FAIL(timeout)→CASCADE (dir_reuse/fence/fault/soak/tcp_dlm_scaling all cascade-fail after it). PRIMARY 8/tcp BLOCKER NOW = crash_consistency FLAKY IN-SUITE HANG: standalone=29s PASS 8/8, rsync_paired+crash_consistency=38s PASS, but in full suite it HUNG >300s (run B) though PASSED (run A) — a RARE race under full accumulated suite state (11 tests), NOT slow work. NOT caused by my changes (run.sh budget only touches dir_reuse; pre-existing flaky race).** dir_reuse's own fix is VALID (8/8 standalone); it only cascade-failed here. NEXT: fix crash_consistency flaky in-suite hang (reproduce via full suite or contaminating prefix; capture stuck phase = death-detect/replay/8-way-rejoin under accumulated DLM locks) → likely unblocks the whole tail. See ccmemory `sess21-NEW-blocker-crash_consistency-8node-straddles-300s-timeout` (head), `sess21-FIX-union-merge-rebase-shortform...`, `sess21-dir_reuse-8node-speed-is-fundamental...`. — Prior **sess20**: srcversion `4E047A49`. **BREAKTHROUGH: format-gated MHT resolved the 19-session tcp_dlm_scaling-vs-dir_reuse mht tradeoff.** New param `mxfs_dir_sf_mht_ms=100` (xfs_mxfs_dlm.c, both mht sites ~7693 + ~12296): SHORTFORM (LOCAL) dirs use 100ms (inline data → handoff coherent via whole-inode reload, no separate blocks to leave stale), leaf/btree dirs keep `inode_mht_ms=300`. tcp_dlm_scaling dir stays shortform (1-file churn) → fast (126s→39s); dir_reuse dir = 800-entry leaf/btree → keeps high mht → coherent. **STATUS at default: FULL `./run.sh 8 tcp` = 16/17** (tcp_dlm_scaling + ALL coherency tests PASS). 2 residuals, BOTH 8-node PERF (not correctness): (1) **dir_reuse in-suite 348s vs 300s cap** (277s standalone +71s cumulative; passes at TEST_TIMEOUT=420) — bound by per-handoff drain cost (log_force+iflush+blkdev_flush ~hundreds ms, Invariant-1, needed for coherency); (2) **dlm_scaling FLAKY 7/8** (one node >60s for 2000 journaled private-subdir ops = commit-latency under 8-node load). TODO: re-verify 1/2/4 tcp on this build (4/tcp running); 8/tcp needs dir_reuse speedup (deep: low-mht coherency fix → fast like tcp_dlm_scaling, OR cheaper drain) + dlm_scaling reliability. Also carries (default-off) `dir_postread_leaf_only` leaf-staleness fix (DABUF_MAP_HOLE root PROVEN = stale cached leaf). See ccmemory `sess20-BREAKTHROUGH-format-gated-mht...`, `sess20-8tcp-residuals-dir_reuse-348s...`, `sess20-8tcp-16of17...`. — Prior **sess58**: srcversion `60EFBE5E23421FC56645074`. ✅ **CRITERION MET: `./run.sh 2 tcp` = 17/17 on 8 CONSECUTIVE clean-reboot runs (100%).** Two roots PROVEN (RULE 4) + FIXED, both in `xfs/xfs_inode.c`: (1) **create↔remove AG↔dir ABBA deadlock** (tcp_dlm_scaling within-window/shutdown) — create(dialloc) & rename(preacquire) are AG→dir but remove was dir→AG; FIX = xfs_remove pre-acquires the child AG (standalone `mxfs_ag_dlm_lock`) before `xfs_trans_alloc_dir` → all AG→dir. (2) **durable dirent RESURRECTION** — `xfs_ilock_nowait` skips the DLM grant for ILOCK (IOLOCK-only atomic carve-out), so a DIR locked via the AIL-nowait path in `xfs_lock_two_inodes` (remove/link) and `xfs_lock_inodes` (rename) was modified+committed at a stale NL/PR mode (P58-DIRPIN-NONEX → P119-NONEX-FLUSH-SKIP → TDS-LEFTOVER); FIX = acquire the dir's DLM grant (`mxfs_dlm_ilock_begin`, DIR-only) in both nowait paths. KEPT detectors: P58-DIRPIN-NONEX (xfs_inode_item.c), P58-AGSTATE (xfs_mxfs_dlm.c). See ccmemory `sess58-CRITERION-MET-2tcp-17of17-8consecutive`, `sess58-FIX-create-remove-AG-dir-ABBA-deadlock`, `sess58-FIX-resurrection-lock-two-inodes-nowait-skips-dlm`. — Prior **sess49** (historical): srcversion `DBA88871C703D09B94BA06B`. **sess49 FIXED the 90-session cache_coherency uv blocker (P43 EX-gate) + got full `./run.sh 2 tcp` = 17/17 ONCE.** ROOT (PROVEN, raw-disk+dmesg, storage is COHERENT LIO not SCST): the `P43-DIR-FMTREVERT-SKIP` guard (xfs_mxfs_dlm.c ~6837 + P43B ~7112) wrongly refused a peer's LEGIT block→shortform shrink, pinning a stale BLOCK inode. FIX (KEEP): gate the guard on `dfr_dirty || (i_dlm_mode==EX)` — a PR/NL clean holder is a passive cacher that MUST adopt the durable disk shortform; only an EX owner keeps in-core. Validated cache_coherency 10/10 standalone + 17/17 full once. ⚠️ **CRITERION NOT MET (reliability)**: 3 clean full runs = 17/17, 16/17, 16/17. RESIDUAL = `tcp_dlm_scaling` + `dlm_fairness` (both PASS standalone, fail in-suite = contamination): durable shared-dir dirent RESURRECTION (sess10/sess17 root: TCP double-grant / stale-cached-EX concurrent RMW; read-side `sf_disk_check`+3-way-merge & write-side release-durability already correct — it's the DLM tenure-serialization, GPT verdict on file, not converged). FAST REPRO: `bash tests/tcp/repro_rename_drain.sh 150 8`. See ccmemory `sess49-ROOT-cache_coherency-uv-is-P43-fmtrevert-guard-misfire`, `sess49-residual-tcp-doublegrant-dir-resurrection-complete-diagnosis`. — Prior sess45 (historical): partial-iwrite fix (KEEP), **fixed the deep 90-session inode-cluster wedge.** ROOT (PROVEN, raw-disk evidence + probes): `mxfs_submit_partial_inode_write` (pal/linux/xfs_buf.c:1559, sess115 false-sharing protection) SKIPS writing free-on-buffer inode slots (di_mode==0) that aren't logged this round; a freshly-`xfs_ialloc_inode_init`'d chunk stamps all inodes magic-IN via an ORDERED buffer (v3 ICREATE, not physically logged) → the brand-new FREE inodes have no log item → SKIPPED → the reused block's old freed-data survives on disk (payload "node2-d4-f24-pay" found IN a free inode slot) → later xfs_iget read-verify fails → EFSCORRUPTED shutdown. **FIX (KEEP): in mxfs_submit_partial_inode_write, after the b_addr check, `if (bp->b_log_item && (bp->b_log_item->bli_flags & XFS_BLI_INODE_ALLOC_BUF)) return false;` (write WHOLE buffer — a freshly-allocated chunk has NO peer-owned slots).** Verified: crash_consistency 2/tcp 0/2→2/2; BADVERIFY=0. **⚠️ CRITERION NOT MET but close**: clean FULL `./run.sh 2 tcp` = **14 PASS / 3 FAIL** (was effectively 8/17 sess44). Remaining 3 (ALL node1/test1, dir/visibility/perf coherency family, NO cascade): (1) **cache_coherency** uv unlink-visibility — node1 sees 10 of node2's unlinked files (`uv none remain exp=0 got=10`), no shutdown; (2) **dir_reuse_coherency** `leaf-hash lookup_fail got=1` (1/145, leaf-hash hole, sess20/26 family), no shutdown; (3) **tcp_dlm_scaling** node1 trans_commit corruption shutdown (`__xfs_trans_commit:890 Corruption of in-memory data 0x8` ~round 41/150). Reliable repros (~90s, nodes stay reachable, crash_consistency does NOT crash — drop_caches only): reboot clean; `./run.sh 2 tcp <test>`. NOTE: a TaskStop'd mid-prep run contaminates the NEXT full run to 0/17 (false cascade) — always reboot clean between full runs. See ccmemory `sess45-FIX-partial-iwrite-skips-fresh-free-inodes-INODE_ALLOC_BUF`, `sess45-PROVEN-data-over-inode-doublealloc-clobber-payload-visible`, `sess45-next-blocker-dabuf-map-hole-dir-extent-xfs_create-shutdown`.
- **Prior build (sess39)**: srcversion `B090AF8CED0E5CE307134E9` (sync_iflush default OFF — whole-AIL push deadlocks; next session: per-AG push + redeploy after hard-reset) (= dir-data durability barrier + scoped inode-lock CAS backoff + di_size reload-skip fix [PROVEN+VALIDATED: stops a node clobbering its own just-created file size to 0 on BAST-reload] + 3 always-on detectors). **Remaining hard blocker: AG free-space double-allocation under concurrent rename → dir block shares a daddr with an inode cluster → EFSBADCRC shutdown. See sess39_lessons.md.** Prior tag `84458BF8` — **cache_coherency
  rename_visibility partially fixed: 40→~9-15 fails/240, no DLM-timeout shutdown.** sess38's
  "nearly done / 1-in-16 transient" claim was WRONG (an instr=1 ~100x-slowdown artifact that
  hides races — ALWAYS test with `instr=0`). The criterion fails because 4-node concurrent
  same-directory rename causes ON-DISK metadata corruption + FS shutdown (dir extent map points
  to an inode-cluster block = double-allocation → EFSBADCRC err74; AGI iunlink-list corruption
  with all CAW locks succeeding; SB write-verify corruption). Two fixes landed this session:
  (1) **dir-data-block durability barrier** in the dir-inode BAST release path
  (`mxfs_dir_data_durable`/`mxfs_dir_push_data_ags` in xfs_mxfs_dlm.c) — the old drain loop broke
  on the dinode's AIL/pin only, leaving dir DATA blocks dirty/in-flight (or cross-AG) at release
  → peer torn/stale read; (2) **scoped inode-lock CAS backoff** (`caw_inode_backoff` in
  dlm/dlm_caw.c, INODE locks only) — node-phased jitter desyncs the hot-slot CAS storm that
  exhausted MXFS_CAW_MAX_RETRIES=100 → -ETIMEDOUT → force-shutdown. Residuals (next session, see
  sess39_lessons.md): reader-side file empty-content (proved NOT writer durability), dir-block
  lost-update on EX-acquire, deep AG/alloc/SB corruption. Reproducers: tests/reset4.sh,
  tests/repro_rename_concurrent.sh, tests/char_rename.sh. Transport is CAW (not TCP).
- **v0.4.9 prior** (srcversion `DB50B8DB0DC35D761C9E90F`, sess38) — **concurrent-mkdir
  coherency FIXED, repro_modea 0/16** (was ~30-50%).  Root cause: a cross-node create-race loser's
  iget of the peer-allocated child inode read a STALE cached inode-cluster buffer
  (XBF_DONE|_XBF_FUA_FRESH, mode=0 from when the inode was free); the FUA gate skips re-reading a
  _XBF_FUA_FRESH buffer, so the peer's allocation was invisible → xfs_iget_check_free_state returns
  -ENOENT → stat/lookup fails.  xfs_lookup igets with lock_flags=0, so the existing stale-buffer
  invalidation (gated on dlm_acquired) was skipped.  FIX (xfs/xfs_icache.c xfs_iget_cache_miss):
  invalidate the stale cluster buffer for EVERY multi-node cache-miss read (no-op when not cached).
  Also carries 4 supporting fixes: block-dir read-time gen invalidation, writer-flush durability
  wait in bast_process, d_revalidate DISABLED (was destabilizing — per-lookup CAW poll caused barrier timeouts;
  kept in xfs_super.c but s_d_op commented), cross-AG new-inode + parent-dirent durability push
  in xfs_create.  Includes v0.4.7 below:
- **v0.4.7** (srcversion `1AFD468549F9289BF3B08DF`, sess38) — block-format-dir
  lost-update FIXED via read-time lazy generation-counter invalidation (deadlock-safe).
  In `xfs_da_read_buf` (xfs/libxfs/xfs_da_btree.c), BEFORE the read, a stale cached dir DATA
  block (b_mxfs_dir_gen < dp->i_dlm_dir_gen) is invalidated (clear XBF_DONE|_XBF_FUA_FRESH)
  so the sanctioned read path FUA-re-reads the peer's committed block.  Uses XBF_TRYLOCK
  (blocking lock deadlocks vs writeback/peer-grant) + dirty/pin/delwri guards (clearing
  XBF_DONE on a dirty buf → CORRUPT_INCORE shutdown).  i_dlm_dir_gen bumped on slow-path DLM
  reload for dirs (xfs_mxfs_dlm.c); gen=0 fast-path skips single-node entirely.
  Verified: cross_visibility coherent on 4 nodes, no wedge/shutdown; repro_barrier_coherency
  5/5 PASS (4-node concurrent distinct-entry add → converges <1s).
  NOTE: cache_coherency failures correlate with CONTAMINATED cluster state (orphaned procs
  after mid-run kills); always reboot all nodes to a clean slate before a criterion run.
  See memory sess37_lessons.md (sess38 section).
