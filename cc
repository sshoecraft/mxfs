. ~/.bashrc
#ccloop --model=fable --effort=max --cutoff=500 "get 1/2/4/8/16/32 node caw dlm multipath test working 100%" "."
#ccloop --model=fable --effort=max --cutoff=500 "get all 4 conditions in ./conditions.md working 100% up to 32 nodes" "."
ccloop --model=opusplan --effort=max --cutoff=500 "get all 4 conditions in ./conditions.md working 100% up to 32 nodes" "."
