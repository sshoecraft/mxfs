run.sh:567
-    # forever in an unattended loop -- the exact RULE 2b/2c failure shape.
+    # forever in an unattended loop -- the exact the permission-prompt rule/2c failure shape.
run.sh:1433
-    # 0/8 cascade FAILs.  The timeout stays a FAIL for THIS test (RULE 0); this
+    # 0/8 cascade FAILs.  The timeout stays a FAIL for THIS test (budget); this
run.sh:1576
-        # sess465: the artifact lives in the TREE (RULE 3), not /tmp, and the
+        # sess465: the artifact lives in the TREE (the source-tree rule), not /tmp, and the
run.sh:1901
-    # sess2(ccloop 26c41354): optional inter-test SETTLE — RULE-4 diagnostic for
+    # sess2(ccloop 26c41354): optional inter-test SETTLE — instrumented diagnostic for
tools/mkfs_mxfs.c:1404
-     * sess389 (D-RSYNC-LAP-PACE-AG-SHARING-388, RULE-5 ruling): the kernel
+     * sess389 (D-RSYNC-LAP-PACE-AG-SHARING-388, design-consult ruling): the kernel
tools/chk_mxfs.c:4008
-         * audit (RULE-5 ruling) needs the ino/mode/nlink/gen of each
+         * audit (design-consult ruling) needs the ino/mode/nlink/gen of each
tools/chk_mxfs.c:4083
-             * ZERO-CORE-408, RULE-5 verification item "inobt allocated
+             * ZERO-CORE-408, design-consult verification item "inobt allocated
tools/chk_mxfs.c:5201
- * D-QUARANTINED-SLOT-EXHAUSTS-CLUSTER-ADMISSION-376, sess377 RULE-5 ruling
+ * D-QUARANTINED-SLOT-EXHAUSTS-CLUSTER-ADMISSION-376, sess377 design-consult ruling
tools/chk_mxfs.c:5759
- * that, and the sess377 RULE-5 ruling fixes its shape:
+ * that, and the sess377 design-consult ruling fixes its shape:
tools/prep_tcm_node_scst.sh:52
-#     which RULE 2 reserves for the user).  The measured guest timeout here was
+#     which the never-reboot-the-host rule reserves for the user).  The measured guest timeout here was
tools/prep_tcm_node_scst.sh:55
-#     does NOT mask mxfs slowness: the per-iter test budget (RULE 0) still fails
+#     does NOT mask mxfs slowness: the per-iter test budget (budget) still fails
tools/recov_forge.c:16
- * The sess333 RULE-5 review demanded pre-disposition checks that a MALFORMED
+ * The sess333 design-consult review demanded pre-disposition checks that a MALFORMED
tools/recover_wedged_domain.sh:3
-# process cannot be killed, WITHOUT resetting the host (RULE 2 forbids that).
+# process cannot be killed, WITHOUT resetting the host (the never-reboot-the-host rule forbids that).
tools/recover_wedged_domain.sh:32
-# RULE 2: this NEVER reboots or sysrqs the host.  It restarts libvirtd/virtlogd,
+# the never-reboot-the-host rule: this NEVER reboots or sysrqs the host.  It restarts libvirtd/virtlogd,
tools/mxfs_revive_node.sh:4
-# wedged, WITHOUT rebooting clyde (RULE 2).
+# wedged, WITHOUT rebooting clyde (the never-reboot-the-host rule).
tools/mxfs_revive_node.sh:31
-# RULE 0: every step is bounded.  RULE 2b: no rm on a variable or glob path.
+# budget: every step is bounded.  the permission-prompt rule: no rm on a variable or glob path.
tools/mxfs_revive_node.sh:32
-# RULE 2c: no `pgrep -f`, no unbounded dmsetup/umount.
+# the unkillable-wedge rule: no `pgrep -f`, no unbounded dmsetup/umount.
tools/mxfs_revive_node.sh:203
-echo "    unreclaimable until clyde is reset by a human (RULE 2)."
+echo "    unreclaimable until clyde is reset by a human (the never-reboot-the-host rule)."
tools/clyde_wedge_diag.sh:2
-# clyde_wedge_diag.sh — RULE-2c-safe triage of a wedged clyde.
+# clyde_wedge_diag.sh — the unkillable-wedge rule-safe triage of a wedged clyde.
tools/clyde_wedge_diag.sh:13
-# Per RULE 2 this script NEVER reboots, shuts down or sysrqs the host.  It only
+# Per the never-reboot-the-host rule this script NEVER reboots, shuts down or sysrqs the host.  It only
tools/clyde_wedge_diag.sh:114
-echo "Per RULE 2 a host reset is the USER'S call — document and report, never act."
+echo "Per the never-reboot-the-host rule a host reset is the USER'S call — document and report, never act."
tools/clyde_kmsg_guard.sh:29
-# Deliberately NOT done, and why (RULE 2 / RULE 2c):
+# Deliberately NOT done, and why (the never-reboot-the-host rule / the unkillable-wedge rule):
tools/clyde_kmsg_guard.sh:93
-    # Cheap and RULE-2c-safe.  No cmdline, no maps, no pgrep -f, no ps aux.
+    # Cheap and the unkillable-wedge rule-safe.  No cmdline, no maps, no pgrep -f, no ps aux.
tools/clyde_kmsg_guard.sh:121
-        echo "cause is understood.  RULE 2: only the user reboots clyde."
+        echo "cause is understood.  the never-reboot-the-host rule: only the user reboots clyde."
tools/clyde_kmsg_guard.sh:139
-        # only adds another stuck task (RULE 2c).
+        # only adds another stuck task (the unkillable-wedge rule).
tools/clyde_crash_latch.sh:82
-RULE 2: only the user reboots clyde.
+the never-reboot-the-host rule: only the user reboots clyde.
tools/mxfs_admit_check.sh:9
-# sess377 RULE-5 ruling: "serialize against re-registration for the departing
+# sess377 design-consult ruling: "serialize against re-registration for the departing
tools/scratch_build.sh:19
-# RULE 0: a from-scratch build of the tree measured 3-5 min at -j$(nproc).
+# budget: a from-scratch build of the tree measured 3-5 min at -j$(nproc).
tools/sole_survivor_audit.py:198
-    # The classes are the ones a RULE-5 ruling named (sess574).  The axis is
+    # The classes are the ones a design-consult ruling named (sess574).  The axis is
scripts/README.md:15
-These scripts live in the source tree per `CLAUDE.md` RULE 3
+These scripts live in the source tree per `CLAUDE.md` the source-tree rule
scripts/README.md:83
-`bench/` per RULE 3 if still in use, or deleted if obsolete.
+`bench/` so it survives a reboot if still in use, or deleted if obsolete.
scripts/cluster_mkfs_mount.sh:3
-# (use cluster_reset_n.sh first).  Per RULE 3 this lives in the source tree.
+# (use cluster_reset_n.sh first).  Per the source-tree rule this lives in the source tree.
scripts/cluster_reset_n.sh:4
-# Per RULE 3, this lives in the source tree because each session needs the same flow.
+# Per the source-tree rule, this lives in the source tree because each session needs the same flow.
scripts/sess36_e1_xinit_durability.sh:9
-# Per RULE 3: persistent script lives in source tree.
+# Per the source-tree rule: persistent script lives in source tree.
scripts/rig.sh:28
-# RULE 3: this is cluster-management infrastructure; it lives in scripts/.
+# the source-tree rule: this is cluster-management infrastructure; it lives in scripts/.
scripts/run_concurrent_mkdir.sh:3
-# This is the canonical sess34 test runner.  Per RULE 3 it lives in the source tree.
+# This is the canonical sess34 test runner.  Per the source-tree rule it lives in the source tree.
scripts/sess34_repro.sh:5
-# This is the canonical sess34 repro recipe.  Per RULE 3 it lives in source tree
+# This is the canonical sess34 repro recipe.  Per the source-tree rule it lives in source tree
scripts/sess35_capture.sh:9
-# Per RULE 3: persistent script lives in source tree.
+# Per the source-tree rule: persistent script lives in source tree.
scripts/sess36_quickstart.sh:9
-# Per RULE 3: persistent script lives in source tree.
+# Per the source-tree rule: persistent script lives in source tree.
scripts/sess36_capture_with_correlation.sh:10
-# Per RULE 3.
+# Per the source-tree rule.
scripts/sess36_e1b_concurrent_xinit.sh:7
-# Per RULE 3: persistent script lives in source tree.
+# Per the source-tree rule: persistent script lives in source tree.
scripts/diag_2node_rsync.sh:2
-# diag_2node_rsync.sh — RULE 4 instrumentation for the scaling_curve /
+# diag_2node_rsync.sh — instrumentation for the scaling_curve /
scripts/diag_par_rsync.sh:2
-# diag_par_rsync.sh — RULE 4 instrumentation for the scaling_curve
+# diag_par_rsync.sh — instrumentation for the scaling_curve
scripts/diag_logforce_profile.sh:2
-# diag_logforce_profile.sh — RULE 4 step 2 for the scaling_curve
+# diag_logforce_profile.sh — instrument step 2 for the scaling_curve
scripts/diag_vnop_profile.sh:2
-# diag_vnop_profile.sh — RULE 4 step 2 (continued) for the scaling_curve
+# diag_vnop_profile.sh — instrument step 2 (continued) for the scaling_curve
scripts/diag_logforce_stacks.sh:2
-# diag_logforce_stacks.sh — RULE 4 instrumentation for the scaling_curve
+# diag_logforce_stacks.sh — instrumentation for the scaling_curve
scripts/diag_funcgraph.sh:2
-# diag_funcgraph.sh — RULE 4 instrumentation: function_graph trace of a
+# diag_funcgraph.sh — instrumentation: function_graph trace of a
scripts/diag_stacksample.sh:2
-# diag_stacksample.sh — RULE 4 instrumentation: off-CPU profile of the
+# diag_stacksample.sh — instrumentation: off-CPU profile of the
scripts/diag_inodelock_callers.sh:2
-# diag_inodelock_callers.sh — RULE 4 instrumentation for the scaling_curve
+# diag_inodelock_callers.sh — instrumentation for the scaling_curve
scripts/ccloop_reset.sh:7
-# (RULE 2: rebooting test VMs is allowed; only the host is off-limits) and waits
+# (the never-reboot-the-host rule: rebooting test VMs is allowed; only the host is off-limits) and waits
scripts/klog_tail.sh:6
-# wedge-era kernel log — the primary evidence for a RULE-4 diagnosis.
+# wedge-era kernel log — the primary evidence for a instrumented diagnosis.
scripts/suite_cycle_run.sh:12
-# RULE 0: healthy 4/tcp suite wall is ~11-13 min; the 1100s cap makes a hung
+# budget: healthy 4/tcp suite wall is ~11-13 min; the 1100s cap makes a hung
scripts/scst_setup.sh:130
-# deterministically, and the guard's number must not be widened (RULE 2d).
+# deterministically, and the guard's number must not be widened (the host-guard rule).
scripts/caw_ladder_fua.sh:5
-# RULE 3: lives in tree so it survives reboots.
+# the source-tree rule: lives in tree so it survives reboots.
scripts/caw_ladder_fua.sh:15
-  # No outer timeout wrapper: run.sh enforces per-test RULE-0 budgets
+  # No outer timeout wrapper: run.sh enforces per-test derived time budgets
scripts/probe_sweep.sh:4
-# run (RULE 4 evidence step): a PASS with any probe hit is NOT clean —
+# run (instrumented evidence step): a PASS with any probe hit is NOT clean —
scripts/revalidate_cell.sh:3
-# CURRENT build with fresh prep, cleared dmesg rings, RULE-0-derived
+# CURRENT build with fresh prep, cleared dmesg rings, the budget rule-derived
scripts/revalidate_cell.sh:15
-# TEST_TIMEOUT (per-test budget, RULE 0):
+# TEST_TIMEOUT (per-test budget, the budget rule):
scripts/ladder_rung.sh:41
-# Host hygiene (RULE 4, 2026-07-20): 1/tcp fio_perf_vs_xfs + fio_vs_xfs_baseline
+# Host hygiene (instrumented, 2026-07-20): 1/tcp fio_perf_vs_xfs + fio_vs_xfs_baseline
scripts/raw_fio_ceiling.sh:20
-# RULE 3: measurement infrastructure lives in scripts/.
+# the source-tree rule: measurement infrastructure lives in scripts/.
scripts/run_adhoc_suite_test.sh:4
-# criteria.json/manifest.  For RULE-4 diagnostic tests that must not become
+# criteria.json/manifest.  For instrumented diagnostic tests that must not become
scripts/tcp16_dircrc_repro.sh:2
-# tcp16_dircrc_repro.sh — RULE-4 repro driver for the tcp/16 stale-dir-read
+# tcp16_dircrc_repro.sh — instrumented repro driver for the tcp/16 stale-dir-read
scripts/tcp16_dircrc_repro.sh:10
-# run output into tests/logs/tcp16_forensics_r<iter>/ (persistent, RULE 3).
+# run output into tests/logs/tcp16_forensics_r<iter>/ (persistent, the source-tree rule).
scripts/vm_cycle.sh:13
-# RULE 0: a VM boots to ssh in 60-120 s; 240 s per node bound.
+# budget: a VM boots to ssh in 60-120 s; 240 s per node bound.
scripts/ccloop_behavior_audit.py:7
-  #4 handoff tier (RULE 8) -- how often is .ccloop/handoff.md FRESH at the
+  #4 handoff tier (the queue-integrity rule) -- how often is .ccloop/handoff.md FRESH at the
scripts/ccloop_behavior_audit.py:9
-  #5 RULE 7 (Read tool)    -- Read-tool calls vs Bash file-reads per session
+  #5 the Read-tool rule (Read tool)    -- Read-tool calls vs Bash file-reads per session
scripts/ccloop_behavior_audit.py:149
-    print("=== #4 handoff tier (RULE 8) ===")
+    print("=== #4 handoff tier (the queue-integrity rule) ===")
scripts/ccloop_behavior_audit.py:159
-    print("\n=== #5 RULE 7 (Read tool vs Bash file-reads) ===")
+    print("\n=== #5 the Read-tool rule (Read tool vs Bash file-reads) ===")
scripts/ccloop_delegation_audit.py:4
-The Fable pool is metered in REQUESTS (cost-audit banner).  RULE 10 exists to
+The Fable pool is metered in REQUESTS (cost-audit banner).  the delegation rule exists to
scripts/clyde_preflight.sh:39
-# Every check here is cheap, read-only, and RULE-2c-safe: it never runs
+# Every check here is cheap, read-only, and the unkillable-wedge rule-safe: it never runs
scripts/clyde_preflight.sh:127
-    bad "  RULE 2: a session never reboots clyde.  Report it and stop."
+    bad "  the never-reboot-the-host rule: a session never reboots clyde.  Report it and stop."
scripts/clyde_preflight.sh:133
-# maps (RULE 2c: those take each task's mmap_lock and hang forever on a task
+# maps (the unkillable-wedge rule: those take each task's mmap_lock and hang forever on a task
scripts/freepub_claim_chain.py:7
-the RULE-5 ruling asks to be proven per inode:
+the design-consult ruling asks to be proven per inode:
scripts/inode_dio_release/inode_dio_release.c:227
-	 * semantics — never hand-roll the count arithmetic (GPT RULE-5
+	 * semantics — never hand-roll the count arithmetic (design-consult
tests/catch_rename_fail.sh:5
-# good); on FAIL we stop and the caller resets.  RULE 3: lives in the tree.
+# good); on FAIL we stop and the caller resets.  the source-tree rule: lives in the tree.
tests/repro_fua_always.sh:2
-# sess12 (ccloop 4eef1f39): RULE-4 experiment — does forcing AG-meta reads FUA
+# sess12 (ccloop 4eef1f39): instrumented experiment — does forcing AG-meta reads FUA
tests/tcp_lkt_doublegrant.sh:2
-# RULE-4 decisive test: is the tcp_dlm_scaling MODE-B resurrection a TCP dir-EX
+# Instrumented decisive test: is the tcp_dlm_scaling MODE-B resurrection a TCP dir-EX
tests/sf_mkdir_storm.sh:49
-# RULE 0 budget: a round is N concurrent mkdirs in one directory; native XFS
+# derived time budget: a round is N concurrent mkdirs in one directory; native XFS
tests/repro_fdw_only_loop.sh:5
-# Per RULE 3 this lives in the source tree (reusable for any future rare-bug
+# Per the source-tree rule this lives in the source tree (reusable for any future rare-bug
tests/drc_phase_profile.sh:7
-# and asserts it fits at least DRC_MIN_ROUNDS=8 reuse rounds (RULE 0: slowness
+# and asserts it fits at least DRC_MIN_ROUNDS=8 reuse rounds (budget: slowness
tests/openunlink_deaths.sh:15
-# Budget note (RULE 0): each case = fence detect (lease ~6-15s) + replay +
+# Budget note (budget): each case = fence detect (lease ~6-15s) + replay +
tests/hb_guard_clobber_probe.sh:5
-# WHY THIS EXISTS (sess77 GPT RULE-5 ruling, D-FENCED-STAGE-WITHOUT-PROVEN-
+# WHY THIS EXISTS (sess77 design-consult ruling, D-FENCED-STAGE-WITHOUT-PROVEN-
tests/excl_lapse_probe.sh:9
-#   evidence that the host stays fenced (GPT RULE-5 ruling, sess93 follow-up:
+#   evidence that the host stays fenced (design-consult ruling, sess93 follow-up:
tests/excl_lapse_probe.sh:35
-# TIMING (RULE 0 — derived)
+# TIMING (the budget rule — derived)
tests/dirty_slice_release_repro.sh:20
-# Budget (RULE 0): mkfs 4G loop ~2s + mount/umount ~2s + module reload ~5s;
+# Budget (budget): mkfs 4G loop ~2s + mount/umount ~2s + module reload ~5s;
tests/p32e_fence_ab.sh:35
-# RULE 0: this script sets NO timeout of its own around run.sh -- run.sh
+# budget: this script sets NO timeout of its own around run.sh -- run.sh
tests/fence_capability_admission.sh:32
-# Arms 2 and 3 matter as much as arm 1.  The sess93 RULE-5 ruling requires that
+# Arms 2 and 3 matter as much as arm 1.  The sess93 design-consult ruling requires that
tests/fence_capability_admission.sh:45
-# RULE 0 (derived): one arm = rmmod + insmod + a refused mount + a dmesg
+# the budget rule (derived): one arm = rmmod + insmod + a refused mount + a dmesg
tests/fence_stage3_real.sh:5
-# The sess133 RULE-5 ruling is explicit that the sg_persist/prprobe A/B (stages
+# The sess133 design-consult ruling is explicit that the sg_persist/prprobe A/B (stages
tests/fence_stage3_real.sh:6
-# (i) and (ii)) is NOT RULE-6 closure evidence, because it does not exercise the
+# (i) and (ii)) is NOT defect-bar closure evidence, because it does not exercise the
tests/fence_stage3_real.sh:50
-# RULE 0 budgets, each derived rather than picked:
+# derived time budgets, each derived rather than picked:
tests/d513_lone_mount_refusal.sh:43
-# RULE 0 budget for the lone mount: the barrier pays the full dead-confirm
+# derived time budget for the lone mount: the barrier pays the full dead-confirm
tests/fence_live_node.sh:37
-# RULE 0: ssh bounded (20 s); preempt + observation bounded (WITHDRAW_S);
+# budget: ssh bounded (20 s); preempt + observation bounded (WITHDRAW_S);
tests/fence_live_node.sh:38
-# per-node rc files.  RULE 2c: no pgrep/ps.  RULE 3: lives in tests/.
+# per-node rc files.  the unkillable-wedge rule: no pgrep/ps.  the source-tree rule: lives in tests/.
tests/d512_t1_reuse.sh:23
-# RULE 0 (derived): arm1 ~15s + arm2 ~15s + dmesg 6s => ~36s.  Bound 90s.
+# the budget rule (derived): arm1 ~15s + arm2 ~15s + dmesg 6s => ~36s.  Bound 90s.
tests/d512_t2_pause.sh:21
-# RULE 0 (derived): per stage ~10s (setup 2 + pause 3 + reads 2 + rider 3)
+# the budget rule (derived): per stage ~10s (setup 2 + pause 3 + reads 2 + rider 3)
tests/tcp_token_plumbing_verify.sh:23
-# RULE 0 (derived): the workload is 3 x FILES small-file ops in a PRIVATE
+# the budget rule (derived): the workload is 3 x FILES small-file ops in a PRIVATE
tests/sess422_chain.sh:20
-# RULE 0: build 500 + proof 500 + tauth 120 + 3 preps x 300 + token 400 +
+# budget: build 500 + proof 500 + tauth 120 + 3 preps x 300 + token 400 +
tests/sess425_chain.sh:3
-# DOUBLE-GRANT page wedge fix) on the CURRENT fleet.  Same shape and RULE 0
+# DOUBLE-GRANT page wedge fix) on the CURRENT fleet.  Same shape and the budget rule
tests/sess425_chain.sh:14
-# RULE 0: build 500 + proof 500 + tauth 180 + 3 preps x 300 + 4 sweeps x 60
+# budget: build 500 + proof 500 + tauth 180 + 3 preps x 300 + 4 sweeps x 60
tests/lone_mount_create.sh:20
-# RULE 0 budget: fleet umount sweep <=20 s (32 nodes parallel, 12 s each),
+# derived time budget: fleet umount sweep <=20 s (32 nodes parallel, 12 s each),
tests/lone_mount_create.sh:23
-# ARMS (4th arg, sess432 step 2 — RULE-5 ruling's directed coverage):
+# ARMS (4th arg, sess432 step 2 — design-consult ruling's directed coverage):
tests/sess433_lap2_d0355.sh:2
-# sess433: second clean lap of the D-0355 LUN arms on 0.40.0 (RULE 6 closure
+# sess433: second clean lap of the D-0355 LUN arms on 0.40.0 (defect-bar closure
tests/chk_guard_inprogress_verify.sh:14
-# RULE 0 budget: umount sweep 20 s, save/forge/restore ~3 s each (SG_IO),
+# derived time budget: umount sweep 20 s, save/forge/restore ~3 s each (SG_IO),
tests/sess445_chain38_0512_sf_to_block_void.sh:3
-# SLICE-REFUSED-0512 — RULE 4 step 2 (measure the producer, not the corpse).
+# SLICE-REFUSED-0512 — instrument step 2 (measure the producer, not the corpse).
tests/sess445_chain38_0512_sf_to_block_void.sh:17
-# RULE 0: prep 79-120 s (measured); handoff_anatomy 32x4 creates ~10 s burst +
+# budget: prep 79-120 s (measured); handoff_anatomy 32x4 creates ~10 s burst +
tests/sess447_chain53_0514_fix_verify.sh:4
-# the rig, then the RULE 4 verification:
+# the rig, then the instrumented verification:
tests/sess447_chain53_0514_fix_verify.sh:12
-# RULE 0: build ~180 s (300); prep 80 (300); lap F ~290 s measured chain 52
+# budget: build ~180 s (300); prep 80 (300); lap F ~290 s measured chain 52
tests/sess449_chain68_pr_restamp.sh:5
-# the P303 string.  RULE 0: harness bound 300 s (derived in its header);
+# the P303 string.  budget: harness bound 300 s (derived in its header);
tests/dirshard_stage1_selftest.sh:17
-# RULE 0 budgets (derived): sharded mkdir N=16 = 18 transactions < 2 s,
+# derived time budgets (derived): sharded mkdir N=16 = 18 transactions < 2 s,
tests/cas_nocaw_arms.sh:2
-# tests/cas_nocaw_arms.sh — sess460, RULE-5 review #5 condition 4 (runtime
+# tests/cas_nocaw_arms.sh — sess460, design-consult review #5 condition 4 (runtime
tests/cas_nocaw_arms.sh:50
-# RULE 0 budgets: heartbeat/release = 62 s stale + fence + recovery ~80 s +
+# derived time budgets: heartbeat/release = 62 s stale + fence + recovery ~80 s +
tests/sess467_chain101_fln_churn_alive.sh:12
-# RULE 0: prep 300 (measured 86-117 s); fln churn 420 (chain 11 shape: prep
+# budget: prep 300 (measured 86-117 s); fln churn 420 (chain 11 shape: prep
tests/sess468_chain104_joiner_rerun.sh:12
-# RULE 0: prep 300 (80-117 s measured); joiner 560 (chain 94 measured 189 s
+# budget: prep 300 (80-117 s measured); joiner 560 (chain 94 measured 189 s
tests/d0535_xattr_reuse.sh:26
-# RULE 0: every step is a handful of creates/xattr ops (< 1 s native); the
+# budget: every step is a handful of creates/xattr ops (< 1 s native); the
tests/mxfs_sb_bytecmp.sh:3
-# MXFS envelope image (sess474, D-0133 RULE-5 verification bar: the SB summary
+# MXFS envelope image (sess474, D-0133 design-consult verification bar: the SB summary
tests/d482_phantom_epoch_2node.sh:33
-# RULE 0 (derived): prep 50 s measured (2/tcp QNAP) + setup 10 s + pause 5 s +
+# the budget rule (derived): prep 50 s measured (2/tcp QNAP) + setup 10 s + pause 5 s +
tests/ftrace_create_probe.sh:3
-# create on one mounted node (D-0349 RULE 4 step 2 instrument).
+# create on one mounted node (D-0349 instrument step 2 instrument).
tests/ftrace_create_probe.sh:12
-# RULE 0: <count> creates at the measured ~10 ms each = count/100 s; the ssh
+# budget: <count> creates at the measured ~10 ms each = count/100 s; the ssh
tests/leaf_scan_ab.sh:14
-# RULE 0 (derived): create_grow_probe bounds itself at total*30/1000+20 s
+# the budget rule (derived): create_grow_probe bounds itself at total*30/1000+20 s
tests/cross_grant_workload.sh:16
-# RULE 0 (derived): COUNT creates at 7-20 ms each on the two-node rig =
+# the budget rule (derived): COUNT creates at 7-20 ms each on the two-node rig =
tests/cluster_restart_nomkfs.sh:20
-# RULE 0 (derived): workload ~10 s; unload ~10 s; joins 1-5 s healthy, bound
+# the budget rule (derived): workload ~10 s; unload ~10 s; joins 1-5 s healthy, bound
tests/ghost_slot_restart_probe.sh:24
-# RULE 0 (derived): boot ~30-60 s per VM (parallel); dead window 31 x 2 s =
+# the budget rule (derived): boot ~30-60 s per VM (parallel); dead window 31 x 2 s =
tests/d0944_unpub_owned_meta_ab.sh:34
-# RULE 0 budget: prep 2 nodes measured ~90 s (bounded 300 s), lap measured 13 s
+# derived time budget: prep 2 nodes measured ~90 s (bounded 300 s), lap measured 13 s
tests/tcp_lkt_dirtrace.sh:2
-# RULE-4: capture the SHARED-DIR inode's clean cross-node P-LKT timeline at a
+# Instrumented: capture the SHARED-DIR inode's clean cross-node P-LKT timeline at a
tests/suite_iter.sh:27
-# assertion — pace lives in run.sh's per-test budgets (RULE 0).  1100s fit a
+# assertion — pace lives in run.sh's per-test budgets (budget).  1100s fit a
tests/d3_dirring.sh:56
-    # 3. cache_coherency (budget enforced inside run.sh; RULE 0)
+    # 3. cache_coherency (budget enforced inside run.sh; the budget rule)
tests/drc_loop.sh:7
-# RULE 0 budget: each iter = prep(mkfs/mount/load ~20s) + test(~284s) ~= 306s;
+# derived time budget: each iter = prep(mkfs/mount/load ~20s) + test(~284s) ~= 306s;
tests/inode_reuse_typeflip.sh:38
-# RULE 0 budget: a round is per_node creates + per_node dir churn cycles in one
+# derived time budget: a round is per_node creates + per_node dir churn cycles in one
tests/ysr_dd_ab.sh:11
-# RULE 0 budget: dirent_durability 240s manifest budget + ~60s overhead.
+# derived time budget: dirent_durability 240s manifest budget + ~60s overhead.
tests/racebail_loss_join.sh:38
-# RULE 0: derived from the manifest budget plus measured harness overhead.
+# budget: derived from the manifest budget plus measured harness overhead.
tests/openunlink_matrix.sh:20
-# RULE 0 note: reap convergence is paced by MXFS_REAP_FIRST_MS=5s /
+# the budget rule note: reap convergence is paced by MXFS_REAP_FIRST_MS=5s /
tests/openunlink_outage.sh:15
-# Budget (RULE 0): setup 20s + destroy 5s + boot 2x ~120s (parallel) +
+# Budget (budget): setup 20s + destroy 5s + boot 2x ~120s (parallel) +
tests/fence_evidence_probe.sh:24
-#      evidence for the sess93 RULE-5 ruling Q1: "one durable intent, one
+#      evidence for the sess93 design-consult ruling Q1: "one durable intent, one
tests/fence_evidence_probe.sh:40
-# TIMING (RULE 0 — derived, not chosen)
+# TIMING (the budget rule — derived, not chosen)
tests/mxfs_stackprof.py:5
-code did, not where the wall went.  RULE 4 needs wall time tied to a NAMED
+code did, not where the wall went.  Instrumented needs wall time tied to a NAMED
tests/bastq_snap.sh:16
-# RULE 3: lives in the tree, not /tmp — this gets re-run every measurement.
+# the source-tree rule: lives in the tree, not /tmp — this gets re-run every measurement.
tests/p248_census.sh:6
-# sess153 RULE-5 fleet requirement: >=3x 32-node prep cycles, zero P248
+# sess153 design-consult fleet requirement: >=3x 32-node prep cycles, zero P248
tests/clean_depart_mixed_death.sh:30
-# RULE 0 budget: preflight+slots 20s + I/O spin-up 8s + death declare 62s +
+# derived time budget: preflight+slots 20s + I/O spin-up 8s + death declare 62s +
tests/closure_purge_scrub.sh:3
-# verification (sess363 RULE-5 ruling, landed sess374 / 0.14.3).
+# verification (sess363 design-consult ruling, landed sess374 / 0.14.3).
tests/closure_purge_scrub.sh:52
-# RULE 0 budget: the probe is one `touch` in the root dir.  Native XFS does
+# derived time budget: the probe is one `touch` in the root dir.  Native XFS does
tests/closure_purge_scrub.sh:281
-#     a FAIL, not a retry (RULE 0).
+#     a FAIL, not a retry (budget).
tests/lba_probe.sh:5
-# sess379 (D-HOT-SLOT-CAW-SERIALIZES-LUN-PER-LBA-379): the RULE-5 review
+# sess379 (D-HOT-SLOT-CAW-SERIALIZES-LUN-PER-LBA-379): the design-consult review
tests/closure_fault_matrix.sh:2
-# closure_fault_matrix.sh — the sess363 RULE-5 ruling's Hazards-§7 fault
+# closure_fault_matrix.sh — the sess363 design-consult ruling's Hazards-§7 fault
tests/rig_after.sh:9
-# stderr go there too.  RULE 0: <max-wait-s> is the CALLER's derived bound
+# stderr go there too.  budget: <max-wait-s> is the CALLER's derived bound
tests/d513_forged_record_checks.sh:2
-# d513_forged_record_checks.sh — the sess333 RULE-5 "pre-rig unit checks" for
+# d513_forged_record_checks.sh — the sess333 design-consult "pre-rig unit checks" for
tests/d513_forged_record_checks.sh:45
-# RULE 0 budget.  Measured on this rig: umount ~2 s, a clean mount ~8-15 s.
+# derived time budget.  Measured on this rig: umount ~2 s, a clean mount ~8-15 s.
tests/rsync_stall_stacks.sh:30
-# Detached in-node sampler.  pgrep -x matches comm only (safe per RULE 2c).
+# Detached in-node sampler.  pgrep -x matches comm only (safe per the unkillable-wedge rule).
tests/d0286_tcp_wedge.sh:3
-# (sess418, 0.29.0 departure state machine; sess416 RULE-5 ruling "Shape B
+# (sess418, 0.29.0 departure state machine; sess416 design-consult ruling "Shape B
tests/d0286_tcp_wedge.sh:31
-# RULE 0 (derived): T8 ~370s (its own derivation) + 5 dmesg sweeps ~15s +
+# the budget rule (derived): T8 ~370s (its own derivation) + 5 dmesg sweeps ~15s +
tests/d0286_race_chain.sh:10
-# RULE 0 (derived, 32/caw): prep 236 s measured (cap 300) + lap <=135 s
+# the budget rule (derived, 32/caw): prep 236 s measured (cap 300) + lap <=135 s
tests/no_survivor_crash_replay.sh:32
-# RULE 0 (derived, FIRST MEASUREMENT — record the wall): payload 32 x 64 KiB
+# the budget rule (derived, FIRST MEASUREMENT — record the wall): payload 32 x 64 KiB
tests/no_survivor_crash_replay.sh:148
-# per-slice timeline for the RULE 0 record
+# per-slice timeline for the the budget rule record
tests/sess423_chain.sh:8
-# RULE 0: 3 x prep 300 + 180 + 220 + 160 = 1460 s.
+# budget: 3 x prep 300 + 180 + 220 + 160 = 1460 s.
tests/lone_crash_replay.sh:5
-# Shape (RULE-5 ruling sess432, Q4): node A mounts the shared LUN ALONE
+# Shape (design-consult ruling sess432, Q4): node A mounts the shared LUN ALONE
tests/lone_crash_replay.sh:20
-# RULE 0 budget: fleet umount sweep 20 s, A mount 15 s, workload 2 s, destroy
+# derived time budget: fleet umount sweep 20 s, A mount 15 s, workload 2 s, destroy
tests/sess426_chain.sh:17
-# RULE 0: build 500 + proof 500 + tauth 180 + 4 preps x 300 + dre 90 +
+# budget: build 500 + proof 500 + tauth 180 + 4 preps x 300 + dre 90 +
tests/free_foreign_realloc_repro.sh:2
-# free_foreign_realloc_repro.sh — RULE 4 reproducer for the s433 (0.39.0)
+# free_foreign_realloc_repro.sh — instrumented reproducer for the s433 (0.39.0)
tests/free_foreign_realloc_repro.sh:32
-# Budget (RULE 0): N=100 x (dd+sync+rm+dd+rm) ~ 100 x 60 ms = 6 s native;
+# Budget (budget): N=100 x (dd+sync+rm+dd+rm) ~ 100 x 60 ms = 6 s native;
tests/sess432_board_03913.sh:5
-# for EVERY node, so the whole board is the regression check (RULE-5 ruling
+# for EVERY node, so the whole board is the regression check (design-consult ruling
tests/sess432_board_03913.sh:7
-# RULE 0: prep 300, params 60, board 1400) with the sweep extended by the
+# budget: prep 300, params 60, board 1400) with the sweep extended by the
tests/sess434_chain3_0410.sh:8
-#   lone_rsync_bench x2                 (RULE 0 vs the chain-2b 0.40.1 baseline, 120 s each)
+#   lone_rsync_bench x2                 (the budget rule vs the chain-2b 0.40.1 baseline, 120 s each)
tests/sess442_chain28_0480_bootstrap_resume.sh:2
-# sess442 chain 28: build 0.48.0 (item 5d fixes from the RULE-5 code review +
+# sess442 chain 28: build 0.48.0 (item 5d fixes from the design-consult code review +
tests/sess444_chain33_0510_icreate_refuse_negative.sh:8
-# cannot verify.  RULE 6 F&V for D-0510 needs this arm exercised, not only
+# cannot verify.  the zero-defect bar F&V for D-0510 needs this arm exercised, not only
tests/sess445_chain43_0511_prefetch_ring.sh:8
-# RULE 0: prep 79-120 s (300); bootstrap_full_restart (1080); prep; run.sh
+# budget: prep 79-120 s (300); bootstrap_full_restart (1080); prep; run.sh
tests/umount_under_quarantine.sh:31
-#      mass-umount wall <= UMOUNT_BUDGET (RULE 0: 31-way clean umount measured
+#      mass-umount wall <= UMOUNT_BUDGET (budget: 31-way clean umount measured
tests/umount_under_quarantine.sh:41
-# RULE 0 (derived): srcgate+arm 30 s; churn 6 s; fence+refuse+publish+import
+# the budget rule (derived): srcgate+arm 30 s; churn 6 s; fence+refuse+publish+import
tests/umount_under_quarantine.sh:138
-[ "$UW" -le "$UMOUNT_BUDGET" ] && pass "mass umount wall ${UW}s <= ${UMOUNT_BUDGET}s" || fail "mass umount wall ${UW}s > ${UMOUNT_BUDGET}s (RULE 0)"
+[ "$UW" -le "$UMOUNT_BUDGET" ] && pass "mass umount wall ${UW}s <= ${UMOUNT_BUDGET}s" || fail "mass umount wall ${UW}s > ${UMOUNT_BUDGET}s (budget)"
tests/d513_write_eio_containment.sh:41
-# RULE 0: load 20 + kill + HB confirm ~62 s + fence + replay + publish +
+# budget: load 20 + kill + HB confirm ~62 s + fence + replay + publish +
tests/sess460_chain86_review6_conditions.sh:5
-# next RULE-5 review (#6) needs.  Gated on chain 84 (tests/sess459_chain84_
+# next design-consult review (#6) needs.  Gated on chain 84 (tests/sess459_chain84_
tests/sess460_chain86_review6_conditions.sh:22
-# RULE 0: budgets from each harness header (see there); build 600; prep 300.
+# budget: budgets from each harness header (see there); build 600; prep 300.
tests/sess455_chain78_0611.sh:5
-# departure-gate rework (RULE-5 STOP-SHIP items 1-10, docs/pr-fencing-departure.md
+# departure-gate rework (design-consult STOP-SHIP items 1-10, docs/pr-fencing-departure.md
tests/sess455_chain78_0611.sh:16
-# RULE 0 budgets: build measured 326 s in tree (chain 77) → 600; prep 300;
+# derived time budgets: build measured 326 s in tree (chain 77) → 600; prep 300;
tests/sess460_chain88_matrix_resolve_census.sh:17
-# RULE 0: build 600; prep 300; slot_cost 32x8 measured ~2-3 min -> 300;
+# budget: build 600; prep 300; slot_cost 32x8 measured ~2-3 min -> 300;
tests/sess465_chain94_d0524_freeob_race.sh:3
-# FAILCLOSED-SHUTDOWN-0524 — RULE 4 verification of the 0.63.1 entry-
+# FAILCLOSED-SHUTDOWN-0524 — instrumented verification of the 0.63.1 entry-
tests/sess465_chain94_d0524_freeob_race.sh:24
-# RULE 0: build 420 (measured 311-332 s), tools 120, prep 8 nodes 240
+# budget: build 420 (measured 311-332 s), tools 120, prep 8 nodes 240
tests/sess488_p126_producer.sh:25
-# RULE 0 budgets, derived: prep 300 (measured 106-146 s); each cc row 160
+# derived time budgets, derived: prep 300 (measured 106-146 s); each cc row 160
tests/sess462_chain92_d0522_crashcuts_verify.sh:17
-# RULE 0 budgets are chain 86's: prep 300, workerhang 200, workerhangheld 240,
+# derived time budgets are chain 86's: prep 300, workerhang 200, workerhangheld 240,
tests/sess473_chain115_d0533_dirshard_06419.sh:13
-#      arm A peer list / arm B peer rmdir, RULE 0 ceiling 300 ms per listing
+#      arm A peer list / arm B peer rmdir, 2x-native-XFS ceiling 300 ms per listing
tests/sess473_chain115_d0533_dirshard_06419.sh:19
-# RULE 0 budgets: install 30; prep 300 (80-123 s measured); selftest 240;
+# derived time budgets: install 30; prep 300 (80-123 s measured); selftest 240;
tests/sess468_chain105_intentsA_burst.sh:19
-# RULE 0: prep 300 (80-117 s measured); burst/clean 180 (chain 93 measured
+# budget: prep 300 (80-117 s measured); burst/clean 180 (chain 93 measured
tests/sess470_chain109_dirshard_06411.sh:2
-# sess470 chain 109: dir-sharding stage 1 re-run on frozen 0.64.12 (0.64.11 + the RULE-5 review items: probe split, locator removed in the holder-free txn) — the three
+# sess470 chain 109: dir-sharding stage 1 re-run on frozen 0.64.12 (0.64.11 + the design-consult review items: probe split, locator removed in the holder-free txn) — the three
tests/sess470_chain109_dirshard_06411.sh:19
-# RULE 0 budgets: install 30; prep 300 (80-117 s measured); selftest 240;
+# derived time budgets: install 30; prep 300 (80-117 s measured); selftest 240;
tests/sess480_chain121_lab_build_06437.sh:23
-# RULE 0: the macro touches one object, so this is one compile plus a relink of
+# budget: the macro touches one object, so this is one compile plus a relink of
tests/sess480_chain125_iclus_matrix_anchored.sh:37
-# RULE 0: install 30 s; prep 300 s (measured 88-117 s); pre-flight 120 s (two
+# budget: install 30 s; prep 300 s (measured 88-117 s); pre-flight 120 s (two
tests/sess480_chain127_fastpoll_ab.sh:36
-# RULE 0: prep 300 s (measured 88-117 s); the cc row keeps its UNCHANGED 90 s
+# budget: prep 300 s (measured 88-117 s); the cc row keeps its UNCHANGED 90 s
tests/sess487_chain139_persig_ab.sh:22
-# GPT RULE-5 ruling (sess487): make the flush RELEASE-ONLY first -- the largest
+# Design-consult ruling (sess487): make the flush RELEASE-ONLY first -- the largest
tests/sess487_chain139_persig_ab.sh:56
-# RULE 0 budgets, derived: prep 300 (measured 106-146 s); each cc row 160
+# derived time budgets, derived: prep 300 (measured 106-146 s); each cc row 160
tests/sess487_chain139_persig_ab.sh:164
-    [ "$rc" = 124 ] && echo "  RULE 0: the row set hit its wrapper budget — a result, not a number to widen."
+    [ "$rc" = 124 ] && echo "  budget: the row set hit its wrapper budget — a result, not a number to widen."
tests/sess493_d0492_crash_durability.sh:42
-# RULE 0 budgets, derived: prep 300 (measured 113-127 s); rounds R x 2 x NF
+# derived time budgets, derived: prep 300 (measured 113-127 s); rounds R x 2 x NF
tests/sameboot_remount.sh:25
-# RULE 0: 7 mount/umount cycles at <= 25 s each => bound 200 s.
+# budget: 7 mount/umount cycles at <= 25 s each => bound 200 s.
tests/sameboot_remount.sh:141
-# departure work to report before capturing, bounded well inside RULE 0
+# departure work to report before capturing, bounded well inside the budget rule
tests/tauth_slot_reuse.sh:27
-# RULE 0: expiry+replay on this rig measures ~80 s (tcp_death_replay
+# budget: expiry+replay on this rig measures ~80 s (tcp_death_replay
tests/live_holder_wait.sh:17
-# RULE 0 (derived): setup ~5 s + PAUSE_MS + drain (< 10 s) + captures ~10 s.
+# the budget rule (derived): setup ~5 s + PAUSE_MS + drain (< 10 s) + captures ~10 s.
tests/peer_truncate_under_append_2node.sh:21
-# RULE 0 (derived): prep 55 s measured (2/tcp QNAP) + loop <= 3 s + capture
+# the budget rule (derived): prep 55 s measured (2/tcp QNAP) + loop <= 3 s + capture
tests/hot_inode_peer_unlink_2node.sh:21
-# RULE 0 (derived): prep 55 s measured (2/tcp QNAP) + create 1 s + loop ~3.5 s
+# the budget rule (derived): prep 55 s measured (2/tcp QNAP) + create 1 s + loop ~3.5 s
tests/create_grow_probe.sh:9
-# RULE 0: measured 0.75.60 on the 2/tcp rig = 6-7 ms per create small, 13-22
+# budget: measured 0.75.60 on the 2/tcp rig = 6-7 ms per create small, 13-22
tests/d0930_root_iget_bound.sh:25
-# RULE 0 (derived): leave <= 20 s per node; deploy <= 20 s; B join <= 60 s
+# the budget rule (derived): leave <= 20 s per node; deploy <= 20 s; B join <= 60 s
tests/d0944_death_rejoin_ab.sh:32
-# RULE 0 budget: prep measured 50 s (bounded 300 s), death lap measured 76 s
+# derived time budget: prep measured 50 s (bounded 300 s), death lap measured 76 s
tests/dir_node_middle_free_2node.sh:18
-# RULE 0 (derived): A's ENTRIES creates at ~7 ms = 9 s for 1200; B's prime
+# the budget rule (derived): A's ENTRIES creates at ~7 ms = 9 s for 1200; B's prime
tests/d0932_fence_takeover_probe.sh:38
-# RULE 0 (derived, measured today): prep 48 s (bound 300); B's death detected
+# the budget rule (derived, measured today): prep 48 s (bound 300); B's death detected
tests/sole_survivor_gate_probe.sh:50
-# RULE 0 (derived, not rounded): VM destroy+start+boot ~150 s (measured 152 s
+# the budget rule (derived, not rounded): VM destroy+start+boot ~150 s (measured 152 s
tests/dirent_visibility_2node.sh:39
-# RULE 0 (derived, for the clock-aligned shape): each round is one 6 s wall-clock
+# the budget rule (derived, for the clock-aligned shape): each round is one 6 s wall-clock
tests/d0941_inode_reuse_estale.sh:51
-# RULE 0 (derived): native XFS writes 128 small files in well under a second; the
+# the budget rule (derived): native XFS writes 128 small files in well under a second; the
tests/d0941_inode_reuse_estale.sh:154
-    [ "$wall" -le 60 ] || bad_round=1        # RULE 0: a timeout IS a failure
+    [ "$wall" -le 60 ] || bad_round=1        # budget: a timeout IS a failure
tests/d0521_own_slice_cross_stamp_probe.sh:50
-# RULE 0 (derived, measured parts named): prep 45 s (bound 300); phase-1 is
+# the budget rule (derived, measured parts named): prep 45 s (bound 300); phase-1 is
tests/d0945_inofree_poison_ab.sh:31
-# RULE 0 budget, derived from the measured agmeta_shutdown_retire steps: prep
+# derived time budget, derived from the measured agmeta_shutdown_retire steps: prep
tests/d0952_sole_create_rejoin_coherency.sh:42
-# RULE 0 budget, measured on this rig 2026-09-10 (2-node TCP, QNAP LUN):
+# derived time budget, measured on this rig 2026-09-10 (2-node TCP, QNAP LUN):
tests/host_tmp_clean.sh:8
-# wiped on reboot by design (RULE 3), so nothing here is durable evidence —
+# wiped on reboot by design (the source-tree rule), so nothing here is durable evidence —
tests/recov_takeover_doublefault_probe.sh:44
-# TIMING (RULE 0 — derived, not chosen)
+# TIMING (the budget rule — derived, not chosen)
tests/caw_pw_selftest.sh:44
-# RULE 0 budgets (native op is ms; walls are ssh + boot dominated):
+# derived time budgets (native op is ms; walls are ssh + boot dominated):
tests/cc_yieldcensus.sh:4
-# WHY (ccloop c7ee71c6 sess130, GPT RULE-5 closure requirement for
+# WHY (ccloop c7ee71c6 sess130, design-consult closure requirement for
tests/tcp_scaling_capture.sh:3
-# nodes' dmesg at the failing run.  RULE-4 evidence collector.
+# nodes' dmesg at the failing run.  Instrumented evidence collector.
tests/creator_baseline_ab.sh:29
-# RULE 0.  Every timeout below is budget + measured harness overhead, never a
+# the budget rule.  Every timeout below is budget + measured harness overhead, never a
tests/creator_stale_base.sh:9
-#   measured.  RULE 6 is explicit that "cannot reproduce" is not a disposition,
+#   measured.  the zero-defect bar is explicit that "cannot reproduce" is not a disposition,
tests/creator_stale_base.sh:58
-# RULE 0 budget: a round is 63 mkdirs spread over N nodes into one directory;
+# derived time budget: a round is 63 mkdirs spread over N nodes into one directory;
tests/closure_reuse_directed.sh:18
-# exercised separately"; the RULE-5 review refuted it — resource_hash_raw
+# exercised separately"; the design-consult review refuted it — resource_hash_raw
tests/closure_reuse_directed.sh:86
-# ABA ARM (sess376 RULE-5 review, question 3).  The default arm proves the
+# ABA ARM (sess376 design-consult review, question 3).  The default arm proves the
tests/cc_phase_attrib.sh:6
-# its RULE-0 budget the harness records only NO_TERMINAL_RECORD=N, which says
+# its derived time budget the harness records only NO_TERMINAL_RECORD=N, which says
tests/cc_dirvis_probe.sh:13
-# Loops until a short count is seen or $ITERS exhausted.  RULE 3: lives in-tree.
+# Loops until a short count is seen or $ITERS exhausted.  the source-tree rule: lives in-tree.
tests/mass_umount_stall_probe.sh:3
-# RULE-4 step 2 instrumentation run.
+# instrument step 2 instrumentation run.
tests/mass_umount_stall_probe.sh:22
-# RULE 0: native XFS umount of a quiescent fs is <1s.  The budget asserted
+# budget: native XFS umount of a quiescent fs is <1s.  The budget asserted
tests/mass_umount_stall_probe.sh:25
-# hang the harness; any node over BUDGET_S is reported as a RULE 0 FAIL with
+# hang the harness; any node over BUDGET_S is reported as a the budget rule FAIL with
tests/mass_umount_stall_probe.sh:162
-    # sess380: the CONCLUSIVE per-LBA control the sess379 RULE-5 review
+    # sess380: the CONCLUSIVE per-LBA control the sess379 design-consult review
tests/mass_umount_stall_probe.sh:352
-echo "=== mass_umount_stall_probe FAIL (RULE 0) — $NOVER/$N node(s) over ${BUDGET_S}s, max=${MAXW}s ==="
+echo "=== mass_umount_stall_probe FAIL (budget) — $NOVER/$N node(s) over ${BUDGET_S}s, max=${MAXW}s ==="
tests/d513_fswide_abort_preserves_death.sh:38
-# RULE 0: both mounts pay the ~62 s dead-confirm window for the frozen peer.
+# budget: both mounts pay the ~62 s dead-confirm window for the frozen peer.
tests/d513_forged_matrix.sh:25
-# RULE 0: measured 2026-08-20, every shape completes in 13-20 s (the mount
+# budget: measured 2026-08-20, every shape completes in 13-20 s (the mount
tests/agifc_churn_experiment.sh:2
-# agifc_churn_experiment.sh — RULE-4 experiment driver for the sess399
+# agifc_churn_experiment.sh — instrumented experiment driver for the sess399
tests/agifc_churn_experiment.sh:14
-# RULE 0: every step is bounded by its MEASURED wall (prep 83 s measured sess398
+# budget: every step is bounded by its MEASURED wall (prep 83 s measured sess398
tests/agifc_churn_experiment.sh:17
-# RULE 2c: all remote calls bounded, per-node rc captured, no pgrep -f.
+# the unkillable-wedge rule: all remote calls bounded, per-node rc captured, no pgrep -f.
tests/tmpfile_churn_ftrace.sh:3
-# O_TMPFILE churn loop (D-TMPFILE-CHURN-RULE0-PERF-400) with the kernel's
+# O_TMPFILE churn loop (D-TMPFILE-CHURN-the budget rule-PERF-400) with the kernel's
tests/tmpfile_churn_ftrace.sh:17
-# RULE 4: this is the instrumentation step for the sess400 finding that the
+# Instrumented: this is the instrumentation step for the sess400 finding that the
tests/tmpfile_churn_ftrace.sh:21
-# RULE 0: iteration budget is derived on the spot (2 x native + ssh), not
+# budget: iteration budget is derived on the spot (2 x native + ssh), not
tests/tmpfile_churn_ftrace.sh:23
-# RULE 2c: one bounded ssh per phase; the python loop runs under `timeout`.
+# the unkillable-wedge rule: one bounded ssh per phase; the python loop runs under `timeout`.
tests/tmpfile_churn_ftrace.sh:24
-# RULE 3: lives in tests/.
+# the source-tree rule: lives in tests/.
tests/fleet_set_params.sh:16
-# RULE 0: each ssh is bounded (40 s); the whole fleet runs in parallel.
+# budget: each ssh is bounded (40 s); the whole fleet runs in parallel.
tests/fleet_set_params.sh:17
-# RULE 2c: no pgrep -f / ps; per-node rc files; nothing unbounded.
+# the unkillable-wedge rule: no pgrep -f / ps; per-node rc files; nothing unbounded.
tests/fleet_set_params.sh:18
-# RULE 3: lives in tests/.
+# the source-tree rule: lives in tests/.
tests/sess415_verify_0282.sh:7
-# Stage caps (RULE 0, derived):
+# Stage caps (budget, derived):
tests/sess428_chain.sh:15
-# RULE 0: wait <= 1800 + build 500 + proof 500 + tools 120 + tauth 240 +
+# budget: wait <= 1800 + build 500 + proof 500 + tools 120 + tauth 240 +
tests/d0287_remaster_measure.sh:2
-# d0287_remaster_measure.sh — RULE-4 step-2 MEASUREMENT for
+# d0287_remaster_measure.sh — instrumented step-2 MEASUREMENT for
tests/d0287_remaster_measure.sh:30
-# RULE 0 (derived, revised sess418 run 1): the disklock monitor expires a
+# the budget rule (derived, revised sess418 run 1): the disklock monitor expires a
tests/d0287_remaster_measure.sh:136
-# THE VERDICT LINES (RULE 4): the defect is MEASURED if either fires.
+# THE VERDICT LINES (instrumented): the defect is MEASURED if either fires.
tests/sess433_chain_0400.sh:11
-# RULE 0: every bound is the harness's own derived budget; the board wait is
+# budget: every bound is the harness's own derived budget; the board wait is
tests/crossnode_unlink_ubsweep.sh:31
-# RULE 0 (derived): J umount 30 s + setup 15 s + U umount 30 s + J mount
+# the budget rule (derived): J umount 30 s + setup 15 s + U umount 30 s + J mount
tests/dialloc_disklive_inject.sh:2
-# dialloc_disklive_inject.sh — RULE 4 harness for the D-0351 dialloc
+# dialloc_disklive_inject.sh — instrumented harness for the D-0351 dialloc
tests/dialloc_disklive_inject.sh:3
-# CONTAINMENT (two-phase candidate validation, 0.39.3; RULE-5 ruling ccmemory
+# CONTAINMENT (two-phase candidate validation, 0.39.3; design-consult ruling ccmemory
tests/dialloc_disklive_inject.sh:31
-# Budget (RULE 0): 16 creates + 4 syncs ~ 1 s native; sweeps ~ 10 s.
+# Budget (budget): 16 creates + 4 syncs ~ 1 s native; sweeps ~ 10 s.
tests/sess434_chain6_0411.sh:15
-#   lone_rsync_bench                 RULE 0 (120 s)
+#   lone_rsync_bench                 the budget rule (120 s)
tests/sess435_chain9_rman_vergate.sh:8
-#   tests/rman_matrix.sh  all 9 arms (RULE 0: each arm carries its own wrap 260-330 s)
+#   tests/rman_matrix.sh  all 9 arms (budget: each arm carries its own wrap 260-330 s)
tests/cc_tenure_modesplit.py:17
-Persisted per RULE 3 (tests/ harness, not /tmp). See sess436 evidence dir
+Persisted so it survives a reboot (tests/ harness, not /tmp). See sess436 evidence dir
tests/sess436_chain2_0417_intents_mht.sh:3
-# deterministic intents burst arm, then the RULE-5 ruling's measurement 3
+# deterministic intents burst arm, then the design-consult ruling's measurement 3
tests/iclus_relmark_faults.sh:3
-# certificate (0.55.0, sess448; RULE-5 ruling ccmemory
+# certificate (0.55.0, sess448; design-consult ruling ccmemory
tests/iclus_relmark_faults.sh:35
-# RULE 0: each arm bounded like rman_matrix arms (measured 188-325 s on
+# budget: each arm bounded like rman_matrix arms (measured 188-325 s on
tests/iclus_relmark_faults.sh:36
-# 0.41.2; 300 s wrap + the fault delay for the crash arms).  RULE 2c: every
+# 0.41.2; 300 s wrap + the fault delay for the crash arms).  the unkillable-wedge rule: every
tests/iclus_relmark_faults.sh:37
-# remote call bounded, per-node rc files.  RULE 3: lives in tests/.
+# remote call bounded, per-node rc files.  the source-tree rule: lives in tests/.
tests/sess447_chain52_0514_forced_repro.sh:3
-# RULE 4 step 2 for D-TWO-VICTIM-DEATH-SECOND-SLICE-REPLAY-NOT-STARTED-0514.
+# instrument step 2 for D-TWO-VICTIM-DEATH-SECOND-SLICE-REPLAY-NOT-STARTED-0514.
tests/sess447_chain52_0514_forced_repro.sh:10
-# RULE 0: prep 300; harness churn 11 + gap 20 + detect 62 + replay 5 + park
+# budget: prep 300; harness churn 11 + gap 20 + detect 62 + replay 5 + park
tests/sess447_chain57_pending_rows.sh:7
-# node_death_replay row (chains 55/56 run it separately).  RULE 0: budgets sum
+# node_death_replay row (chains 55/56 run it separately).  budget: budgets sum
tests/fleet_probe_sweep.sh:4
-# from that one capture; per-node output + rc files (RULE 2c), parallel, 25 s
+# from that one capture; per-node output + rc files (the unkillable-wedge rule), parallel, 25 s
tests/sess448_chain63_p6p8_rows.sh:7
-# while the 24 with a window measured zero hits.  RULE 0: 240 + 60 + 60 + 12x3
+# while the 24 with a window measured zero hits.  budget: 240 + 60 + 60 + 12x3
tests/sess448_chain59_iclus_relmark_lab.sh:3
-# D-INODE-CLUSTER-PUBLISH-WITHOUT-AUTHORITY; RULE-5 ruling ccmemory
+# D-INODE-CLUSTER-PUBLISH-WITHOUT-AUTHORITY; design-consult ruling ccmemory
tests/sess448_chain59_iclus_relmark_lab.sh:17
-# RULE 0 bounds: incremental build 300 s (chain 55 measured well inside),
+# the budget rule bounds: incremental build 300 s (chain 55 measured well inside),
tests/sess448_chain64_matrix_r8.sh:5
-# (sess448 RULE-5 ruling; D-0288 containment).  RULE 0: matrix measured 34-35 s
+# (sess448 design-consult ruling; D-0288 containment).  budget: matrix measured 34-35 s
tests/caw_samenode_selftest.sh:11
-# RULE-6 closure vehicle for D-SAMENODE-WAITER-CANCEL-COLLISION and its
+# defect-bar closure vehicle for D-SAMENODE-WAITER-CANCEL-COLLISION and its
tests/caw_samenode_selftest.sh:44
-# RULE 0: per arm = 2 s stagger + 14 s hold + <=8 s owed discharge + ssh
+# budget: per arm = 2 s stagger + 14 s hold + <=8 s owed discharge + ssh
tests/sess451_chain70_retire_pending.sh:2
-# sess451 chain 70: 0.59.1 RETIRE_PENDING — the sess450 RULE-5 STOP-SHIP fixes
+# sess451 chain 70: 0.59.1 RETIRE_PENDING — the sess450 design-consult STOP-SHIP fixes
tests/sess451_chain70_retire_pending.sh:14
-# RULE 0: restamp 300 s, crash 330 s, sameboot 200 s, joiner 270 s (derived
+# budget: restamp 300 s, crash 330 s, sameboot 200 s, joiner 270 s (derived
tests/sess454_chain75_admission_arms.sh:9
-# RULE 0: vergate loop arm ~90 s (header) -> 120; fence_cap 3 arms measured
+# budget: vergate loop arm ~90 s (header) -> 120; fence_cap 3 arms measured
tests/sess456_chain80_d0517_lab_postmark.sh:3
-# DANGLING-0517 — RULE 4 step 2 (instrumented reproduction).  Gated on chain 79
+# DANGLING-0517 — instrument step 2 (instrumented reproduction).  Gated on chain 79
tests/sess456_chain80_d0517_lab_postmark.sh:22
-# RULE 0: build 600 (measured 286-326 s in tree), prep 300, lap 420 (harness
+# budget: build 600 (measured 286-326 s in tree), prep 300, lap 420 (harness
tests/depart_crash_cuts.sh:2
-# tests/depart_crash_cuts.sh — sess460, RULE-5 review #5 condition 3 (ccmemory
+# tests/depart_crash_cuts.sh — sess460, design-consult review #5 condition 3 (ccmemory
tests/depart_crash_cuts.sh:48
-# RULE 0 budgets: arm+umount-to-cut <= 20 s; cut 1 peers 62 s stale + fence +
+# derived time budgets: arm+umount-to-cut <= 20 s; cut 1 peers 62 s stale + fence +
tests/sess460_chain87_ndr_streak_0616.sh:11
-# RULE 0: per NDR lap prep 300 + row 500 (measured 342-392 s); board ~12 min
+# budget: per NDR lap prep 300 + row 500 (measured 342-392 s); board ~12 min
tests/sess459_chain82_d0517_fix_verify_lab.sh:3
-# DANGLING-0517 — RULE 4 step 2b (fix verification).  Gated on chain 81 DONE
+# DANGLING-0517 — instrument step 2b (fix verification).  Gated on chain 81 DONE
tests/sess459_chain82_d0517_fix_verify_lab.sh:18
-# RULE 0: build 600 (measured 286-326 s), prep 300, lap 420 + 120 sweep,
+# budget: build 600 (measured 286-326 s), prep 300, lap 420 + 120 sweep,
tests/sess465_chain95_samenode_bastguard.sh:8
-# P275-SAMENODE-BAST-HELD).  RULE-6 closure vehicle for
+# P275-SAMENODE-BAST-HELD).  defect-bar closure vehicle for
tests/sess465_chain95_samenode_bastguard.sh:13
-# RULE 0: build 420 (measured 311-332 s), tools 120, prep 300 (measured
+# budget: build 420 (measured 311-332 s), tools 120, prep 300 (measured
tests/sess470_chain111_d488_unlock_exits.sh:2
-# sess470 chain 111: D-488 unlock-exit arms (RULE-5 disposition ruling,
+# sess470 chain 111: D-488 unlock-exit arms (design-consult disposition ruling,
tests/sess470_chain111_d488_unlock_exits.sh:8
-# RULE 0: install 30; prep 300; each arm 240 (measured shape < 3 min).
+# budget: install 30; prep 300; each arm 240 (measured shape < 3 min).
tests/sess474_chain116_d0133_sb_recount.sh:3
-# after a CLEAN fleet unmount) — the RULE-4 measurement + fix lap on the
+# after a CLEAN fleet unmount) — the instrumented measurement + fix lap on the
tests/sess474_chain116_d0133_sb_recount.sh:17
-# RULE 0: prep 300 (86-118 measured); reuse 10 laps 150 (152 s for 20);
+# budget: prep 300 (86-118 measured); reuse 10 laps 150 (152 s for 20);
tests/sess474_chain116_d0133_sb_recount.sh:28
-# sess474 (RULE-5 A+ verification bar): rotate the worker pair per lap so the
+# sess474 (design-consult A+ verification bar): rotate the worker pair per lap so the
tests/sess482_chain129_create_ladder_F.sh:50
-# RULE 0 budgets, derived rather than rounded.  Chain 124 measured the full
+# derived time budgets, derived rather than rounded.  Chain 124 measured the full
tests/sess487_chain137_create_split.sh:52
-# RULE 0 budgets, derived: build 600 s (measured 37 s scratch incremental,
+# derived time budgets, derived: build 600 s (measured 37 s scratch incremental,
tests/sess490_untokened_verify.sh:25
-# RULE 0 budgets, derived: prep 300 (measured 106-146 s); each cc row 160
+# derived time budgets, derived: prep 300 (measured 106-146 s); each cc row 160
tests/sess495_chain140_board_07013.sh:20
-# RULE 0 budget: the last complete board (0.64.37, sess480 chain 123) took
+# derived time budget: the last complete board (0.64.37, sess480 chain 123) took
tests/sess495_chain140_board_07013.sh:24
-# 1470 s: a board that overruns it is a RULE 0 failure to diagnose, never a
+# 1470 s: a board that overruns it is a budget failure to diagnose, never a
tests/rejoin_residue.sh:54
-# RULE 0: an armed cycle = leave (~5 s) + rejoin (6-19 s measured, 40 s
+# budget: an armed cycle = leave (~5 s) + rejoin (6-19 s measured, 40 s
tests/cluster_slot_clobber_2node.sh:36
-# RULE 0: per lap ~6 s (three syncs + drop_caches + reads); N laps x 6 s +
+# budget: per lap ~6 s (three syncs + drop_caches + reads); N laps x 6 s +
tests/sess583_chain_controls.sh:30
-# RULE 0 (derived from each harness's own stated bound, not rounded):
+# the budget rule (derived from each harness's own stated bound, not rounded):
tests/sess570_chain_ghost.sh:23
-# RULE 0 (derived, measured 2026-09-09): prep 47 s (bound 300); workload 11 s
+# the budget rule (derived, measured 2026-09-09): prep 47 s (bound 300); workload 11 s
tests/shared_unlink_pace.sh:12
-#   this in single-digit milliseconds, so under RULE 0 it is disqualifying on its
+#   this in single-digit milliseconds, so under the budget rule it is disqualifying on its
tests/shared_unlink_pace.sh:30
-# RULE 0 BUDGET
+# the budget rule BUDGET
tests/shared_unlink_pace.sh:52
-# previous run's tree (RULE 2b: no rm on a variable path from this script).
+# previous run's tree (the permission-prompt rule: no rm on a variable path from this script).
tests/shared_unlink_pace.sh:172
-# RULE 0: 2x native XFS is the ceiling.  Native does this in <100 ms total.
+# budget: 2x native XFS is the ceiling.  Native does this in <100 ms total.
tests/drc_capture_clobber.sh:4
-# node's dmesg into the SOURCE TREE (RULE 3: survives the reboot that wipes /tmp
+# node's dmesg into the SOURCE TREE (the source-tree rule: survives the reboot that wipes /tmp
tests/vergate.sh:34
-# RULE 0 budgets: loop arms ~90s; hb arms ~120s.
+# derived time budgets: loop arms ~90s; hb arms ~120s.
tests/ag_strand_repair.sh:13
-#   The fix then NEVER RAN — no strand recurred after it landed.  RULE 6 will
+#   The fix then NEVER RAN — no strand recurred after it landed.  the zero-defect bar will
tests/ag_strand_repair.sh:32
-# WHAT COUNTS AS PASS (all four, per RULE 6)
+# WHAT COUNTS AS PASS (all four, per the zero-defect bar)
tests/ag_strand_repair.sh:53
-# RULE 0 budget: the injected strands must be repaired and released inside the
+# derived time budget: the injected strands must be repaired and released inside the
tests/trace_reads.sh:4
-# buffer the 32-node dlm_scaling op-loop re-reads ~18x/op (RULE 4).
+# buffer the 32-node dlm_scaling op-loop re-reads ~18x/op (instrumented).
tests/pr_reregister_probe.sh:12
-#   GPT (RULE-5 ruling sess93, release-blocking requirement B): "A successful
+#   GPT (design-consult ruling sess93, release-blocking requirement B): "A successful
tests/ds_probe.sh:3
-# op, to locate where the ~22ms/op at 32 nodes goes (RULE 4 instrumentation).
+# op, to locate where the ~22ms/op at 32 nodes goes (instrumentation).
tests/reset2.sh:11
-# PROHIBITED (CLAUDE.md RULE 2) — VM reboot via the system libvirt URI is fine
+# PROHIBITED (CLAUDE.md the never-reboot-the-host rule) — VM reboot via the system libvirt URI is fine
tests/drc_pace_ab.sh:5
-# (mxfs.caw_pr_defer_max_ms), stated in the units RULE 0 cares about: how many
+# (mxfs.caw_pr_defer_max_ms), stated in the units the budget rule cares about: how many
tests/d385_publication_verify.sh:8
-# control arm's 167, which is nowhere near enough exposure to satisfy RULE 6
+# control arm's 167, which is nowhere near enough exposure to satisfy the zero-defect bar
tests/d385_publication_verify.sh:30
-# RULE 0: budgets are derived from MEASURED walls, never padded.  Re-derive
+# budget: budgets are derived from MEASURED walls, never padded.  Re-derive
tests/d385_publication_verify.sh:33
-# RULE 2c: no `pgrep -f`, no unbounded ssh; every remote call is bounded and
+# the unkillable-wedge rule: no `pgrep -f`, no unbounded ssh; every remote call is bounded and
tests/d385_publication_verify.sh:185
-        # overhead but every row lands and keeps its RULE 0 budget inside
+        # overhead but every row lands and keeps its derived time budget inside
tests/d385_publication_verify.sh:249
-    print("NOT CLOSED. RULE 6 keeps this defect OPEN:")
+    print("NOT CLOSED. the zero-defect bar keeps this defect OPEN:")
tests/iunl_mismatch_negative.sh:4
-# mismatch test", sess396 RULE-5 ruling; injector landed sess402:
+# mismatch test", sess396 design-consult ruling; injector landed sess402:
tests/iunl_mismatch_negative.sh:22
-# RULE 0 (derived): open 200 tmpfiles ~0.2 s + close 200 ~1-2 s (5-8 ms each,
+# the budget rule (derived): open 200 tmpfiles ~0.2 s + close 200 ~1-2 s (5-8 ms each,
tests/iunl_mismatch_negative.sh:23
-# D-400 numbers) -> node wall budget 20 s; whole script <= 90 s.  RULE 2c: every
+# D-400 numbers) -> node wall budget 20 s; whole script <= 90 s.  the unkillable-wedge rule: every
tests/iunl_mismatch_negative.sh:24
-# remote call bounded.  RULE 3: lives in tests/.  Leaves the knob at 0.  If the
+# remote call bounded.  the source-tree rule: lives in tests/.  Leaves the knob at 0.  If the
tests/rman_prover_kill.sh:21
-# RULE 0: every ssh bounded (20 s), fleet sweep in parallel; virsh bounded.
+# budget: every ssh bounded (20 s), fleet sweep in parallel; virsh bounded.
tests/rman_prover_kill.sh:22
-# RULE 2c: per-node rc files, no pgrep/ps.  RULE 3: lives in tests/.
+# the unkillable-wedge rule: per-node rc files, no pgrep/ps.  the source-tree rule: lives in tests/.
tests/sess415_verify_0283.sh:6
-# Stage caps (RULE 0): prep 300 (new srcversion, full prep), d512 gate 100,
+# Stage caps (budget): prep 300 (new srcversion, full prep), d512 gate 100,
tests/d512_t8_inject.sh:30
-# RULE 0 (derived): srcgate 10s + kind2 ~45s + 3 destructive triggers
+# the budget rule (derived): srcgate 10s + kind2 ~45s + 3 destructive triggers
tests/sess416_board_0286.sh:11
-# (RULE 0): prep 300, params 60, board 1400 (sess415-derived walls).
+# (budget): prep 300, params 60, board 1400 (sess415-derived walls).
tests/d0286_depart_race.sh:27
-# RULE 0 (derived): srcgate 5s + umount <=45s + 4 sweeps ~10s + P=1's
+# the budget rule (derived): srcgate 5s + umount <=45s + 4 sweeps ~10s + P=1's
tests/d0286_depart_race.sh:93
-    # RULE 0: the grace is DERIVED from the observer's dead-declaration
+    # budget: the grace is DERIVED from the observer's dead-declaration
tests/sess419_post_chain.sh:9
-#      the clean-departure trigger (MODE=umount default) — first RULE-4
+#      the clean-departure trigger (MODE=umount default) — first instrumented
tests/sess419_post_chain.sh:13
-# RULE 0 (derived): wait <= 3600 s (chain: 5 x ~370 s + board ~29 min from
+# the budget rule (derived): wait <= 3600 s (chain: 5 x ~370 s + board ~29 min from
tests/d_purge_nonatomic_verify.sh:3
-# (sess419 RULE-5 ruling, ccmemory
+# (sess419 design-consult ruling, ccmemory
tests/d_purge_nonatomic_verify.sh:36
-# RULE 0 (derived): destroy + 31x2 s dead window 62 s + fence/replay ~15 s
+# the budget rule (derived): destroy + 31x2 s dead window 62 s + fence/replay ~15 s
tests/d_recov_advance_bounded_verify.sh:31
-# RULE 0 (derived): V expiry 62 s + replay ~20 s + ladder; transient: +5 s
+# the budget rule (derived): V expiry 62 s + replay ~20 s + ladder; transient: +5 s
tests/d_recov_advance_bounded_verify.sh:105
-# sess437 (RULE-5 ruling, ccmemory ccloop-c7ee71c6-sess437-GPT-ruling-incarnation-
+# sess437 (design-consult ruling, ccmemory ccloop-c7ee71c6-sess437-GPT-ruling-incarnation-
tests/handoff_anatomy.sh:37
-# RULE 0: a measurement harness, not a criterion; the burst's own budget is the
+# budget: a measurement harness, not a criterion; the burst's own budget is the
tests/handoff_anatomy.sh:39
-# RULE 2c: no pgrep -f / ps; every ssh is bounded.
+# the unkillable-wedge rule: no pgrep -f / ps; every ssh is bounded.
tests/sess446_chain50_owncrash_nosurvivor2.sh:9
-# RULE 0: prep (300); no_survivor_crash_replay (its own derived bound, mount
+# budget: prep (300); no_survivor_crash_replay (its own derived bound, mount
tests/sess447_chain55_0540_default_on.sh:4
-# RULE-5 ruling's phases 3-5 for the scoped coherence-only default flip:
+# Design-consult ruling's phases 3-5 for the scoped coherence-only default flip:
tests/sess447_chain55_0540_default_on.sh:14
-# RULE 0: build 300; matrix 240; prep 300; board ~12 min measured (900);
+# budget: build 300; matrix 240; prep 300; board ~12 min measured (900);
tests/sess448_chain58_phase5_rman_vergate_bootstrap.sh:3
-# remaining RULE-5 phase-5 coverage for D-FOREIGN-REPLAY-UNGATED-IMAGES after
+# remaining design-consult phase-5 coverage for D-FOREIGN-REPLAY-UNGATED-IMAGES after
tests/sess448_chain58_phase5_rman_vergate_bootstrap.sh:7
-# RULE 0 bounds are the MEASURED walls: rman 9 arms summed 2217 s on 0.41.2
+# the budget rule bounds are the MEASURED walls: rman 9 arms summed 2217 s on 0.41.2
tests/pr_unregister_fail_restamp.sh:48
-# RULE 0: umount ≤ 15 s + WITHDRAWN first-sight (monitor lap ≤ 5 s) + fence
+# budget: umount ≤ 15 s + WITHDRAWN first-sight (monitor lap ≤ 5 s) + fence
tests/sess450_chain69_retire_pending.sh:8
-# RULE 0: restamp bound 300 s, crash bound 330 s (derived in the harness
+# budget: restamp bound 300 s, crash bound 330 s (derived in the harness
tests/sess466_chain97_dirshard_stage1.sh:19
-# RULE 0 (derived): install 30 / build 420; prep 300 (measured 86-106 s);
+# the budget rule (derived): install 30 / build 420; prep 300 (measured 86-106 s);
tests/sess468_chain106_peerloss_rerun.sh:12
-# RULE 0: prep 300; peerloss 700 (480 mount cap + 150 restore + setup; chain
+# budget: prep 300; peerloss 700 (480 mount cap + 150 restore + setup; chain
tests/d0527_untrusted_iget_peer.sh:13
-# Shape (RULE 4 instrumentation, not a fix):
+# Shape (instrumentation, not a fix):
tests/d0527_untrusted_iget_peer.sh:29
-# RULE 0: whole harness < 60 s (creates ~1 s, handle opens ~10 s).
+# budget: whole harness < 60 s (creates ~1 s, handle opens ~10 s).
tests/d0527_untrusted_iget_peer.sh:77
-# RULE 0: 5 attempts with 2000 ms sleeps between = 8 s of sleep; the cold
+# budget: 5 attempts with 2000 ms sleeps between = 8 s of sleep; the cold
tests/readdir_peer_pace.sh:11
-#               (native XFS lists a 200-entry dir in ~1 ms; RULE 0 ceiling is
+#               (native XFS lists a 200-entry dir in ~1 ms; 2x-native-XFS ceiling is
tests/readdir_peer_pace.sh:15
-# RULE 0: ndirs x nfiles creates (native ~ms each) + 2 x ndirs readdirs; the
+# budget: ndirs x nfiles creates (native ~ms each) + 2 x ndirs readdirs; the
tests/sess480_chain122_sequencer.sh:19
-# RULE 0 budgets, all from measured walls: board harvest 120 s (showstat only);
+# derived time budgets, all from measured walls: board harvest 120 s (showstat only);
tests/sess483_chain131_agfree_window.sh:79
-# RULE 0 budgets, derived rather than rounded.  build 338 s measured (chain
+# derived time budgets, derived rather than rounded.  build 338 s measured (chain
tests/sess483_chain131_agfree_window.sh:188
-      echo "    RULE 0: the umount step hit its budget. That is a RESULT — a stalling clean unmount is its own defect — not a number to widen."
+      echo "    budget: the umount step hit its budget. That is a RESULT — a stalling clean unmount is its own defect — not a number to widen."
tests/sess494_lkp_attrib.sh:19
-# RULE 0 budgets: prep 300 (measured 106-146 s); cc row 160 (manifest 90 +
+# derived time budgets: prep 300 (measured 106-146 s); cc row 160 (manifest 90 +
tests/sess496_bogus_imode_joiner.sh:19
-# RULE 0 budgets, derived: prep 300 (measured 104-146 s); joiner arm cap 560
+# derived time budgets, derived: prep 300 (measured 104-146 s); joiner arm cap 560
tests/sess496_bogus_imode_joiner.sh:90
-  [ "$jrc" = 124 ] && echo "  RULE 0: the joiner arm hit its cap — a result, not a number to widen."
+  [ "$jrc" = 124 ] && echo "  budget: the joiner arm hit its cap — a result, not a number to widen."
tests/dlm_wait_signal.sh:19
-# RULE 0 (derived): setup ~5 s + PAUSE_MS + 20 s grace + captures.  Default
+# the budget rule (derived): setup ~5 s + PAUSE_MS + 20 s grace + captures.  Default
tests/d0924_alternating_boot.sh:37
-# RULE 0 (derived, from the walls those harnesses recorded themselves): the
+# the budget rule (derived, from the walls those harnesses recorded themselves): the
tests/d0949_sole_survivor_chunkfree.sh:44
-# RULE 0 budget: creates measured in-line and printed; the whole lap is bounded
+# derived time budget: creates measured in-line and printed; the whole lap is bounded
tests/d0949_sole_survivor_chunkfree.sh:121
-# RULE 0 BUDGET, DERIVED AND MEASURED, NOT ROUNDED.  12000 empty files into a
+# the budget rule BUDGET, DERIVED AND MEASURED, NOT ROUNDED.  12000 empty files into a
tests/d0949_sole_survivor_chunkfree.sh:202
-# RULE 0 budget: the chunks already exist and were KEPT by step 3, so these
+# derived time budget: the chunks already exist and were KEPT by step 3, so these
tests/storm_fresh_pass.sh:41
-# RULE 0: prep is infrastructure, not workload.  Measured 48-126s at 32 nodes;
+# budget: prep is infrastructure, not workload.  Measured 48-126s at 32 nodes;
tests/d384_terminal_record_guarantee.sh:3
-# the RULE-0 terminal-record guarantee added in sess384 for
+# the terminal-record guarantee added in sess384 for
tests/d384_terminal_record_guarantee.sh:6
-# The defect: run.sh kills each node's ssh at the criterion's RULE-0 budget,
+# The defect: run.sh kills each node's ssh at the criterion's derived time budget,
tests/d384_terminal_record_guarantee.sh:66
-# RULE 0.  What must be true is that the RECORD was produced before the kill and
+# the budget rule.  What must be true is that the RECORD was produced before the kill and
tests/yield_starvation_repro.sh:21
-# RULE 0 budget: DURATION + ~45s fan-out/harvest overhead.
+# derived time budget: DURATION + ~45s fan-out/harvest overhead.
tests/degraded_member_cascade.sh:50
-# Budget (RULE 0): baseline ~90s, degraded ~90s + 45s thaw/settle.
+# Budget (budget): baseline ~90s, degraded ~90s + 45s thaw/settle.
tests/sess419_master_chain.sh:12
-#     (MODE=umount) — first RULE-4 measurement of D-0287
+#     (MODE=umount) — first instrumented measurement of D-0287
tests/sess419_master_chain.sh:21
-# RULE 0 (derived): build 2x ~450 s; preps 236 s (cap 300) x ~12; tests as
+# the budget rule (derived): build 2x ~450 s; preps 236 s (cap 300) x ~12; tests as
tests/sess415_armed_board_0282.sh:7
-# Stage caps (RULE 0): prep 300 (fleet unmounted by the prior board's ndr
+# Stage caps (budget): prep 300 (fleet unmounted by the prior board's ndr
tests/typeflip_dead_incarn_repro.sh:21
-# RULE 0: every remote step is bounded by a derived timeout (mkdir/rmdir/stat
+# budget: every remote step is bounded by a derived timeout (mkdir/rmdir/stat
tests/typeflip_dead_incarn_repro.sh:25
-# RULE 2c: bounded ssh only, per-node rc captured.
+# the unkillable-wedge rule: bounded ssh only, per-node rc captured.
tests/rig_dmesg_grep.sh:71
-# Budget (RULE 0): a dmesg snapshot + grep is a sub-second operation.  ssh
+# Budget (budget): a dmesg snapshot + grep is a sub-second operation.  ssh
tests/sess433_chain3_0401.sh:9
-# RULE 0 bounds = each harness's own.  Fleet is left mounted after prep (the
+# the budget rule bounds = each harness's own.  Fleet is left mounted after prep (the
tests/kmsg_fresh_ssh_miss_probe.sh:35
-#                           Capture the poll index and escalate (RULE 5).
+#                           Capture the poll index and escalate (design consult).
tests/sess415_verify_0284.sh:4
-# unarmed) + regression.  Stage caps (RULE 0): prep 300 (new srcversion),
+# unarmed) + regression.  Stage caps (budget): prep 300 (new srcversion),
tests/sess433_chain2_0400.sh:7
-# again so the fleet is left healthy.  RULE 0 bounds = each harness's own.
+# again so the fleet is left healthy.  The budget rule bounds = each harness's own.
tests/tmpfile_churn_kill.sh:4
-# crash/replay ... tests", sess396 RULE-5 ruling ccmemory
+# crash/replay ... tests", sess396 design-consult ruling ccmemory
tests/tmpfile_churn_kill.sh:27
-# RULE 0 (derived, not padded): per-node mxfs churn measured 15-20 ms/iter at
+# the budget rule (derived, not padded): per-node mxfs churn measured 15-20 ms/iter at
tests/tmpfile_churn_kill.sh:32
-# RULE 2c: every remote call bounded; per-node rc files; virsh calls bounded.
+# the unkillable-wedge rule: every remote call bounded; per-node rc files; virsh calls bounded.
tests/tmpfile_churn_kill.sh:33
-# RULE 3: lives in tests/.
+# the source-tree rule: lives in tests/.
tests/tmpfile_churn_kill.sh:461
-    # sess407 RULE-5 ruling (GPT): an FSWIDE TERMINAL must stop EVERY further
+    # sess407 design-consult ruling (GPT): an FSWIDE TERMINAL must stop EVERY further
tests/d0133_sb_mutation_gate.sh:28
-# RULE 0 (derived): srcgate 5 s + 3 ioctls ~3 s + umount <=45 s + 3 mount
+# the budget rule (derived): srcgate 5 s + 3 ioctls ~3 s + umount <=45 s + 3 mount
tests/repro_lost_entry.sh:2
-# RULE-4 repro for the concurrent shared-dir "one lost entry" bug seen in
+# Instrumented repro for the concurrent shared-dir "one lost entry" bug seen in
tests/sess429_chain.sh:20
-# RULE 0: wait <= 1800 + build 500 + proof 500 + tools 120 + tauth 240 +
+# budget: wait <= 1800 + build 500 + proof 500 + tools 120 + tauth 240 +
tests/cc_loop.sh:28
-    # budget: prep ~120s + drc 100*N + cc ~90s (RULE 0)
+    # budget: prep ~120s + drc 100*N + cc ~90s (budget)
tests/sess424_chain.sh:5
-# Same shape as sess422_chain.sh, plus RULE 4 evidence capture the s422 run
+# Same shape as sess422_chain.sh, plus instrumented evidence capture the s422 run
tests/sess424_chain.sh:18
-# RULE 0: build 500 + proof 500 + tauth 120 + 3 preps x 300 + 4 sweeps x 60
+# budget: build 500 + proof 500 + tauth 120 + 3 preps x 300 + 4 sweeps x 60
tests/lone_rsync_bench.sh:2
-# tests/lone_rsync_bench.sh — RULE 0 lone-node metadata bench (sess434).
+# tests/lone_rsync_bench.sh — the budget rule lone-node metadata bench (sess434).
tests/lone_rsync_bench.sh:10
-# RULE 0 budget: fleet umount sweep 20 s, mount 15 s, rsync (native XFS ~4 s
+# derived time budget: fleet umount sweep 20 s, mount 15 s, rsync (native XFS ~4 s
tests/lone_rsync_bench.sh:11
-# for ~700 MB; the multi-node mxfs parity is the expected ceiling, RULE 0's
+# for ~700 MB; the multi-node mxfs parity is the expected ceiling, the budget rule's
tests/dialloc_validate_ab.sh:2
-# dialloc_validate_ab.sh — the RULE 4 A/B for the D-0351 dialloc containment
+# dialloc_validate_ab.sh — the instrumented A/B for the D-0351 dialloc containment
tests/dialloc_validate_ab.sh:15
-# Budget (RULE 0): 2 x injector (<= 60 s each) + prep 300 s.
+# Budget (budget): 2 x injector (<= 60 s each) + prep 300 s.
tests/sbclean_fence_idle.sh:35
-# RULE 0 budget: activity 10s + idle 90s + fence wait ~70s + replay poll
+# derived time budget: activity 10s + idle 90s + fence wait ~70s + replay poll
tests/shared_dir_slot_cost.sh:35
-# RULE 0: this is a measurement harness, not a criterion. It asserts nothing.
+# budget: this is a measurement harness, not a criterion. It asserts nothing.
tests/kmsg_flood_skip_probe.sh:122
-# Budget (RULE 0): the flood is $FLOOD userspace writes to /dev/kmsg (~10k/s on
+# Budget (budget): the flood is $FLOOD userspace writes to /dev/kmsg (~10k/s on
tests/kmsg_flood_skip_probe.sh:157
-    echo "         during it.  Escalate (RULE 5); this is worse than a skip."
+    echo "         during it.  Escalate (design consult); this is worse than a skip."
tests/d_intents_undischarged_verify.sh:32
-# RULE 0 (derived): file build ~2 s (8 x 0.19 s measured) + hold wait
+# the budget rule (derived): file build ~2 s (8 x 0.19 s measured) + hold wait
tests/d_intents_undischarged_verify.sh:134
-    # sess476 (CANCEL authority tokens, RULE-5 negative arms): FORGE=1 skews
+    # sess476 (CANCEL authority tokens, design-consult negative arms): FORGE=1 skews
tests/d_intents_undischarged_verify.sh:213
-        # sess479: the RULE-5 ruling's assertion set has SIX members, and
+        # sess479: the design-consult ruling's assertion set has SIX members, and
tests/d_mount_window_death_verify.sh:3
-# window arm + negative control, per the sess420 RULE-5 barrier ruling
+# window arm + negative control, per the sess420 design-consult barrier ruling
tests/d_mount_window_death_verify.sh:26
-# RULE 0 (derived): umount ~5 s + hold 100 s + inline replay ~15 s +
+# the budget rule (derived): umount ~5 s + hold 100 s + inline replay ~15 s +
tests/d_mount_window_death_verify.sh:114
-    # sess424 (RULE-5 ruling ccloop-c7ee71c6-sess424-GPT-ruling-barrier-
+    # sess424 (design-consult ruling ccloop-c7ee71c6-sess424-GPT-ruling-barrier-
tests/knob_ab.sh:18
-# RULE 0: no outer timeout is wrapped around run.sh -- run.sh already enforces
+# budget: no outer timeout is wrapped around run.sh -- run.sh already enforces
tests/knob_ab.sh:21
-# RULE 9: the fleet sweeps are one call each, per-node output file, per-node rc,
+# the request-batching rule: the fleet sweeps are one call each, per-node output file, per-node rc,
tests/badhead_attribute.sh:22
-# RULE 2c: every remote call is `timeout`-bounded; no pgrep -f; per-node rc.
+# the unkillable-wedge rule: every remote call is `timeout`-bounded; no pgrep -f; per-node rc.
tests/freepub_platter_home_inject.sh:2
-# freepub_platter_home_inject.sh — the ONE directed measurement the RULE-5
+# freepub_platter_home_inject.sh — the ONE directed measurement the design-consult
tests/freepub_platter_home_inject.sh:22
-# Budget (RULE 0): <= 20 x (create+sync+rm+sync ~0.3 s native) + 15 s
+# Budget (budget): <= 20 x (create+sync+rm+sync ~0.3 s native) + 15 s
tests/incarnation_death_probe.sh:36
-# TIMING (RULE 0 — derived, not chosen)
+# TIMING (the budget rule — derived, not chosen)
tests/incarnation_death_probe.sh:138
-    echo "       Death was not detected/dispatched inside the derived budget (RULE 0)."
+    echo "       Death was not detected/dispatched inside the derived budget (budget)."
tests/classify_free_foreign.py:4
-32-node MXFS board journal capture (RULE 3: persistent, not /tmp).
+32-node MXFS board journal capture (the source-tree rule: persistent, not /tmp).
tests/d526_mass_unmount_verify.sh:16
-# EMPTY) — blessed by the sess413 RULE-5 ruling (ccmemory
+# EMPTY) — blessed by the sess413 design-consult ruling (ccmemory
tests/d526_mass_unmount_verify.sh:31
-# RULE 0 (derived): 31 parallel unmounts <=120s (post-#93 serialize fix,
+# the budget rule (derived): 31 parallel unmounts <=120s (post-#93 serialize fix,
tests/sess444_chain34_0510_board.sh:6
-# RULE 0: summed measured walls (showstat 32/caw, 2026-08-29) = 1002 s;
+# budget: summed measured walls (showstat 32/caw, 2026-08-29) = 1002 s;
tests/sess445_chain41_0510_icreate_negative3.sh:10
-# RULE 0: prep (300); negative full restart (1080: chain 37 measured 4 min
+# budget: prep (300); negative full restart (1080: chain 37 measured 4 min
tests/sess445_chain42_0512_Aprime_arms.sh:10
-# RULE 0: 3 x (prep 79-120 s (300) + arm <= 300 s) + prep2.
+# budget: 3 x (prep 79-120 s (300) + arm <= 300 s) + prep2.
tests/sess446_chain46_0510_icreate_negative4.sh:13
-# RULE 0: prep 79-120 s (300); negative full restart (1080: lap 3 measured
+# budget: prep 79-120 s (300); negative full restart (1080: lap 3 measured
tests/sess446_chain47_0512_Aprime_arms3.sh:10
-# RULE 0: 3 x (prep 79-120 s (300) + arm <= 300 s) + prep2.
+# budget: 3 x (prep 79-120 s (300) + arm <= 300 s) + prep2.
tests/sess449_chain66_samenode_exerciser.sh:3
-# tests/caw_samenode_selftest.sh) — the RULE-6 closure vehicle for
+# tests/caw_samenode_selftest.sh) — the defect-bar closure vehicle for
tests/sess449_chain66_samenode_exerciser.sh:9
-# re-preps first).  RULE 0: harness bound 90 s (2 arms ≈ 45 s); build 300;
+# re-preps first).  budget: harness bound 90 s (2 arms ≈ 45 s); build 300;
tests/retire_pending_admission.sh:6
-# 0.59.1 (sess451) arms — the sess450 RULE-5 STOP-SHIP fixes:
+# 0.59.1 (sess451) arms — the sess450 design-consult STOP-SHIP fixes:
tests/retire_pending_admission.sh:27
-# 0.59.2 (sess452) arms — the sess451 RULE-5 STOP-SHIP #2 required tests
+# 0.59.2 (sess452) arms — the sess451 design-consult STOP-SHIP #2 required tests
tests/retire_pending_admission.sh:103
-# RULE 0 budgets (native umount/mount are seconds):
+# derived time budgets (native umount/mount are seconds):
tests/retire_pending_admission.sh:148
-# (RULE 2c: bounded, parallel, per-node files)
+# (the unkillable-wedge rule: bounded, parallel, per-node files)
tests/retire_pending_admission.sh:169
-# set one knob on a set of nodes, parallel, per-node rc (RULE 9)
+# set one knob on a set of nodes, parallel, per-node rc (the request-batching rule)
tests/retire_pending_admission.sh:284
-    [ "$R_SINCE" -lt "$GRACE_S" ] || fail "RULE 0: the remount completed ${R_SINCE}s after the departure, outside the ${GRACE_S}s grace — the arm's premise (settle before any peer expires it) was not exercised"
+    [ "$R_SINCE" -lt "$GRACE_S" ] || fail "budget: the remount completed ${R_SINCE}s after the departure, outside the ${GRACE_S}s grace — the arm's premise (settle before any peer expires it) was not exercised"
tests/retire_pending_admission.sh:338
-    [ "$since" -lt "$GRACE_S" ] || fail "RULE 0: the joiner's mount started ${since}s after the departure, outside the ${GRACE_S}s grace — the peers' expiry may have done the barrier's job"
+    [ "$since" -lt "$GRACE_S" ] || fail "budget: the joiner's mount started ${since}s after the departure, outside the ${GRACE_S}s grace — the peers' expiry may have done the barrier's job"
tests/sess459_chain83_untokened_gate.sh:13
-# RULE 0 budgets: build measured 310-326 s in tree → 600; prep 300; settle
+# derived time budgets: build measured 310-326 s in tree → 600; prep 300; settle
tests/sess459_chain84_g3_arms.sh:11
-# RULE 0: per-arm budget 160 (harness header); prep 300.
+# budget: per-arm budget 160 (harness header); prep 300.
tests/sess461_chain89_relmark_matrix_prekill.sh:19
-# RULE 0: build 600 (measured 286-326 s), prep 300, matrix 1500 (4 arms x
+# budget: build 600 (measured 286-326 s), prep 300, matrix 1500 (4 arms x
tests/sess462_chain93_intents_evidence_verify.sh:16
-# RULE 0: harness header derives ~115 s per arm, caller bound 180; prep 300.
+# budget: harness header derives ~115 s per arm, caller bound 180; prep 300.
tests/dirshard_hash_vectors.sh:15
-# Exit 0 PASS, 1 FAIL, 2 INFRA.  RULE 0: whole script < 10 s.
+# Exit 0 PASS, 1 FAIL, 2 INFRA.  budget: whole script < 10 s.
tests/intents_classless_attribute.sh:2
-# intents_classless_attribute.sh — D-FOREIGN-SLICE-INTENTS-ABANDONED RULE-4
+# intents_classless_attribute.sh — D-FOREIGN-SLICE-INTENTS-ABANDONED instrumented
tests/intents_classless_attribute.sh:23
-# RULE 0 (derived): file build 2 x 4096 pwrite ~0.4 s (8 x 0.19 s measured
+# the budget rule (derived): file build 2 x 4096 pwrite ~0.4 s (8 x 0.19 s measured
tests/sess467_chain99_d0523_peerloss_attrib.sh:12
-#      (D-FOREIGN-SLICE-INTENTS-ABANDONED RULE-4 step: WHY the fragmented
+#      (D-FOREIGN-SLICE-INTENTS-ABANDONED instrumented step: WHY the fragmented
tests/sess467_chain99_d0523_peerloss_attrib.sh:16
-# RULE 0 (derived): install 30 / build 420; prep 300 (measured 86-113 s);
+# the budget rule (derived): install 30 / build 420; prep 300 (measured 86-113 s);
tests/sess467_chain99_d0523_peerloss_attrib.sh:65
-  #    RULE 0: 150 s per invocation (script header: 4 arms x ~26 s + harvest).
+  #    budget: 150 s per invocation (script header: 4 arms x ~26 s + harvest).
tests/sess467_chain100_relmark_matrix_release.sh:25
-# RULE 0: install 30; prep 300 (measured 86-117 s); matrix 1500 (4 arms x
+# budget: install 30; prep 300 (measured 86-117 s); matrix 1500 (4 arms x
tests/inact_cert_arms.sh:3
-# verification arms the sess469 RULE-5 ruling made MANDATORY, driven by the
+# verification arms the sess469 design-consult ruling made MANDATORY, driven by the
tests/inact_cert_arms.sh:61
-# RULE 0 budgets: refuse 120 s (reap cadence 5 s + 30 s), escalate 330 s
+# derived time budgets: refuse 120 s (reap cadence 5 s + 30 s), escalate 330 s
tests/d488_unlock_exit_arms.sh:4
-# D-AGLOCK-ORPHAN-EX-TRACKING-LOSS-LIVELOCK-488, sess470 RULE-5 disposition
+# D-AGLOCK-ORPHAN-EX-TRACKING-LOSS-LIVELOCK-488, sess470 design-consult disposition
tests/d488_unlock_exit_arms.sh:35
-# RULE 0: Y's creates 60 s (healthy ~1 s; the REARM cadence is bounded
+# budget: Y's creates 60 s (healthy ~1 s; the REARM cadence is bounded
tests/d0532_reuse_lap.sh:17
-# RULE 0: laps x (create+rm+create) is milliseconds each; 100 laps < 10 s;
+# budget: laps x (create+rm+create) is milliseconds each; 100 laps < 10 s;
tests/sess469_chain108_inactcert_arms.sh:3
-# verification arms (RULE-5 sess469 ruling) on frozen production 0.64.10
+# verification arms (design-consult sess469 ruling) on frozen production 0.64.10
tests/sess469_chain108_inactcert_arms.sh:7
-# RULE 0: prep 300 s (80-117 s measured); refuse 120; escalate 330; others 60.
+# budget: prep 300 s (80-117 s measured); refuse 120; escalate 330; others 60.
tests/sess475_chain116_d0133_sb_seal.sh:3
-# (RULE-5 ruling ccmemory ccloop-c7ee71c6-sess475-GPT-ruling-d0133-lock-inert-
+# (design-consult ruling ccmemory ccloop-c7ee71c6-sess475-GPT-ruling-d0133-lock-inert-
tests/sess475_chain116_d0133_sb_seal.sh:43
-# RULE 0 budgets: prep 300 (95-118 s measured); reuse 10 laps 150 (87 s);
+# derived time budgets: prep 300 (95-118 s measured); reuse 10 laps 150 (87 s);
tests/sess475_chain116_d0133_sb_seal.sh:235
-  # HF_KNOBS (default "0 1") runs the arm once per knob value: 0 = the RULE-4
+  # HF_KNOBS (default "0 1") runs the arm once per knob value: 0 = the instrumented
tests/sess475_chain117_d0532_relog_concurrency.sh:2
-# sess475 chain 117: D-0532 directed concurrency arm (RULE-5 bar, ccmemory
+# sess475 chain 117: D-0532 directed concurrency arm (design-consult bar, ccmemory
tests/sess475_chain117_d0532_relog_concurrency.sh:45
-# RULE 0: prep 300 (95-118 s measured); arm wall ~ PAUSE_MS + HOLD_MS + 10 s.
+# budget: prep 300 (95-118 s measured); arm wall ~ PAUSE_MS + HOLD_MS + 10 s.
tests/sess475_chain118_d0133_mutation_gate.sh:9
-# RULE 0: prep 300 (95-118 s measured); gate 100 (harness bound ~80 s).
+# budget: prep 300 (95-118 s measured); gate 100 (harness bound ~80 s).
tests/sess480_chain120_ndr_streak_06437.sh:6
-# Why 10, and why again.  The sess463 RULE-5 ruling is explicit that the streak
+# Why 10, and why again.  The sess463 design-consult ruling is explicit that the streak
tests/sess480_chain120_ndr_streak_06437.sh:23
-# RULE 0 budgets, from measured walls rather than round numbers: prep 300 s
+# derived time budgets, from measured walls rather than round numbers: prep 300 s
tests/sess480_chain124_pace_uncensored.sh:35
-# RULE 0.  The 1800 s guard below is a HANG BACKSTOP, not a performance budget,
+# the budget rule.  The 1800 s guard below is a HANG BACKSTOP, not a performance budget,
tests/relgate_reachability_preflight.sh:34
-# RULE 0: this is a two-node, few-operation probe.  Each remote step gets 20-30 s,
+# budget: this is a two-node, few-operation probe.  Each remote step gets 20-30 s,
tests/sess480_chain126_cc_private_barrier.sh:40
-# RULE 0: prep 300 s (measured 88-117 s); the cc row keeps its UNCHANGED 90 s
+# budget: prep 300 s (measured 88-117 s); the cc row keeps its UNCHANGED 90 s
tests/sess494_d0493_forged_guard_arms.sh:38
-# RULE 0 budgets, derived: the survivor scan waits one dead window (~62 s)
+# derived time budgets, derived: the survivor scan waits one dead window (~62 s)
tests/sess495_chain141_adopt_skip.sh:26
-# RULE 0 budgets, derived: prep 300 (measured 104-146 s); cc row 160 (manifest
+# derived time budgets, derived: prep 300 (measured 104-146 s); cc row 160 (manifest
tests/sess495_chain141_adopt_skip.sh:153
-    [ "$rc" = 124 ] && echo "  RULE 0: the row set hit its wrapper budget — a result, not a number to widen."
+    [ "$rc" = 124 ] && echo "  budget: the row set hit its wrapper budget — a result, not a number to widen."
tests/d0940_unknown_blocker_probe.sh:39
-# RULE 0 (derived): prep 45 s (bound 300); workload ~10 s (bound 60); unload
+# the budget rule (derived): prep 45 s (bound 300); workload ~10 s (bound 60); unload
tests/d0946_disklive_knob_vs_aging.sh:41
-# RULE 0 budget: 400 creates+deletes per round, measured on this rig at
+# derived time budget: 400 creates+deletes per round, measured on this rig at
tests/d0946_disklive_knob_vs_aging.sh:45
-# its bound is itself a RULE 0 failure and is reported as one.
+# its bound is itself a budget failure and is reported as one.
tests/d0946_disklive_knob_vs_aging.sh:440
-    # RULE 0: the per-round bound is an assertion, not a safety net.
+    # budget: the per-round bound is an assertion, not a safety net.
tests/d0946_disklive_knob_vs_aging.sh:441
-    [ "$ms" -gt 30000 ] && echo "     RULE0 round wall ${ms}ms exceeds the 30000ms derived bound"
+    [ "$ms" -gt 30000 ] && echo "     the budget rule round wall ${ms}ms exceeds the 30000ms derived bound"
tests/scst_pr_abort_shutdown_race.sh:48
-# RULE 0: every ssh bounded; window arm budget = 2 x (logout + DELAY*2 + login)
+# budget: every ssh bounded; window arm budget = 2 x (logout + DELAY*2 + login)
tests/scst_pr_abort_shutdown_race.sh:50
-# per iteration.  RULE 2c: no pgrep/ps.  RULE 3: lives in tests/.
+# per iteration.  the unkillable-wedge rule: no pgrep/ps.  the source-tree rule: lives in tests/.
tests/sess446_chain44_0512_takeover13.sh:11
-# RULE 0: prep 79-120 s (300); bootstrap_takeover point 13 (1500+180, derived in
+# budget: prep 79-120 s (300); bootstrap_takeover point 13 (1500+180, derived in
tests/sess446_chain49_ndr_repro.sh:8
-# RULE 0: per lap prep (300) + row (500); x2.
+# budget: per lap prep (300) + row (500); x2.
tests/cc_df_capture.sh:6
-# mechanism is captured (RULE 4).  Loops until a capture is taken or ITERS done.
+# mechanism is captured (instrumented).  Loops until a capture is taken or ITERS done.
tests/drc_timeline.sh:2
-# drc_timeline.sh (sess50 ccloop) — RULE-4: reconstruct daddr-120 dirent-count
+# drc_timeline.sh (sess50 ccloop) — instrumented: reconstruct daddr-120 dirent-count
tests/recov_takeover_retry_probe.sh:55
-# TIMING (RULE 0 — derived, not chosen)
+# TIMING (the budget rule — derived, not chosen)
tests/bootstrap_full_restart.sh:26
-# RULE 0 (derived, FIRST MEASUREMENT): crash 10 s; boot 26-120 s; LUN/NFS
+# the budget rule (derived, FIRST MEASUREMENT): crash 10 s; boot 26-120 s; LUN/NFS
tests/bootstrap_full_restart.sh:58
-# sess446: 0.53.0 measured 203/206 s (chains 43/41); RULE 0 derivation in
+# sess446: 0.53.0 measured 203/206 s (chains 43/41); the budget rule derivation in
tests/bootstrap_full_restart.sh:234
-[ "$MW" -le "$MOUNT_BOUND" ] && pass "mount wall ${MW}s <= ${MOUNT_BOUND}s" || fail "mount wall ${MW}s > ${MOUNT_BOUND}s (RULE 0)"
+[ "$MW" -le "$MOUNT_BOUND" ] && pass "mount wall ${MW}s <= ${MOUNT_BOUND}s" || fail "mount wall ${MW}s > ${MOUNT_BOUND}s (budget)"
tests/bootstrap_full_restart.sh:279
-[ "$PW" -le "$PEER_BOUND" ] && pass "peer admission wall ${PW}s <= ${PEER_BOUND}s" || fail "peer admission wall ${PW}s > ${PEER_BOUND}s (RULE 0)"
+[ "$PW" -le "$PEER_BOUND" ] && pass "peer admission wall ${PW}s <= ${PEER_BOUND}s" || fail "peer admission wall ${PW}s > ${PEER_BOUND}s (budget)"
tests/demoter_punt_ab.sh:26
-# RULE 0
+# the budget rule
tests/dump_faceb.sh:4
-# stops on FAIL, so the failing iter's events are intact.  RULE 3: lives in the tree.
+# stops on FAIL, so the failing iter's events are intact.  the source-tree rule: lives in the tree.
tests/drc_single_node.sh:4
-# DECISIVE RULE-4 experiment (sess32): is the dir_reuse_coherency readdir-short /
+# DECISIVE instrumented experiment (sess32): is the dir_reuse_coherency readdir-short /
tests/tcp2_characterize.sh:5
-# flake rate of the 2/tcp criterion and capture failure evidence (RULE 4).
+# flake rate of the 2/tcp criterion and capture failure evidence (instrumented).
tests/bootstrap_takeover.sh:3
-# item 5f, the TAKEOVER (docs/whole-cluster-restart.md §6.8; sess442 RULE-5
+# item 5f, the TAKEOVER (docs/whole-cluster-restart.md §6.8; sess442 design-consult
tests/bootstrap_takeover.sh:20
-# RULE 0 (derived): crash 10 s; boot <= 120 s; owner attempt = two dead-window
+# the budget rule (derived): crash 10 s; boot <= 120 s; owner attempt = two dead-window
tests/bootstrap_takeover.sh:150
-# its boot + module load).  RULE 0: contender boot <= 120 s; refusal ~5 s
+# its boot + module load).  budget: contender boot <= 120 s; refusal ~5 s
tests/bootstrap_takeover.sh:170
-    [ "$LW" -le 60 ] && pass "live probe: refusal wall ${LW}s <= 60s" || fail "live probe: refusal wall ${LW}s > 60s (RULE 0)"
+    [ "$LW" -le 60 ] && pass "live probe: refusal wall ${LW}s <= 60s" || fail "live probe: refusal wall ${LW}s > 60s (budget)"
tests/bootstrap_takeover.sh:207
-[ "$W2" -le "$TAKEOVER_BOUND" ] && pass "takeover wall ${W2}s <= ${TAKEOVER_BOUND}s" || fail "takeover wall ${W2}s > ${TAKEOVER_BOUND}s (RULE 0)"
+[ "$W2" -le "$TAKEOVER_BOUND" ] && pass "takeover wall ${W2}s <= ${TAKEOVER_BOUND}s" || fail "takeover wall ${W2}s > ${TAKEOVER_BOUND}s (budget)"
tests/bootstrap_takeover.sh:273
-[ "$PW" -le "$PEER_BOUND" ] && pass "peer admission wall ${PW}s <= ${PEER_BOUND}s" || fail "peer admission wall ${PW}s > ${PEER_BOUND}s (RULE 0)"
+[ "$PW" -le "$PEER_BOUND" ] && pass "peer admission wall ${PW}s <= ${PEER_BOUND}s" || fail "peer admission wall ${PW}s > ${PEER_BOUND}s (budget)"
tests/shared_dir_pace.sh:9
-# against a native-XFS cost of microseconds -- a RULE 0 violation by a factor of
+# against a native-XFS cost of microseconds -- a the budget rule violation by a factor of
tests/withdraw_slot_reclaim_probe.sh:77
-# RULE 0 budgets, derived (not round numbers):
+# derived time budgets, derived (not round numbers):
tests/withdraw_slot_reclaim_probe.sh:187
-    echo "RULE0-FAIL: payload exceeded its derived budget (${PAY}s > ${BUDGET_PAYLOAD}s)"
+    echo "the budget rule-FAIL: payload exceeded its derived budget (${PAY}s > ${BUDGET_PAYLOAD}s)"
tests/withdraw_slot_reclaim_probe.sh:216
-    echo "RULE0-FAIL: mount exceeded its derived budget (${MNTW}s > ${BUDGET_MOUNT}s)"
+    echo "the budget rule-FAIL: mount exceeded its derived budget (${MNTW}s > ${BUDGET_MOUNT}s)"
tests/clean_depart_lineage_race.sh:43
-# RULE 0 budget (blind): 2 slotdumps 4s + ACK poll <=8s + CYCLES x
+# derived time budget (blind): 2 slotdumps 4s + ACK poll <=8s + CYCLES x
tests/clean_depart_lineage_race.sh:46
-# RULE 0 budget (suspend): + detached-mount poll <=30s + mount-completion
+# derived time budget (suspend): + detached-mount poll <=30s + mount-completion
tests/relgate_fault_inject.sh:11
-#   (5 natural episodes, sess310).  RULE 6 requires the enforcement
+#   (5 natural episodes, sess310).  the zero-defect bar requires the enforcement
tests/relgate_fault_inject.sh:26
-# WHAT COUNTS AS PASS — defer mode (all of these, per RULE 6)
+# WHAT COUNTS AS PASS — defer mode (all of these, per the zero-defect bar)
tests/relgate_fault_inject.sh:77
-# RULE 0 budget:
+# derived time budget:
tests/sess446_chain45_owncrash_nosurvivor.sh:9
-# RULE 0: prep (300); no_survivor_crash_replay (its own derived bound, mount
+# budget: prep (300); no_survivor_crash_replay (its own derived bound, mount
tests/sess449_chain65_relgate_inode_ticket_defer.sh:8
-# RULE 0: per stage = arm (~2 s) + 15 s churn window + 5 s settle + dmesg
+# budget: per stage = arm (~2 s) + 15 s churn window + 5 s settle + dmesg
tests/uv_disktruth.sh:2
-# uv_disktruth.sh — decisive RULE-4 probe for the unlink_visibility blocker.
+# uv_disktruth.sh — decisive instrumented probe for the unlink_visibility blocker.
tests/uv_disktruth.sh:20
-# Lives in the source tree per RULE 3.  Assumes the 4-node cluster is mounted.
+# Lives in the source tree so it survives a reboot.  Assumes the 4-node cluster is mounted.
tests/guard_race_arms.sh:26
-# Budget (RULE 0):
+# Budget (budget):
tests/guard_race_arms.sh:371
-  # Budget (RULE 0, measured): virsh-start -> claim attempt 35 s (chain 88);
+  # Budget (budget, measured): virsh-start -> claim attempt 35 s (chain 88);
tests/bootstrap_resume.sh:4
-# RULE-5 review S2/S4): the bootstrap owner's mount fails at a chosen durable
+# Design-consult review S2/S4): the bootstrap owner's mount fails at a chosen durable
tests/bootstrap_resume.sh:16
-# RULE 0 (derived): crash 10 s; boot <= 120 s; attempt 1 = two dead-window
+# the budget rule (derived): crash 10 s; boot <= 120 s; attempt 1 = two dead-window
tests/bootstrap_resume.sh:112
-[ "$W1" -le "$ATTEMPT1_BOUND" ] && pass "attempt 1 wall ${W1}s <= ${ATTEMPT1_BOUND}s" || fail "attempt 1 wall ${W1}s > ${ATTEMPT1_BOUND}s (RULE 0)"
+[ "$W1" -le "$ATTEMPT1_BOUND" ] && pass "attempt 1 wall ${W1}s <= ${ATTEMPT1_BOUND}s" || fail "attempt 1 wall ${W1}s > ${ATTEMPT1_BOUND}s (budget)"
tests/bootstrap_resume.sh:137
-[ "$W2" -le "$ATTEMPT2_BOUND" ] && pass "attempt 2 wall ${W2}s <= ${ATTEMPT2_BOUND}s" || fail "attempt 2 wall ${W2}s > ${ATTEMPT2_BOUND}s (RULE 0)"
+[ "$W2" -le "$ATTEMPT2_BOUND" ] && pass "attempt 2 wall ${W2}s <= ${ATTEMPT2_BOUND}s" || fail "attempt 2 wall ${W2}s > ${ATTEMPT2_BOUND}s (budget)"
tests/bootstrap_resume.sh:188
-[ "$PW" -le "$PEER_BOUND" ] && pass "peer admission wall ${PW}s <= ${PEER_BOUND}s" || fail "peer admission wall ${PW}s > ${PEER_BOUND}s (RULE 0)"
+[ "$PW" -le "$PEER_BOUND" ] && pass "peer admission wall ${PW}s <= ${PEER_BOUND}s" || fail "peer admission wall ${PW}s > ${PEER_BOUND}s (budget)"
tests/sess435_chain8_0412.sh:10
-#   lone_rsync_bench                RULE 0 (120 s) — rerun: s434j ran on a rebooted node
+#   lone_rsync_bench                the budget rule (120 s) — rerun: s434j ran on a rebooted node
tests/d512_incarn_gate_verify.sh:5
-# Exercises the sess413 RULE-5 ruling's poison-time revocation +
+# Exercises the sess413 design-consult ruling's poison-time revocation +
tests/d512_incarn_gate_verify.sh:17
-# RULE 0 (derived): compile 2s + ship 2s + phase1 2s + trigger 2s +
+# the budget rule (derived): compile 2s + ship 2s + phase1 2s + trigger 2s +
tests/sess434_chain2b_0401.sh:3
-# the RULE 0 lone-node rsync BASELINE on 0.40.1 (memory-only lone grants) so
+# the the budget rule lone-node rsync BASELINE on 0.40.1 (memory-only lone grants) so
tests/dd_loss_capture.sh:131
-echo "    (RULE 6: 'cannot reproduce' is not a disposition) — it reproduces ~1 run in 3-10."
+echo "    (the zero-defect bar: 'cannot reproduce' is not a disposition) — it reproduces ~1 run in 3-10."
tests/cc_grantwait.sh:22
-# Classification (sess24 RULE-5): long wait + 0-1 attempts -> admission POLICY;
+# Classification (sess24 design-consult): long wait + 0-1 attempts -> admission POLICY;
tests/dir_reuse_stale_shell.sh:16
-# RULE 0 budget: cycle = 2 mkdir + walk + rm -rf + reuse-retry (<=12 * 0.5s)
+# derived time budget: cycle = 2 mkdir + walk + rm -rf + reuse-retry (<=12 * 0.5s)
tests/sess415_chain.sh:8
-# Stage timeouts derived (RULE 0):
+# Stage timeouts derived (budget):
tests/suite_loop.sh:7
-# local .ko.  RULE 0: a full 2/tcp suite is ~15-25 min (17 tests; dir_reuse
+# local .ko.  budget: a full 2/tcp suite is ~15-25 min (17 tests; dir_reuse
tests/sess456_chain79_0612_incmis.sh:22
-# RULE 0 budgets: build measured 286 s in tree (chain 78) -> 600; prep 300.
+# derived time budgets: build measured 286 s in tree (chain 78) -> 600; prep 300.
tests/sess461_chain91_settle_arms_0617.sh:10
-# RULE 0: prep 300; arms per tests/settle_token_arms.sh header (plain/inval/
+# budget: prep 300; arms per tests/settle_token_arms.sh header (plain/inval/
tests/sess466_chain96_crashcut5_rerun.sh:14
-# RULE 0: prep 300 (measured 86-106 s), cut-5 arm 300 (harness header: whole
+# budget: prep 300 (measured 86-106 s), cut-5 arm 300 (harness header: whole
tests/sess468_chain103_dirshard_fix_pace_intentsB_ndr_board.sh:36
-# RULE 0 budgets: install 30; prep 300 (80-117 s measured); selftest 240;
+# derived time budgets: install 30; prep 300 (80-117 s measured); selftest 240;
tests/sess467_chain98b_dirshard_stage1_pace_0644.sh:18
-# RULE 0: install 30; objdump 60; prep 300 (86-117 s measured); selftest 240;
+# budget: install 30; objdump 60; prep 300 (86-117 s measured); selftest 240;
tests/dirshard_reuse_peer_list.sh:16
-#   peer:    ls B  -> must list 8, rc 0, and take < 300 ms (RULE 0: a 16-shard
+#   peer:    ls B  -> must list 8, rc 0, and take < 300 ms (budget: a 16-shard
tests/dirshard_reuse_peer_list.sh:32
-# RULE 0 budgets: sharded mkdir N=16 < 2 s, rmdir < 2 s, 8 creates < 1 s,
+# derived time budgets: sharded mkdir N=16 < 2 s, rmdir < 2 s, 8 creates < 1 s,
tests/dirshard_reuse_peer_list.sh:80
-    [ -n "$ms" ] && [ "$ms" -lt "$PACE_MS" ] || fail "lap $lap: peer list of $B took ${ms:-?} ms (RULE 0 ceiling $PACE_MS)"
+    [ -n "$ms" ] && [ "$ms" -lt "$PACE_MS" ] || fail "lap $lap: peer list of $B took ${ms:-?} ms (2x-native-XFS ceiling $PACE_MS)"
tests/sess481_chain128_create_cost.sh:47
-# RULE 0 budgets, derived: build 600 s (measured 69-71 s for a comparable full
+# derived time budgets, derived: build 600 s (measured 69-71 s for a comparable full
tests/sess482_chain130_affine_audit.sh:55
-# measurement into a RULE 0 failure that says nothing about correctness.
+# measurement into a budget failure that says nothing about correctness.
tests/sess482_chain130_affine_audit.sh:66
-# RULE 0 budgets, derived: prep 300 s (measured 88-112 s); each node_death_replay
+# derived time budgets, derived: prep 300 s (measured 88-112 s); each node_death_replay
tests/sess482_chain130_affine_audit.sh:71
-# not an excuse to widen it, and a timeout there is a real RULE 0 finding about
+# not an excuse to widen it, and a timeout there is a real the budget rule finding about
tests/sess483_chain132_dirtenure.sh:66
-# RULE 0 budgets, derived.  build 338 s measured (chain 128) -> 600.  prep
+# derived time budgets, derived.  build 338 s measured (chain 128) -> 600.  prep
tests/sess485_chain133_agfree_hoist_ab.sh:43
-# RULE 0 budgets, derived.  prep 88-112 s measured -> 300 per leg.  The node
+# derived time budgets, derived.  prep 88-112 s measured -> 300 per leg.  The node
tests/sess485_chain133_agfree_hoist_ab.sh:55
-# stuck (node journals are volatile).  GPT RULE-5 ruling (sess486): the A/B
+# stuck (node journals are volatile).  Design-consult ruling (sess486): the A/B
tests/sess485_chain133_agfree_hoist_ab.sh:102
-# RULE-5 ruling (sess487): the SB-lock convoy is proven (filed D-...-0487);
+# Design-consult ruling (sess487): the SB-lock convoy is proven (filed D-...-0487);
tests/sess485_chain133_agfree_hoist_ab.sh:243
-    [ "$wwall" -ge 240 ] && echo "    RULE 0: the workload+umount step hit its budget. That is a RESULT, not a number to widen."
+    [ "$wwall" -ge 240 ] && echo "    budget: the workload+umount step hit its budget. That is a RESULT, not a number to widen."
tests/sess487_handoff_coldread.sh:14
-# shape must pass WITHOUT it.  GPT RULE-5 ruling (sess487): a targeted handoff
+# shape must pass WITHOUT it.  Design-consult ruling (sess487): a targeted handoff
tests/sess487_handoff_coldread.sh:46
-# RULE 0 budgets, derived: prep 300 (measured 88-137 s) + readback; a normal
+# derived time budgets, derived: prep 300 (measured 88-137 s) + readback; a normal
tests/sess488_ailpin_inject.sh:38
-# RULE 0 budgets, derived: prep 300 (measured 106-138 s this session); the
+# derived time budgets, derived: prep 300 (measured 106-138 s this session); the
tests/sess489_ailpin_census.sh:24
-# RULE 0 budgets, derived: prep 300 (measured 106-146 s); each cc row 160
+# derived time budgets, derived: prep 300 (measured 106-146 s); each cc row 160
tests/sess498_heal_off_board.sh:24
-# This is the experiment the RULE-5 ruling (ccmemory
+# This is the experiment the design-consult ruling (ccmemory
tests/sess498_heal_off_board.sh:28
-# RULE 0 budgets, derived: prep 300 (measured 107-146 s); board 1411 =
+# derived time budgets, derived: prep 300 (measured 107-146 s); board 1411 =
tests/sess491_evict_undest.sh:29
-# RULE 0 budgets, derived: prep 300 (measured 110-146 s); each cc row 160
+# derived time budgets, derived: prep 300 (measured 110-146 s); each cc row 160
tests/tcp_death_replay.sh:26
-# RULE 0 (derived): setup ~10 s + write KILL_AFTER s + death detection ~62 s
+# the budget rule (derived): setup ~10 s + write KILL_AFTER s + death detection ~62 s
tests/tcp_2node_death_chain.sh:7
-# RULE 0 (derived): prep 2/tcp on the QNAP LUN measured 48 s wall incl.
+# the budget rule (derived): prep 2/tcp on the QNAP LUN measured 48 s wall incl.
tests/sb_runtime_cover_2node.sh:22
-# RULE 0 (derived): prep 50 s measured (2/tcp QNAP) + pause 20 s + umount
+# the budget rule (derived): prep 50 s measured (2/tcp QNAP) + pause 20 s + umount
tests/affine_stale_dentry_2node.sh:35
-# RULE 0 (derived): prep 50 s measured (2/tcp QNAP) + three legs ~5 s each
+# the budget rule (derived): prep 50 s measured (2/tcp QNAP) + three legs ~5 s each
tests/hot_inode_peer_rename_2node.sh:22
-# RULE 0 (derived): prep 55 s measured (2/tcp QNAP) + create 1 s + loop ~3.5 s
+# the budget rule (derived): prep 55 s measured (2/tcp QNAP) + create 1 s + loop ~3.5 s
tests/unload_laps.sh:13
-# RULE 0 (derived): prep 45-70 s + idle + unload 12-15 s per lap.
+# the budget rule (derived): prep 45-70 s + idle + unload 12-15 s per lap.
tests/sess430_containment_chain.sh:2
-# sess430_containment_chain.sh — RULE 4 measure-then-fix chain for the D-0351
+# sess430_containment_chain.sh — instrumented measure-then-fix chain for the D-0351
tests/sess430_containment_chain.sh:4
-# quarantine; RULE-5 ruling ccmemory ccloop-c7ee71c6-sess430-GPT-ruling-d0351-
+# quarantine; design-consult ruling ccmemory ccloop-c7ee71c6-sess430-GPT-ruling-d0351-
tests/sess430_containment_chain.sh:19
-#      DISKLIVE fleet-wide; sustained_load + dirent pace rows are the RULE 0
+#      DISKLIVE fleet-wide; sustained_load + dirent pace rows are the the budget rule
tests/sess430_containment_chain.sh:21
-# RULE 0: wait <= 2400 + prep 300 + inject 120 + build 500 + proof 500 + tools
+# budget: wait <= 2400 + prep 300 + inject 120 + build 500 + proof 500 + tools
tests/sess430_containment_chain.sh:111
-  # sess431 (0.39.9): the RULE-5-required directed measurement — buffer != media
+  # sess431 (0.39.9): the design-consult-required directed measurement — buffer != media
tests/sess454_chain77_0610.sh:14
-# RULE 0 budgets: build = measured scratch compile (see the START line) → 900
+# derived time budgets: build = measured scratch compile (see the START line) → 900
tests/sess453_chain73_0593_board.sh:9
-# a RULE 6 defect, not a harness fault).
+# a the zero-defect bar defect, not a harness fault).
tests/sess453_chain73_0593_board.sh:10
-# RULE 0: summed measured walls (showstat 32/caw, 2026-08-29 board) = 1057 s;
+# budget: summed measured walls (showstat 32/caw, 2026-08-29 board) = 1057 s;
tests/native_xfs_ref.sh:2
-# tests/native_xfs_ref.sh — native-XFS reference numbers for the RULE-0 pace
+# tests/native_xfs_ref.sh — native-XFS reference numbers for the the budget rule pace
tests/dir_recreate_estale.sh:3
-# RULE 4 step 1: reproduce and NAME the stale object.
+# instrument step 1: reproduce and NAME the stale object.
tests/dir_recreate_estale.sh:20
-# RULE 0 (derived): laps L=20 at 250 ms => 5 s workload; per-node ssh ~1 s x
+# the budget rule (derived): laps L=20 at 250 ms => 5 s workload; per-node ssh ~1 s x
tests/sess448_chain61_incarnation_zero.sh:10
-# (P237-RECOV-SUPERSEDED).  RULE 0 (harness header): detect 45 s + heal 180 s
+# (P237-RECOV-SUPERSEDED).  The budget rule (harness header): detect 45 s + heal 180 s
tests/sess427_chain.sh:16
-# RULE 0: build 500 + proof 500 + tools 120 + tauth 240 + 3 preps x 300 +
+# budget: build 500 + proof 500 + tools 120 + tauth 240 + 3 preps x 300 +
tests/orphan_audit_arm.sh:15
-# Budget (RULE 0): NEG ~40s (umount+chk+remount).  Per POS try: setup ~10s
+# Budget (budget): NEG ~40s (umount+chk+remount).  Per POS try: setup ~10s
tests/sess461_chain90_samenode_exerciser.sh:6
-# the RULE-6 closure vehicle for D-SAMENODE-WAITER-CANCEL-COLLISION,
+# the defect-bar closure vehicle for D-SAMENODE-WAITER-CANCEL-COLLISION,
tests/sess461_chain90_samenode_exerciser.sh:13
-# RULE 0: per invocation 4 arms x ~26 s + harvest ≈ 110 s -> bound 150 s
+# budget: per invocation 4 arms x ~26 s + harvest ≈ 110 s -> bound 150 s
tests/sess420_token_chain.sh:10
-#     entry points and RULE 6 wants the board green on every build boarded.
+#     entry points and the zero-defect bar wants the board green on every build boarded.
tests/sess420_token_chain.sh:12
-# RULE 0 (derived): build 2x ~450 s; preps 236 s (cap 300) x2; token verify
+# the budget rule (derived): build 2x ~450 s; preps 236 s (cap 300) x2; token verify
tests/incarnation_mismatch_probe.sh:73
-# TIMING (RULE 0 — derived, not chosen)
+# TIMING (the budget rule — derived, not chosen)
tests/incarnation_mismatch_probe.sh:404
-    echo "probe: FAIL — slot $SLOT NOT reclaimed within ${HEAL_S}s (RULE 0 budget)."
+    echo "probe: FAIL — slot $SLOT NOT reclaimed within ${HEAL_S}s (derived time budget)."
tests/sess446_chain48_0356_umount_quarantine.sh:6
-# RULE 0: prep (300); umount_under_quarantine ~520 s (600); prep2 (300).
+# budget: prep (300); umount_under_quarantine ~520 s (600); prep2 (300).
tests/auth_counters.sh:8
-# The counters answer the sess96 RULE-5 ruling's question for
+# The counters answer the sess96 design-consult ruling's question for
tests/free_home_settle_repro.sh:2
-# free_home_settle_repro.sh — RULE 4 reproducer for the s432 (0.38.3) collapse:
+# free_home_settle_repro.sh — instrumented reproducer for the s432 (0.38.3) collapse:
tests/free_home_settle_repro.sh:21
-# Budget (RULE 0): 200 files x (dd+rm) ~ 2 s native; reclaim wait <= 20 s.
+# Budget (budget): 200 files x (dd+rm) ~ 2 s native; reclaim wait <= 20 s.
tests/sess466_chain98_dirshard_pace.sh:15
-# RULE 0: prep 300 (measured 86-106 s); crash_consistency 160 per run (90 s
+# budget: prep 300 (measured 86-106 s); crash_consistency 160 per run (90 s
tests/quarantine_admission.sh:4
-# acceptance test for patch item 1 of the sess377 RULE-5 ruling
+# acceptance test for patch item 1 of the sess377 design-consult ruling
tests/pr_departure.sh:4
-# THE INVARIANT UNDER TEST (sess377 RULE-5 ruling, ccmemory
+# THE INVARIANT UNDER TEST (sess377 design-consult ruling, ccmemory
tests/vergate_collapse_repro.sh:20
-# Budget (RULE 0): arm a ~4 min (writer 180s + settle), arm b ~5, arm c ~6;
+# Budget (budget): arm a ~4 min (writer 180s + settle), arm b ~5, arm c ~6;
tests/sess448_chain60_iclus_relmark_faults.sh:6
-# RULE 0: build 300, prep 300, matrix 4 arms x <=420 s + sweeps => 2000 s bound.
+# budget: build 300, prep 300, matrix 4 arms x <=420 s + sweeps => 2000 s bound.
tests/sess452_chain71_retire_pending.sh:2
-# sess452 chain 71: 0.59.2 RETIRE_PENDING — the sess451 RULE-5 STOP-SHIP #2
+# sess452 chain 71: 0.59.2 RETIRE_PENDING — the sess451 design-consult STOP-SHIP #2
tests/sess452_chain71_retire_pending.sh:21
-# RULE 0: per-arm budgets from the harness header; tools 120; prep 300
+# budget: per-arm budgets from the harness header; tools 120; prep 300
tests/sess452_chain71_retire_pending.sh:50
-  # crash 76/71 s) at ~2x measured, per RULE 0.
+  # crash 76/71 s) at ~2x measured, per the budget rule.
tests/sess447_chain51_0515_nsop_verify.sh:7
-# RULE 0: build ~120 s (bound 300); prep (300); harness ~520 s (660); prep2 (300).
+# budget: build ~120 s (bound 300); prep (300); harness ~520 s (660); prep2 (300).
tests/iflush_fence_wedge.sh:29
-# RULE 0: the 60s no-progress bound is the thing being waited on, so the poll
+# budget: the 60s no-progress bound is the thing being waited on, so the poll
tests/sess436_chain5_cc_private.sh:2
-# sess436 chain 5: RULE-5 ruling measurement 5 (headroom) — crash_consistency
+# sess436 chain 5: design-consult ruling measurement 5 (headroom) — crash_consistency
tests/sess442_chain29_0481_bootstrap_resume.sh:3
-# Original chain-28 header: build 0.48.1 (item 5d fixes from the RULE-5 code review +
+# Original chain-28 header: build 0.48.1 (item 5d fixes from the design-consult code review +
tests/sess445_chain39_0512_Aprime.sh:7
-# RULE 0: build 3-4 min (bound 500) + tools (120); prep 79-120 s (bound 300);
+# budget: build 3-4 min (bound 500) + tools (120); prep 79-120 s (bound 300);
tests/sl_straggler_capture.sh:21
-# RULE 0
+# the budget rule
tests/d379b_dirty_depart_peer_fence.sh:28
-# RULE 0 budget (measured components): sweep <=20 s, 2 mounts <=30 s,
+# derived time budget (measured components): sweep <=20 s, 2 mounts <=30 s,
tests/drc_lap_probe.sh:7
-# dir_reuse run-over-run degradation RULE-4 loop: diff consecutive tags to
+# dir_reuse run-over-run degradation troubleshooting loop: diff consecutive tags to
tests/sess449_chain66b_samenode_exerciser.sh:6
-# / collide_owed (C)) — RULE-6 closure vehicle for
+# / collide_owed (C)) — defect-bar closure vehicle for
tests/sess449_chain66b_samenode_exerciser.sh:9
-# strings.  RULE 0: harness bound 150 s (4 arms ≈ 110 s); build 300; prep
+# strings.  budget: harness bound 150 s (4 arms ≈ 110 s); build 300; prep
tests/d512_ref_matrix.c:6
- * sess413 RULE-5 ruling's long-lived-ref set against a debug-forced
+ * sess413 design-consult ruling's long-lived-ref set against a debug-forced
tests/reap_midlist_repro.sh:19
-# RULE 0 budget: setup 10s + defer<=20s + reclaim 10s + close/retire<=40s +
+# derived time budget: setup 10s + defer<=20s + reclaim 10s + close/retire<=40s +
tests/sess472_chain114_d0532_rdpace.sh:6
-# mounted from the previous chain.  RULE 0: d0532 90 s, rdpace 120 s.
+# mounted from the previous chain.  budget: d0532 90 s, rdpace 120 s.
tests/audit_ilock_nowait_pairs.sh:2
-# audit_ilock_nowait_pairs.sh — D-0532 mechanical check (RULE-5 sess474 ruling,
+# audit_ilock_nowait_pairs.sh — D-0532 mechanical check (design-consult sess474 ruling,
tests/rig_wait_free.sh:24
-# RULE 0 note: the timeout here is a DEADLOCK backstop, not a performance
+# the budget rule note: the timeout here is a DEADLOCK backstop, not a performance
tests/sess488_ailpin_fleet_umount.sh:26
-# RULE 0 budgets, derived: prep 300 (measured 106-146 s); inject 30; the mass
+# derived time budgets, derived: prep 300 (measured 106-146 s); inject 30; the mass
tests/pr_preempt_sark0_probe.sh:40
-# RULE 0 (derived): ~12 PROUT/PRIN round trips + 4 dd + ssh ~ 60 s; bound 120 s.
+# the budget rule (derived): ~12 PROUT/PRIN round trips + 4 dd + ssh ~ 60 s; bound 120 s.
tests/transport_conformance.sh:22
-# RULE 0: a TCP join measured 5-15 s, a CAW formation ~10 s, a refused mount
+# budget: a TCP join measured 5-15 s, a CAW formation ~10 s, a refused mount
tests/hot_writer_peer_reader_2node.sh:32
-# RULE 0 (derived): prep 55 s measured (2/tcp QNAP) + loop <= ITERS*0.1 s
+# the budget rule (derived): prep 55 s measured (2/tcp QNAP) + loop <= ITERS*0.1 s
tests/dir_retype_authcap_probe.sh:21
-# RULE 0 (derived): the same 300-file create the cross-grant workload does takes
+# the budget rule (derived): the same 300-file create the cross-grant workload does takes
tests/agmeta_shutdown_retire.sh:37
-# RULE 0 (derived, from the s596a-c laps): the churn is now DURATION-bounded at
+# the budget rule (derived, from the s596a-c laps): the churn is now DURATION-bounded at
tests/d0944_unpub_forms_probe.sh:30
-# RULE 0 budget: prep 2 nodes measured ~50 s (bounded 300 s); the four stages
+# derived time budget: prep 2 nodes measured ~50 s (bounded 300 s); the four stages
tests/d0941_fdw_estale_ab.sh:26
-# RULE 0 budget: the row measured 21 s of test wall on the failing board and 18 s
+# derived time budget: the row measured 21 s of test wall on the failing board and 18 s
tests/d0945_chokepoint_positive_control.sh:48
-# RULE 0 budget: one agmeta death lap, measured at 76 s; two laps plus knob
+# derived time budget: one agmeta death lap, measured at 76 s; two laps plus knob
tests/clean_depart_mass_umount.sh:27
-# RULE 0 budget: 31-way umount ~100s (sess342 measured; the serialize is
+# derived time budget: 31-way umount ~100s (sess342 measured; the serialize is
tests/clean_depart_mass_umount.sh:47
-#    RULE 2c).
+#    the unkillable-wedge rule).
tests/closure_hb_slot_reuse.sh:3
-# D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356 (sess376 RULE-5 review, question 2).
+# D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356 (sess376 design-consult review, question 2).
tests/sess447_chain56_default_on_streak.sh:2
-# sess447 chain 56 (0.54.0, production defaults): RULE-5 phase-5 streak after
+# sess447 chain 56 (0.54.0, production defaults): design-consult phase-5 streak after
tests/sess447_chain56_default_on_streak.sh:7
-# RULE 0: matrix 240; per unlinker_death case ~4 min (400) + prep 300; per NDR
+# budget: matrix 240; per unlinker_death case ~4 min (400) + prep 300; per NDR
tests/sess447_chain54_0514_lapF2.sh:14
-# RULE 0: prep 300; lap: churn 11 + gap 45 + detect 62 + replay 5 + park/hold
+# budget: prep 300; lap: churn 11 + gap 45 + detect 62 + replay 5 + park/hold
tests/sess459_chain81_d0517_buflsn_lab.sh:3
-# DANGLING-0517 — RULE 4 step 2, hypothesis H4 (cross-slice on-disk LSN skip in
+# DANGLING-0517 — instrument step 2, hypothesis H4 (cross-slice on-disk LSN skip in
tests/sess459_chain81_d0517_buflsn_lab.sh:22
-# RULE 0: build 600 (measured 286-326 s in tree), prep 300, lap 420 (harness
+# budget: build 600 (measured 286-326 s in tree), prep 300, lap 420 (harness
tests/sess470_chain110_d0527_handle_probe.sh:8
-# RULE 0: each harness run < 60 s (budget 90).
+# budget: each harness run < 60 s (budget 90).
tests/sess479_chain119_board_06437.sh:22
-# RULE 0 budget: the 29-row board summed 960 s of measured walls on the
+# derived time budget: the 29-row board summed 960 s of measured walls on the
tests/sess479_chain119_board_06437.sh:27
-# that overruns it is a RULE 0 failure to diagnose, never a timeout to widen.
+# that overruns it is a budget failure to diagnose, never a timeout to widen.
tests/hold_grant_liveness.sh:27
-# RULE 0: the wait window is derived from the thing being measured -- the base
+# budget: the wait window is derived from the thing being measured -- the base
tests/tmpfile_churn.sh:4
-# WHY (sess398, D-AGI-UNLINKED-CROSSNODE-RECOVERY-SHUTDOWN, RULE-5 ruling
+# WHY (sess398, D-AGI-UNLINKED-CROSSNODE-RECOVERY-SHUTDOWN, design-consult ruling
tests/tmpfile_churn.sh:18
-# RULE 0: the budget is DERIVED, never padded.  Each node first runs the same
+# budget: the budget is DERIVED, never padded.  Each node first runs the same
tests/tmpfile_churn.sh:22
-# RULE 2c: every remote call is bounded (timeout) and captures its own rc.
+# the unkillable-wedge rule: every remote call is bounded (timeout) and captures its own rc.
tests/p248_inject.sh:56
-# RULE 0 budgets: 20-create workload ~1s (30s cap incl. ssh), umount incl.
+# derived time budgets: 20-create workload ~1s (30s cap incl. ssh), umount incl.
tests/sess419_purge_chain.sh:9
-# RULE 0 (derived): build ~400 s cap 500; prep 236 s cap 300 x4; arm 130 s
+# the budget rule (derived): build ~400 s cap 500; prep 236 s cap 300 x4; arm 130 s
tests/d0287_2node_death_rejoin.sh:27
-# RULE 0 (derived): setup ~10 s + pause entry <= 15 s + TCP death ~62 s +
+# the budget rule (derived): setup ~10 s + pause entry <= 15 s + TCP death ~62 s +
tests/sess449_chain67_d513b_write_eio.sh:6
-# lacks the sess449 knob string.  RULE 0: harness bound 300 s (derived in the
+# lacks the sess449 knob string.  budget: harness bound 300 s (derived in the
tests/unmount_flush_clean.sh:21
-# Budget (RULE 0): a umount+prep_node cycle is ~10-15s/node; 3 cycles across
+# Budget (budget): a umount+prep_node cycle is ~10-15s/node; 3 cycles across
tests/sess445_chain40_0511_prefetch_ring.sh:8
-# RULE 0: prep 79-120 s (300); bootstrap_full_restart (1080); prep; run.sh
+# budget: prep 79-120 s (300); bootstrap_full_restart (1080); prep; run.sh
tests/d_recov_zero_epoch_verify.sh:29
-# RULE 0 (derived): setup 15 s; kill+inject 5 s; dead window 62 s + 15 s
+# the budget rule (derived): setup 15 s; kill+inject 5 s; dead window 62 s + 15 s
tests/sess421_chain.sh:15
-# RULE 0: build 500 + proof 500 + 5 preps x 300 + 740 s of arms + 1900 board
+# budget: build 500 + proof 500 + 5 preps x 300 + 740 s of arms + 1900 board
tests/drc_grantgen_probe.sh:2
-# drc_grantgen_probe.sh (sess50 ccloop 4cb2d0a2) — RULE-4 decisive measurement.
+# drc_grantgen_probe.sh (sess50 ccloop 4cb2d0a2) — instrumented decisive measurement.
tests/closure_footprint_shapes.sh:4
-# RULE-5 review of this test's own design.
+# Design-consult review of this test's own design.
tests/closure_footprint_shapes.sh:198
-# Baseline pace for the functional progress assertion (RULE 0: the post-repair
+# Baseline pace for the functional progress assertion (budget: the post-repair
tests/sess459_chain85_d0517_slots2526_lab.sh:3
-# DANGLING-0517 — RULE 4 step 2b (fix verification).  Gated on chain 81 DONE
+# DANGLING-0517 — instrument step 2b (fix verification).  Gated on chain 81 DONE
tests/sess459_chain85_d0517_slots2526_lab.sh:18
-# RULE 0: build 600 (measured 286-326 s), prep 300, lap 420 + 120 sweep,
+# budget: build 600 (measured 286-326 s), prep 300, lap 420 + 120 sweep,
tests/d0512_sf_to_block_replay.sh:3
-# SLICE-REFUSED-0512, the RULE-4 verification leg for ruling A′ (ccmemory
+# SLICE-REFUSED-0512, the instrumented verification leg for ruling A′ (ccmemory
tests/d0512_sf_to_block_replay.sh:28
-# RULE 0 (derived): srcgate 32 x ssh ~15 s; knobs ~10 s; 24 creates on an
+# the budget rule (derived): srcgate 32 x ssh ~15 s; knobs ~10 s; 24 creates on an
tests/d0512_sf_to_block_replay.sh:129
-[ "$RW" -le 200 ] && pass "death->verdict wall ${RW}s <= 200s" || fail "death->verdict wall ${RW}s > 200s (RULE 0)"
+[ "$RW" -le 200 ] && pass "death->verdict wall ${RW}s <= 200s" || fail "death->verdict wall ${RW}s > 200s (budget)"
tests/fr_mount_barrier_fail.sh:29
-# RULE 0 (derived): umount J ~5s + HB expiry ~62s + refusal observe 40s +
+# the budget rule (derived): umount J ~5s + HB expiry ~62s + refusal observe 40s +
tests/fr_mount_barrier_fail.sh:32
-# RULE 2c: comm-safe process handling only; all ssh bounded; per-node rc.
+# the unkillable-wedge rule: comm-safe process handling only; all ssh bounded; per-node rc.
tests/fr_mount_barrier_fail.sh:50
-# RULE 0 cold2 (derived): prep2 incl. 30-node teardown ~240s + churn/kill
+# the budget rule cold2 (derived): prep2 incl. 30-node teardown ~240s + churn/kill
tests/bootstrap_seal_fence.sh:31
-# RULE 0 (derived, first measurement): payload ~5 s; destroy ~10 s; boot to
+# the budget rule (derived, first measurement): payload ~5 s; destroy ~10 s; boot to
tests/bootstrap_seal_fence.sh:118
-[ "$MW" -le "$MOUNT_BOUND" ] && pass "mount attempt wall ${MW}s <= ${MOUNT_BOUND}s" || fail "mount attempt wall ${MW}s > ${MOUNT_BOUND}s (RULE 0)"
+[ "$MW" -le "$MOUNT_BOUND" ] && pass "mount attempt wall ${MW}s <= ${MOUNT_BOUND}s" || fail "mount attempt wall ${MW}s > ${MOUNT_BOUND}s (budget)"
tests/bootstrap_seal_fence.sh:141
-# RULE-5 review SS-1 (sess441): the owner's key is EVIDENCE the record names —
+# Design-consult review SS-1 (sess441): the owner's key is EVIDENCE the record names —
tests/create_stall_stacks.sh:20
-# Guessing which lock that is would violate RULE 4.  This harness instead reads
+# Guessing which lock that is would violate instrumented.  This harness instead reads
tests/domain_admission_matrix.sh:3
-# default-on, RULE-5 ruling phase 3): the MOUNT-TIME durability-domain
+# default-on, design-consult ruling phase 3): the MOUNT-TIME durability-domain
tests/domain_admission_matrix.sh:24
-# RULE 0: per row rmmod+insmod+mount attempt <= 25 s (measured insmod ~2 s,
+# budget: per row rmmod+insmod+mount attempt <= 25 s (measured insmod ~2 s,
tests/sess480_chain123_board_repeat.sh:16
-# RULE 0: board budget 1470 s, RE-DERIVED from the 0.64.37 board this repeats.
+# budget: board budget 1470 s, RE-DERIVED from the 0.64.37 board this repeats.
tests/openprotect_race_ab.sh:9
-# cold-open restart (0.11.483+) did NOT close it: per the sess238 RULE-5
+# cold-open restart (0.11.483+) did NOT close it: per the sess238 design-consult
tests/openprotect_race_ab.sh:31
-# RULE 0 budgets (derived, not round numbers):
+# derived time budgets (derived, not round numbers):
tests/uv_disktruth2.sh:11
-# Assumes the 2-node cluster is already mounted (run.sh prep).  RULE 3: in-tree.
+# Assumes the 2-node cluster is already mounted (run.sh prep).  the source-tree rule: in-tree.
tests/sess430_chain.sh:2
-# sess430_chain.sh — RULE 4 measure-then-fix chain for the D-0351 CHAIN defect
+# sess430_chain.sh — instrumented measure-then-fix chain for the D-0351 CHAIN defect
tests/sess430_chain.sh:4
-# the live image left under an inobt-free bit; RULE-5 ruling ccmemory
+# the live image left under an inobt-free bit; design-consult ruling ccmemory
tests/sess430_chain.sh:20
-# RULE 0: wait <= 2400 + repro 2 x 150 + build 500 + proof 500 + tools 120 +
+# budget: wait <= 2400 + repro 2 x 150 + build 500 + proof 500 + tools 120 +
tests/d513_lone_mount_torn.sh:49
-# 1. Unmount every node except the victim (parallel, bounded per RULE 2c).
+# 1. Unmount every node except the victim (parallel, bounded per the unkillable-wedge rule).
tests/pr_session_drop_probe.sh:35
-# RULE 0 (derived): gate 10 s + poll 110 s + victim boot to ssh ~60-90 s +
+# the budget rule (derived): gate 10 s + poll 110 s + victim boot to ssh ~60-90 s +
tests/cwr_repro.sh:6
-# lines from every node's dmesg.  Lives in the source tree per RULE 3.
+# lines from every node's dmesg.  Lives in the source tree so it survives a reboot.
tests/settle_token_arms.sh:4
-# under the host-wide departure mutex with a single-use proof token (RULE-5
+# under the host-wide departure mutex with a single-use proof token (design-consult
tests/settle_token_arms.sh:116
-# RULE 0 budgets (derived): plain/inval/double = umount 5 s + worker settle
+# derived time budgets (derived): plain/inval/double = umount 5 s + worker settle
tests/rman_matrix.sh:24
-# RULE 0: each arm is bounded by the kill harness's own derived budget (230 s
+# budget: each arm is bounded by the kill harness's own derived budget (230 s
tests/rman_matrix.sh:29
-# RULE 3: lives in tests/.  Evidence under tests/evidence/ (never the scratchpad).
+# the source-tree rule: lives in tests/.  Evidence under tests/evidence/ (never the scratchpad).
tests/rman_matrix.sh:54
-# wrap (RULE 0): measured 0.26.2 walls = non-recovery part 122..143 s + the
+# wrap (budget): measured 0.26.2 walls = non-recovery part 122..143 s + the
tests/d512_race_verify.sh:28
-# RULE 0 (derived): per leg ~8s (setup 2 + window 1.5 + waits 3 + dmesg 2)
+# the budget rule (derived): per leg ~8s (setup 2 + window 1.5 + waits 3 + dmesg 2)
tests/f2_iclus_refusal.sh:16
-# RULE 0 (derived): srcgate 5s + umount <=45s + rmmod/insmod 2x ~6s +
+# the budget rule (derived): srcgate 5s + umount <=45s + rmmod/insmod 2x ~6s +
tests/faceb_stress.sh:8
-# contiguous suffix of some node's creations.  RULE 3: lives in the tree.
+# contiguous suffix of some node's creations.  the source-tree rule: lives in the tree.
tests/vergate_livecap.sh:12
-# RULE 0: vergate mixed_build ~90 s + prep restore; caller bound 200 s.
+# budget: vergate mixed_build ~90 s + prep restore; caller bound 200 s.
tests/typeflip_ab.sh:77
-# RULE 0: every timeout below is the criterion's own recorded budget from
+# budget: every timeout below is the criterion's own recorded budget from
tests/typeflip_ab.sh:84
-# overrun -- that is the RULE 0 assertion.  The timeouts here are OUTER
+# overrun -- that is the budget assertion.  The timeouts here are OUTER
tests/append_contention_2node.sh:32
-# RULE 0 (derived): prep 55 s measured (2/tcp QNAP) + loop <= ITERS*0.1 s
+# the budget rule (derived): prep 55 s measured (2/tcp QNAP) + loop <= ITERS*0.1 s
tests/concurrent_create_race_2node.sh:28
-# RULE 0: R rounds x STEP_MS + 3 s barrier + capture ~10 s.  R=200 x 25 ms
+# budget: R rounds x STEP_MS + 3 s barrier + capture ~10 s.  R=200 x 25 ms
tests/agmeta_stale_leak_2node.sh:25
-# RULE 0 (derived): punch loop 1024 x ~3 ms = 3 s; rm + sync 1 s; two B
+# the budget rule (derived): punch loop 1024 x ~3 ms = 3 s; rm + sync 1 s; two B
tests/fleet_unload_check.sh:20
-# RULE 0 (derived): umount 5-15 s each (the 3 s ack bound + the quiesce),
+# the budget rule (derived): umount 5-15 s each (the 3 s ack bound + the quiesce),
tests/sole_survivor_restart.sh:44
-# RULE 0 (derived, not rounded).  prep 45 s measured (bound 300); write+fsync
+# the budget rule (derived, not rounded).  prep 45 s measured (bound 300); write+fsync
tests/d0941_cc_loop.sh:50
-# RULE 0 (derived): the row's manifest budget is 60 s; run.sh adds ~15 s startup
+# the budget rule (derived): the row's manifest budget is 60 s; run.sh adds ~15 s startup
tests/net2/gate1_wire.sh:11
-# ── RULE-0 budget (written BEFORE first run; tighten after healthy PASS) ──
+# ── derived time budget (written BEFORE first run; tighten after healthy PASS) ──
tests/net2/gate1_wire.sh:19
-#   run).  A timeout or overrun is a FAIL (RULE 0.3), not a retry.
+#   run).  A timeout or overrun is a FAIL (the budget rule.3), not a retry.
tests/net2/gate1_wire.sh:76
-    echo "RULE-0 overrun: ${harness_total}s > ${HARNESS_BUDGET_S}s — FAIL"
+    echo "the budget rule overrun: ${harness_total}s > ${HARNESS_BUDGET_S}s — FAIL"
tests/net2/gate2_midcomms.sh:23
-# ── RULE-0 budget (written BEFORE first run; tighten after healthy PASS) ──
+# ── derived time budget (written BEFORE first run; tighten after healthy PASS) ──
tests/net2/gate2_midcomms.sh:30
-#   tightened toward it per RULE 0)
+#   tightened toward it per the budget rule)
tests/net2/gate2_midcomms.sh:32
-#   pinning run).  A timeout or overrun is a FAIL (RULE 0.3), never a
+#   pinning run).  A timeout or overrun is a FAIL (the budget rule.3), never a
tests/net2/gate2_midcomms.sh:60
-    # ── RULE-0 budget (written BEFORE first run; success.md provisional):
+    # ── derived time budget (written BEFORE first run; success.md provisional):
tests/net2/gate2_midcomms.sh:70
-    #    power-cycle) => pinned 60 (RULE-0 tighten-toward-actual; the
+    #    power-cycle) => pinned 60 (the budget rule tighten-toward-actual; the
tests/net2/gate2_midcomms.sh:73
-    #    Overrun = FAIL (RULE 0.3).
+    #    Overrun = FAIL (the budget rule.3).
tests/net2/gate2_midcomms.sh:119
-    # the HOST is never touched — RULE 2).
+    # the HOST is never touched — the never-reboot-the-host rule).
tests/net2/gate2_midcomms.sh:258
-    echo "RULE-0 overrun: ${t_scen}s > ${SCEN_BUDGET_S}s — FAIL"
+    echo "the budget rule overrun: ${t_scen}s > ${SCEN_BUDGET_S}s — FAIL"
tests/net2/gate3_cawsanity.sh:13
-# ── RULE-0 budget (written BEFORE first run) ──
+# ── derived time budget (written BEFORE first run) ──
tests/net2/gate3_cawsanity.sh:21
-#   32 s (prep 18 s + tests 5 s) => pinned 120 s (RULE-0
+#   32 s (prep 18 s + tests 5 s) => pinned 120 s (the budget rule
tests/net2/gate4_shard.sh:23
-# ── RULE-0 budget (written BEFORE first run; tighten after healthy PASS) ──
+# ── derived time budget (written BEFORE first run; tighten after healthy PASS) ──
tests/net2/gate4_shard.sh:31
-#   tightened toward it per RULE 0.)
+#   tightened toward it per the budget rule.)
tests/net2/gate4_shard.sh:33
-#   pinning run).  A timeout or overrun is a FAIL (RULE 0.3), never a
+#   pinning run).  A timeout or overrun is a FAIL (the budget rule.3), never a
tests/net2/gate4_shard.sh:105
-    echo "gate4: RULE-0 overrun: $t_scen s > $SCEN_BUDGET_S s — FAIL"
+    echo "gate4: the budget rule overrun: $t_scen s > $SCEN_BUDGET_S s — FAIL"
tests/net2/gate5_mepoch.sh:22
-# ── RULE-0 budget (written BEFORE first run; tighten after healthy PASS) ──
+# ── derived time budget (written BEFORE first run; tighten after healthy PASS) ──
tests/net2/gate5_mepoch.sh:31
-#   A timeout or overrun is a FAIL (RULE 0.3), never a retry with a
+#   A timeout or overrun is a FAIL (the budget rule.3), never a retry with a
tests/net2/gate5_mepoch.sh:110
-    echo "gate5: RULE-0 overrun: $t_scen s > $SCEN_BUDGET_S s — FAIL"
+    echo "gate5: the budget rule overrun: $t_scen s > $SCEN_BUDGET_S s — FAIL"
tests/tauth/formation_test.c:4
- * RULE 4 step 2 for D-0287's step-3/4 landing).
+ * instrument step 2 for D-0287's step-3/4 landing).
tests/tauth/concurrent_release_test.c:4
- * waiter is queued (sess425, RULE 4 step 2 for the 0.35.0 32/tcp wedge).
+ * waiter is queued (sess425, instrument step 2 for the 0.35.0 32/tcp wedge).
tests/tauth/unowned_page_test.c:4
- * RULE 4 step 2, in usermode (sess426).
+ * instrument step 2, in usermode (sess426).
tests/tauth/bootstrap_race_test.c:4
- * RULE 4 step 2 (sess426): can two nodes that both believe they are the
+ * instrument step 2 (sess426): can two nodes that both believe they are the
tests/criteria/online_resize.sh:28
-#     RULE 0 performance criteria.)
+#     the budget rule performance criteria.)
tests/criteria/scaling_curve.sh:27
-# contention again.  RULE-4 discriminator that proved this: at 16
+# contention again.  discriminator that proved this: at 16
tests/criteria/verify_ship.sh:129
-# prep_tcm_node) inside its RULE-0 budget: chk_clean's 60s watchdog
+# prep_tcm_node) inside its derived time budget: chk_clean's 60s watchdog
tests/criteria/zero_silent_loss.sh:17
-# RULE 0 budget (tests/criteria/TIMEOUT_BUDGETS.md): 3 iters × ~120 s
+# derived time budget (tests/criteria/TIMEOUT_BUDGETS.md): 3 iters × ~120 s
tests/criteria/led115.py:8
-        e['next_step']=("sess115 RULE-5 RULING (supersedes the sess114 plan): the cached-lock code fact was ACCEPTED and blockers 2 and 3 are WITHDRAWN. "
+        e['next_step']=("sess115 design-consult RULING (supersedes the sess114 plan): the cached-lock code fact was ACCEPTED and blockers 2 and 3 are WITHDRAWN. "
tests/criteria/TIMEOUT_BUDGETS.md:1
-# Per-criterion time budgets (RULE 0 in /src/mxfs/CLAUDE.md)
+# Per-criterion time budgets
tests/criteria/TIMEOUT_BUDGETS.md:93
-| dir_reuse_coherency (>4) | shared mount | workload is O(N): every node creates DRC_NFILES into ONE shared dir/round so wall ~linear with N. NO native-XFS equivalent (XFS is not clustered); cost is NECESSARY coherent FUA I/O for concurrent same-dir adds (must read fresh leaf+data to avoid bestfree double-alloc) not waste -- see ccmemory sess21-dir_reuse-8node-speed-is-fundamental. Budget=60*N gives the 8-node case LESS relative headroom than 300s gives 4-node (144s actual). | **tcp: 100*N (8->800s); caw: 140*N (8->1120s)** | sess13(a9a03929) build 3D4A350E: 8-node=~530s standalone 8/8 PASS (round pace 10.5-25s, create wave ~15s = ~19ms/create-handoff; +30-60% vs sess21's 332s from the sess8-13 coherency pipeline [publish-before-notify, per-commit durable signal, FIX-C/D/E] — load-bearing correctness work, perf debt tracked via P131/P138 stage probes). Prior: 332s standalone (sess21). sess6(186320ae) 8/CAW build 57773CBD: steady 40s/round (create 9-16s, verify 4-14s, rm ~21s = ~26ms/unlink) -> ~1000s projected for 24 rounds. RULE-4 PROVEN structural: dirop_durable_caw=0 A/B halved the create wave but round 17 durably lost a dirent cluster-wide (799/800 on all 8 nodes) — CAW coherency is FUA-read (platter) based, so the per-op platter publish IS the pace. caw 140*N = floor x ~1.12; record the healthy PASS wall and tighten. |
+| dir_reuse_coherency (>4) | shared mount | workload is O(N): every node creates DRC_NFILES into ONE shared dir/round so wall ~linear with N. NO native-XFS equivalent (XFS is not clustered); cost is NECESSARY coherent FUA I/O for concurrent same-dir adds (must read fresh leaf+data to avoid bestfree double-alloc) not waste -- see ccmemory sess21-dir_reuse-8node-speed-is-fundamental. Budget=60*N gives the 8-node case LESS relative headroom than 300s gives 4-node (144s actual). | **tcp: 100*N (8->800s); caw: 140*N (8->1120s)** | sess13(a9a03929) build 3D4A350E: 8-node=~530s standalone 8/8 PASS (round pace 10.5-25s, create wave ~15s = ~19ms/create-handoff; +30-60% vs sess21's 332s from the sess8-13 coherency pipeline [publish-before-notify, per-commit durable signal, FIX-C/D/E] — load-bearing correctness work, perf debt tracked via P131/P138 stage probes). Prior: 332s standalone (sess21). sess6(186320ae) 8/CAW build 57773CBD: steady 40s/round (create 9-16s, verify 4-14s, rm ~21s = ~26ms/unlink) -> ~1000s projected for 24 rounds. PROVEN BY INSTRUMENT structural: dirop_durable_caw=0 A/B halved the create wave but round 17 durably lost a dirent cluster-wide (799/800 on all 8 nodes) — CAW coherency is FUA-read (platter) based, so the per-op platter publish IS the pace. caw 140*N = floor x ~1.12; record the healthy PASS wall and tighten. |
tests/criteria/TIMEOUT_BUDGETS.md:120
-seconds-to-minutes or "they will never use it."  This is RULE 0
+seconds-to-minutes or "they will never use it."  This is the budget rule
tests/criteria/TIMEOUT_BUDGETS.md:136
-Known product debt these budgets EXPOSE (task list, RULE-4 sequence):
+Known product debt these budgets EXPOSE (task list, instrument-first sequence):
tests/criteria/TIMEOUT_BUDGETS.md:157
-flat bar is below the RULE-0 native×2 ceiling formula.  The recorded
+flat bar is below the 2x-native-XFS ceiling formula.  The recorded
tests/criteria/TIMEOUT_BUDGETS.md:159
-moves is a user decision (RULE 0 forbids widening toward a wall, but the
+moves is a user decision (the budget rule forbids widening toward a wall, but the
tests/criteria/TIMEOUT_BUDGETS.md:160
-native×2 formula is RULE 0's own ceiling standard).  FS-side debt that
+native×2 formula is the budget rule's own ceiling standard).  FS-side debt that
tests/criteria/TIMEOUT_BUDGETS.md:166
-Written down BEFORE the run, per RULE 0.  The criterion carries its own
+Written down BEFORE the run, per the budget rule.  The criterion carries its own
tests/criteria/TIMEOUT_BUDGETS.md:178
-on these same steps — up to 7x the derived budget on a 72s prep.  RULE 0 names
+on these same steps — up to 7x the derived budget on a 72s prep.  The budget rule names
tests/criteria/TIMEOUT_BUDGETS.md:184
-`run.sh` is itself the RULE 0 enforcer: it measures each criterion and prints
+`run.sh` is itself the budget enforcer: it measures each criterion and prints
tests/criteria/TIMEOUT_BUDGETS.md:208
-Actual walls vs budgets, for the RULE 0 tightening pass once a second healthy
+Actual walls vs budgets, for the the budget rule tightening pass once a second healthy
tests/criteria/TIMEOUT_BUDGETS.md:307
-re-runs on a populated fs are 16 s.  Its 90 s budget is a RULE 0 assertion
+re-runs on a populated fs are 16 s.  Its 90 s budget is a budget assertion
tests/criteria/TIMEOUT_BUDGETS.md:335
-expiry recovery window) — that is the lap's RULE 0 shape, not slack.  Bound
+expiry recovery window) — that is the lap's budget shape, not slack.  Bound
tests/criteria/TIMEOUT_BUDGETS.md:349
-a prep 1.2-2.5 s), native 0.02 s — the D-400 RULE 0 failure unchanged.
+a prep 1.2-2.5 s), native 0.02 s — the D-400 budget failure unchanged.
tests/criteria/TIMEOUT_BUDGETS.md:394
-~62 s = ~200 s measured 203; RULE 0 = fixed + 2x the variable part = 129 +
+~62 s = ~200 s measured 203; the budget rule = fixed + 2x the variable part = 129 +
tests/criteria/TIMEOUT_BUDGETS.md:396
-A mount over 300 s is a RULE 0 failure to diagnose.  node_death_replay:
+A mount over 300 s is a budget failure to diagnose.  node_death_replay:
tests/criteria/TIMEOUT_BUDGETS.md:440
-fault chunk) and derive each chunk from its own rows.  The CLAUDE.md RULE 0
+fault chunk) and derive each chunk from its own rows.  The CLAUDE.md the budget rule
tests/criteria/posix_one_test.sh:5
-# fast RULE-4 iteration on a single failing cluster test (e.g.
+# fast instrumented iteration on a single failing cluster test (e.g.
tests/criteria/records/D-TCP-RECOVERY-COMPLETE-IGNORES-PARTIAL-LEDGER-PURGE-0342.json:10
-  "next": "FIX SHAPE (not landed — sequenced AFTER the 0.35.1 rig verdict for D-0341 so the two are attributable, RULE 4 one-fix-per-measurement): in mxfs_v5_dlm_recovery_complete2 treat a non-zero return of mxfs_dlm_ledger_purge_owner as a HELD failure exactly like the CAW hb-purge failure at the same site — log P-TAUTH-COMPLETE-LEDGER-PURGEFAIL, V5_COMPLETE_HELD_FAIL(res, rc, \"ledger-purge\", MXFS_RECOV_STAGE_GRANTS_RELEASED, true), return before mxfs_dlm_purge_node / takeover / disklock purge, so the ladder retries the purge (the ledger purge is idempotent per page) and the slot is never republished over retained bits. Also audit the other five purge_owner sites (v5_mount.c:970 GOODBYE, :2825, :3840, :3904, :3937 and dlm.c:1389 handoff) for the same ignored return: a departure/GOODBYE path that publishes departure over a partial purge has the same hole. Verify: usermode — ledger fault knob (fail_commit_once_rc) on a purge page in a dlm_ledger_test scenario proves the ladder holds and retries; rig — d0287_remaster_measure / node-death runs show zero P-TAUTH-PURGE-PARTIAL without a matching held-fail + retry line, slot republished only after a clean purge."
+  "next": "FIX SHAPE (not landed — sequenced AFTER the 0.35.1 rig verdict for D-0341 so the two are attributable, instrumented one-fix-per-measurement): in mxfs_v5_dlm_recovery_complete2 treat a non-zero return of mxfs_dlm_ledger_purge_owner as a HELD failure exactly like the CAW hb-purge failure at the same site — log P-TAUTH-COMPLETE-LEDGER-PURGEFAIL, V5_COMPLETE_HELD_FAIL(res, rc, \"ledger-purge\", MXFS_RECOV_STAGE_GRANTS_RELEASED, true), return before mxfs_dlm_purge_node / takeover / disklock purge, so the ladder retries the purge (the ledger purge is idempotent per page) and the slot is never republished over retained bits. Also audit the other five purge_owner sites (v5_mount.c:970 GOODBYE, :2825, :3840, :3904, :3937 and dlm.c:1389 handoff) for the same ignored return: a departure/GOODBYE path that publishes departure over a partial purge has the same hole. Verify: usermode — ledger fault knob (fail_commit_once_rc) on a purge page in a dlm_ledger_test scenario proves the ladder holds and retries; rig — d0287_remaster_measure / node-death runs show zero P-TAUTH-PURGE-PARTIAL without a matching held-fail + retry line, slot republished only after a clean purge."
tests/criteria/records/D-TCP-CONCURRENT-RELEASE-DOUBLE-GRANT-PAGE-WEDGE-0341.json:9
-  "mechanism": "dlm/dlm.c: (1) promote_waiters used lk_is_live_holder(), which excludes PENDING_RELEASE — documented as 'the one being retired in this very transition' but applied to ALL of them; releases are processed concurrently on the master (local unlock threads vs the receive thread), so a sibling holder in PENDING_RELEASE (its own commit in flight) was invisible and the EX was decided over its durable bit. (2) dlm_txn_commit bundles release + successor grants in one ledger commit; tauth_ledger.c apply_op refuses the grant with -EBUSY (DOUBLE-GRANT, fail-closed by the sess424 ruling) which fails the whole bundle. (3) dlm_txn_finalize on a refused bundle kept the release entry PENDING_RELEASE ('the releaser retries') and ACKed MXFS_ERR_LEDGER; the releaser's resend hit process_remote_release's PENDING_RELEASE branch, which ACKed MXFS_OK as a 'duplicate' — the releaser stopped, the master never re-drove it. (4) A denied grant (MXFS_ERR_LEDGER) mapped to -EIO at the requester. Reproduced deterministically in usermode (tests/tauth/concurrent_release_test, one-shot ledger commit-delay knob): first lap on the unfixed engine = P-TAUTH-DOUBLE-GRANT 'EX vs holders=0x2', B's EX rc=-5 after 70 ms, platter record holders=0x4 stuck, 1 in-flight entry on the page forever, A's release ACKed OK. RULE-5 ruling (sess425 consult, banked in docs/rulings/concurrent-release-fix-shape.md): (1)+(2) sound with 'every retirement triggers a promotion re-scan'; the master must re-commit the release alone and never depend on the releaser; a retry finding PENDING_RELEASE must not be ACKed OK; invariant: every PENDING_* entry has exactly one live master-owned driver.",
+  "mechanism": "dlm/dlm.c: (1) promote_waiters used lk_is_live_holder(), which excludes PENDING_RELEASE — documented as 'the one being retired in this very transition' but applied to ALL of them; releases are processed concurrently on the master (local unlock threads vs the receive thread), so a sibling holder in PENDING_RELEASE (its own commit in flight) was invisible and the EX was decided over its durable bit. (2) dlm_txn_commit bundles release + successor grants in one ledger commit; tauth_ledger.c apply_op refuses the grant with -EBUSY (DOUBLE-GRANT, fail-closed by the sess424 ruling) which fails the whole bundle. (3) dlm_txn_finalize on a refused bundle kept the release entry PENDING_RELEASE ('the releaser retries') and ACKed MXFS_ERR_LEDGER; the releaser's resend hit process_remote_release's PENDING_RELEASE branch, which ACKed MXFS_OK as a 'duplicate' — the releaser stopped, the master never re-drove it. (4) A denied grant (MXFS_ERR_LEDGER) mapped to -EIO at the requester. Reproduced deterministically in usermode (tests/tauth/concurrent_release_test, one-shot ledger commit-delay knob): first lap on the unfixed engine = P-TAUTH-DOUBLE-GRANT 'EX vs holders=0x2', B's EX rc=-5 after 70 ms, platter record holders=0x4 stuck, 1 in-flight entry on the page forever, A's release ACKed OK. Design-consult ruling (sess425 consult, banked in docs/rulings/concurrent-release-fix-shape.md): (1)+(2) sound with 'every retirement triggers a promotion re-scan'; the master must re-commit the release alone and never depend on the releaser; a retry finding PENDING_RELEASE must not be ACKed OK; invariant: every PENDING_* entry has exactly one live master-owned driver.",
tests/criteria/records/D-TAUTH-BOOTSTRAP-CLAIM-NOT-EXCLUSIVE-ACROSS-FALSE-DEATH-0347.json:7
-  "found": "sess426 (ccloop c7ee71c6), 2026-08-28: RULE-5 design review of the D-0345 fix (docs/rulings/unowned-page-route.md, points 2-3) plus code reading of dlm/disklock.c mxfs_disklock_lowest_live_slot (7342: a node's OWN slot is returned before any other; other slots count only while node_track[slot].live) and dlm/tauth_ledger.c mxfs_tauth_ledger_activate(bootstrap=true) (520-588: read page, require auth_state==UNOWNED, then lpage_write_fresh_locked = write + exact readback). Not yet observed on the rig; evidence-backed by the interleaving below.",
+  "found": "sess426 (ccloop c7ee71c6), 2026-08-28: design-consult design review of the D-0345 fix (docs/rulings/unowned-page-route.md, points 2-3) plus code reading of dlm/disklock.c mxfs_disklock_lowest_live_slot (7342: a node's OWN slot is returned before any other; other slots count only while node_track[slot].live) and dlm/tauth_ledger.c mxfs_tauth_ledger_activate(bootstrap=true) (520-588: read page, require auth_state==UNOWNED, then lpage_write_fresh_locked = write + exact readback). Not yet observed on the rig; evidence-backed by the interleaving below.",
tests/criteria/records/D-TAUTH-BOOTSTRAP-CLAIM-NOT-EXCLUSIVE-ACROSS-FALSE-DEATH-0347.json:9
-  "mechanism": "PROVEN sess426 (RULE 4 2b, usermode): tests/tauth/bootstrap_race_test (2 vnodes, divergent views A:{A,B} B:{B}, force_bootstrap_all, A's activate held 200 ms between platter read and write via the new ledger knob activate_hold_once_ms): before the fix A rc=0 EX AND B rc=0 EX on the same resource; A mine=1 B mine=1; platter ACTIVE(A) seq=5 after B's ACTIVATE seq=2 + grant seq=3 \u2014 A's write (base = the UNOWNED image at seq 1) became the highest seq and ERASED B's activation and committed grant. Root (dlm/tauth_store.c mxfs_tauth_page_write, pre-0.36.0): the write re-read only the copies' seqs (next=max+1, target=spare copy) and verified only its own copy's readback; nothing tied the commit to the image the caller patched. The bootstrap election cannot be made exclusive at the requester (a falsely-dead node cannot know), so the ledger write itself must arbitrate. || DESIGN-PROVEN interleaving (RULE 4 step 1/2 pending): A = slot 1 (HB thread stalled > death timeout, process alive), B = slot 2. B: node_track[1].live=false -> lowest_live_slot()==2 -> bootstrap_cb true. A: lowest_live_slot()==1 (own slot short-circuit). Page P UNOWNED. A: activate(P,bootstrap): read UNOWNED, write ACTIVE(A) seq1, readback == own -> MINE. B: activate(P,bootstrap): read UNOWNED (before A's write lands) , write ACTIVE(B) seq1, readback == own -> MINE. Platter ends ACTIVE(B); A's cache says ACTIVE(A). A's later txn commits are page writes that overwrite B's (same-seq race is only -EIO if the readback happens to see the other image). Fence of A lands seconds later. Also needs proof of whether the death->fence ordering (fence_before_recovery) covers the bootstrap election: if the election waited for fence completion (mxfs_disklock node_track fenced flag) the window closes for (2) but not for (1) (A never learns it is dead until its own writes fail).",
+  "mechanism": "PROVEN sess426 (instrumented 2b, usermode): tests/tauth/bootstrap_race_test (2 vnodes, divergent views A:{A,B} B:{B}, force_bootstrap_all, A's activate held 200 ms between platter read and write via the new ledger knob activate_hold_once_ms): before the fix A rc=0 EX AND B rc=0 EX on the same resource; A mine=1 B mine=1; platter ACTIVE(A) seq=5 after B's ACTIVATE seq=2 + grant seq=3 \u2014 A's write (base = the UNOWNED image at seq 1) became the highest seq and ERASED B's activation and committed grant. Root (dlm/tauth_store.c mxfs_tauth_page_write, pre-0.36.0): the write re-read only the copies' seqs (next=max+1, target=spare copy) and verified only its own copy's readback; nothing tied the commit to the image the caller patched. The bootstrap election cannot be made exclusive at the requester (a falsely-dead node cannot know), so the ledger write itself must arbitrate. || DESIGN-PROVEN interleaving (instrument step 1/2 pending): A = slot 1 (HB thread stalled > death timeout, process alive), B = slot 2. B: node_track[1].live=false -> lowest_live_slot()==2 -> bootstrap_cb true. A: lowest_live_slot()==1 (own slot short-circuit). Page P UNOWNED. A: activate(P,bootstrap): read UNOWNED, write ACTIVE(A) seq1, readback == own -> MINE. B: activate(P,bootstrap): read UNOWNED (before A's write lands) , write ACTIVE(B) seq1, readback == own -> MINE. Platter ends ACTIVE(B); A's cache says ACTIVE(A). A's later txn commits are page writes that overwrite B's (same-seq race is only -EIO if the readback happens to see the other image). Fence of A lands seconds later. Also needs proof of whether the death->fence ordering (fence_before_recovery) covers the bootstrap election: if the election waited for fence completion (mxfs_disklock node_track fenced flag) the window closes for (2) but not for (1) (A never learns it is dead until its own writes fail).",
tests/criteria/records/D-TAUTH-BOOTSTRAP-CLAIM-NOT-EXCLUSIVE-ACROSS-FALSE-DEATH-0347.json:10
-  "next": "SESS426 FIX LANDED IN TREE as 0.36.0 (UNBUILT at write time; builds in the queued s428 chain, tests/evidence/rig_after_s428_main.log -> sess425_s428.log) per the RULE-5 ruling docs/rulings/tauth-conditional-commit-ticket-caw.md: mxfs_tauth_page_write is now a CONDITIONAL COMMIT \u2014 (1) the caller's image carries its base token (hdr.seq + hdr.write_nonce of the committed image it was derived from; 0/0 = repair of a page with no valid copy) and the write refuses -ESTALE (P-TAUTH-STALE-BASE) unless the platter's unique highest valid image matches it exactly; (2) the spare copy's sector 0 as read is the compare value of a SCSI COMPARE AND WRITE that installs a 512 B TICKET (struct mxfs_tauth_ticket, magic ATTK, page/fs_gen/proposed_seq/base_seq/base_nonce/writer node+inc/nonce/crc) making that copy an invalid image; miscompare = -ESTALE (one winner per spare); a live foreign ticket = -EBUSY unless the store's fenced_cb (dlm_owner_purged, i.e. recovery-purged after the PR fence) says that incarnation is fenced (P-TAUTH-TICKET-TAKEOVER) or it is our own abandoned ticket (resume); (3) ticket flushed before any body byte; body sectors 1..7 FUA + flush; (4) publish = CAW(ticket -> final sector 0) then flush; miscompare = P-TAUTH-TICKET-STOLEN -EIO (poison + reconcile); (5) readback: exact match, or a later valid seq / a ticket citing our seq as base on our copy = SUPERSEDED (committed). Ledger: -ESTALE/-EBUSY from the store reload the cache and return without poisoning (stale_writes counter). PAL: usermode mxfs_pal_bdev_compare_and_write emulates CAW on a regular file under a process-wide mutex (kernel path unchanged: SCSI CAW with FUA). VERIFIED usermode: bootstrap_race_test PASS (A refused P-TAUTH-STALE-BASE base=1/0 platter=3, B alone MINE, platter ACTIVE(B)); unowned_page/formation/concurrent_release/dlm_ledger/ledger/tauth suites PASS (torn-write tests exercise the abandoned-ticket resume path). RIG VERIFY (s428, 0.36.0 on 32/tcp mpatha): build rc=0; prep 32/32 mounts; token + d0287 stages; sweep tags: zero P-TAUTH-STALE-BASE/-TICKET-STOLEN/-VERIFY-FAIL/-CONFLICT under normal operation (STALE-BASE is legal only when two masters race \u2014 none expected in a converged view); P-TAUTH-STATS commit avg_ms must stay within 2x of the s427 figures (each commit is now 2 CAW + 1 body write + 3 flushes). Then a stalled-heartbeat injection on the lowest-slot node (tests/d512_t2_pause.sh pattern) with both claimers targeting one never-decided page must show exactly one authority (RULE 6 verification of the rig shape) before FIXED AND VERIFIED. Remaining hazard from the ruling, NOT yet addressed: bootstrap_cb should also require the lower slots to be FENCED (not merely !live) and a node whose own heartbeat is older than the death timeout must not claim \u2014 the ticket makes the race safe, that item only narrows the window; track under this record until the injection run says the fence ordering is irrelevant. || 1) MEASURE (RULE 4): usermode tests/tauth harness with two vnodes both answering bootstrap_cb=true and a shared UNOWNED page \u2014 assert exactly one ends ACTIVE(self) in its cache and the other refuses (expected: both MINE -> proven). 2) RULE-5 design: make the claim a true CAS \u2014 the rig's LUN supports SCSI COMPARE AND WRITE (the CAW transport's primitive, dlm/dlm_caw.c / mxfs_pal_scsi_caw); tauth_store claim path = CAW on the page header sector {UNOWNED, seq} -> ACTIVE(self); fall back to refusing claims until the fence has landed (bootstrap_cb must require node_track[lower].fenced, not merely !live) and to re-validating own-liveness (a node whose own HB write is older than the death timeout must not claim). 3) Verify: the 2-claimer harness shows one winner; rig 32/tcp with a stalled-HB injection on the lowest slot (tests/d512_t2_pause.sh pattern) shows one authority per page and zero P-TAUTH-DOUBLE-GRANT."
+  "next": "SESS426 FIX LANDED IN TREE as 0.36.0 (UNBUILT at write time; builds in the queued s428 chain, tests/evidence/rig_after_s428_main.log -> sess425_s428.log) per the design-consult ruling docs/rulings/tauth-conditional-commit-ticket-caw.md: mxfs_tauth_page_write is now a CONDITIONAL COMMIT \u2014 (1) the caller's image carries its base token (hdr.seq + hdr.write_nonce of the committed image it was derived from; 0/0 = repair of a page with no valid copy) and the write refuses -ESTALE (P-TAUTH-STALE-BASE) unless the platter's unique highest valid image matches it exactly; (2) the spare copy's sector 0 as read is the compare value of a SCSI COMPARE AND WRITE that installs a 512 B TICKET (struct mxfs_tauth_ticket, magic ATTK, page/fs_gen/proposed_seq/base_seq/base_nonce/writer node+inc/nonce/crc) making that copy an invalid image; miscompare = -ESTALE (one winner per spare); a live foreign ticket = -EBUSY unless the store's fenced_cb (dlm_owner_purged, i.e. recovery-purged after the PR fence) says that incarnation is fenced (P-TAUTH-TICKET-TAKEOVER) or it is our own abandoned ticket (resume); (3) ticket flushed before any body byte; body sectors 1..7 FUA + flush; (4) publish = CAW(ticket -> final sector 0) then flush; miscompare = P-TAUTH-TICKET-STOLEN -EIO (poison + reconcile); (5) readback: exact match, or a later valid seq / a ticket citing our seq as base on our copy = SUPERSEDED (committed). Ledger: -ESTALE/-EBUSY from the store reload the cache and return without poisoning (stale_writes counter). PAL: usermode mxfs_pal_bdev_compare_and_write emulates CAW on a regular file under a process-wide mutex (kernel path unchanged: SCSI CAW with FUA). VERIFIED usermode: bootstrap_race_test PASS (A refused P-TAUTH-STALE-BASE base=1/0 platter=3, B alone MINE, platter ACTIVE(B)); unowned_page/formation/concurrent_release/dlm_ledger/ledger/tauth suites PASS (torn-write tests exercise the abandoned-ticket resume path). RIG VERIFY (s428, 0.36.0 on 32/tcp mpatha): build rc=0; prep 32/32 mounts; token + d0287 stages; sweep tags: zero P-TAUTH-STALE-BASE/-TICKET-STOLEN/-VERIFY-FAIL/-CONFLICT under normal operation (STALE-BASE is legal only when two masters race \u2014 none expected in a converged view); P-TAUTH-STATS commit avg_ms must stay within 2x of the s427 figures (each commit is now 2 CAW + 1 body write + 3 flushes). Then a stalled-heartbeat injection on the lowest-slot node (tests/d512_t2_pause.sh pattern) with both claimers targeting one never-decided page must show exactly one authority (the zero-defect bar verification of the rig shape) before FIXED AND VERIFIED. Remaining hazard from the ruling, NOT yet addressed: bootstrap_cb should also require the lower slots to be FENCED (not merely !live) and a node whose own heartbeat is older than the death timeout must not claim \u2014 the ticket makes the race safe, that item only narrows the window; track under this record until the injection run says the fence ordering is irrelevant. || 1) MEASURE (instrumented): usermode tests/tauth harness with two vnodes both answering bootstrap_cb=true and a shared UNOWNED page \u2014 assert exactly one ends ACTIVE(self) in its cache and the other refuses (expected: both MINE -> proven). 2) design-consult design: make the claim a true CAS \u2014 the rig's LUN supports SCSI COMPARE AND WRITE (the CAW transport's primitive, dlm/dlm_caw.c / mxfs_pal_scsi_caw); tauth_store claim path = CAW on the page header sector {UNOWNED, seq} -> ACTIVE(self); fall back to refusing claims until the fence has landed (bootstrap_cb must require node_track[lower].fenced, not merely !live) and to re-validating own-liveness (a node whose own HB write is older than the death timeout must not claim). 3) Verify: the 2-claimer harness shows one winner; rig 32/tcp with a stalled-HB injection on the lowest slot (tests/d512_t2_pause.sh pattern) shows one authority per page and zero P-TAUTH-DOUBLE-GRANT."
tests/criteria/records/D-TCP-LOCAL-ABANDONED-REQUEST-ORPHAN-DURABLE-GRANT-0343.json:7
-  "found": "sess425 (ccloop c7ee71c6), 2026-08-28: RULE-5 audit item (c) of the D-0341 ruling ('cancel/release during PENDING_DURABLE'), proven by usermode reproducer tests/tauth/concurrent_release_test scenario 5 on the 0.35.1 engine before the fix",
+  "found": "sess425 (ccloop c7ee71c6), 2026-08-28: design-consult audit item (c) of the D-0341 ruling ('cancel/release during PENDING_DURABLE'), proven by usermode reproducer tests/tauth/concurrent_release_test scenario 5 on the 0.35.1 engine before the fix",
tests/criteria/records/D-TCP-ROOT-INODE-LOCK-RETRY-EXHAUSTION-DURING-RAMP-0345.json:8
-  "summary": "On the TCP transport (0.35.1, 32 nodes, mpatha) a node that (re)joins while the membership is still moving cannot lock the ROOT directory inode (ino=128, EX): every request exhausts its 10-retry budget (~6 s each) and the file operation fails (-EAGAIN to the caller: dd/create on /mnt/shared fails), repeatedly for 80+ s; the 4-node token workload made no progress for its whole 30 s budget (tokens did not advance on any of the 4 nodes). No deny status (REMASTER/LEDGER_BUSY/LEDGER) appears in the node's dmesg for the window, so the RETRY answers came from a LOCAL site of dlm_lock_impl (membership-purge pending completion, ledger-prepare -EAGAIN 'page not ours/frozen', or the requester's own in-flight entry). The node also logs a BAST storm for ino=128 with ex=0 pr=0 pin=1 — it is BASTed for a lock it does not hold while its own request keeps failing. RULE 0: the workload's native equivalent is seconds; a node unable to create a file in the root for 80 s is a correctness/availability defect of the join ramp.",
+  "summary": "On the TCP transport (0.35.1, 32 nodes, mpatha) a node that (re)joins while the membership is still moving cannot lock the ROOT directory inode (ino=128, EX): every request exhausts its 10-retry budget (~6 s each) and the file operation fails (-EAGAIN to the caller: dd/create on /mnt/shared fails), repeatedly for 80+ s; the 4-node token workload made no progress for its whole 30 s budget (tokens did not advance on any of the 4 nodes). No deny status (REMASTER/LEDGER_BUSY/LEDGER) appears in the node's dmesg for the window, so the RETRY answers came from a LOCAL site of dlm_lock_impl (membership-purge pending completion, ledger-prepare -EAGAIN 'page not ours/frozen', or the requester's own in-flight entry). The node also logs a BAST storm for ino=128 with ex=0 pr=0 pin=1 — it is BASTed for a lock it does not hold while its own request keeps failing. budget: the workload's native equivalent is seconds; a node unable to create a file in the root for 80 s is a correctness/availability defect of the join ramp.",
tests/criteria/records/D-TCP-ROOT-INODE-LOCK-RETRY-EXHAUSTION-DURING-RAMP-0345.json:9
-  "mechanism": "UNDER MEASUREMENT (RULE 4 step 2). 0.35.1's exhaustion message named no cause. sess425 instrumented dlm/dlm.c: every MXFS_DLM_RETRY-producing site in dlm_lock_impl is tagged (dlm_retry(ctx, why): 1 remote pending completed RETRY, 2 DENY REMASTER, 3 DENY LEDGER_BUSY, 4 dlm_ledger_prepare -EAGAIN, 5 own PENDING_DURABLE attach-wait without grant, 6 own PENDING_RELEASE, 7 local decision refused, 8 local grant txn remaster, 9 local WAITING pending completed RETRY) and mxfs_dlm_lock_retries prints the per-request tally in the 'lock request failed after N retries' line (type/ino/mode/last_rc/why[...]). Also fixed in 0.35.2 (candidate contributor, proven in usermode formation_test lap 'fails=1'): a requester whose own grant was mid-commit (PENDING_DURABLE) was answered RETRY after 10 ms and burnt its budget under a slow page write; it now attaches to the in-flight transition (pending registered under the table lock, waits one acquire window). Candidate for the rig shape: site 4 (dlm_ledger_prepare -EAGAIN while the root inode's page is being handed off during the ramp — 100 ms retry cadence x 10 = ~1 s per request, but the observed 6.2 s per failure suggests site 9/1 (1 s pending wait each) — the tally decides.",
+  "mechanism": "UNDER MEASUREMENT (instrument step 2). 0.35.1's exhaustion message named no cause. sess425 instrumented dlm/dlm.c: every MXFS_DLM_RETRY-producing site in dlm_lock_impl is tagged (dlm_retry(ctx, why): 1 remote pending completed RETRY, 2 DENY REMASTER, 3 DENY LEDGER_BUSY, 4 dlm_ledger_prepare -EAGAIN, 5 own PENDING_DURABLE attach-wait without grant, 6 own PENDING_RELEASE, 7 local decision refused, 8 local grant txn remaster, 9 local WAITING pending completed RETRY) and mxfs_dlm_lock_retries prints the per-request tally in the 'lock request failed after N retries' line (type/ino/mode/last_rc/why[...]). Also fixed in 0.35.2 (candidate contributor, proven in usermode formation_test lap 'fails=1'): a requester whose own grant was mid-commit (PENDING_DURABLE) was answered RETRY after 10 ms and burnt its budget under a slow page write; it now attaches to the in-flight transition (pending registered under the table lock, waits one acquire window). Candidate for the rig shape: site 4 (dlm_ledger_prepare -EAGAIN while the root inode's page is being handed off during the ramp — 100 ms retry cadence x 10 = ~1 s per request, but the observed 6.2 s per failure suggests site 9/1 (1 s pending wait each) — the tally decides.",
tests/criteria/records/D-RECREATED-SHARED-DIR-PEER-MKDIR-ESTALE-0346.json:9
-  "mechanism": "SESS426 NARROWED (RULE 4 step 1->2) from the board archive /tmp/run_fence_during_write_20260828T103802Z kernlog_test{5,6,8,9,11,12,13,14,15,16,31}: on every failing node, at the FDW-MKDIR-FAIL second, the lookup of '.fence_during_write' under dp=128 (comm=mkdir) hits a POISONED SHELL of the previous incarnation of that directory (test5: ino=538051 gen=153860121, nlink=2, i_state=0x10100 = I_DONTCACHE|I_REFERENCED) with i_count=2 (test16: 3); P34H-POISON-EVICT try 1..4 (d_mark_dontcache + d_prune_aliases + irele + retry_iget) never retires it, P34H-POISON-UNRETIRED tries=5 fails the lookup -ESTALE (P-LKERR stage=3 err=-116), x3 per node (mkdir -p's two components + the retry). Also present on every failing node: P-DIRCRC-RETRY-FAIL daddr=34029648 ops=xfs_dir3_data err=-117 'durable, not transient' x27 \u2014 a dir data block of the recreated directory family failing CRC (separate arm to explain). Hypothesis H1: d_prune_aliases drops only UNUSED aliases; the peer's cached CHILD dentries of the old directory (node<R>, hot, f0..f39 from the previous fence_during_write row) pin the old parent dentry's d_count, so the dentry keeps its inode reference, i_count never reaches the last iput, and every retry_iget cache-hits the same dead shell. Fix shape if proven: d_invalidate() (shrink_dcache_parent + d_drop) on each alias instead of d_prune_aliases, in the same retire loop. Instrumentation landed (0.36.0 tree, builds in s428): P34H-POISON-ALIAS ino= dentry=%pd d_count= children= d_flags= printed for every alias before d_prune_aliases. || NOT YET MEASURED (RULE 4 step 1). Candidates: the peer's negative/positive dentry for '.fence_during_write' (dir_epoch / P165-AFFINE-STALE machinery) not invalidated by the cross-node unlink+create of a directory whose inode number is reused (P128-REARM-UNPUB 'cache-hit CREATE on reused inode' and P165-AFFINE-STALE lines are in kernlog_test5 at the failure second); mkdir's lookup of the parent returning the stale in-core inode (i_mxfs_self_created / incarnation shell, cf. D-INCARN-STALE-SHELL-UNGATED-FILE-READS-512) whose generation no longer matches the on-disk dinode -> ESTALE from xfs_iget. The test's retry masks it after 0.5 s, so the window is bounded by a BAST/epoch refresh.",
+  "mechanism": "SESS426 NARROWED (instrument step 1->2) from the board archive /tmp/run_fence_during_write_20260828T103802Z kernlog_test{5,6,8,9,11,12,13,14,15,16,31}: on every failing node, at the FDW-MKDIR-FAIL second, the lookup of '.fence_during_write' under dp=128 (comm=mkdir) hits a POISONED SHELL of the previous incarnation of that directory (test5: ino=538051 gen=153860121, nlink=2, i_state=0x10100 = I_DONTCACHE|I_REFERENCED) with i_count=2 (test16: 3); P34H-POISON-EVICT try 1..4 (d_mark_dontcache + d_prune_aliases + irele + retry_iget) never retires it, P34H-POISON-UNRETIRED tries=5 fails the lookup -ESTALE (P-LKERR stage=3 err=-116), x3 per node (mkdir -p's two components + the retry). Also present on every failing node: P-DIRCRC-RETRY-FAIL daddr=34029648 ops=xfs_dir3_data err=-117 'durable, not transient' x27 \u2014 a dir data block of the recreated directory family failing CRC (separate arm to explain). Hypothesis H1: d_prune_aliases drops only UNUSED aliases; the peer's cached CHILD dentries of the old directory (node<R>, hot, f0..f39 from the previous fence_during_write row) pin the old parent dentry's d_count, so the dentry keeps its inode reference, i_count never reaches the last iput, and every retry_iget cache-hits the same dead shell. Fix shape if proven: d_invalidate() (shrink_dcache_parent + d_drop) on each alias instead of d_prune_aliases, in the same retire loop. Instrumentation landed (0.36.0 tree, builds in s428): P34H-POISON-ALIAS ino= dentry=%pd d_count= children= d_flags= printed for every alias before d_prune_aliases. || NOT YET MEASURED (instrument step 1). Candidates: the peer's negative/positive dentry for '.fence_during_write' (dir_epoch / P165-AFFINE-STALE machinery) not invalidated by the cross-node unlink+create of a directory whose inode number is reused (P128-REARM-UNPUB 'cache-hit CREATE on reused inode' and P165-AFFINE-STALE lines are in kernlog_test5 at the failure second); mkdir's lookup of the parent returning the stale in-core inode (i_mxfs_self_created / incarnation shell, cf. D-INCARN-STALE-SHELL-UNGATED-FILE-READS-512) whose generation no longer matches the on-disk dinode -> ESTALE from xfs_iget. The test's retry masks it after 0.5 s, so the window is bounded by a BAST/epoch refresh.",
tests/criteria/records/D-RECREATED-SHARED-DIR-PEER-MKDIR-ESTALE-0346.json:10
-  "next": "SESS426: 1) run tests/dir_recreate_estale.sh <label> test1 test2 test3 test4 20 250 on 32/caw with 0.36.0 (budget 60 s) \u2014 peers must first populate children under D (the harness's mkdir D/nodeN does) so H1's shape exists; read P34H-POISON-ALIAS on the ESTALE node: children=1 d_count>=1 proves H1; children=0 d_count=0 refutes it (then the extra i_count ref is not a dentry: look at watch_ino/open fds/xfs internal grabs). 2) If proven: replace d_prune_aliases with a per-alias d_invalidate (dget under i_lock, drop lock, d_invalidate, dput) in xfs_inode.c ~1564, keep the fail-closed -ESTALE after the budget; verify: dir_recreate_estale 20 laps x 3 peers zero ESTALE, fence_during_write 32/caw 5 boards zero FDW-MKDIR-FAIL. 3) Explain the P-DIRCRC-RETRY-FAIL err=-117 x27 per node in the same window (which block, whose incarnation) before closing. || 1) Reproduce on 2-4 nodes with a tight loop: node A `rm -rf D; mkdir D` while B/C/D `mkdir D/nodeN` immediately (tests/dir_recreate_estale.sh, RULE 0 budget seconds) and capture P165/P128/P-IGET lines for D's inode on the ESTALE node; 2) name the stale object (parent dentry vs child vs shell inode) from the P-lines and the errno site (xfs_iget -ESTALE vs lookup); 3) fix at the invalidation site; verify: loop clean 20 laps on 4 nodes and fence_during_write 32/caw with zero FDW-MKDIR-FAIL lines across 5 boards. Not caused by 0.35.2's DLM/ledger changes (CAW does not instantiate the dlm.c engine; kernlog_test5 has zero P-TAUTH lines) \u2014 but the 0.35.1 board of the same day PASSed, so keep the recurrence rate in view."
+  "next": "SESS426: 1) run tests/dir_recreate_estale.sh <label> test1 test2 test3 test4 20 250 on 32/caw with 0.36.0 (budget 60 s) \u2014 peers must first populate children under D (the harness's mkdir D/nodeN does) so H1's shape exists; read P34H-POISON-ALIAS on the ESTALE node: children=1 d_count>=1 proves H1; children=0 d_count=0 refutes it (then the extra i_count ref is not a dentry: look at watch_ino/open fds/xfs internal grabs). 2) If proven: replace d_prune_aliases with a per-alias d_invalidate (dget under i_lock, drop lock, d_invalidate, dput) in xfs_inode.c ~1564, keep the fail-closed -ESTALE after the budget; verify: dir_recreate_estale 20 laps x 3 peers zero ESTALE, fence_during_write 32/caw 5 boards zero FDW-MKDIR-FAIL. 3) Explain the P-DIRCRC-RETRY-FAIL err=-117 x27 per node in the same window (which block, whose incarnation) before closing. || 1) Reproduce on 2-4 nodes with a tight loop: node A `rm -rf D; mkdir D` while B/C/D `mkdir D/nodeN` immediately (tests/dir_recreate_estale.sh, derived time budget seconds) and capture P165/P128/P-IGET lines for D's inode on the ESTALE node; 2) name the stale object (parent dentry vs child vs shell inode) from the P-lines and the errno site (xfs_iget -ESTALE vs lookup); 3) fix at the invalidation site; verify: loop clean 20 laps on 4 nodes and fence_during_write 32/caw with zero FDW-MKDIR-FAIL lines across 5 boards. Not caused by 0.35.2's DLM/ledger changes (CAW does not instantiate the dlm.c engine; kernlog_test5 has zero P-TAUTH lines) \u2014 but the 0.35.1 board of the same day PASSed, so keep the recurrence rate in view."
tests/criteria/records/D-TAUTH-SLOT-HASH-COLLISION-REFUSES-GRANT-EIO-0348.json:10
-  "next": "1) RULE-5 design consult: options (a) open addressing / chaining within the page (31 entries per page: probe the next free entry on the same page, key = full resource id, so a slot collision only fails when the PAGE is full); (b) a collision -> wait/retry (LEDGER_BUSY semantics) instead of EIO while the colliding holder lives \u2014 availability only, still a livelock under sustained contention; (c) grow the slot space (region is 16.5 MiB for 65536 slots; 1M slots = 264 MiB envelope) \u2014 still a hard cap; (d) per-inode records only for cross-node-shared inodes (local-only tenures unrecorded) \u2014 changes the authority proof. 2) Harness: tests/tcp_token_plumbing_verify.sh must capture per-file errno (the s429 wl outputs held only rc=124) and count EIO. 3) Verify: 4x2000 files on 32/tcp with zero P-TAUTH-COLLISION-caused EIO, and a directed collision test (two inodes chosen to hash to one slot: tests/tauth/pageof can compute slots) on the usermode mesh + rig."
+  "next": "1) design-consult design consult: options (a) open addressing / chaining within the page (31 entries per page: probe the next free entry on the same page, key = full resource id, so a slot collision only fails when the PAGE is full); (b) a collision -> wait/retry (LEDGER_BUSY semantics) instead of EIO while the colliding holder lives \u2014 availability only, still a livelock under sustained contention; (c) grow the slot space (region is 16.5 MiB for 65536 slots; 1M slots = 264 MiB envelope) \u2014 still a hard cap; (d) per-inode records only for cross-node-shared inodes (local-only tenures unrecorded) \u2014 changes the authority proof. 2) Harness: tests/tcp_token_plumbing_verify.sh must capture per-file errno (the s429 wl outputs held only rc=124) and count EIO. 3) Verify: 4x2000 files on 32/tcp with zero P-TAUTH-COLLISION-caused EIO, and a directed collision test (two inodes chosen to hash to one slot: tests/tauth/pageof can compute slots) on the usermode mesh + rig."
tests/criteria/records/D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-RULE0-0349.json:2
-  "id": "D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-RULE0-0349",
+  "id": "D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-the budget rule-0349",
tests/criteria/records/D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-RULE0-0349.json:8
-  "summary": "RULE 0 failure on the TCP transport with the durable authority ledger: a private-directory small-file workload (5000 ops/node, 4 nodes) does not finish in 30 s (native ~2-3 s, ceiling 2x = 6 s). Every inode/AG grant and release is a durable page commit; since 0.36.0 (D-0347) each commit is 2 x SCSI COMPARE AND WRITE + 1 x 3.5 KiB FUA write + 3 flushes on the shared LUN (before 0.36.0: 1 FUA write + 1 flush + 1 readback), and a create needs several grants/releases across AG + inode + parent-dir resources. The per-commit cost has never been measured on the rig (P-TAUTH-STATS prints only at release_all/unmount).",
+  "summary": "budget failure on the TCP transport with the durable authority ledger: a private-directory small-file workload (5000 ops/node, 4 nodes) does not finish in 30 s (native ~2-3 s, ceiling 2x = 6 s). Every inode/AG grant and release is a durable page commit; since 0.36.0 (D-0347) each commit is 2 x SCSI COMPARE AND WRITE + 1 x 3.5 KiB FUA write + 3 flushes on the shared LUN (before 0.36.0: 1 FUA write + 1 flush + 1 readback), and a create needs several grants/releases across AG + inode + parent-dir resources. The per-commit cost has never been measured on the rig (P-TAUTH-STATS prints only at release_all/unmount).",
tests/criteria/records/D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-RULE0-0349.json:9
-  "mechanism": "NOT YET MEASURED (RULE 4 step 1). Candidates: (1) durable-commit count per create x per-commit LUN latency (CAW ~1-2 ms each on iSCSI/mpatha, flush ~1-5 ms) => 10-30 ms per create; (2) the 500 ms FREEZE_REQ cadence / handoff parking on first touch of each of the ~2115 pages (1293 P-TAUTH-ACTIVATE + 1118 REMASTER-PARKED in the s429 token sweep: every page's FIRST use pays a bootstrap-for-request round trip + 3 page writes, and a parked requester retries at 100 ms); (3) the requester retry cadence (dlm_retry sleeps) rather than a wakeup on FROZEN (GPT ruling hazard 4, docs/rulings/unowned-page-route.md).",
+  "mechanism": "NOT YET MEASURED (instrument step 1). Candidates: (1) durable-commit count per create x per-commit LUN latency (CAW ~1-2 ms each on iSCSI/mpatha, flush ~1-5 ms) => 10-30 ms per create; (2) the 500 ms FREEZE_REQ cadence / handoff parking on first touch of each of the ~2115 pages (1293 P-TAUTH-ACTIVATE + 1118 REMASTER-PARKED in the s429 token sweep: every page's FIRST use pays a bootstrap-for-request round trip + 3 page writes, and a parked requester retries at 100 ms); (3) the requester retry cadence (dlm_retry sleeps) rather than a wakeup on FROZEN (GPT ruling hazard 4, docs/rulings/unowned-page-route.md).",
tests/criteria/records/D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-RULE0-0349.json:10
-  "next": "1) MEASURE: print P-TAUTH-STATS periodically (already every 1000 commits \u2014 the token dmesg sweep shows none, so <1000 commits per node in 30 s: that alone bounds the rate at <33 commits/s/node) and add a per-node commit histogram (CAW ms, body ms, flush ms) + per-create grant count (P-TAUTH-DLM-STATS at the end of the workload via a sysfs/knob dump rather than only at unmount); 2) attribute: page-first-touch handoff cost vs steady-state commit cost (run the workload twice on the same mount: the second lap has every page MINE); 3) RULE-5 design for the cost: batching grants per page commit, local-only tenures without durable records (the caller's own node is the master and the only holder), coalescing the ticket+publish into one CAW where the body fits a sector, or wake-on-FROZEN instead of 100 ms retry; 4) verify: tcp_token_plumbing_verify workload < 6 s (2x native) on 32/tcp."
+  "next": "1) MEASURE: print P-TAUTH-STATS periodically (already every 1000 commits \u2014 the token dmesg sweep shows none, so <1000 commits per node in 30 s: that alone bounds the rate at <33 commits/s/node) and add a per-node commit histogram (CAW ms, body ms, flush ms) + per-create grant count (P-TAUTH-DLM-STATS at the end of the workload via a sysfs/knob dump rather than only at unmount); 2) attribute: page-first-touch handoff cost vs steady-state commit cost (run the workload twice on the same mount: the second lap has every page MINE); 3) design-consult design for the cost: batching grants per page commit, local-only tenures without durable records (the caller's own node is the master and the only holder), coalescing the ticket+publish into one CAW where the body fits a sector, or wake-on-FROZEN instead of 100 ms retry; 4) verify: tcp_token_plumbing_verify workload < 6 s (2x native) on 32/tcp."
tests/tooling/manifest:9
-# BUDGET_S: see tests/suite/manifest header — same RULE 0 semantics.
+# BUDGET_S: see tests/suite/manifest header — same the budget rule semantics.
tests/tooling/manifest:14
-# mkfs_timing/chk_clean/online_resize/cluster_ops_timing: REAL RULE 0 numbers,
+# mkfs_timing/chk_clean/online_resize/cluster_ops_timing: REAL budget numbers,
tests/tooling/paired_perf_diag.sh:5
-# artifacts to /root/pdiag/.  NOT a pass/fail test — a RULE-4 measurement tool.
+# artifacts to /root/pdiag/.  NOT a pass/fail test — a instrumented measurement tool.
tests/tooling/guard_census.sh:44
-# RULE 0 budget: a static walk of ~465k lines of C, measured under one second
+# derived time budget: a static walk of ~465k lines of C, measured under one second
tests/fence_inflight/stack.sh:76
-# a RULE-0 budget, using a scratch area at the end of the image that is outside
+# a derived time budget, using a scratch area at the end of the image that is outside
tests/fence_inflight/stack.sh:235
-# RULE-0 budget.  Non-destructive: it writes only in the reserved tail, which
+# derived time budget.  Non-destructive: it writes only in the reserved tail, which
tests/fence_inflight/stack.sh:614
-  # RULE 0 budget: suspend must drain bios ALREADY QUEUED at the CURRENT write
+  # derived time budget: suspend must drain bios ALREADY QUEUED at the CURRENT write
tests/fence_inflight/prprobe.c:5
- * Why this exists instead of sg_persist(8): the sess133 RULE-5 ruling requires
+ * Why this exists instead of sg_persist(8): the sess133 design-consult ruling requires
tests/fence_inflight/loop_flush_probe.sh:29
-# RULE 0 budget: a flush of a 1 MiB freshly-written file on NVMe is a few ms.
+# derived time budget: a flush of a 1 MiB freshly-written file on NVMe is a few ms.
tests/fence_inflight/inflight_ab.sh:15
-# Per the sess133 RULE-5 ruling the completion boundary is chosen PER ARM so
+# Per the sess133 design-consult ruling the completion boundary is chosen PER ARM so
tests/fence_inflight/verdict.py:8
-sess133 RULE-5 validity predicate.
+sess133 design-consult validity predicate.
tests/fence_inflight/blocklayer_selftest.sh:8
-# The RULE-5 ruling on how to proceed requires empirical proof, before
+# The design-consult ruling on how to proceed requires empirical proof, before
tests/fence_inflight/blocklayer_selftest.sh:24
-# RULE 0 budgets, derived from what each step should cost on NVMe, not
+# derived time budgets, derived from what each step should cost on NVMe, not
tests/fence_inflight/delay_selftest.sh:4
-# The sess133 RULE-5 ruling makes this a precondition of every A/B run:
+# The sess133 design-consult ruling makes this a precondition of every A/B run:
tests/tcp/fg_one_run.sh:55
-[ "$rc" -eq 124 ] && echo "    (timeout: suite exceeded ${RUN_TIMEOUT}s — RULE 0 slowness FAIL)"
+[ "$rc" -eq 124 ] && echo "    (timeout: suite exceeded ${RUN_TIMEOUT}s — the budget rule slowness FAIL)"
tests/tcp/drc4_capture.sh:4
-# + per-daddr release/acquire timeline probes.  RULE-4 decisive experiment for
+# + per-daddr release/acquire timeline probes.  Instrumented decisive experiment for
tests/tcp/drc_time_runsh.sh:3
-# at a given MHT, to compare against the RULE-0 budget (60*N). sess2 ccloop.
+# at a given MHT, to compare against the derived time budget (60*N). sess2 ccloop.
tests/death/node_death_replay.sh:29
-# RULE 0 budgets (derived from MEASURED walls, not padded):
+# derived time budgets (derived from MEASURED walls, not padded):
tests/death/node_death_replay.sh:47
-# defaults; the RULE-5 phase-4 condition).
+# defaults; the design-consult phase-4 condition).
tests/suite/lib.sh:18
-# ─── RULE-0 TERMINAL-RECORD GUARANTEE (sess384) ────────────────────────────
+# ─── TERMINAL-RECORD GUARANTEE (sess384) ────────────────────────────
tests/suite/lib.sh:21
-# each node under `timeout <RULE-0 budget> ssh ...` and aggregates by grepping
+# each node under `timeout <derived time budget> ssh ...` and aggregates by grepping
tests/suite/fio_perf.sh:86
-# matters for RULE 0 budgets and the vs-xfs comparison.
+# matters for derived time budgets and the vs-xfs comparison.
tests/suite/cache_coherency.sh:51
-# sess1 (ccloop 0220f43f) RULE-4 + GPT consult: RPN was a T-INDEPENDENT
+# sess1 (ccloop 0220f43f) instrumented + GPT consult: RPN was a T-INDEPENDENT
tests/suite/cache_coherency.sh:85
-# sess1 (ccloop 0220f43f) RULE-4 PROVEN: this subtest never cleared its dir
+# sess1 (ccloop 0220f43f) PROVEN BY INSTRUMENT: this subtest never cleared its dir
tests/suite/coord.sh:39
-# ssh at the criterion's RULE-0 budget (30-90s for most of the board).  So a
+# ssh at the criterion's derived time budget (30-90s for most of the board).  So a
tests/suite/coord.sh:161
-# sess1 (ccloop 0220f43f) RULE-4 PROVEN: mosquitto_sub's -W only accepts
+# sess1 (ccloop 0220f43f) PROVEN BY INSTRUMENT: mosquitto_sub's -W only accepts
tests/suite/coord.sh:205
-# sess1 (ccloop 0220f43f) RULE-4: a FIRST decoupled-watcher attempt had the
+# sess1 (ccloop 0220f43f) instrumented: a FIRST decoupled-watcher attempt had the
tests/suite/dirent_publish_integrity.sh:18
-#   Established sess21 (RULE-5 GPT review): once local mutations exist on an
+#   Established sess21 (design-consult GPT review): once local mutations exist on an
tests/suite/dirent_publish_integrity.sh:30
-# tenure to the operation).  That is deliberate: RULE 6 forbids carrying an
+# tenure to the operation).  That is deliberate: the zero-defect bar forbids carrying an
tests/suite/posix_multi.sh:25
-# sess1 (ccloop 0220f43f) RULE-4 PROVEN: FPN was a T-INDEPENDENT constant
+# sess1 (ccloop 0220f43f) PROVEN BY INSTRUMENT: FPN was a T-INDEPENDENT constant
tests/suite/dlm_scaling.sh:12
-# RULE 0: native XFS does create+stat+unlink in tens of microseconds; OPS ops
+# budget: native XFS does create+stat+unlink in tens of microseconds; OPS ops
tests/suite/scaling_curve.sh:11
-# RULE 0: budget derived — PAYLOAD at a conservative floor rate must finish well
+# budget: budget derived — PAYLOAD at a conservative floor rate must finish well
tests/suite/rsync_paired.sh:9
-# inside the budget (RULE 0: a clustered FS that can't keep up is a FAIL).
+# inside the budget (budget: a clustered FS that can't keep up is a FAIL).
tests/suite/crash_consistency.sh:81
-# sess436 (D-32NODE-SHARED-DIR-CREATE-PACE, RULE-5 ruling measurement 5):
+# sess436 (D-32NODE-SHARED-DIR-CREATE-PACE, design-consult ruling measurement 5):
tests/suite/dir_reuse_coherency.sh:92
-#     (RULE 0), it just fails FAST now instead of after 24 slow rounds.
+#     (budget), it just fails FAST now instead of after 24 slow rounds.
tests/suite/dir_reuse_coherency.sh:127
-# timeouts across 31 peers — itself a RULE 0 violation — before the outer
+# timeouts across 31 peers — itself a the budget rule violation — before the outer
tests/suite/dir_reuse_coherency.sh:152
-# sess9 (16/tcp pace RULE-4): the original `yes | head -c` spawned a 2-fork
+# sess9 (16/tcp pace instrumented): the original `yes | head -c` spawned a 2-fork
tests/suite/dir_reuse_coherency.sh:258
-    # needed (RULE 0: a masking delay is debt — removed sess18 after the
+    # needed (budget: a masking delay is debt — removed sess18 after the
tests/suite/dir_reuse_coherency.sh:268
-    # hung task's kernel stack to kmsg (the RULE-4 instrument for the
+    # hung task's kernel stack to kmsg (the instrumented instrument for the
tests/suite/dir_reuse_coherency.sh:278
-    # sess1 (ccloop 0220f43f) RULE-4: TRIED a 0.05s poll granularity here
+    # sess1 (ccloop 0220f43f) instrumented: TRIED a 0.05s poll granularity here
tests/suite/dir_reuse_coherency.sh:455
-# Pace assertion (RULE 0): the time box must have fit at least MIN_ROUNDS
+# Pace assertion (budget): the time box must have fit at least MIN_ROUNDS
tests/suite/console_capture.sh:15
-# `start` after every VM cycle.  RULE 3: lives in the source tree.
+# `start` after every VM cycle.  the source-tree rule: lives in the source tree.
tests/suite/kernel_health.sh:22
-# in a filesystem that has to stay up (RULE 6 — no accepted known defects).
+# in a filesystem that has to stay up (the zero-defect bar — no accepted known defects).
tests/suite/node_responsive.sh:73
-#    blocked for 10 s straight is itself a RULE 0 problem, so the assertion
+#    blocked for 10 s straight is itself a the budget rule problem, so the assertion
tests/suite/sustained_load.sh:20
-#   Under RULE 0 this is disqualifying on its own: a clustered filesystem that
+#   Under the budget rule this is disqualifying on its own: a clustered filesystem that
tests/suite/ag_strand_repair.sh:15
-#   then never recurred — so the repair path NEVER EXECUTED.  RULE 6 does not
+#   then never recurred — so the repair path NEVER EXECUTED.  the zero-defect bar does not
tests/suite/dirent_type_integrity.sh:39
-#   RULE 6 forbids carrying an unresolved credible defect as a green cell.  Do
+#   the zero-defect bar forbids carrying an unresolved credible defect as a green cell.  Do
docs/dlm-protocol.md:380
-RULE-5 ruling `docs/rulings/intents-item5-efi-completion-design.md`.
+design-consult ruling `docs/rulings/intents-item5-efi-completion-design.md`.
docs/dlm-protocol.md:425
-* **Increment 2 (0.63.0, in tree; RULE-5 ruling
+* **Increment 2 (0.63.0, in tree; design-consult ruling
docs/dlm-protocol.md:478
-* **Increments 3+4 — completion + takeover (RULE-5 ruling sess463,
+* **Increments 3+4 — completion + takeover (design-consult ruling sess463,
docs/dlm-protocol.md:598
-  bit is RULE 0, not correctness (the timeout never strips or bypasses).
+  bit is the budget rule, not correctness (the timeout never strips or bypasses).
docs/dlm-protocol.md:659
-released earlier tenure).  RULE-5 ruling (ccmemory
+released earlier tenure).  Design-consult ruling (docs/dlm-protocol.md:681
-Design (RULE-5 ruling `ccloop-c7ee71c6-sess403-GPT-ruling-release-marker-log-
+Design (design-consult ruling `ccloop-c7ee71c6-sess403-GPT-ruling-release-marker-log-
docs/dlm-protocol.md:702
-* **Cluster (ICLUS) certificate (0.55.0, sess448; RULE-5 ruling
+* **Cluster (ICLUS) certificate (0.55.0, sess448; design-consult ruling
docs/dlm-protocol.md:790
-Rule (RULE-5 ruling `ccloop-c7ee71c6-sess404-GPT-ruling-ag-dead-waiter-ticket-
+Rule (design-consult ruling `ccloop-c7ee71c6-sess404-GPT-ruling-ag-dead-waiter-ticket-
docs/dlm-protocol.md:851
-The refusal holds even under `single_node_exclusive=1`.  The RULE-5 ruling
+The refusal holds even under `single_node_exclusive=1`.  The design-consult ruling
docs/dlm-protocol.md:874
-RULE-5 ruling A′ (ccmemory
+Design-consult ruling A′ (docs/dlm-protocol.md:1021
-+ harness `tests/caw_samenode_selftest.sh`.  RULE-6 closure vehicle for
++ harness `tests/caw_samenode_selftest.sh`.  defect-bar closure vehicle for
docs/dlm-protocol.md:1048
-Scenarios B and C (sess449 RULE-5 review) are covered by two one-shot barrier
+Scenarios B and C (sess449 design-consult review) are covered by two one-shot barrier
docs/dlm-protocol.md:1124
-The sess450 RULE-5 review ruled the 0.59.0 implementation STOP-SHIP on
+The sess450 design-consult review ruled the 0.59.0 implementation STOP-SHIP on
docs/dlm-protocol.md:1216
-RULE-5 disposition review of D-AGLOCK-ORPHAN-EX-TRACKING-LOSS-LIVELOCK-488
+design-consult disposition review of D-AGLOCK-ORPHAN-EX-TRACKING-LOSS-LIVELOCK-488
docs/perf.md:162
-### 2026-08-23 (sess401-402) — O_TMPFILE churn breakdown, D-TMPFILE-CHURN-RULE0-PERF-400
+### 2026-08-23 (sess401-402) — O_TMPFILE churn breakdown, D-TMPFILE-CHURN-the budget rule-PERF-400
docs/perf.md:176
-Ceiling (RULE 0) = 2× native = 0.14 ms/iter.  Shared-dir shape (32 nodes, one
+Ceiling (budget) = 2× native = 0.14 ms/iter.  Shared-dir shape (32 nodes, one
docs/perf.md:206
-latency) goes to a RULE-5 consult.  Ledger: D-32NODE-SHARED-DIR-CREATE-PACE,
+latency) goes to a design-consult consult.  Ledger: D-32NODE-SHARED-DIR-CREATE-PACE,
docs/perf.md:209
-### sess436 — cycle anatomy by lock mode (0.41.6) and RULE-5 ruling
+### sess436 — cycle anatomy by lock mode (0.41.6) and design-consult ruling
docs/perf.md:237
-RULE-5 ruling (ccmemory
+Design-consult ruling (docs/ag-metadata-coherency.md:376
-Fixes (0.23.11-0.23.13, sess400), one per RULE 4 cycle:
+Fixes (0.23.11-0.23.13, sess400), one by instrument cycle:
docs/ag-metadata-coherency.md:423
-**Fixes (RULE-5 ruling `docs/rulings/single-node-false-fresh-discard.md`).**
+**Fixes (design-consult ruling `docs/rulings/single-node-false-fresh-discard.md`).**
docs/ag-metadata-coherency.md:499
-cost a cohort member pays (RULE 0: measure `bench/rsync_bench.sh` lone vs 0.40.1).
+cost a cohort member pays (budget: measure `bench/rsync_bench.sh` lone vs 0.40.1).
docs/ag-metadata-coherency.md:507
-native XFS 3-4 s — under the RULE 0 2× ceiling); `d379b` PASS; **lone_crash_replay
+native XFS 3-4 s — under the the budget rule 2× ceiling); `d379b` PASS; **lone_crash_replay
docs/ag-metadata-coherency.md:526
-**Fixes (RULE-5 reviewed):**
+**Fixes (design-consult reviewed):**
docs/ag-metadata-coherency.md:639
-Fix (sess470 RULE-5 answer D, landed 0.64.15 in `xfs_iget_cache_miss`):
+Fix (sess470 design-consult answer D, landed 0.64.15 in `xfs_iget_cache_miss`):
docs/ledger-split.md:4
-**Scope:** this is a CORRECTNESS fix for the RULE 6 gate. It is *not* a
+**Scope:** this is a CORRECTNESS fix for the the zero-defect bar gate. It is *not* a
docs/ledger-split.md:81
-RULE 6). `showstat.sh` tests *inclusion*: only a literal `"OPEN"` counts.
+the zero-defect bar). `showstat.sh` tests *inclusion*: only a literal `"OPEN"` counts.
docs/ledger-split.md:87
-Under RULE 6 that is the dangerous direction. Over-counting blocks a
+Under the zero-defect bar that is the dangerous direction. Over-counting blocks a
docs/ledger-split.md:117
-`status` ∈ `OPEN | FIXED_AND_VERIFIED | DISPROVED` — exactly the two RULE 6
+`status` ∈ `OPEN | FIXED_AND_VERIFIED | DISPROVED` — exactly the two the zero-defect bar
docs/ledger-split.md:127
-RULE 4 loop history. One file per defect, markdown, loaded on demand by
+troubleshooting loop history. One file per defect, markdown, loaded on demand by
docs/ledger-split.md:152
-2. Run dry, review the unplaceable-field report by hand. **RULE 6: no
+2. Run dry, review the unplaceable-field report by hand. **the zero-defect bar: no
docs/ledger-split.md:156
-3. Keep `OPEN_DEFECTS.json.backup` (per CLAUDE.md: back up in place, no
+3. Keep `OPEN_DEFECTS.json.backup` (per the project rules: back up in place, no
docs/ledger-split.md:180
-- RULE 6 (`CLAUDE.md`) — the two permitted closure dispositions
+- the zero-defect bar (`CLAUDE.md`) — the two permitted closure dispositions
docs/net2.md:348
-  instrumented RULE-4 loops: (1) self-appointed empty rank-0 could
+  instrumented troubleshooting loops: (1) self-appointed empty rank-0 could
docs/test_infra_lio_tcm.md:39
-## Scripts (all in `scripts/`, RULE 3)
+## Scripts (all in `scripts/`, the source-tree rule)
docs/test_suite_design.md:6
-`TIMEOUT_BUDGETS.md` (RULE-0 per-criterion budgets), `docs/test_infra_lio_tcm.md`
+`TIMEOUT_BUDGETS.md` (the budget rule per-criterion budgets), `docs/test_infra_lio_tcm.md`
docs/test_suite_design.md:195
-## 8. Timing — first class (RULE 0)
+## 8. Timing — first class (budget)
docs/test_infra_scst_caw.md:35
-## Scripts (all in `scripts/`, RULE 3)
+## Scripts (all in `scripts/`, the source-tree rule)
docs/test_infra_scst_caw.md:136
-  live wedge with `scripts/scst_unwedge/` (no host reboot — RULE 2).
+  live wedge with `scripts/scst_unwedge/` (no host reboot — the never-reboot-the-host rule).
docs/foreign-replay-slice-snapshot.md:27
-RULE-5 ruling:
+design-consult ruling:
docs/foreign-replay-slice-snapshot.md:58
-RULE-5 ruling explicitly forbids the read/validate/re-read shape).
+design-consult ruling explicitly forbids the read/validate/re-read shape).
docs/cost-audit.md:253
-| 5 | RULE 7 (Read tool) | Read share rises from 11% | **BEST RESULT** — 8% → **42%**; 1.9 → 8.1 Read calls/session |
+| 5 | the Read-tool rule (Read tool) | Read share rises from 11% | **BEST RESULT** — 8% → **42%**; 1.9 → 8.1 Read calls/session |
docs/cost-audit.md:284
-event: RULE 7 made auto-injection fire, so sessions stopped having to search
+event: the Read-tool rule made auto-injection fire, so sessions stopped having to search
docs/cost-audit.md:290
-**Where #4 fell short.** RULE 8 is being *obeyed* — 96% of sessions started
+**Where #4 fell short.** the queue-integrity rule is being *obeyed* — 96% of sessions started
docs/cost-audit.md:506
-does not converge," and RULE 6 forbids the alternative of shortening the list
+does not converge," and the zero-defect bar forbids the alternative of shortening the list
docs/cost-audit.md:542
-| project `CLAUDE.md` | ~5.6k → ~6.6k after RULE 2c/7/8 | yes |
+| project `CLAUDE.md` | ~5.6k → ~6.6k after the unkillable-wedge rule/7/8 | yes |
docs/cost-audit.md:584
-*(Era B figures. Superseded by §3.1 — RULE 7 moved Read share 8% → 42%.)*
+*(Era B figures. Superseded by §3.1 — the Read-tool rule moved Read share 8% → 42%.)*
docs/cost-audit.md:704
-Raised by RULE-5 review, 2026-08-15, and it is the strongest objection to
+Raised by design-consult review, 2026-08-15, and it is the strongest objection to
docs/cost-audit.md:721
-**Why it does not change RULE 9.** The review argued the two hypotheses
+**Why it does not change the request-batching rule.** The review argued the two hypotheses
docs/cost-audit.md:805
-   `mmap_lock` — see RULE 2c and `tools/mxfs_pgrep.sh`. This bit the audit
+   `mmap_lock` — see the unkillable-wedge rule and `tools/mxfs_pgrep.sh`. This bit the audit
docs/cost-audit.md:889
-   Each is under an hour (RULE-5 review, 2026-08-15):
+   Each is under an hour (design-consult review, 2026-08-15):
docs/cost-audit.md:897
-   The third is the load-bearing one for RULE 10: model *attribution* was
+   The third is the load-bearing one for the delegation rule: model *attribution* was
docs/cost-audit.md:973
-Opus if it has capacity.** Revised after RULE-5 review: the 500k case rests
+Opus if it has capacity.** Revised after design-consult review: the 500k case rests
docs/cost-audit.md:1103
-throughput, it is **an empty open queue** (RULE 6) — and by that measure Era
+throughput, it is **an empty open queue** (zero-defect bar) — and by that measure Era
docs/cost-audit.md:1130
-almost entirely release-blocking under RULE 6, and `tests/suite/open_defects.sh`
+almost entirely release-blocking under the zero-defect bar, and `tests/suite/open_defects.sh`
docs/cost-audit.md:1221
-first two days of Era F. That is RULE 9 finally biting, and it is the
+first two days of Era F. That is the request-batching rule finally biting, and it is the
docs/cost-audit.md:1247
-| Read | 543.1 | 17.6% | no (RULE 7/10) |
+| Read | 543.1 | 17.6% | no (the Read-tool rule/10) |
docs/cost-audit.md:1259
-### 12.4 RULE 8 landed — and exposed that the old ratios were wrong
+### 12.4 the queue-integrity rule landed — and exposed that the old ratios were wrong
docs/cost-audit.md:1301
-statements are true and the second one is the one RULE 6 cares about.
+statements are true and the second one is the one the zero-defect bar cares about.
docs/cost-audit.md:1315
-3. **Not single-variable, for the third era running.** RULE 9 batching and
+3. **Not single-variable, for the third era running.** the request-batching rule batching and
docs/cost-audit.md:1316
-   RULE 10 delegation both went from near-zero to fully adopted inside this
+   the delegation rule delegation both went from near-zero to fully adopted inside this
docs/cost-audit.md:1415
-- `CLAUDE.md` RULE 2c / 7 / 8 — the behavioural changes
+- `CLAUDE.md` the unkillable-wedge rule / 7 / 8 — the behavioural changes
docs/cost-audit.md:1499
-eras out of date, and the improvement since is 7×. Batching (RULE 9) and
+eras out of date, and the improvement since is 7×. Batching (the request-batching rule) and
docs/cost-audit.md:1500
-delegation (RULE 10) are the only changes that fit the timeline, and the
+delegation (the delegation rule) are the only changes that fit the timeline, and the
docs/cost-audit.md:1622
-quiet relabel, which RULE 6 forbids outright.
+quiet relabel, which the zero-defect bar forbids outright.
docs/host-safety.md:140
-  widened (RULE 2d); the source is removed: `scst_setup.sh::reset_trace`
+  widened (the host-guard rule); the source is removed: `scst_setup.sh::reset_trace`
docs/host-safety.md:283
-**The fix — `+caw-abort-reclaim.5`** (`/src/scst`, RULE-5 reviewed):
+**The fix — `+caw-abort-reclaim.5`** (`/src/scst`, design-consult reviewed):
docs/host-safety.md:465
-RULE 2 only the user may perform — and `iommu=off` is presumed deliberate (GPU
+the never-reboot-the-host rule only the user may perform — and `iommu=off` is presumed deliberate (GPU
docs/host-safety.md:482
-**What the guard deliberately does not do** (RULE 2 / RULE 2c): it never
+**What the guard deliberately does not do** (the never-reboot-the-host rule / the unkillable-wedge rule): it never
docs/host-safety.md:550
-  earlier that nobody has read.  Under RULE 6 a host-kernel oops is a
+  earlier that nobody has read.  Under the zero-defect bar a host-kernel oops is a
docs/delegation.md:7
-regardless of size) and to CLAUDE.md RULE 10.  Measurement script:
+regardless of size) and to CLAUDE.md the delegation rule.  Measurement script:
docs/delegation.md:25
-| **Read** (source, RULE 7 injection) | 15.0% | no |
+| **Read** (source, the Read-tool rule injection) | 15.0% | no |
docs/delegation.md:43
-  chains, 0 Agent calls.  Since RULE 10 landed (08-15) the `Agent` tool has
+  chains, 0 Agent calls.  Since the delegation rule landed (08-15) the `Agent` tool has
docs/delegation.md:56
-- **Delegating a single Bash call saves nothing.**  A 32-node poll that RULE 9
+- **Delegating a single Bash call saves nothing.**  A 32-node poll that the request-batching rule
docs/delegation.md:79
-1. **RULE 10 is written as optional guidance and argues against itself.**
+1. **the delegation rule is written as optional guidance and argues against itself.**
docs/delegation.md:84
-   sits at line 435 of 700, below RULE 9, whose "batch side-effect-free calls"
+   sits at line 435 of 700, below the request-batching rule, whose "batch side-effect-free calls"
docs/delegation.md:93
-3. **RULE 7 + "never delegate reading" pull locate-and-read into the parent.**
+3. **the Read-tool rule + "never delegate reading" pull locate-and-read into the parent.**
docs/delegation.md:94
-   RULE 7 says read source with `Read` for the ccmemory injection; RULE 10
+   the Read-tool rule says read source with `Read` for the ccmemory injection; the delegation rule
docs/delegation.md:115
-   actually stuck was mechanical: the RULE 2b allowlist, `clyde_preflight.sh`
+   actually stuck was mechanical: the the permission-prompt rule allowlist, `clyde_preflight.sh`
docs/delegation.md:117
-   that moved the Read share from 8% to 42%.  RULE 10 has prose only.
+   that moved the Read share from 8% to 42%.  the delegation rule has prose only.
docs/delegation.md:126
-  (RULE 10's cap hazard stands; it belongs in the agent definitions, not as a
+  (the delegation rule's cap hazard stands; it belongs in the agent definitions, not as a
docs/delegation.md:130
-- **The parent keeps**: `Read` of the files it must understand (RULE 7),
+- **The parent keeps**: `Read` of the files it must understand (the Read-tool rule),
docs/delegation.md:155
-the roster in §5.2, the RULE 10 rewrite in §5.3 (CLAUDE.md then only has to
+the roster in §5.2, the the delegation rule rewrite in §5.3 (CLAUDE.md then only has to
docs/delegation.md:195
-### 5.3 RULE 10 rewrite (text to be reviewed before it lands)
+### 5.3 the delegation rule rewrite (text to be reviewed before it lands)
docs/delegation.md:198
-  "the next step is X → `Agent` Y.  A parent Bash that does X is a RULE 10
+  "the next step is X → `Agent` Y.  A parent Bash that does X is a the delegation rule
docs/delegation.md:215
-  re-audit command list; §10: the RULE 10 pool-debit check at next exhaustion.
+  re-audit command list; §10: the the delegation rule pool-debit check at next exhaustion.
docs/delegation.md:242
-- Capped reports hide evidence (RULE 10 hazard).  Mitigation is in the agent
+- Capped reports hide evidence (the delegation rule hazard).  Mitigation is in the agent
docs/recovery-manifest.md:96
-before `P238-RECOV-LEASE` and the test hook).  RULE-5 ruling (sess407, GPT):
+before `P238-RECOV-LEASE` and the test hook).  Design-consult ruling (sess407, GPT):
docs/recovery-manifest.md:199
-## sess405 RULE-5 review (GPT, 19 findings) — dispositions
+## sess405 design-consult review (GPT, 19 findings) — dispositions
docs/recovery-manifest.md:243
-on R with `owner_node == R`).  Per the sess437 RULE-5 ruling (ccmemory
+on R with `owner_node == R`).  Per the sess437 design-consult ruling (docs/foreign-replay-inode-ordering.md:36
-GPT RULE-5 ruling (sess408): option A — continue the count across
+Design-consult ruling (sess408): option A — continue the count across
docs/foreign-replay-inode-ordering.md:81
-### Pace (RULE 0) — AG-tenure stamp (0.26.6)
+### Pace (budget) — AG-tenure stamp (0.26.6)
docs/foreign-replay-inode-ordering.md:103
-restart-at-1 behaviour for RULE-4 cost attribution ONLY — it re-opens the
+restart-at-1 behaviour for instrumented cost attribution ONLY — it re-opens the
docs/foreign-replay-inode-ordering.md:113
-D-TMPFILE-CHURN-RULE0-PERF-400), not a new cost class.
+D-TMPFILE-CHURN-the budget rule-PERF-400), not a new cost class.
docs/foreign-replay-inode-ordering.md:194
-RULE-5 ruling (ccmemory
+design-consult ruling (docs/superblock-cluster-mode.md:99
-`P-SB-SYNC-POST` (durable counters after the cover) — the RULE-5 (sess474)
+`P-SB-SYNC-POST` (durable counters after the cover) — the design-consult (sess474)
docs/superblock-cluster-mode.md:106
-### 0.64.28 — the fix (RULE-5 design A+)
+### 0.64.28 — the fix (design-consult design A+)
docs/superblock-cluster-mode.md:137
-RULE-5 ruling (ccmemory `ccloop-c7ee71c6-sess475-GPT-ruling-d0133-lock-inert-
+Design-consult ruling (ccmemory `ccloop-c7ee71c6-sess475-GPT-ruling-d0133-lock-inert-
docs/superblock-cluster-mode.md:210
-the RULE-4 proof lap: `action=RELEASE-under-live-holder`).  Harness: the
+the instrumented proof lap: `action=RELEASE-under-live-holder`).  Harness: the
docs/tcp-authority-ledger.md:3
-Status: DESIGN (sess418, 2026-08-28), RULE-5 ruled (ccmemory
+Status: DESIGN (sess418, 2026-08-28), design-consult ruled (docs/tcp-authority-ledger.md:204
-   **SPEC FROZEN sess421** (two RULE-5 rulings, ccmemory
+   **SPEC FROZEN sess421** (two design-consult rulings, docs/tcp-authority-ledger.md:343
-   **RULE-5 ruling sess423** (ccmemory
+   **design-consult ruling sess423** (docs/free-publish.md:4
-VISIBLE-PEER-DOUBLE-ALLOC-SHUTDOWN-0351.  RULE-5 rulings: ccmemory
+VISIBLE-PEER-DOUBLE-ALLOC-SHUTDOWN-0351.  Design-consult rulings: docs/free-publish.md:91
-## 0.38.1 → 0.38.3: two defects in the first landing (sess428, RULE 4 proven)
+## 0.38.1 → 0.38.3: two defects in the first landing (sess428, proven by instrument)
docs/free-publish.md:131
-RULE-5 ruling (ccmemory `ccloop-c7ee71c6-sess429-GPT-ruling-home-free-
+Design-consult ruling (ccmemory `ccloop-c7ee71c6-sess429-GPT-ruling-home-free-
docs/free-publish.md:160
-on the same number.  Mechanism (RULE 4, code): life 1 of ino X reaches the
+on the same number.  Mechanism (instrumented, code): life 1 of ino X reaches the
docs/free-publish.md:174
-RULE-5 ruling (ccmemory `ccloop-c7ee71c6-sess430-GPT-ruling-free-foreign-
+Design-consult ruling (ccmemory `ccloop-c7ee71c6-sess430-GPT-ruling-free-foreign-
docs/free-publish.md:227
-RULE 4 instrument that names the path if any crossing remains.  A deadshell
+instrumented instrument that names the path if any crossing remains.  A deadshell
docs/free-publish.md:247
-RULE-5 ruling (ccmemory `ccloop-c7ee71c6-sess430-GPT-ruling-d0351-dialloc-
+design-consult ruling (ccmemory `ccloop-c7ee71c6-sess430-GPT-ruling-d0351-dialloc-
docs/free-publish.md:284
-rows are the RULE 0 check.
+rows are the budget check.
docs/free-publish.md:330
-RULE-5 ruling (`docs/rulings/freepub-claim.md`):
+Design-consult ruling (`docs/rulings/freepub-claim.md`):
docs/free-publish.md:436
-RULE-5 review of 0.39.6/0.39.7 (ccmemory
+design-consult review of 0.39.6/0.39.7 (docs/free-publish.md:480
-(RULE 4); the reproducers and three consecutive 32/caw boards pass under
+(instrumented); the reproducers and three consecutive 32/caw boards pass under
docs/free-publish.md:521
-**The rule now** (RULE-5 ruling, ccmemory
+**The rule now** (design-consult ruling, docs/pr-fencing-departure.md:80
-## Known boundaries (from the sess433 RULE-5 review)
+## Known boundaries (from the sess433 design-consult review)
docs/pr-fencing-departure.md:159
-RULE-5 review ruled it STOP-SHIP.  0.59.1 closes the three blockers and the
+design-consult review ruled it STOP-SHIP.  0.59.1 closes the three blockers and the
docs/pr-fencing-departure.md:195
-The sess451 RULE-5 review ruled 0.59.1 STOP-SHIP a second time (ccmemory
+The sess451 design-consult review ruled 0.59.1 STOP-SHIP a second time (docs/pr-fencing-departure.md:297
-## RULE-5 review #3 of 0.59.2/0.59.3 — NO-GO (sess453)
+## design-consult review #3 of 0.59.2/0.59.3 — NO-GO (sess453)
docs/pr-fencing-departure.md:598
-## RULE-5 review #5 of 0.61.1 — NO-GO (sess456)
+## design-consult review #5 of 0.61.1 — NO-GO (sess456)
docs/tauth-view-table.md:12
-ballots, the barrier) are NOT built.  Design history: v3 after two RULE-5
+ballots, the barrier) are NOT built.  Design history: v3 after two design-consult
docs/tauth-view-table.md:18
-D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-RULE0-0349.  Rulings: ccmemory
+D-TCP-LEDGER-SMALLFILE-WORKLOAD-PACE-the budget rule-0349.  Rulings: docs/whole-cluster-restart.md:9
-RULE-5 ruling: ccmemory
+Design-consult ruling: docs/whole-cluster-restart.md:140
-**The registrant ledger cannot be deferred** (RULE-5 ruling, ccmemory
+**The registrant ledger cannot be deferred** (design-consult ruling, docs/whole-cluster-restart.md:193
-RULE-5 ruling: ccmemory
+design-consult ruling: docs/whole-cluster-restart.md:330
-anything.  RULE-5 ruling: ccmemory
+anything.  Design-consult ruling: docs/whole-cluster-restart.md:400
-RULE-5 build review: ccmemory
+design-consult build review: docs/whole-cluster-restart.md:630
-    - RULE-5 code review (sess441, ccmemory
+    - design-consult code review (sess441, docs/whole-cluster-restart.md:645
-5c. The `m_log` audit + poison run.  **SUPERSEDED (sess441 RULE-5 ruling,
+5c. The `m_log` audit + poison run.  **SUPERSEDED (sess441 design-consult ruling,
docs/whole-cluster-restart.md:691
-    **RULE-5 code review of the landing (sess442, ccmemory
+    **design-consult code review of the landing (sess442, docs/whole-cluster-restart.md:949
-**RULE-5 rulings (two consults).**  (1) An AG-class APPLY sibling proves
+**design-consult rulings (two consults).**  (1) An AG-class APPLY sibling proves
docs/whole-cluster-restart.md:1021
-failure is RULE 0: the mount took 545 s (bound 540) because each
+failure is budget: the mount took 545 s (bound 540) because each
docs/dir-sharding.md:9
-RULE-5 rulings: `docs/rulings/symmetric-directory-sharding-design.md`
+Design-consult rulings: `docs/rulings/symmetric-directory-sharding-design.md`
docs/dir-sharding.md:17
-budget is unchanged (RULE 0); the fix is this design.
+budget is unchanged (budget); the fix is this design.
docs/dir-sharding.md:619
-### 0.64.12: the RULE-5 review of the 0.64.11 fix (sess470)
+### 0.64.12: the design-consult review of the 0.64.11 fix (sess470)
docs/dir-sharding.md:693
-Rule this leaves behind (RULE-5 review, sess472): **every refusal that can
+Rule this leaves behind (design-consult review, sess472): **every refusal that can
docs/dir-sharding.md:740
-is also the RULE 4 measurement of the mechanism.
+is also the instrumented measurement of the mechanism.
docs/dir-sharding.md:745
-RULE 0 < 300 ms) and the peer removing (arm B) the reused directory;
+the budget rule < 300 ms) and the peer removing (arm B) the reused directory;
docs/dir-sharding.md:752
-**RULE-5 review of the first cut (sess473, STOP-SHIP with 12 findings) and
+**design-consult review of the first cut (sess473, STOP-SHIP with 12 findings) and
docs/authority-certificate.md:83
-### 0.64.8/0.64.9 — the RULE-5 review rework (sess469)
+### 0.64.8/0.64.9 — the design-consult review rework (sess469)
docs/authority-certificate.md:127
-  RULE-5 ruling rejected shifting `auth_incarn` after the free (it is the
+  design-consult ruling rejected shifting `auth_incarn` after the free (it is the
docs/authority-certificate.md:190
-RULE-5 ruling (ccmemory
+Design-consult ruling (docs/notes/DESIGN.md:556
-This is the active blocker. The proven picture (RULE 4 — instrumented, not
+This is the active blocker. The proven picture (instrumented — instrumented, not
docs/notes/journal.md:234
-and the timing-is-first-class rule, asked Gemini (RULE 5 escalation — 3 failed
+and the timing-is-first-class rule, asked Gemini (design-consult escalation — 3 failed
xfs/xfs_buf_item.h:50
- * sess102 RULE-5 ruling declared that UNSOUND (release blocker P0):
+ * sess102 design-consult ruling declared that UNSOUND (release blocker P0):
xfs/xfs_extent_busy.c:77
-			 * ccloop-4dd7 (RULE-4 proven, 2-CPU soft-lockup
+			 * ccloop-4dd7 (proven by instrument, 2-CPU soft-lockup
xfs/xfs_icache.h:99
- * lifecycle op still running under the grant (RULE-5 sess390 ruling item 1).
+ * lifecycle op still running under the grant (design-consult sess390 ruling item 1).
xfs/xfs_icreate_item.c:163
- * MXFS (sess444, RULE-5 ruling on ICREATE authority): verify ONE cluster of
+ * MXFS (sess444, design-consult ruling on ICREATE authority): verify ONE cluster of
xfs/xfs_icreate_item.c:370
-	 * MXFS (sess444, RULE-5 ruling; D-ICREATE-REPLAY-REINIT-CLOBBERS-
+	 * MXFS (sess444, design-consult ruling; D-ICREATE-REPLAY-REINIT-CLOBBERS-
xfs/xfs_icreate_item.c:411
-					 * RULE-5 landing review (6): only a
+					 * Design-consult landing review (6): only a
xfs/xfs_inode_item.c:706
-	 * sess58 (ccloop 8ddb16a2) RULE-4 DECISIVE PROBE.  A directory can only
+	 * sess58 (ccloop 8ddb16a2) instrumented DECISIVE PROBE.  A directory can only
xfs/xfs_inode_item.c:1107
-		/* sess35 P241 (RULE 4, classifier round 2): this discharge is
+		/* sess35 P241 (instrumented, classifier round 2): this discharge is
xfs/xfs_inode_item.c:1193
-			/* sess431 (RULE-5 review of 0.39.6): nothing of this
+			/* sess431 (design-consult review of 0.39.6): nothing of this
xfs/xfs_inode_item.c:1204
-		 * ccloop c7ee71c6 sess20 (H4 ROOT FIX, RULE-5 GPT-reviewed):
+		 * ccloop c7ee71c6 sess20 (H4 ROOT FIX, design-consult GPT-reviewed):
xfs/xfs_inode_item.c:1287
-	 * obligation's dirty conversion (RULE-5 ruling: the obligation must
+	 * obligation's dirty conversion (design-consult ruling: the obligation must
xfs/xfs_iunlink_item.c:125
-		 * mxfs sess396 INSERT mode (RULE-5 ruling, see the header):
+		 * mxfs sess396 INSERT mode (design-consult ruling, see the header):
xfs/xfs_iunlink_item.c:191
-		 * sess53 (RULE 4, PROVEN tcp_dlm_scaling iunlink corruption fix):
+		 * sess53 (instrumented, PROVEN tcp_dlm_scaling iunlink corruption fix):
xfs/xfs_iunlink_item.h:21
-	 * mxfs sess396 (D-AGI-UNLINKED-CROSSNODE-RECOVERY-SHUTDOWN, RULE-5
+	 * mxfs sess396 (D-AGI-UNLINKED-CROSSNODE-RECOVERY-SHUTDOWN, design-consult
xfs/xfs_log.c:665
-		 * sess442 (RULE-5 review S1): the bootstrap finish records
+		 * sess442 (design-consult review S1): the bootstrap finish records
xfs/xfs_log.c:796
-			 * sess442 (RULE-5 review (a)): on the adopted slice K
+			 * sess442 (design-consult review (a)): on the adopted slice K
xfs/xfs_log.c:1040
-	 * sess407 (RULE-5 ruling Q5, D-FSWIDE-TERMINAL-REPLAY-CONTINUES-407):
+	 * sess407 (design-consult ruling Q5, D-FSWIDE-TERMINAL-REPLAY-CONTINUES-407):
xfs/xfs_log.c:1231
-	 * sess462 (item 5 increment 2, RULE-5 ruling STOP-SHIP 1): the
+	 * sess462 (item 5 increment 2, design-consult ruling STOP-SHIP 1): the
xfs/xfs_log.c:1324
-	 * sess233 (#21 incident-481 RULE-5 ruling): generalized to EVERY
+	 * sess233 (#21 incident-481 design-consult ruling): generalized to EVERY
xfs/xfs_log.c:1639
-			 * producer straddles the cut (RULE-5 ruling stop-ship
+			 * producer straddles the cut (design-consult ruling stop-ship
xfs/xfs_log.c:1909
- * sess474/475 (D-0133, RULE-5 design A+ and the sess475 placement ruling,
+ * sess474/475 (D-0133, design-consult design A+ and the sess475 placement ruling,
xfs/xfs_log.c:2657
-	 * sess166: eval state/missed-count explicitly initialized (RULE-5
+	 * sess166: eval state/missed-count explicitly initialized (design-consult
xfs/xfs_log_priv.h:492
-	 * a partial count reading as a full one (RULE-5 sess166 review).
+	 * a partial count reading as a full one (design-consult sess166 review).
xfs/xfs_log_priv.h:561
-	 * sess403 (RULE-5 ruling, REDUNDANT_CLEAN): pass-1 table of the
+	 * sess403 (design-consult ruling, REDUNDANT_CLEAN): pass-1 table of the
xfs/xfs_log_priv.h:572
-	 * sess476 (D-FOREIGN-SLICE-INTENTS-ABANDONED, CANCEL tokens — RULE-5
+	 * sess476 (D-FOREIGN-SLICE-INTENTS-ABANDONED, CANCEL tokens — design-consult
xfs/xfs_log_priv.h:628
-	/* sess459 (D-0517 RULE 4 step 2): buffer images an untrusted replay
+	/* sess459 (D-0517 instrument step 2): buffer images an untrusted replay
xfs/xfs_mount.h:550
-	 * sess475 (D-0133 fix placement, RULE-5 ruling ccmemory
+	 * sess475 (D-0133 fix placement, design-consult ruling ccmemory
xfs/xfs_mount.h:654
-	 * sess452 (0.59.2, D-377/D-0356 RULE-5 STOP-SHIP #2 blocker 5): the
+	 * sess452 (0.59.2, D-377/D-0356 design-consult STOP-SHIP #2 blocker 5): the
xfs/xfs_mount.h:669
-	 * sess454 (0.61.0, RULE-5 design rulings D2/D3/D4 — review-#3
+	 * sess454 (0.61.0, design-consult design rulings D2/D3/D4 — review-#3
xfs/xfs_mount.h:697
-	struct inode		*m_mxfs_pr_sweep_pinned;	/* ccloop cc87fed3 sess6: sweep-pin tripwire. The `struct inode *` mxfs_dlm_pr_sweep_work_fn currently holds a live igrab() reference on (its `toput` variable's window, which can span many s_inodes loop iterations), or NULL when the sweep holds no reference. Set/cleared via WRITE_ONCE around every igrab/iput in that function; read via READ_ONCE at the P25-INSTR sync-inactive site (xfs_inode_mark_reclaimable, xfs_icache.c) to prove/refute whether an inode pr_sweep believes is pinned is entering real eviction anyway — which would mean something ELSE over-released a reference it didn't hold (RULE 4 direct-evidence probe for the P135-PRSWEEP-CYCLE self-loop hang; NOT a correctness dependency, diagnostic-only, best-effort unlocked pointer, no ordering/locking guarantees needed beyond WRITE_ONCE/READ_ONCE). */
+	struct inode		*m_mxfs_pr_sweep_pinned;	/* ccloop cc87fed3 sess6: sweep-pin tripwire. The `struct inode *` mxfs_dlm_pr_sweep_work_fn currently holds a live igrab() reference on (its `toput` variable's window, which can span many s_inodes loop iterations), or NULL when the sweep holds no reference. Set/cleared via WRITE_ONCE around every igrab/iput in that function; read via READ_ONCE at the P25-INSTR sync-inactive site (xfs_inode_mark_reclaimable, xfs_icache.c) to prove/refute whether an inode pr_sweep believes is pinned is entering real eviction anyway — which would mean something ELSE over-released a reference it didn't hold (direct-evidence probe for the P135-PRSWEEP-CYCLE self-loop hang; NOT a correctness dependency, diagnostic-only, best-effort unlocked pointer, no ordering/locking guarantees needed beyond WRITE_ONCE/READ_ONCE). */
xfs/xfs_mount.h:759
-	 * RULE-5 ruling): the VICTIM-DOMAIN QUARANTINE map.  When a foreign
+	 * Design-consult ruling): the VICTIM-DOMAIN QUARANTINE map.  When a foreign
xfs/xfs_mount.h:778
-	 * sess383 (RULE-5 ruling Q3): the ADMISSION handshake.  True from the
+	 * sess383 (design-consult ruling Q3): the ADMISSION handshake.  True from the
xfs/xfs_trans_buf.c:561
-	 * sess103 step 5.3 (sess102 RULE-5 ruling, P0/P1): capture the
+	 * sess103 step 5.3 (sess102 design-consult ruling, P0/P1): capture the
xfs/xfs_trans_buf.c:734
-	 * sess476 (D-FOREIGN-SLICE-INTENTS-ABANDONED, RULE-4 proven on chain
+	 * sess476 (D-FOREIGN-SLICE-INTENTS-ABANDONED, proven by instrument on chain
xfs/xfs_mxfs_dlm.h:304
- * sess383 (RULE-5 ruling Q3): close the admission transaction opened by
+ * sess383 (design-consult ruling Q3): close the admission transaction opened by
xfs/xfs_mxfs_dlm.h:424
-/* <ccloop sess49> dir-fork delalloc tripwire (RULE 4): localize where a dir
+/* <ccloop sess49> dir-fork delalloc tripwire (instrumented): localize where a dir
xfs/xfs_mxfs_dlm.h:585
-/* sess120 (Gemini RULE-5): LSN-precise "is this AG-meta buffer ahead of disk?"
+/* sess120 (Gemini design-consult): LSN-precise "is this AG-meta buffer ahead of disk?"
xfs/xfs_mxfs_dlm.h:878
-/* sess60 RULE-4 probe: log bmbt-leaf WRITE numrecs + owner hold state. */
+/* sess60 instrumented probe: log bmbt-leaf WRITE numrecs + owner hold state. */
xfs/xfs_mxfs_dlm.c:232
- * sess391 (RULE-5 ruling ccloop-c7ee71c6-sess391-GPT-ruling-ag-handoff-latch-
+ * sess391 (design-consult ruling ccloop-c7ee71c6-sess391-GPT-ruling-ag-handoff-latch-
xfs/xfs_mxfs_dlm.c:428
- * sess294 P296-BASTQLAT (D-503 RULE-4 instrumentation): measure the pure
+ * sess294 P296-BASTQLAT (D-503 instrumentation): measure the pure
xfs/xfs_mxfs_dlm.c:627
- * sess25 ROOT FIX for D-BAST-IRELE-INACTIVE-SELF-WEDGE (RULE 4 PROVEN by the
+ * sess25 ROOT FIX for D-BAST-IRELE-INACTIVE-SELF-WEDGE (PROVEN by the
xfs/xfs_mxfs_dlm.c:1135
- * Contract (sess96 RULE-5 ruling):
+ * Contract (sess96 design-consult ruling):
xfs/xfs_mxfs_dlm.c:1166
- * sess163 (RULE-5 ruling A): a phantom-recovery arm proved the wire lost a
+ * sess163 (design-consult ruling A): a phantom-recovery arm proved the wire lost a
xfs/xfs_mxfs_dlm.c:1258
- * sess469 fix shape A (RULE-5 ruling, STOP-SHIP 1 of the re-review): while
+ * sess469 fix shape A (design-consult ruling, STOP-SHIP 1 of the re-review): while
xfs/xfs_mxfs_dlm.c:1362
- * sess163 (RULE-5 ruling A on the sess162 wiring): phantom-loss invalidation.
+ * sess163 (design-consult ruling A on the sess162 wiring): phantom-loss invalidation.
xfs/xfs_mxfs_dlm.c:1392
- * sess403 (RULE-5 ruling ccloop-c7ee71c6-sess403-GPT-ruling-release-marker-
+ * sess403 (design-consult ruling ccloop-c7ee71c6-sess403-GPT-ruling-release-marker-
xfs/xfs_mxfs_dlm.c:1615
-	 * sess403 (RULE-5 ruling: the release is IRREVOCABLE once its
+	 * sess403 (design-consult ruling: the release is IRREVOCABLE once its
xfs/xfs_mxfs_dlm.c:1710
- * sess468 fix shape A (D-FOREIGN-SLICE-INTENTS-ABANDONED; RULE-5 ruling
+ * sess468 fix shape A (D-FOREIGN-SLICE-INTENTS-ABANDONED; design-consult ruling
xfs/xfs_mxfs_dlm.c:1909
-		   * RULE-5 ruling (sess471): recognise that ONE local
+		   * Design-consult ruling (sess471): recognise that ONE local
xfs/xfs_mxfs_dlm.c:1940
-	/* RULE-5 sess469 ruling: a malformed certificate (or one an
+	/* design-consult sess469 ruling: a malformed certificate (or one an
xfs/xfs_mxfs_dlm.c:1983
- * while our certificate still claimed the tenure.  As of sess163 (RULE-5
+ * while our certificate still claimed the tenure.  As of sess163 (design-consult
xfs/xfs_mxfs_dlm.c:2036
-	/* sess238 #23 forensics (RULE-5 ruling): if the task recording this
+	/* sess238 #23 forensics (design-consult ruling): if the task recording this
xfs/xfs_mxfs_dlm.c:2090
- * ccloop a864 sess3 (RULE 4): the dir_reuse@32/caw wedge is an ORPHANED on-disk
+ * ccloop a864 sess3 (instrumented): the dir_reuse@32/caw wedge is an ORPHANED on-disk
xfs/xfs_mxfs_dlm.c:2198
-		 * sess14(a9a03929) RULE-4 PROVEN (r3 dlm_scaling node6 got=0,
+		 * sess14(a9a03929) PROVEN BY INSTRUMENT (r3 dlm_scaling node6 got=0,
xfs/xfs_mxfs_dlm.c:2510
- * sess132 (RULE 4): take ip->i_lock for read in the BAST drain WITHOUT
+ * sess132 (instrumented): take ip->i_lock for read in the BAST drain WITHOUT
xfs/xfs_mxfs_dlm.c:2580
-	 * ccloop c7ee71c6 sess2 (RULE 4, dir_reuse@8/tcp P68-EVDECIDE
+	 * ccloop c7ee71c6 sess2 (instrumented, dir_reuse@8/tcp P68-EVDECIDE
xfs/xfs_mxfs_dlm.c:2695
- * (RULE 4, dpn=100 storm).  Once the shared dir's data fork converts to
+ * (instrumented, dpn=100 storm).  Once the shared dir's data fork converts to
xfs/xfs_mxfs_dlm.c:2721
-	/* sess61 RULE-4 decisive probe state (flush path only): how many leaf
+	/* sess61 instrumented decisive probe state (flush path only): how many leaf
xfs/xfs_mxfs_dlm.c:2874
-	 * sess61 RULE-4 decisive probe (always-on, rate-limited, BTREE flush
+	 * sess61 instrumented decisive probe (always-on, rate-limited, BTREE flush
xfs/xfs_mxfs_dlm.c:2954
- * REFUTED (RULE 4 2a, 8/caw dir_reuse build 9C1728FB, 2026-07-06): with 0,
+ * REFUTED (instrumented 2a, 8/caw dir_reuse build 9C1728FB, 2026-07-06): with 0,
xfs/xfs_mxfs_dlm.c:3022
-/* sess238 (D-OPEN-PROTECT-DEMOTE-RACE-SPURIOUS-EIO) RULE-4 build-1 fault
+/* sess238 (D-OPEN-PROTECT-DEMOTE-RACE-SPURIOUS-EIO) instrumented build-1 fault
xfs/xfs_mxfs_dlm.c:3059
-/* sess7 (a9a03929) RULE-4 probe: format the CURRENT task's transaction
+/* sess7 (a9a03929) instrumented probe: format the CURRENT task's transaction
xfs/xfs_mxfs_dlm.c:3299
-/* sess7(a9a03929) FIX-16 (RULE-4 PROVEN, run89 r6): the noino BAST release
+/* sess7(a9a03929) FIX-16 (PROVEN BY INSTRUMENT, run89 r6): the noino BAST release
xfs/xfs_mxfs_dlm.c:3509
-		 * ccloop-4dd7 sess4 (b57r5 -117 ROOT, RULE 4 PROVEN): the caller
+		 * ccloop-4dd7 sess4 (b57r5 -117 ROOT, PROVEN BY INSTRUMENT): the caller
xfs/xfs_mxfs_dlm.c:3531
-			/* sess48 (RULE 4): a CONFIRMED-handoff owned dir block that
+			/* sess48 (instrumented): a CONFIRMED-handoff owned dir block that
xfs/xfs_mxfs_dlm.c:3573
- * sess62 WRITER-SIDE ORDERING FIX (RULE 4, PROVEN root in
+ * sess62 WRITER-SIDE ORDERING FIX (instrumented, PROVEN root in
xfs/xfs_mxfs_dlm.c:3751
-	 * sess65 (zero_silent_loss — Face-A nheld=0 hole + Face-B root, RULE-5
+	 * sess65 (zero_silent_loss — Face-A nheld=0 hole + Face-B root, design-consult
xfs/xfs_mxfs_dlm.c:3871
-	 * (RULE 4, the path sess73 handed off): sess73 PROVED the on-disk
+	 * (instrumented, the path sess73 handed off): sess73 PROVED the on-disk
xfs/xfs_mxfs_dlm.c:3961
- * sess59 ROOT FIX (RULE 4, PROVEN by the post-reload EFSCORRUPTED shutdown:
+ * sess59 ROOT FIX (instrumented, PROVEN by the post-reload EFSCORRUPTED shutdown:
xfs/xfs_mxfs_dlm.c:4226
- * sess474 (D-0133 RULE-5 instrumentation): the DURABLE superblock summary
+ * sess474 (D-0133 design-consult instrumentation): the DURABLE superblock summary
xfs/xfs_mxfs_dlm.c:4257
- * sess474 (D-0133 counter arm, RULE-5 design A+).  The durable SB summary
+ * sess474 (D-0133 counter arm, design-consult design A+).  The durable SB summary
xfs/xfs_mxfs_dlm.c:4299
- * (RULE-5 ruling: per-node clocks cannot prove non-overlap; a monotonically
+ * (design-consult ruling: per-node clocks cannot prove non-overlap; a monotonically
xfs/xfs_mxfs_dlm.c:4407
- * the RULE-4 proof lap), 1 = refuse (default).
+ * the instrumented proof lap), 1 = refuse (default).
xfs/xfs_mxfs_dlm.c:4437
- * sess475 D-0532 directed concurrency arm (RULE-5 bar: one genuine DLM
+ * sess475 D-0532 directed concurrency arm (design-consult bar: one genuine DLM
xfs/xfs_mxfs_dlm.c:4695
-		 * <ccloop a864 sess7> DECISIVE R-a/R-b DISCRIMINATOR (RULE 4) for
+		 * <ccloop a864 sess7> DECISIVE R-a/R-b DISCRIMINATOR (instrumented) for
xfs/xfs_mxfs_dlm.c:4839
- * sess4 (ccloop 46efd8b6) POST-RELEASE PLATTER AUDIT (RULE 4).  Runs 072239Z-
+ * sess4 (ccloop 46efd8b6) POST-RELEASE PLATTER AUDIT (instrumented).  Runs 072239Z-
xfs/xfs_mxfs_dlm.c:4845
- * instrumented clean, leaving ONE question: does a release's drain actually
+ * Instrumented clean, leaving ONE question: does a release's drain actually
xfs/xfs_mxfs_dlm.c:5039
- * sess60 RULE-4 DECISIVE PROBE.  At a dir's BAST release (after the drain loop
+ * sess60 instrumented DECISIVE PROBE.  At a dir's BAST release (after the drain loop
xfs/xfs_mxfs_dlm.c:5112
- * sess60 RULE-4 probe: log EVERY bmbt-leaf WRITE submission with its numrecs
+ * sess60 instrumented probe: log EVERY bmbt-leaf WRITE submission with its numrecs
xfs/xfs_mxfs_dlm.c:5304
-		/* sess42(ccloop) RULE-4: the VACUOUS-DURABLE bail.  A BTREE dir
+		/* sess42(ccloop) instrumented: the VACUOUS-DURABLE bail.  A BTREE dir
xfs/xfs_mxfs_dlm.c:5358
-			 * sess98 ROOT FIX (RULE 4, PROVEN via P-DIRWR write trace):
+			 * sess98 ROOT FIX (instrumented, PROVEN via P-DIRWR write trace):
xfs/xfs_mxfs_dlm.c:5421
-			      /* sess3(a9a03929) ROOT FIX (RULE 4, PROVEN run64
+			      /* sess3(a9a03929) ROOT FIX (instrumented, PROVEN run64
xfs/xfs_mxfs_dlm.c:5438
-			/* sess42(ccloop) RULE-4: release-drain per-data-block
+			/* sess42(ccloop) instrumented: release-drain per-data-block
xfs/xfs_mxfs_dlm.c:5510
- * sess15 (RULE 4): defensively walk a dir2 DATA/BLOCK-format block buffer and
+ * sess15 (instrumented): defensively walk a dir2 DATA/BLOCK-format block buffer and
xfs/xfs_mxfs_dlm.c:5919
-			 * blocks) — see mxfs_dir_coherent_leaf above for the RULE-4
+			 * blocks) — see mxfs_dir_coherent_leaf above for the instrumented
xfs/xfs_mxfs_dlm.c:6285
-				      /* sess30(ccloop) RULE-4 EXPERIMENT: force-flush
+				      /* sess30(ccloop) instrumented EXPERIMENT: force-flush
xfs/xfs_mxfs_dlm.c:6360
-				 * sess29(ccloop, GPT-5.5 RULE-5): close the residual
+				 * sess29(ccloop, GPT-5.5 design-consult): close the residual
xfs/xfs_mxfs_dlm.c:6411
-			/* sess38(ccloop, GPT-5.5 RULE-5 architectural fix): the
+			/* sess38(ccloop, GPT-5.5 design-consult architectural fix): the
xfs/xfs_mxfs_dlm.c:6612
-				/* sess34 (ccloop) P34-DRAINEPOCH (RULE 4): at the
+				/* sess34 (ccloop) P34-DRAINEPOCH (instrumented): at the
xfs/xfs_mxfs_dlm.c:6733
-				 * sess21 (ccloop 8ddb16a2) RULE-4 DETECTOR: the
+				 * sess21 (ccloop 8ddb16a2) INSTRUMENTED DETECTOR: the
xfs/xfs_mxfs_dlm.c:6861
- * sess491 (D-0491) RULE-4 instrument: count this directory's cached data-fork
+ * sess491 (D-0491) instrumented instrument: count this directory's cached data-fork
xfs/xfs_mxfs_dlm.c:7164
- * D-MASS-UMOUNT-ROOT-EX-SERIALIZE-100S-526B (RULE-5 ruling items 5 and 6:
+ * D-MASS-UMOUNT-ROOT-EX-SERIALIZE-100S-526B (design-consult ruling items 5 and 6:
xfs/xfs_mxfs_dlm.c:7238
- * sess402 (D-TMPFILE-CHURN-RULE0-PERF-400 term (c), RULE 4 step 2): per-site
+ * sess402 (D-TMPFILE-CHURN-the budget rule-PERF-400 term (c), instrument step 2): per-site
xfs/xfs_mxfs_dlm.c:7598
- * reverting them.  ROOT (PROVEN sess40, RULE 4 + sess41 reproduction round 14):
+ * reverting them.  ROOT (PROVEN sess40, instrumented + sess41 reproduction round 14):
xfs/xfs_mxfs_dlm.c:7616
-				 * per-write plain bdev read overhead (RULE 0). */
+				 * per-write plain bdev read overhead (budget). */
xfs/xfs_mxfs_dlm.c:7731
- * ROOT (PROVEN RULE 4 + GPT-5.5): a dir DATA/leaf write bio THIS node submitted
+ * ROOT (PROVEN instrumented + GPT-5.5): a dir DATA/leaf write bio THIS node submitted
xfs/xfs_mxfs_dlm.c:7766
- * (RULE 4, P-WGHOST=0 + P-WMERGE DONE=0): a CLEAN, already-destaged dir DATA
+ * (instrumented, P-WGHOST=0 + P-WMERGE DONE=0): a CLEAN, already-destaged dir DATA
xfs/xfs_mxfs_dlm.c:7935
- * Validated by Gemini (RULE 5, sess109): cluster-buffer write is safe (the
+ * Validated by Gemini (design-consult, sess109): cluster-buffer write is safe (the
xfs/xfs_mxfs_dlm.c:7963
-	 * the node — Gemini (RULE 5) confirms: xfs_bwrite on a buffer still
+	 * the node — Gemini (design consult) confirms: xfs_bwrite on a buffer still
xfs/xfs_mxfs_dlm.c:8045
-		 * sess43 P136-DRAIN-RESCUE (RULE 4 instrument+recover): the
+		 * sess43 P136-DRAIN-RESCUE (instrumented instrument+recover): the
xfs/xfs_mxfs_dlm.c:8269
-	 * sess56 (ccloop 8ddb16a2) — RULE 4 PROVEN (P55D self-skip 10x then
+	 * sess56 (ccloop 8ddb16a2) — PROVEN BY INSTRUMENT (P55D self-skip 10x then
xfs/xfs_mxfs_dlm.c:8290
-	/* sess2 (ccloop 46efd8b6) RULE-4 lap-reason counters: P9-ICD-FAIL fired
+	/* sess2 (ccloop 46efd8b6) instrumented lap-reason counters: P9-ICD-FAIL fired
xfs/xfs_mxfs_dlm.c:8363
-		 * RULE 4 PROVEN (dlm_fairness 4/caw run 022138Z, ino 6291584):
+		 * PROVEN BY INSTRUMENT (dlm_fairness 4/caw run 022138Z, ino 6291584):
xfs/xfs_mxfs_dlm.c:8416
-		 * REVERTED here.  RULE-4 measurement (build 99A4E4905, full 2/tcp suite):
+		 * REVERTED here.  Instrumented measurement (build 99A4E4905, full 2/tcp suite):
xfs/xfs_mxfs_dlm.c:8442
-		 * sess9 ROOT FIX (RULE 4 PROVEN — P9-ICD-FAIL pin=0 in_ail=1
+		 * sess9 ROOT FIX (PROVEN BY INSTRUMENT — P9-ICD-FAIL pin=0 in_ail=1
xfs/xfs_mxfs_dlm.c:8460
-			 * sess115 (ccloop 4eef1f39) — RULE 4 + RULE 5 (Gemini).
+			 * sess115 (ccloop 4eef1f39) — instrumented + design-consult (Gemini).
xfs/xfs_mxfs_dlm.c:8517
-			 * sess55 (ccloop 8ddb16a2; GPT-5.5 RULE-5 — the proven FACE 3
+			 * sess55 (ccloop 8ddb16a2; GPT-5.5 design-consult — the proven FACE 3
xfs/xfs_mxfs_dlm.c:8577
-	/* sess9 RULE-4 diag: why did the durable flush give up?  pin_bails==8
+	/* sess9 instrumented diag: why did the durable flush give up?  pin_bails==8
xfs/xfs_mxfs_dlm.c:8601
- * PROVEN ROOT (RULE 4, test_unlink_visibility): a node mkdir's a child dir under
+ * PROVEN ROOT (instrumented, test_unlink_visibility): a node mkdir's a child dir under
xfs/xfs_mxfs_dlm.c:8627
- * sess3(a9a03929) ROOT FIX (RULE 4, PROVEN run60 t8 277.0s + run61 all-node):
+ * sess3(a9a03929) ROOT FIX (instrumented, PROVEN run60 t8 277.0s + run61 all-node):
xfs/xfs_mxfs_dlm.c:8659
-	 * sess68 (ccloop 4cb2d0a2) PROVEN gap-B FIX (RULE 4): for a grown
+	 * sess68 (ccloop 4cb2d0a2) PROVEN gap-B FIX (instrumented): for a grown
xfs/xfs_mxfs_dlm.c:8876
- * sess68 (ccloop 4cb2d0a2, RULE 4): OWNER-based eviction of every cached dir
+ * sess68 (ccloop 4cb2d0a2, instrumented): OWNER-based eviction of every cached dir
xfs/xfs_mxfs_dlm.c:8982
- * sess15 (RULE 4 — block-dir concurrent-create durable WHOLE-BLOCK loss):
+ * sess15 (instrumented — block-dir concurrent-create durable WHOLE-BLOCK loss):
xfs/xfs_mxfs_dlm.c:9233
-int mxfs_dir_reintro_skip;	/* sess2(ccloop 26c41354): DEFAULT 0.  P-REINTRO-SKIP fix — drop the xfsaild write of a CLEAN superseded zombie dir DATA/BLOCK buffer (in-AIL, !dirty, !pinned, !undestaged, same-incarnation, dc_stale) whose in-core-extra dirents are PURELY free reintroduces (live_extra==0), and mark it for re-read.  Never drops a live add (live_extra>0 => write proceeds).  Lightweight: the disk read + per-inum FUA only run on the rare dc_stale+incore-extra path, so no per-handoff latency (RULE 0).  Targets the 16-node durable dangling-dirent (CASE B). */
+int mxfs_dir_reintro_skip;	/* sess2(ccloop 26c41354): DEFAULT 0.  P-REINTRO-SKIP fix — drop the xfsaild write of a CLEAN superseded zombie dir DATA/BLOCK buffer (in-AIL, !dirty, !pinned, !undestaged, same-incarnation, dc_stale) whose in-core-extra dirents are PURELY free reintroduces (live_extra==0), and mark it for re-read.  Never drops a live add (live_extra>0 => write proceeds).  Lightweight: the disk read + per-inum FUA only run on the rare dc_stale+incore-extra path, so no per-handoff latency (budget).  Targets the 16-node durable dangling-dirent (CASE B). */
xfs/xfs_mxfs_dlm.c:9377
- * sess13run (ccloop 4cb2d0a2; GPT-5.5 Option-1, RULE 4/5): RELEASE-side dir
+ * sess13run (ccloop 4cb2d0a2; GPT-5.5 Option-1, instrumented/5): RELEASE-side dir
xfs/xfs_mxfs_dlm.c:9446
- * sess38 (ccloop, GPT-5.5 RULE-5): retire (instead of bwrite) a DONE +
+ * sess38 (ccloop, GPT-5.5 design-consult): retire (instead of bwrite) a DONE +
xfs/xfs_mxfs_dlm.c:9466
-/* sess30(ccloop) RULE-4 EXPERIMENT, DEFAULT 0: force-flush every XBF_DONE
+/* sess30(ccloop) instrumented EXPERIMENT, DEFAULT 0: force-flush every XBF_DONE
xfs/xfs_mxfs_dlm.c:9502
- * sess13run (ccloop, RULE 4): targeted SCSI FUA WRITE-THROUGH of each released
+ * sess13run (ccloop, instrumented): targeted SCSI FUA WRITE-THROUGH of each released
xfs/xfs_mxfs_dlm.c:9538
-/* sess20: default 1 (ON for multinode shared dirs). PROVEN (RULE 4, build
+/* sess20: default 1 (ON for multinode shared dirs). PROVEN (instrumented, build
xfs/xfs_mxfs_dlm.c:9561
-/* sess26: DEFAULT 0 (OFF).  PROVEN (RULE 4, experiment B6F7AC64): firing the
+/* sess26: DEFAULT 0 (OFF).  PROVEN (instrumented, experiment B6F7AC64): firing the
xfs/xfs_mxfs_dlm.c:9669
-	/* sess69 (RULE 4): always-on capped entry probe — multinode dirs only.
+	/* sess69 (instrumented): always-on capped entry probe — multinode dirs only.
xfs/xfs_mxfs_dlm.c:9756
-	 * sess37 RULE-4 DECISIVE: is THIS node's in-core dir bmap STALE vs the
+	 * sess37 instrumented DECISIVE: is THIS node's in-core dir bmap STALE vs the
xfs/xfs_mxfs_dlm.c:9766
-	/* sess35 (RULE 4): RE-GATED to instr after CONFIRMING the round-1 stale-
+	/* sess35 (instrumented): RE-GATED to instr after CONFIRMING the round-1 stale-
xfs/xfs_mxfs_dlm.c:9771
-	 * run pays no per-modify plain-read cost (RULE 0). */
+	 * run pays no per-modify plain-read cost (budget). */
xfs/xfs_mxfs_dlm.c:9955
-				 * ccloop cc87fed3 sess9 (RULE 4): the cache_coherency@32
+				 * ccloop cc87fed3 sess9 (instrumented): the cache_coherency@32
xfs/xfs_mxfs_dlm.c:10123
-			/* sess68 (RULE 4, minimal-repro): always-on, low-volume
+			/* sess68 (instrumented, minimal-repro): always-on, low-volume
xfs/xfs_mxfs_dlm.c:10476
-				 * sess21 (ccloop 8ddb16a2) RULE-4 DETECTOR (always-on,
+				 * sess21 (ccloop 8ddb16a2) INSTRUMENTED DETECTOR (always-on,
xfs/xfs_mxfs_dlm.c:10636
- * PROVEN (sess97, RULE 4): the writer side now publishes its dir blocks
+ * PROVEN (sess97, instrumented): the writer side now publishes its dir blocks
xfs/xfs_mxfs_dlm.c:10730
-	 * sess104 (Gemini RULE-5 ABA fix): mirror the modify-path fix — a
+	 * sess104 (Gemini design-consult ABA fix): mirror the modify-path fix — a
xfs/xfs_mxfs_dlm.c:10799
- * sess13 (RULE 4 PROVEN — tcp_dlm_scaling MODE B resurrection): refresh a
+ * sess13 (PROVEN BY INSTRUMENT — tcp_dlm_scaling MODE B resurrection): refresh a
xfs/xfs_mxfs_dlm.c:10847
-/* sess67 (RULE 4): force a disk-superset extent-map adopt at the dir-modify
+/* sess67 (instrumented): force a disk-superset extent-map adopt at the dir-modify
xfs/xfs_mxfs_dlm.c:10863
-int mxfs_dir_modify_extent_reload;	/* sess14(ccloop) DEFAULT 0 — TESTED, did NOT fire (P14-MODEXT-RELOAD=0×) and did NOT fix the 8-node create/lookup DABUF_MAP_HOLE (dir_reuse stayed 0/8). The epoch trigger (grant_epoch > valid_epoch) never matched for the non-SF dir. AND sess68 (dir_modify_extent_adopt P68-MAPDIVERGE=0) PROVED in-core extent maps AGREE with disk at modify-prelock time — so the DABUF hole is NOT modify-time extent-map staleness; the fresh-leaf/stale-map hypothesis is REFUTED for the create path. The hole's block-N origin needs INSTRUMENTATION (RULE 4): log, at the xfs_dabuf_map invalid_mapping, where block N's reference came from (a stale LEAF re-read? an in-transaction xfs_trans_roll re-read? a genuinely orphaned daddr?) — do not guess. Kept gated-off as an inert reference. */
+int mxfs_dir_modify_extent_reload;	/* sess14(ccloop) DEFAULT 0 — TESTED, did NOT fire (P14-MODEXT-RELOAD=0×) and did NOT fix the 8-node create/lookup DABUF_MAP_HOLE (dir_reuse stayed 0/8). The epoch trigger (grant_epoch > valid_epoch) never matched for the non-SF dir. AND sess68 (dir_modify_extent_adopt P68-MAPDIVERGE=0) PROVED in-core extent maps AGREE with disk at modify-prelock time — so the DABUF hole is NOT modify-time extent-map staleness; the fresh-leaf/stale-map hypothesis is REFUTED for the create path. The hole's block-N origin needs INSTRUMENTATION (instrumented): log, at the xfs_dabuf_map invalid_mapping, where block N's reference came from (a stale LEAF re-read? an in-transaction xfs_trans_roll re-read? a genuinely orphaned daddr?) — do not guess. Kept gated-off as an inert reference. */
xfs/xfs_mxfs_dlm.c:10867
-int mxfs_dir_block0_canon = 1; /* ccloop(3e02e7dd) sess3, RULE 4 PROVEN: 32/caw dir_reuse round-10 cluster meltdown — dir_epoch_convert_gate above is a RELATIVE staleness check (epoch advanced since my baseline) with a residual TOCTOU window under high concurrency (several nodes can pass it before any one's commit becomes visible to the others); this is an ABSOLUTE existence check (does a canonical block0 exist for this incarnation, full stop), published via the CAW slot's write-once dir_block0_fsb field (immediate at grant time, no disk-FUA/destage-timing dependency; durability guaranteed by Invariant 1 by the time any peer can observe it — see docs/canonical_block0_fix_plan.md). CAW-only. When 1, checked LAST (immediately before the actual conversion decision) so it closes gaps any earlier check in this function missed. */
+int mxfs_dir_block0_canon = 1; /* ccloop(3e02e7dd) sess3, PROVEN BY INSTRUMENT: 32/caw dir_reuse round-10 cluster meltdown — dir_epoch_convert_gate above is a RELATIVE staleness check (epoch advanced since my baseline) with a residual TOCTOU window under high concurrency (several nodes can pass it before any one's commit becomes visible to the others); this is an ABSOLUTE existence check (does a canonical block0 exist for this incarnation, full stop), published via the CAW slot's write-once dir_block0_fsb field (immediate at grant time, no disk-FUA/destage-timing dependency; durability guaranteed by Invariant 1 by the time any peer can observe it — see docs/canonical_block0_fix_plan.md). CAW-only. When 1, checked LAST (immediately before the actual conversion decision) so it closes gaps any earlier check in this function missed. */
xfs/xfs_mxfs_dlm.c:10869
-/* sess67 (GPT-5.5 RULE-5 design): the read-path under-buffer-lock coherency
+/* sess67 (GPT-5.5 design-consult design): the read-path under-buffer-lock coherency
xfs/xfs_mxfs_dlm.c:10908
-/* sess67 (RULE 4, PROVEN via P-DIRIFLUSH dlm_mode=0/NL flushes of the shared
+/* sess67 (instrumented, PROVEN via P-DIRIFLUSH dlm_mode=0/NL flushes of the shared
xfs/xfs_mxfs_dlm.c:10939
- * root (PROVEN, RULE 4, P32-IFLUSH-NXSHRINK fired dlm_mode=5/EX comm=rm).  At
+ * root (PROVEN, instrumented, P32-IFLUSH-NXSHRINK fired dlm_mode=5/EX comm=rm).  At
xfs/xfs_mxfs_dlm.c:11008
- * (RULE 4: measure, then enforce).  Runtime-writable for the A/B. */
+ * (instrumented: measure, then enforce).  Runtime-writable for the A/B. */
xfs/xfs_mxfs_dlm.c:11009
-/* sess34 (Gemini RULE-5 consult #2, 2b): DEFAULT ON.  The re-log-under-
+/* sess34 (Gemini design-consult consult #2, 2b): DEFAULT ON.  The re-log-under-
xfs/xfs_mxfs_dlm.c:11053
- * sess401 (D-TMPFILE-CHURN-RULE0-PERF-400, RULE 4 PROVEN + RULE 5 ruling):
+ * sess401 (D-TMPFILE-CHURN-the budget rule-PERF-400, PROVEN BY INSTRUMENT + design-consult ruling):
xfs/xfs_mxfs_dlm.c:11095
- * sess401 (D-TMPFILE-CHURN-RULE0-PERF-400, RULE-5 ruling priority 2): the
+ * sess401 (D-TMPFILE-CHURN-the budget rule-PERF-400, design-consult ruling priority 2): the
xfs/xfs_mxfs_dlm.c:11155
-int mxfs_dir_addname_epoch_refresh = 1;	/* sess54(ccloop): DEFAULT 1.  RULE-4
+int mxfs_dir_addname_epoch_refresh = 1;	/* sess54(ccloop): DEFAULT 1.  Instrumented
xfs/xfs_mxfs_dlm.c:11281
-	 * sess53 (GPT-5.5 RULE-5 consult, PROVEN tcp_dlm_scaling durable dirent
+	 * sess53 (GPT-5.5 design-consult consult, PROVEN tcp_dlm_scaling durable dirent
xfs/xfs_mxfs_dlm.c:11301
-	 * sess62 (RULE 4, PROVEN ROOT — 4-way logical-block0 SPLIT) BLOCK-FORMAT
+	 * sess62 (instrumented, PROVEN ROOT — 4-way logical-block0 SPLIT) BLOCK-FORMAT
xfs/xfs_mxfs_dlm.c:11318
-	 * sess62 (RULE 4) CONTENT-LEVEL stale-shortform-base fix.  PROVEN: the
+	 * sess62 (instrumented) CONTENT-LEVEL stale-shortform-base fix.  PROVEN: the
xfs/xfs_mxfs_dlm.c:11335
-	 * sess65 (RULE 4, PROVEN: dir_reuse_coherency node1_f1 = DOUBLE sf->block
+	 * sess65 (instrumented, PROVEN: dir_reuse_coherency node1_f1 = DOUBLE sf->block
xfs/xfs_mxfs_dlm.c:11372
-	 * ccloop(3e02e7dd) sess3 (RULE 4 PROVEN — 32/caw dir_reuse round-10
+	 * ccloop(3e02e7dd) sess3 (PROVEN BY INSTRUMENT — 32/caw dir_reuse round-10
xfs/xfs_mxfs_dlm.c:11502
-	 * sess67 (RULE 4, PROVEN ROOT — xfs_dir3_data_init zeroes a live dir DATA
+	 * sess67 (instrumented, PROVEN ROOT — xfs_dir3_data_init zeroes a live dir DATA
xfs/xfs_mxfs_dlm.c:11524
-	     * sess3(a9a03929) ROOT FIX (RULE 4, PROVEN run65 t2 169.654):
+	     * sess3(a9a03929) ROOT FIX (instrumented, PROVEN run65 t2 169.654):
xfs/xfs_mxfs_dlm.c:11575
-				 * sess68 (RULE 4, sess67's documented-but-never-
+				 * sess68 (instrumented, sess67's documented-but-never-
xfs/xfs_mxfs_dlm.c:11698
- * sess12 (ccloop, GPT-5.5 RULE-5 design) — SHORTFORM tenure REBASE.
+ * sess12 (ccloop, GPT-5.5 design-consult design) — SHORTFORM tenure REBASE.
xfs/xfs_mxfs_dlm.c:11728
- * ccloop c7ee71c6 sess19 (RULE 4, BYTE-EXACT PROOF — fence_during_write @32/caw,
+ * ccloop c7ee71c6 sess19 (instrumented, BYTE-EXACT PROOF — fence_during_write @32/caw,
xfs/xfs_mxfs_dlm.c:11774
- * ccloop c7ee71c6 sess19 TORN-FORK TRIPWIRE (RULE 4).
+ * ccloop c7ee71c6 sess19 TORN-FORK TRIPWIRE (instrumented).
xfs/xfs_mxfs_dlm.c:11835
- * ccloop c7ee71c6 sess19 RULE-4 INSTRUMENT (torn-adopt hypothesis).
+ * ccloop c7ee71c6 sess19 instrumented INSTRUMENT (torn-adopt hypothesis).
xfs/xfs_mxfs_dlm.c:12340
-	 * sess104 (Gemini RULE-5 ABA fix): the old gen==0 / gen==evicted_gen
+	 * sess104 (Gemini design-consult ABA fix): the old gen==0 / gen==evicted_gen
xfs/xfs_mxfs_dlm.c:12380
-		/* sess106 (RULE 4): a modify (create/rename/remove) that SKIPS
+		/* sess106 (instrumented): a modify (create/rename/remove) that SKIPS
xfs/xfs_mxfs_dlm.c:12460
- * sess61 PROVEN-ROOT FIX (RULE 4 + GPT-5.5 design) — adopt a peer's already-
+ * sess61 PROVEN-ROOT FIX (instrumented + GPT-5.5 design) — adopt a peer's already-
xfs/xfs_mxfs_dlm.c:13134
-	 * Perf gate (RULE 0): only merge when a peer advanced this dir since our
+	 * Perf gate (budget): only merge when a peer advanced this dir since our
xfs/xfs_mxfs_dlm.c:13142
-	/* sess31 RULE-4 PROBE: is the once-per-tenure gate skipping the merge for
+	/* sess31 INSTRUMENTED PROBE: is the once-per-tenure gate skipping the merge for
xfs/xfs_mxfs_dlm.c:13524
- * sess32 (GPT RULE-5 A-vs-B probe): called AFTER xfs_dir_create_child (our
+ * sess32 (design-consult A-vs-B probe): called AFTER xfs_dir_create_child (our
xfs/xfs_mxfs_dlm.c:13651
- * sess65 (RULE 4, PROVEN ROOT — dir logical-block0 DOUBLE-ALLOCATION):
+ * sess65 (instrumented, PROVEN ROOT — dir logical-block0 DOUBLE-ALLOCATION):
xfs/xfs_mxfs_dlm.c:13811
- * sess99 (GPT RULE-5 design, `docs/history/session-98-gpt-fix-design.md`): RELEASE-side
+ * sess99 (design-consult design, `docs/history/session-98-gpt-fix-design.md`): RELEASE-side
xfs/xfs_mxfs_dlm.c:14019
-		 * sess60 (RULE 4, PROVEN by P60-RELAUDIT): evict ONLY when the
+		 * sess60 (instrumented, PROVEN by P60-RELAUDIT): evict ONLY when the
xfs/xfs_mxfs_dlm.c:14128
-	 * driven to a coherent base.  Once-per-handoff-acquire, so RULE-0 cheap. */
+	 * driven to a coherent base.  Once-per-handoff-acquire, so the budget rule cheap. */
xfs/xfs_mxfs_dlm.c:14196
-		 * sess97 ACQUIRE INVALIDATION FENCE (GPT design, RULE 5).
+		 * sess97 ACQUIRE INVALIDATION FENCE (GPT design, design-consult).
xfs/xfs_mxfs_dlm.c:14237
-		 * v0.5.2 (RULE 4 PROVEN, solo-rsync error-117 forensics): the
+		 * v0.5.2 (PROVEN BY INSTRUMENT, solo-rsync error-117 forensics): the
xfs/xfs_mxfs_dlm.c:14258
-		/* sess6(ccloop) FIX-14 (RULE-4 PROVEN, run87 r2 node4 daddr
+		/* sess6(ccloop) FIX-14 (PROVEN BY INSTRUMENT, run87 r2 node4 daddr
xfs/xfs_mxfs_dlm.c:14272
-			 * sess101 ROOT FIX (Gemini RULE-5, reconciled the
+			 * sess101 ROOT FIX (Gemini design-consult, reconciled the
xfs/xfs_mxfs_dlm.c:14373
-/* sess6(ccloop) phantom-EX measurement (RULE 4): count dir-EX cached fast-path
+/* sess6(ccloop) phantom-EX measurement (instrumented): count dir-EX cached fast-path
xfs/xfs_mxfs_dlm.c:14467
- * sess124 (ccloop, Gemini RULE-5 fairness design #3 — Minimum Hold Time):
+ * sess124 (ccloop, Gemini design-consult fairness design #3 — Minimum Hold Time):
xfs/xfs_mxfs_dlm.c:14655
- * had not committed yet (drc@32 size=0/nx=0 loss).  RULE-4 PROVEN live on
+ * had not committed yet (drc@32 size=0/nx=0 loss).  PROVEN BY INSTRUMENT live on
xfs/xfs_mxfs_dlm.c:14696
- * sess11(a9a03929) — the ino-131 PR self-revoke storm lever (RULE 4 A/B).
+ * sess11(a9a03929) — the ino-131 PR self-revoke storm lever (instrumented A/B).
xfs/xfs_mxfs_dlm.c:14786
- * sess-tcp (RULE 4 PROVEN root): bound the synchronous inode-inactivation
+ * sess-tcp (PROVEN BY INSTRUMENT root): bound the synchronous inode-inactivation
xfs/xfs_mxfs_dlm.c:14807
-/* ccloop c7ee71c6 sess3 FIX-1 (RULE 4, PROVEN via run_dir_reuse_coherency_
+/* ccloop c7ee71c6 sess3 FIX-1 (instrumented, PROVEN via run_dir_reuse_coherency_
xfs/xfs_mxfs_dlm.c:14829
- * scoped to brand-new (XFS_INEW) inodes per the Gemini RULE-5 design.
+ * scoped to brand-new (XFS_INEW) inodes per the Gemini design-consult design.
xfs/xfs_mxfs_dlm.c:14888
- * concurrent-mkdir/create storm — without ever BAST'ing us).  RULE-4 PROVEN:
+ * concurrent-mkdir/create storm — without ever BAST'ing us).  PROVEN BY INSTRUMENT:
xfs/xfs_mxfs_dlm.c:14932
- * This is the evidence for the RULE-5 GPT prescription that the freshness gate
+ * This is the evidence for the design-consult GPT prescription that the freshness gate
xfs/xfs_mxfs_dlm.c:14979
- * RULE-5 consult (sess27) warns that copying the DLM's CURRENT epoch at an
+ * Design-consult consult (sess27) warns that copying the DLM's CURRENT epoch at an
xfs/xfs_mxfs_dlm.c:15163
- * ccloop c7ee71c6 sess45 — OPTION B: ADOPT AT EX ACQUIRE (GPT RULE-5 ruling,
+ * ccloop c7ee71c6 sess45 — OPTION B: ADOPT AT EX ACQUIRE (design-consult ruling,
xfs/xfs_mxfs_dlm.c:15295
- * (RULE-5 GPT prescription).  When the base is not coherent with the epoch on
+ * (design-consult GPT prescription).  When the base is not coherent with the epoch on
xfs/xfs_mxfs_dlm.c:15363
- * cache no peer wants to modify (i_dlm_dir_want_ex==false).  RULE-4 PROVEN
+ * cache no peer wants to modify (i_dlm_dir_want_ex==false).  PROVEN BY INSTRUMENT
xfs/xfs_mxfs_dlm.c:15392
- * a RULE-0 confound.  The double-alloc it caught is believed resolved (fb=0,
+ * a the budget rule confound.  The double-alloc it caught is believed resolved (fb=0,
xfs/xfs_mxfs_dlm.c:15414
- * before any handoff.  RULE-4 A/B lever for the dir_reuse speed fix. */
+ * before any handoff.  Instrumented A/B lever for the dir_reuse speed fix. */
xfs/xfs_mxfs_dlm.c:15420
-/* sess28 (ccloop 14d31183) RULE-4 A/B gate for the scaling_curve 16-node
+/* sess28 (ccloop 14d31183) instrumented A/B gate for the scaling_curve 16-node
xfs/xfs_mxfs_dlm.c:15443
- * block-layer request tags (RULE-4 PROVEN: 767 kworkers all D-state in
+ * block-layer request tags (PROVEN BY INSTRUMENT: 767 kworkers all D-state in
xfs/xfs_mxfs_dlm.c:15460
-/* sess24 (ccloop 4eef1f39) RULE-4 detector gate: when 1, every multi-node inode
+/* sess24 (ccloop 4eef1f39) instrumented detector gate: when 1, every multi-node inode
xfs/xfs_mxfs_dlm.c:15480
- * sess574 RULE-4 INSTRUMENT — should the candidate validator keep running
+ * sess574 instrumented INSTRUMENT — should the candidate validator keep running
xfs/xfs_mxfs_dlm.c:15514
- * sess432 (0.39.12, D-0353, RULE-5 ruling ccmemory ccloop-c7ee71c6-sess432-
+ * sess432 (0.39.12, D-0353, design-consult ruling ccmemory ccloop-c7ee71c6-sess432-
xfs/xfs_mxfs_dlm.c:15602
-		/* sess19(ccloop) PERF probe (RULE 4): log inode-cluster FUA daddrs
+		/* sess19(ccloop) PERF probe (instrumented): log inode-cluster FUA daddrs
xfs/xfs_mxfs_dlm.c:15748
-/* sess4(a9a03929) P71 RULE-4 probe: run68's wedge kept a leaked
+/* sess4(a9a03929) P71 instrumented probe: run68's wedge kept a leaked
xfs/xfs_mxfs_dlm.c:15772
- * ccloop c7ee71c6 sess7 — FENCE-V1 dir-drain task registry (GPT RULE-5
+ * ccloop c7ee71c6 sess7 — FENCE-V1 dir-drain task registry (design-consult
xfs/xfs_mxfs_dlm.c:15860
- * sess214 — recovery-context task registry (incident474 hole b, GPT RULE-5
+ * sess214 — recovery-context task registry (incident474 hole b, design-consult
xfs/xfs_mxfs_dlm.c:16589
-	 * sess447 (D-FOREIGN-REPLAY-UNGATED-IMAGES default-on, RULE-5 proviso):
+	 * sess447 (D-FOREIGN-REPLAY-UNGATED-IMAGES default-on, design-consult proviso):
xfs/xfs_mxfs_dlm.c:16752
- * sess34 FIX-3 — the loss FINALIZER tripwire (Gemini RULE-5 ruling: GFS2/
+ * sess34 FIX-3 — the loss FINALIZER tripwire (Gemini design-consult ruling: GFS2/
xfs/xfs_mxfs_dlm.c:16758
- * full state for the next RULE-4 loop.
+ * full state for the next troubleshooting loop.
xfs/xfs_mxfs_dlm.c:16932
- * D-512 T8 kind-3 (sess416, RULE-4 proven on rig evidence
+ * D-512 T8 kind-3 (sess416, proven by instrument on rig evidence
xfs/xfs_mxfs_dlm.c:17573
- * sess398 (lap-6 typeflip, RULE-4 chain on test18 19:30:43Z; RULE-5 ruling
+ * sess398 (lap-6 typeflip, instrumented chain on test18 19:30:43Z; design-consult ruling
xfs/xfs_mxfs_dlm.c:17962
-	 * so name which one before building anything (RULE 4):
+	 * so name which one before building anything (instrumented):
xfs/xfs_mxfs_dlm.c:18056
-	/* sess4(a9a03929) P70 RULE-4 probe: run68 wedge — a bast_process
+	/* sess4(a9a03929) P70 instrumented probe: run68 wedge — a bast_process
xfs/xfs_mxfs_dlm.c:18204
-	/* sess35 (ccloop) RULE-4 H1/H2 probe: timestamp when the holder ACTUALLY
+	/* sess35 (ccloop) instrumented H1/H2 probe: timestamp when the holder ACTUALLY
xfs/xfs_mxfs_dlm.c:18386
-	 * D-512 cycle-2 (sess415 RULE-5 ruling, drain-error fail-stop): both
+	 * D-512 cycle-2 (sess415 design-consult ruling, drain-error fail-stop): both
xfs/xfs_mxfs_dlm.c:18546
-			 * sess47 (ccloop 4cb2d0a2, RULE 4 PROVEN — NFS-stream
+			 * sess47 (ccloop 4cb2d0a2, PROVEN BY INSTRUMENT — NFS-stream
xfs/xfs_mxfs_dlm.c:18569
-		/* sess38(ccloop, RULE 4 DECISIVE): immediately AFTER the release
+		/* sess38(ccloop, instrumented DECISIVE): immediately AFTER the release
xfs/xfs_mxfs_dlm.c:18582
-			 * sess3(ccloop 3e02e7dd, RULE 4 PROVEN - 32/caw
+			 * sess3(ccloop 3e02e7dd, PROVEN BY INSTRUMENT - 32/caw
xfs/xfs_mxfs_dlm.c:18813
-				/* sess62 (RULE 4) DECISIVE B-vs-C: decode the on-disk
+				/* sess62 (instrumented) DECISIVE B-vs-C: decode the on-disk
xfs/xfs_mxfs_dlm.c:18890
-			 * sess5 (ccloop a16ec5f2) WEDGE ROOT FIX (RULE 4 —
+			 * sess5 (ccloop a16ec5f2) WEDGE ROOT FIX (instrumented —
xfs/xfs_mxfs_dlm.c:19139
-	 * sess34 (ccloop 14d31183) ROOT FIX (RULE 4 step 2b, PROVEN via the
+	 * sess34 (ccloop 14d31183) ROOT FIX (instrument step 2b, PROVEN via the
xfs/xfs_mxfs_dlm.c:19187
-	 * sess15(ccloop) PROVEN ROOT FIX (RULE 4, P15-NL-WHILE-HOLDER fired:
+	 * sess15(ccloop) PROVEN ROOT FIX (instrumented, P15-NL-WHILE-HOLDER fired:
xfs/xfs_mxfs_dlm.c:19206
-	 * sess12(a9a03929) FIX-A (RULE 4 PROVEN, r10 round-4 t2 trace):
+	 * sess12(a9a03929) FIX-A (PROVEN BY INSTRUMENT, r10 round-4 t2 trace):
xfs/xfs_mxfs_dlm.c:19222
-	 * sess15(a9a03929) FIX-H (RULE 4 PROVEN, r10 round-4 t6 capture): an
+	 * sess15(a9a03929) FIX-H (PROVEN BY INSTRUMENT, r10 round-4 t6 capture): an
xfs/xfs_mxfs_dlm.c:19246
-	 * sess15 FIX-H3 (r13 RULE-4 lesson, test2: 1009x rc=-35 + orph=1
+	 * sess15 FIX-H3 (r13 instrumented lesson, test2: 1009x rc=-35 + orph=1
xfs/xfs_mxfs_dlm.c:19261
-	 * sess15 FIX-H2 strike escalation (r12 RULE-4 lesson): the abort-only
+	 * sess15 FIX-H2 strike escalation (r12 instrumented lesson): the abort-only
xfs/xfs_mxfs_dlm.c:19272
-	 * sess4 (ccloop 46efd8b6) THRESHOLD 4->280 (RULE 4 PROVEN): the 4x25ms
+	 * sess4 (ccloop 46efd8b6) THRESHOLD 4->280 (PROVEN BY INSTRUMENT): the 4x25ms
xfs/xfs_mxfs_dlm.c:19302
-		 * ccloop a864 sess4 — WALL-CLOCK strand escape (RULE-4 PROVEN):
+		 * ccloop a864 sess4 — WALL-CLOCK strand escape (PROVEN BY INSTRUMENT):
xfs/xfs_mxfs_dlm.c:19338
-			 * P15-REL-ABORT print).  RULE-4 PROVEN via init_seq
+			 * P15-REL-ABORT print).  PROVEN BY INSTRUMENT via init_seq
xfs/xfs_mxfs_dlm.c:19438
-	 * sess5 (ccloop 72513a13, GPT RULE-5 consult) — CAW: an orphan-live
+	 * sess5 (ccloop 72513a13, design-consult consult) — CAW: an orphan-live
xfs/xfs_mxfs_dlm.c:19485
-	/* sess10 (ccloop 72513a13) — TCP twin of P15-ORPH-PROCEED.  RULE-4
+	/* sess10 (ccloop 72513a13) — TCP twin of P15-ORPH-PROCEED.  Instrumented
xfs/xfs_mxfs_dlm.c:19677
-		/* sess387 RULE-5 ruling (c): a deferred obligation LIVELOCKS if
+		/* sess387 design-consult ruling (c): a deferred obligation LIVELOCKS if
xfs/xfs_mxfs_dlm.c:19955
-	 * sess236 (D-OPEN-PROTECT-DEMOTE-RACE-SPURIOUS-EIO, sess235 RULE-5
+	 * sess236 (D-OPEN-PROTECT-DEMOTE-RACE-SPURIOUS-EIO, sess235 design-consult
xfs/xfs_mxfs_dlm.c:20139
-			 * sess57 (ccloop 8ddb16a2) — RULE 4 race fix for the
+			 * sess57 (ccloop 8ddb16a2) — instrumented race fix for the
xfs/xfs_mxfs_dlm.c:20165
-			/* sess57 RULE-4 PROOF probe: the blocking ilock acquire
+			/* sess57 instrumented PROOF probe: the blocking ilock acquire
xfs/xfs_mxfs_dlm.c:20252
-			 * sess97 RELEASE CHECKPOINT FENCE (GPT design, RULE 5).
+			 * sess97 RELEASE CHECKPOINT FENCE (GPT design, design-consult).
xfs/xfs_mxfs_dlm.c:20351
-		 * sess16 (GPT demote-drain assertion, RULE 4): dump EVERY dir
+		 * sess16 (GPT demote-drain assertion, instrumented): dump EVERY dir
xfs/xfs_mxfs_dlm.c:20413
-		 * sess491 (D-0491) RULE-4 instrument, always on, capped: the
+		 * sess491 (D-0491) instrumented instrument, always on, capped: the
xfs/xfs_mxfs_dlm.c:20452
-		 * sess-tcp (RULE 4, PROVEN — tcp_dlm_scaling durable lost-update):
+		 * sess-tcp (instrumented, PROVEN — tcp_dlm_scaling durable lost-update):
xfs/xfs_mxfs_dlm.c:20522
-		/* sess-tcp RULE-4 VERIFY: after the shortform cluster flush, FUA-read
+		/* sess-tcp instrumented VERIFY: after the shortform cluster flush, FUA-read
xfs/xfs_mxfs_dlm.c:20547
-		/* sess68 (RULE 4, gap-B VERIFY, ALWAYS-ON): for a GROWN (non-LOCAL)
+		/* sess68 (instrumented, gap-B VERIFY, ALWAYS-ON): for a GROWN (non-LOCAL)
xfs/xfs_mxfs_dlm.c:20572
-		/* sess60 RULE-4 decisive probe — see mxfs_dir_bmbt_release_audit. */
+		/* sess60 instrumented decisive probe — see mxfs_dir_bmbt_release_audit. */
xfs/xfs_mxfs_dlm.c:20627
-						 * P35C (RULE 4, sess35 run14d): the bwrite
+						 * P35C (instrumented, sess35 run14d): the bwrite
xfs/xfs_mxfs_dlm.c:20651
-		 * sess96 NOTE (RULE 4 step 2a — REVERTED): adding a deterministic
+		 * sess96 NOTE (instrument step 2a — REVERTED): adding a deterministic
xfs/xfs_mxfs_dlm.c:20664
-		 * sess99 RELEASE PUBLISH-AND-DISCARD (GPT RULE-5 design,
+		 * sess99 RELEASE PUBLISH-AND-DISCARD (design-consult design,
xfs/xfs_mxfs_dlm.c:20852
-		 * (Gemini RULE-5).  The deterministic loop below flushes the
+		 * (Gemini design-consult).  The deterministic loop below flushes the
xfs/xfs_mxfs_dlm.c:20944
-		 * sess79 (Gemini RULE-5 design, Priority 1): make this regular
+		 * sess79 (Gemini design-consult design, Priority 1): make this regular
xfs/xfs_mxfs_dlm.c:21044
-		 * ccloop c7ee71c6 sess19 (RULE 4, cross-node publish ledger).
+		 * ccloop c7ee71c6 sess19 (instrumented, cross-node publish ledger).
xfs/xfs_mxfs_dlm.c:21212
-				 * ccloop c7ee71c6 sess3 (RULE 4, PROVEN via
+				 * ccloop c7ee71c6 sess3 (instrumented, PROVEN via
xfs/xfs_mxfs_dlm.c:21326
-						 * enforcing (RULE 4).
+						 * enforcing (instrumented).
xfs/xfs_mxfs_dlm.c:21333
-							 * sess18 RULE-4 CLASSIFIER.
+							 * sess18 instrumented CLASSIFIER.
xfs/xfs_mxfs_dlm.c:21733
-	/* sess105 (Gemini RULE-5 consult): PROVE the stale-inode-core hole.
+	/* sess105 (Gemini design-consult consult): PROVE the stale-inode-core hole.
xfs/xfs_mxfs_dlm.c:21835
-	/* sess106 (RULE 4): close the on-disk-slot EX-held window (paired with
+	/* sess106 (instrumented): close the on-disk-slot EX-held window (paired with
xfs/xfs_mxfs_dlm.c:21852
-	/* sess51 RULE-4: ALWAYS-ON (ratelimited) per-dir-release cost decomposition
+	/* sess51 instrumented: ALWAYS-ON (ratelimited) per-dir-release cost decomposition
xfs/xfs_mxfs_dlm.c:21863
-	/* sess57 RULE-4 residual-window probe: in_ail/pinned at the LAST
+	/* sess57 instrumented residual-window probe: in_ail/pinned at the LAST
xfs/xfs_mxfs_dlm.c:21899
-	 * sess3(a9a03929) RULE-4 INVARIANT-1 AUDIT (always-on, capped): the
+	 * sess3(a9a03929) instrumented INVARIANT-1 AUDIT (always-on, capped): the
xfs/xfs_mxfs_dlm.c:21910
-	 * sess4(a9a03929) FIX-2 (RULE-4 PROVEN, run70 r1 node7_f45.md5 loss):
+	 * sess4(a9a03929) FIX-2 (PROVEN BY INSTRUMENT, run70 r1 node7_f45.md5 loss):
xfs/xfs_mxfs_dlm.c:21926
-		/* sess6 (46efd8b6) RULE-4 PROVEN (run 114157Z, test3/test12
+		/* sess6 (46efd8b6) PROVEN BY INSTRUMENT (run 114157Z, test3/test12
xfs/xfs_mxfs_dlm.c:21952
-				/* sess6 (46efd8b6) RULE-4 PROVEN (run 110411Z,
+				/* sess6 (46efd8b6) PROVEN BY INSTRUMENT (run 110411Z,
xfs/xfs_mxfs_dlm.c:22055
-	 * RE-ARM (run46 RULE-4 lesson): the first version set state=BAST here
+	 * RE-ARM (run46 instrumented lesson): the first version set state=BAST here
xfs/xfs_mxfs_dlm.c:22394
-				 * RULE-5 GPT review of this defect: "before
+				 * Design-consult GPT review of this defect: "before
xfs/xfs_mxfs_dlm.c:22502
-					 * sess7 (ccloop 72513a13, GPT RULE-5):
+					 * sess7 (ccloop 72513a13, design-consult):
xfs/xfs_mxfs_dlm.c:22555
-					 * sess403 (RULE-5 ruling): CLEAN-RELEASE
+					 * sess403 (design-consult ruling): CLEAN-RELEASE
xfs/xfs_mxfs_dlm.c:22722
-	/* sess7 (ccloop 72513a13) RULE-4 PROVEN: the resource-scoped orphan/
+	/* sess7 (ccloop 72513a13) PROVEN BY INSTRUMENT: the resource-scoped orphan/
xfs/xfs_mxfs_dlm.c:22975
-	 * release this very thread was driving: a self-deadlock.  RULE-4
+	 * release this very thread was driving: a self-deadlock.  Instrumented
xfs/xfs_mxfs_dlm.c:23036
- * ccloop cc87fed3 sess3 (RULE 4 PROVEN — live crash on test6,
+ * ccloop cc87fed3 sess3 (PROVEN BY INSTRUMENT — live crash on test6,
xfs/xfs_mxfs_dlm.c:23053
- * sess7 (ccloop 72513a13, GPT RULE-5 scoped): targeted old-layer recovery for
+ * sess7 (ccloop 72513a13, design-consult scoped): targeted old-layer recovery for
xfs/xfs_mxfs_dlm.c:23155
-	 * This worker is the right place per the sess382 RULE-5 ruling: it is
+	 * This worker is the right place per the sess382 design-consult ruling: it is
xfs/xfs_mxfs_dlm.c:23198
-			 * sess387 RULE-5 ruling: an UNLINK conversion must never
+			 * sess387 design-consult ruling: an UNLINK conversion must never
xfs/xfs_mxfs_dlm.c:23557
- * RULE-4 PROVEN root of the crash_consistency@32 budget FAIL (0/32): 32
+ * PROVEN BY INSTRUMENT root of the crash_consistency@32 budget FAIL (0/32): 32
xfs/xfs_mxfs_dlm.c:23733
- * blocked the full ~60 s/round (RULE-0 slowness -> 300 s test-timeout FAIL).
+ * blocked the full ~60 s/round (the budget rule slowness -> 300 s test-timeout FAIL).
xfs/xfs_mxfs_dlm.c:23762
- * unlock exhausts even the 5s caw_unlock_backoff deadline; RULE-4 PROVEN:
+ * unlock exhausts even the 5s caw_unlock_backoff deadline; PROVEN BY INSTRUMENT:
xfs/xfs_mxfs_dlm.c:24280
-			 * sess390 (D-474, RULE-5 ruling sess390): the frozen
+			 * sess390 (D-474, design-consult ruling sess390): the frozen
xfs/xfs_mxfs_dlm.c:24367
-	 * sess6(ccloop) FIX-12 (RULE-4 PROVEN, run81 r5): the old drain here
+	 * sess6(ccloop) FIX-12 (PROVEN BY INSTRUMENT, run81 r5): the old drain here
xfs/xfs_mxfs_dlm.c:24459
- * sess390 (ccloop c7ee71c6, RULE-5 ruling item 1): the no-inode BAST path
+ * sess390 (ccloop c7ee71c6, design-consult ruling item 1): the no-inode BAST path
xfs/xfs_mxfs_dlm.c:24476
- * Ship default 1 (RULE-5 sess390 ruling item 1). */
+ * Ship default 1 (design-consult sess390 ruling item 1). */
xfs/xfs_mxfs_dlm.c:24732
-		 * sess88 FIX — durable dir-block lost-update (RULE 4 proven,
+		 * sess88 FIX — durable dir-block lost-update (proven by instrument,
xfs/xfs_mxfs_dlm.c:24733
-		 * RULE 5 Gemini-designed).  The in-core inode is GONE
+		 * Design-consult Gemini-designed).  The in-core inode is GONE
xfs/xfs_mxfs_dlm.c:24830
-	/* sess-tcp (RULE 4): always-on (ratelimited) branch-input capture for
+	/* sess-tcp (instrumented): always-on (ratelimited) branch-input capture for
xfs/xfs_mxfs_dlm.c:24857
-	/* sess1(a9a03929) RULE-4 (run45 120s wedge): capped, unratelimited
+	/* sess1(a9a03929) instrumented (run45 120s wedge): capped, unratelimited
xfs/xfs_mxfs_dlm.c:24896
-		 * change — RULE 4 step 2.
+		 * change — instrument step 2.
xfs/xfs_mxfs_dlm.c:24929
-			 * ccloop a864 sess3 ROOT RECOVERY (RULE 4, PROVEN by
+			 * ccloop a864 sess3 ROOT RECOVERY (instrumented, PROVEN by
xfs/xfs_mxfs_dlm.c:25204
-		 * ccloop-4dd7 sess3 (b54r1 dir 131, RULE-4 PROVEN shape): a
+		 * ccloop-4dd7 sess3 (b54r1 dir 131, PROVEN BY INSTRUMENT shape): a
xfs/xfs_mxfs_dlm.c:25281
-			 * sess-tcp ROOT FIX (RULE 4, PROVEN): an IDLE cached
+			 * sess-tcp ROOT FIX (instrumented, PROVEN): an IDLE cached
xfs/xfs_mxfs_dlm.c:25405
-			/* sess7 (ccloop 72513a13, GPT RULE-5): the gg-based
+			/* sess7 (ccloop 72513a13, design-consult): the gg-based
xfs/xfs_mxfs_dlm.c:25418
-			 * RULE-4 PROVEN at 32/tcp drc r5: test12 mirror gg=2048
+			 * PROVEN BY INSTRUMENT at 32/tcp drc r5: test12 mirror gg=2048
xfs/xfs_mxfs_dlm.c:25485
-			 * sess7(a9a03929) FIX-20 (RULE-4 PROVEN, run91/94): the
+			 * sess7(a9a03929) FIX-20 (PROVEN BY INSTRUMENT, run91/94): the
xfs/xfs_mxfs_dlm.c:25681
- * PROVEN (RULE 4, P-SFM-READD with state+gen): the resurrected dirent
+ * PROVEN (instrumented, P-SFM-READD with state+gen): the resurrected dirent
xfs/xfs_mxfs_dlm.c:25712
- * <ccloop sess49> WRITE-SIDE TRIPWIRE (RULE 4): the 8/tcp dir_reuse shutdown is
+ * <ccloop sess49> WRITE-SIDE TRIPWIRE (instrumented): the 8/tcp dir_reuse shutdown is
xfs/xfs_mxfs_dlm.c:25796
- * <ccloop sess49b> DECISIVE disk-vs-incore extent probe (RULE 4): at a dir
+ * <ccloop sess49b> DECISIVE disk-vs-incore extent probe (instrumented): at a dir
xfs/xfs_mxfs_dlm.c:25815
- * sess35 (c7ee71c6) P242 — EX-EPOCH CHURN TRIPWIRE (RULE 4, classifier
+ * sess35 (c7ee71c6) P242 — EX-EPOCH CHURN TRIPWIRE (instrumented, classifier
xfs/xfs_mxfs_dlm.c:25931
-	 * sess3 (ccloop 46efd8b6) LEAF-vs-MAP PLATTER CONSISTENCY SCAN (RULE 4).
+	 * sess3 (ccloop 46efd8b6) LEAF-vs-MAP PLATTER CONSISTENCY SCAN (instrumented).
xfs/xfs_mxfs_dlm.c:26241
- * D-INCARN-STALE-SHELL-UNGATED-FILE-READS-512 component 3 (sess413 RULE-5
+ * D-INCARN-STALE-SHELL-UNGATED-FILE-READS-512 component 3 (sess413 design-consult
xfs/xfs_mxfs_dlm.c:26394
-	bool			reload_kept_ahead = false; /* sess389 (RULE-5 ruling F3): reload_identical was set because the IN-CORE IS AHEAD of the platter (P3-REFUSE-OLDER-DISK / P34F dirty-data keep / P184 obligation keep) — nothing of ours landed, so the publication ledger must NOT be discharged at the end of this reload (measured: P177 identical=1 pend=1 dur=0 "closed" a laundered unlink conversion; the AGI list then named a LINKED home dinode forever). */
+	bool			reload_kept_ahead = false; /* sess389 (design-consult ruling F3): reload_identical was set because the IN-CORE IS AHEAD of the platter (P3-REFUSE-OLDER-DISK / P34F dirty-data keep / P184 obligation keep) — nothing of ours landed, so the publication ledger must NOT be discharged at the end of this reload (measured: P177 identical=1 pend=1 dur=0 "closed" a laundered unlink conversion; the AGI list then named a LINKED home dinode forever). */
xfs/xfs_mxfs_dlm.c:26409
-	 * ccloop c7ee71c6 sess3 (RULE 4, PROVEN via the 15:21:37 test1
+	 * ccloop c7ee71c6 sess3 (instrumented, PROVEN via the 15:21:37 test1
xfs/xfs_mxfs_dlm.c:26722
-	 * sess389 (RULE-5 ruling F4): the premise above is no longer true —
+	 * sess389 (design-consult ruling F4): the premise above is no longer true —
xfs/xfs_mxfs_dlm.c:26735
-	 * sess5(a9a03929) run78 ROOT FIX (RULE 4, test4 r3 @76.239): a reload
+	 * sess5(a9a03929) run78 ROOT FIX (instrumented, test4 r3 @76.239): a reload
xfs/xfs_mxfs_dlm.c:26763
-		 * sess14(a9a03929) FIX-G (RULE 4 PROVEN, 2/tcp iter s14a test2
+		 * sess14(a9a03929) FIX-G (PROVEN BY INSTRUMENT, 2/tcp iter s14a test2
xfs/xfs_mxfs_dlm.c:26850
-	 * sess6 (ccloop 46efd8b6) RULE-4 PROVEN (runs 120345Z + 121411Z —
+	 * sess6 (ccloop 46efd8b6) PROVEN BY INSTRUMENT (runs 120345Z + 121411Z —
xfs/xfs_mxfs_dlm.c:27195
-			/* v0.6.4 CAW epoch-consume (RULE 4 PROVEN, 4/caw uv run
+			/* v0.6.4 CAW epoch-consume (PROVEN BY INSTRUMENT, 4/caw uv run
xfs/xfs_mxfs_dlm.c:27222
-			/* sess9 (ccloop 72513a13) RULE-4 PROVEN at 32/tcp drc
+			/* sess9 (ccloop 72513a13) PROVEN BY INSTRUMENT at 32/tcp drc
xfs/xfs_mxfs_dlm.c:27277
-	 * sess36 ROOT FIX (RULE 4, PROVEN by the s36 run5-iter1 timeline):
+	 * sess36 ROOT FIX (instrumented, PROVEN by the s36 run5-iter1 timeline):
xfs/xfs_mxfs_dlm.c:27294
-	 * sess49 SCOPE FIX (RULE 4 PROVEN — 16-node cross_write_read durable
+	 * sess49 SCOPE FIX (PROVEN BY INSTRUMENT — 16-node cross_write_read durable
xfs/xfs_mxfs_dlm.c:27318
-	 * sess58 ROOT FIX (RULE 4, PROVEN by P58-SELFSKIP-STALE-DIR +
+	 * sess58 ROOT FIX (instrumented, PROVEN by P58-SELFSKIP-STALE-DIR +
xfs/xfs_mxfs_dlm.c:27338
-	 * sess59 ROOT FIX (RULE 4, PROVEN by the P58-SELFSKIP-STALE-DIR
+	 * sess59 ROOT FIX (instrumented, PROVEN by the P58-SELFSKIP-STALE-DIR
xfs/xfs_mxfs_dlm.c:27399
-		/* sess58 (ccloop 14d31183) RULE-4 PROOF: an ALWAYS-ON (rate-
+		/* sess58 (ccloop 14d31183) instrumented PROOF: an ALWAYS-ON (rate-
xfs/xfs_mxfs_dlm.c:27413
-				/* sess59 RULE-4 DISCRIMINATOR: read the CAW
+				/* sess59 instrumented DISCRIMINATOR: read the CAW
xfs/xfs_mxfs_dlm.c:27640
-	 * sess31 (ccloop 8ddb16a2) P31B-RELOAD-BUF DECISIVE INSTRUMENT (RULE 4,
+	 * sess31 (ccloop 8ddb16a2) P31B-RELOAD-BUF DECISIVE INSTRUMENT (instrumented,
xfs/xfs_mxfs_dlm.c:27688
-	 * sess133 P133-DINO-READSTALE (RULE 4): the dabuf-HOLE shutdown shows
+	 * sess133 P133-DINO-READSTALE (instrumented): the dabuf-HOLE shutdown shows
xfs/xfs_mxfs_dlm.c:27828
-	 * sess103 FIX (RULE 4 step 2b — PROVEN via the inode 6291588 reuse
+	 * sess103 FIX (instrument step 2b — PROVEN via the inode 6291588 reuse
xfs/xfs_mxfs_dlm.c:27847
-	 * sess41 FIX (RULE 4 step 2b — PROVEN by the trunc_legal probe,
+	 * sess41 FIX (instrument step 2b — PROVEN by the trunc_legal probe,
xfs/xfs_mxfs_dlm.c:27910
-	 * sess90 FIX (RULE 4 step 2b — proven via P-RELOAD-TYPEFLIP +
+	 * sess90 FIX (instrument step 2b — proven via P-RELOAD-TYPEFLIP +
xfs/xfs_mxfs_dlm.c:27934
-	 * sess96 FIX (RULE 4 step 2b — proven via P95-TYPEFLIP-RELOAD thrash,
+	 * sess96 FIX (instrument step 2b — proven via P95-TYPEFLIP-RELOAD thrash,
xfs/xfs_mxfs_dlm.c:27946
-	 * ccloop c7ee71c6 sess27 ROOT FIX (RULE 4 step 2b — PROVEN, see below):
+	 * ccloop c7ee71c6 sess27 ROOT FIX (instrument step 2b — PROVEN, see below):
xfs/xfs_mxfs_dlm.c:28057
-	 * sess52 (ccloop 8ddb16a2) FREED-REUSE DIR GUARD (RULE 4 — PROVEN via the
+	 * sess52 (ccloop 8ddb16a2) FREED-REUSE DIR GUARD (instrumented — PROVEN via the
xfs/xfs_mxfs_dlm.c:28084
-		 * ccloop c7ee71c6 sess3 (RULE-5 GPT decision matrix): the
+		 * ccloop c7ee71c6 sess3 (design-consult GPT decision matrix): the
xfs/xfs_mxfs_dlm.c:28158
-		 * 2/tcp, RULE 4 — PROVEN via P31B-RELOAD-BUF + P-RELOAD-IOPS-REWIRE):
+		 * 2/tcp, instrumented — PROVEN via P31B-RELOAD-BUF + P-RELOAD-IOPS-REWIRE):
xfs/xfs_mxfs_dlm.c:28180
-		 * sess6(ccloop) FIX-13 (RULE-4 PROVEN, run84 test3 @186.65):
+		 * sess6(ccloop) FIX-13 (PROVEN BY INSTRUMENT, run84 test3 @186.65):
xfs/xfs_mxfs_dlm.c:28212
-		 * ccloop-4dd7 ZOMBIE ARM (RULE-4 evidence, ino 1862 / ino
+		 * ccloop-4dd7 ZOMBIE ARM (instrumented evidence, ino 1862 / ino
xfs/xfs_mxfs_dlm.c:28293
-	 * dir_reuse_coherency 2/tcp (RULE 4 step 2b, P33-FROMDISK-DIRSHRINK
+	 * dir_reuse_coherency 2/tcp (instrument step 2b, P33-FROMDISK-DIRSHRINK
xfs/xfs_mxfs_dlm.c:28370
-	 * dir_reuse_coherency 2/tcp data loss (RULE 4 step 2b; sess42
+	 * dir_reuse_coherency 2/tcp data loss (instrument step 2b; sess42
xfs/xfs_mxfs_dlm.c:28409
-		 * sess49 (ccloop 8ddb16a2) — SOUNDNESS GATE on P43 (RULE 4 PROVEN,
+		 * sess49 (ccloop 8ddb16a2) — SOUNDNESS GATE on P43 (PROVEN BY INSTRUMENT,
xfs/xfs_mxfs_dlm.c:28490
-	 * sess65 (RULE 4, PROVEN: dir_reuse_coherency node1_f1 loss): within ONE
+	 * sess65 (instrumented, PROVEN: dir_reuse_coherency node1_f1 loss): within ONE
xfs/xfs_mxfs_dlm.c:28575
-		 * ccloop c7ee71c6 sess14 (D5 FIX, GPT RULE-5 design): if the
+		 * ccloop c7ee71c6 sess14 (D5 FIX, design-consult design): if the
xfs/xfs_mxfs_dlm.c:28652
-	 * sess87 ROOT FIX (RULE 4, PROVEN): the shutdown that blocks
+	 * sess87 ROOT FIX (instrumented, PROVEN): the shutdown that blocks
xfs/xfs_mxfs_dlm.c:28688
-		 * sess34 (ccloop 14d31183) ROOT FIX (RULE 4 step 2b, PROVEN
+		 * sess34 (ccloop 14d31183) ROOT FIX (instrument step 2b, PROVEN
xfs/xfs_mxfs_dlm.c:28715
-			 * sess42 (ccloop 14d31183) ROOT FIX (RULE 4, PROVEN via
+			 * sess42 (ccloop 14d31183) ROOT FIX (instrumented, PROVEN via
xfs/xfs_mxfs_dlm.c:28762
-				 * (RULE 4 — PROVEN via run29 t4 ledger, ino
+				 * (instrumented — PROVEN via run29 t4 ledger, ino
xfs/xfs_mxfs_dlm.c:28818
-					 * sess52 (RULE 4 — PROVEN via run-4
+					 * sess52 (instrumented — PROVEN via run-4
xfs/xfs_mxfs_dlm.c:28839
-					 * ccloop c7ee71c6 sess3 (RULE-5 matrix):
+					 * ccloop c7ee71c6 sess3 (design-consult matrix):
xfs/xfs_mxfs_dlm.c:28854
-					 * sess5(a9a03929) run77 ROOT FIX (RULE 4, captured
+					 * sess5(a9a03929) run77 ROOT FIX (instrumented, captured
xfs/xfs_mxfs_dlm.c:28883
-					 * D-512 cycle-2 (sess415 RULE-5 ruling):
+					 * D-512 cycle-2 (sess415 design-consult ruling):
xfs/xfs_mxfs_dlm.c:28942
-						 * ccloop c7ee71c6 sess3 (RULE-5
+						 * ccloop c7ee71c6 sess3 (design-consult
xfs/xfs_mxfs_dlm.c:29135
-			 * sess114+ DECISIVE PROBE (RULE 4): the plain-bio reload
+			 * sess114+ DECISIVE PROBE (instrumented): the plain-bio reload
xfs/xfs_mxfs_dlm.c:29195
-	 * sess62 DECISIVE PROBE (RULE 4) for the uniform zero_silent_loss
+	 * sess62 DECISIVE PROBE (instrumented) for the uniform zero_silent_loss
xfs/xfs_mxfs_dlm.c:29236
-		 * sess3 (ccloop 46efd8b6) DUAL-READ DISCRIMINATOR (RULE 4).
+		 * sess3 (ccloop 46efd8b6) DUAL-READ DISCRIMINATOR (instrumented).
xfs/xfs_mxfs_dlm.c:29313
-	 * ccloop c7ee71c6 sess19 (RULE 4, PROVEN by this tree's own probe).
+	 * ccloop c7ee71c6 sess19 (instrumented, PROVEN by this tree's own probe).
xfs/xfs_mxfs_dlm.c:29479
-	 * sess8 (ccloop 12e0d157) RULE-4 PROVEN 32-node dir EX-loop root fix:
+	 * sess8 (ccloop 12e0d157) PROVEN BY INSTRUMENT 32-node dir EX-loop root fix:
xfs/xfs_mxfs_dlm.c:29528
-	    /* sess389 (RULE-5 ruling F3): "identical" discharges the ledger,
+	    /* sess389 (design-consult ruling F3): "identical" discharges the ledger,
xfs/xfs_mxfs_dlm.c:29546
-		 * ccloop-4dd7 P134 (RULE-4, ino-1862 zombie autopsy): the
+		 * ccloop-4dd7 P134 (instrumented, ino-1862 zombie autopsy): the
xfs/xfs_mxfs_dlm.c:29630
-	 * sess3 (ccloop 46efd8b6) TIME-TRAVEL GUARD (RULE 4, PROVEN run
+	 * sess3 (ccloop 46efd8b6) TIME-TRAVEL GUARD (instrumented, PROVEN run
xfs/xfs_mxfs_dlm.c:29710
-	 * 2026-07-16 DIRTY-DATA GUARD (RULE 4, PROVEN live at fence@8/caw):
+	 * 2026-07-16 DIRTY-DATA GUARD (instrumented, PROVEN live at fence@8/caw):
xfs/xfs_mxfs_dlm.c:29877
-	 * ccloop c7ee71c6 sess20 — TORN-LOCAL-FORK ROOT FIX (RULE 4 step 2b,
+	 * ccloop c7ee71c6 sess20 — TORN-LOCAL-FORK ROOT FIX (instrument step 2b,
xfs/xfs_mxfs_dlm.c:29928
-	 * sess68 (RULE 4, GPT-5.5 architecture): purge owned dir blocks BEFORE
+	 * sess68 (instrumented, GPT-5.5 architecture): purge owned dir blocks BEFORE
xfs/xfs_mxfs_dlm.c:29965
-		 * (RULE 0).  UNVERIFIED — test drc_reliab_iter.sh 8 >=5x before trust. */
+		 * (budget).  UNVERIFIED — test drc_reliab_iter.sh 8 >=5x before trust. */
xfs/xfs_mxfs_dlm.c:30136
-		 * sess389 (RULE-5 ruling F3/F4).  Three distinct outcomes share
+		 * sess389 (design-consult ruling F3/F4).  Three distinct outcomes share
xfs/xfs_mxfs_dlm.c:30246
-		 * sess114 PROVEN ROOT FIX (RULE 4, oops trace): xfs_idestroy_fork
+		 * sess114 PROVEN ROOT FIX (instrumented, oops trace): xfs_idestroy_fork
xfs/xfs_mxfs_dlm.c:30275
-		 * sess72 PROVEN ROOT FIX (RULE 4 step 2b): an in-place reload
+		 * sess72 PROVEN ROOT FIX (instrument step 2b): an in-place reload
xfs/xfs_mxfs_dlm.c:30329
-		/* P-RELOAD-SIZESEVER (sess10 ccloop 72513a13, RULE-4 drc size=0):
+		/* P-RELOAD-SIZESEVER (sess10 ccloop 72513a13, instrumented drc size=0):
xfs/xfs_mxfs_dlm.c:30355
-		/* sess10 FIX (gated for the RULE-4 A/B): when the reload KEPT the
+		/* sess10 FIX (gated for the instrumented A/B): when the reload KEPT the
xfs/xfs_mxfs_dlm.c:30387
-			 * sess53 (GPT-5.5 RULE-5 consult, PROVEN durable dirent
+			 * sess53 (GPT-5.5 design-consult consult, PROVEN durable dirent
xfs/xfs_mxfs_dlm.c:30457
-			/* sess56 DECISIVE PROBE (RULE 4): after the reload adopts disk
+			/* sess56 DECISIVE PROBE (instrumented): after the reload adopts disk
xfs/xfs_mxfs_dlm.c:30541
-		 * spot the RULE-5 review named).  Asking it here closes that:
+		 * spot the design-consult review named).  Asking it here closes that:
xfs/xfs_mxfs_dlm.c:30639
-		/* sess68 (RULE 4): did this adopt SHRINK the dir data fork or
+		/* sess68 (instrumented): did this adopt SHRINK the dir data fork or
xfs/xfs_mxfs_dlm.c:30720
-	 * sess62 ROOT FIX (RULE 4, PROVEN by signature 1 — `ir.loaded !=
+	 * sess62 ROOT FIX (instrumented, PROVEN by signature 1 — `ir.loaded !=
xfs/xfs_mxfs_dlm.c:30948
-	 * sess68 (RULE 4): the per-extent-map evict above only reaches daddrs
+	 * sess68 (instrumented): the per-extent-map evict above only reaches daddrs
xfs/xfs_mxfs_dlm.c:31110
-	 * ccloop cc87fed3 sess7: RULE-4 hypothesis for the fence_during_write
+	 * ccloop cc87fed3 sess7: instrumented hypothesis for the fence_during_write
xfs/xfs_mxfs_dlm.c:31218
-	 * ccloop-4dd7 GENERATION CONVERGE (RULE-4 evidence: P-CR62 disk_di_gen
+	 * ccloop-4dd7 GENERATION CONVERGE (instrumented evidence: P-CR62 disk_di_gen
xfs/xfs_mxfs_dlm.c:31345
- * sess14 (RULE 4 — PROVEN write-side shortform resurrection, n2_r6 stuck):
+ * sess14 (instrumented — PROVEN write-side shortform resurrection, n2_r6 stuck):
xfs/xfs_mxfs_dlm.c:31506
-		/* sess64 (RULE 4 PROVE): a name in THEIRS (disk) + BASE but NOT in
+		/* sess64 (instrumented PROVE): a name in THEIRS (disk) + BASE but NOT in
xfs/xfs_mxfs_dlm.c:31886
-	 * sess56 (ccloop 8ddb16a2) — RULE 4 PROVEN (tds leftover n2_r130, a
+	 * sess56 (ccloop 8ddb16a2) — PROVEN BY INSTRUMENT (tds leftover n2_r130, a
xfs/xfs_mxfs_dlm.c:31947
- * sess13 (RULE 4 PROVEN): cooperative cached-AG yield to break the
+ * sess13 (PROVEN BY INSTRUMENT): cooperative cached-AG yield to break the
xfs/xfs_mxfs_dlm.c:32011
-		/* sess58 RULE-4 diagnostic: when an AG is held/cached but NOT
+		/* sess58 instrumented diagnostic: when an AG is held/cached but NOT
xfs/xfs_mxfs_dlm.c:32753
-	 * sess108 (RULE 4 PROVEN root: P106-STALE-EX): before trusting a
+	 * sess108 (PROVEN BY INSTRUMENT root: P106-STALE-EX): before trusting a
xfs/xfs_mxfs_dlm.c:33169
-			/* P24-DIRSLOW (sess24 ccloop 14d31183, RULE 4): name the
+			/* P24-DIRSLOW (sess24 ccloop 14d31183, instrumented): name the
xfs/xfs_mxfs_dlm.c:33188
-			 * sess107 (RULE 4 PROVEN root, RULE 5 Gemini design):
+			 * sess107 (PROVEN BY INSTRUMENT root, design-consult Gemini design):
xfs/xfs_mxfs_dlm.c:33225
-			 * sess107 RULE-4 STEP 1 (PROVE): when this dir-EX fast-path
+			 * sess107 instrumented STEP 1 (PROVE): when this dir-EX fast-path
xfs/xfs_mxfs_dlm.c:33352
-					/* sess94 DETECTION (RULE-4 prove-first): this
+					/* sess94 DETECTION (instrumented prove-first): this
xfs/xfs_mxfs_dlm.c:33394
-				 * sess100 FIX (RULE 4, proven root): a block/leaf-
+				 * sess100 FIX (instrumented, proven root): a block/leaf-
xfs/xfs_mxfs_dlm.c:33449
-				 * ccloop 72513a13 sess3 (RULE-4 kprobe-counted): on CAW
+				 * ccloop 72513a13 sess3 (instrumented kprobe-counted): on CAW
xfs/xfs_mxfs_dlm.c:33465
-			/* P-TDS (sess10, RULE-4 instrument-first): probe the
+			/* P-TDS (sess10, instrumented instrument-first): probe the
xfs/xfs_mxfs_dlm.c:33522
-				 * sess50(ccloop) RULE-4 UNDER-FIRE DETECTOR (pure instrument,
+				 * sess50(ccloop) instrumented UNDER-FIRE DETECTOR (pure instrument,
xfs/xfs_mxfs_dlm.c:33587
-				 * sess42(ccloop) RULE-4 DECISIVE PROBE (always-on,
+				 * sess42(ccloop) instrumented DECISIVE PROBE (always-on,
xfs/xfs_mxfs_dlm.c:33799
-			 * RULE-5 design "rebuild the fork from the authoritative
+			 * Design-consult design "rebuild the fork from the authoritative
xfs/xfs_mxfs_dlm.c:34034
-					/* v0.6.5 (sess5 186320ae) — ENFORCE.  RULE 4
+					/* v0.6.5 (sess5 186320ae) — ENFORCE.  Instrumented
xfs/xfs_mxfs_dlm.c:34228
-	 * sess47 (ccloop 4cb2d0a2) RULE-4 INSTRUMENT: capture WHY a NON-dir
+	 * sess47 (ccloop 4cb2d0a2) instrumented INSTRUMENT: capture WHY a NON-dir
xfs/xfs_mxfs_dlm.c:34239
-	 * sess47 (ccloop 4cb2d0a2, RULE 4 PROVEN — DEADLOCK CAPTURED) — admit an
+	 * sess47 (ccloop 4cb2d0a2, PROVEN BY INSTRUMENT — DEADLOCK CAPTURED) — admit an
xfs/xfs_mxfs_dlm.c:34284
-	 * sess4(a9a03929) FIX-1 (RULE-4 PROVEN, run68 + run69): nested-hold
+	 * sess4(a9a03929) FIX-1 (PROVEN BY INSTRUMENT, run68 + run69): nested-hold
xfs/xfs_mxfs_dlm.c:34499
-		 * sess7(a9a03929) FIX-18 (RULE-4 PROVEN, run91 ino=8388742):
+		 * sess7(a9a03929) FIX-18 (PROVEN BY INSTRUMENT, run91 ino=8388742):
xfs/xfs_mxfs_dlm.c:34574
-					 * SERIALIZE-100S-526B, RULE-4 step 2):
+					 * SERIALIZE-100S-526B, instrument step 2):
xfs/xfs_mxfs_dlm.c:34749
-				 * sess9 (ccloop 72513a13) RULE-4 PROVEN live on
+				 * sess9 (ccloop 72513a13) PROVEN BY INSTRUMENT live on
xfs/xfs_mxfs_dlm.c:34826
-		/* sess3(a9a03929) RULE-4 probe: a demoter-context thread is
+		/* sess3(a9a03929) instrumented probe: a demoter-context thread is
xfs/xfs_mxfs_dlm.c:34878
-		/* P23-SLOWPATH (sess23 ccloop 14d31183, RULE 4): name every
+		/* P23-SLOWPATH (sess23 ccloop 14d31183, instrumented): name every
xfs/xfs_mxfs_dlm.c:34926
-		/* sess214 (incident474, RULE-5 b2): foreign-slice REPLAY is
+		/* sess214 (incident474, design-consult b2): foreign-slice REPLAY is
xfs/xfs_mxfs_dlm.c:34961
-		/* sess34 (ccloop) RULE-4: pin the per-handoff slow inode-EX/PR
+		/* sess34 (ccloop) instrumented: pin the per-handoff slow inode-EX/PR
xfs/xfs_mxfs_dlm.c:35109
-			 * sess60 ROOT FIX (RULE 4, PROVEN by P-IGRAB-NULL
+			 * sess60 ROOT FIX (instrumented, PROVEN by P-IGRAB-NULL
xfs/xfs_mxfs_dlm.c:35218
-		 * sess214 (incident474, RULE-5 b3): a recovery-context task —
+		 * sess214 (incident474, design-consult b3): a recovery-context task —
xfs/xfs_mxfs_dlm.c:35499
-	/* sess106 (RULE 4): bracket the cross-node ON-DISK-slot EX-held window
+	/* sess106 (instrumented): bracket the cross-node ON-DISK-slot EX-held window
xfs/xfs_mxfs_dlm.c:35647
-	/* sess105 (Gemini RULE-5 consult): PROVE the stale-inode-core hole.
+	/* sess105 (Gemini design-consult consult): PROVE the stale-inode-core hole.
xfs/xfs_mxfs_dlm.c:35928
- * PROVEN ROOT (RULE 4, sess54 dlm_fairness/tcp_dlm_scaling "shared dir drained
+ * PROVEN ROOT (instrumented, sess54 dlm_fairness/tcp_dlm_scaling "shared dir drained
xfs/xfs_mxfs_dlm.c:35989
-	 * v0.11.81 (P125 root fix, RULE-4 ring-proven): the holder-count
+	 * v0.11.81 (P125 root fix, instrumented ring-proven): the holder-count
xfs/xfs_mxfs_dlm.c:36085
-		 * sess11(a9a03929) RULE-4 PROVEN storm engine (P70-BP qsrc=1
+		 * sess11(a9a03929) PROVEN BY INSTRUMENT storm engine (P70-BP qsrc=1
xfs/xfs_mxfs_dlm.c:36187
-		 * ccloop cc87fed3 sess5 (RULE 4 PROVEN — BUG3 root cause): this
+		 * ccloop cc87fed3 sess5 (PROVEN BY INSTRUMENT — BUG3 root cause): this
xfs/xfs_mxfs_dlm.c:36241
-		 * sess47 (ccloop 4cb2d0a2, RULE 4 PROVEN — NFS-stream hung-task
+		 * sess47 (ccloop 4cb2d0a2, PROVEN BY INSTRUMENT — NFS-stream hung-task
xfs/xfs_mxfs_dlm.c:36264
-		 * ccloop cc87fed3 sess5 (RULE 4 PROVEN — BUG3 root cause): same
+		 * ccloop cc87fed3 sess5 (PROVEN BY INSTRUMENT — BUG3 root cause): same
xfs/xfs_mxfs_dlm.c:36382
-	 * DETECTION FIRST (RULE 4): count + stamp; xfs_trans_log_inode
+	 * DETECTION FIRST (instrumented): count + stamp; xfs_trans_log_inode
xfs/xfs_mxfs_dlm.c:36385
-	 * on this measurement + a RULE-5 consult.
+	 * on this measurement + a design-consult consult.
xfs/xfs_mxfs_dlm.c:36723
- * sess128 ROOT FIX (cross_write_read empty-md5, RULE 4 PROVEN via
+ * sess128 ROOT FIX (cross_write_read empty-md5, PROVEN BY INSTRUMENT via
xfs/xfs_mxfs_dlm.c:37204
-			 * sess15(a9a03929) FIX-I (RULE 4 PROVEN, 2/tcp r6 ino
+			 * sess15(a9a03929) FIX-I (PROVEN BY INSTRUMENT, 2/tcp r6 ino
xfs/xfs_mxfs_dlm.c:37229
-			 * stood here.  RULE-4 history, all measured on the
+			 * stood here.  Instrumented history, all measured on the
xfs/xfs_mxfs_dlm.c:37463
- * the caller's wait is BOUNDED.  ROOT (RULE 4, measured live): the old
+ * the caller's wait is BOUNDED.  ROOT (instrumented, measured live): the old
xfs/xfs_mxfs_dlm.c:37565
-	 * sess5 WAVE A (RULE 4, P138-measured): the per-child eager-demote
+	 * sess5 WAVE A (instrumented, P138-measured): the per-child eager-demote
xfs/xfs_mxfs_dlm.c:37739
-	/* P24 RULE-4 probe: confirm the worker runs and claims (capped). */
+	/* P24 instrumented probe: confirm the worker runs and claims (capped). */
xfs/xfs_mxfs_dlm.c:37761
-			/* P24 RULE-4 probe: dump what the scan actually sees. */
+			/* P24 instrumented probe: dump what the scan actually sees. */
xfs/xfs_mxfs_dlm.c:37797
-			/* P24 RULE-4 probe: why the scan came up empty. */
+			/* P24 instrumented probe: why the scan came up empty. */
xfs/xfs_mxfs_dlm.c:37877
-	/* P24 RULE-4 probe (capped): claims per drain. */
+	/* P24 instrumented probe (capped): claims per drain. */
xfs/xfs_mxfs_dlm.c:37927
-		 * sess128 RULE-4 probe: publish-on-create called for a REUSED
+		 * sess128 instrumented probe: publish-on-create called for a REUSED
xfs/xfs_mxfs_dlm.c:38001
-	 * ccloop cc87fed3 sess3 (RULE 4 instrument — hunting a PROVEN but
+	 * ccloop cc87fed3 sess3 (instrumented instrument — hunting a PROVEN but
xfs/xfs_mxfs_dlm.c:38069
-		/* sess429 (RULE-5 ruling, home-free settle): a shell already
+		/* sess429 (design-consult ruling, home-free settle): a shell already
xfs/xfs_mxfs_dlm.c:38180
-	 * measurement, and it is exactly the shape RULE 6 warns about — so
+	 * measurement, and it is exactly the shape the zero-defect bar warns about — so
xfs/xfs_mxfs_dlm.c:38260
-	 * sess491 (D-0491) RULE-4 instrument, always on: until 0.70.5 the
+	 * sess491 (D-0491) instrumented instrument, always on: until 0.70.5 the
xfs/xfs_mxfs_dlm.c:38306
-	 * sess492 (D-0491, RULE-4 PROVEN by the 0.70.4 census, 3 laps of 3):
+	 * sess492 (D-0491, PROVEN by the 0.70.4 census, 3 laps of 3):
xfs/xfs_mxfs_dlm.c:38411
-		 * PEER-EFFECTIVE (RULE-5 sess163 ruling B: that, not
+		 * PEER-EFFECTIVE (design-consult sess163 ruling B: that, not
xfs/xfs_mxfs_dlm.c:38490
-			 * sess37 EVICT-RETAIN-PR (RULE-4 stack capture, 32/caw
+			 * sess37 EVICT-RETAIN-PR (instrumented stack capture, 32/caw
xfs/xfs_mxfs_dlm.c:38515
-			 * sess163 (RULE-5 ruling C): retaining a clean wire-PR
+			 * sess163 (design-consult ruling C): retaining a clean wire-PR
xfs/xfs_mxfs_dlm.c:38694
- * sess469 (RULE-5 ruling 2): the deferred reap re-enters xfs_inactive at
+ * sess469 (design-consult ruling 2): the deferred reap re-enters xfs_inactive at
xfs/xfs_mxfs_dlm.c:38757
-	 * sess214 (incident474, RULE-5 b2): runs FIRST, before any
+	 * sess214 (incident474, design-consult b2): runs FIRST, before any
xfs/xfs_mxfs_dlm.c:39404
- * unlink).  Per the RULE-5 ruling, correctness is per-inode OBLIGATIONS, not
+ * unlink).  Per the design-consult ruling, correctness is per-inode OBLIGATIONS, not
xfs/xfs_mxfs_dlm.c:39423
-	 * sess427 (D-0351, RULE-5 ruling ccmemory ccloop-c7ee71c6-sess427-GPT-
+	 * sess427 (D-0351, design-consult ruling ccmemory ccloop-c7ee71c6-sess427-GPT-
xfs/xfs_mxfs_dlm.c:39439
-	 * sess430 (D-0351 chain, RULE-5 ruling ccmemory ccloop-c7ee71c6-sess430-
+	 * sess430 (D-0351 chain, design-consult ruling ccmemory ccloop-c7ee71c6-sess430-
xfs/xfs_mxfs_dlm.c:39457
-	 * sess465 (D-0524, RULE-5 ruling ccmemory ccloop-c7ee71c6-sess465-GPT-
+	 * sess465 (D-0524, design-consult ruling ccmemory ccloop-c7ee71c6-sess465-GPT-
xfs/xfs_mxfs_dlm.c:39713
-		/* RULE-5: epoch 0 while freeing = a protocol failure, not an
+		/* design-consult: epoch 0 while freeing = a protocol failure, not an
xfs/xfs_mxfs_dlm.c:39981
- * sess429 (0.38.4, RULE-5 ruling ccloop-c7ee71c6-sess429-GPT-ruling-home-free-
+ * sess429 (0.38.4, design-consult ruling ccloop-c7ee71c6-sess429-GPT-ruling-home-free-
xfs/xfs_mxfs_dlm.c:40061
-	 * sess465 (D-0524, RULE-5 ruling S0-1): every decision here is taken
+	 * sess465 (D-0524, design-consult ruling S0-1): every decision here is taken
xfs/xfs_mxfs_dlm.c:40812
- * sess391 handoff latch (RULE-5 ruling ccloop-c7ee71c6-sess391-GPT-ruling-
+ * sess391 handoff latch (design-consult ruling ccloop-c7ee71c6-sess391-GPT-ruling-
xfs/xfs_mxfs_dlm.c:40916
- * sess431 (D-0351, RULE-5 ruling "mint an explicit per-slot FREE-publication
+ * sess431 (D-0351, design-consult ruling "mint an explicit per-slot FREE-publication
xfs/xfs_mxfs_dlm.c:41059
-/* v0.5.4 (sess23 ccloop 14d31183): default flipped 0 -> 1.  RULE 4
+/* v0.5.4 (sess23 ccloop 14d31183): default flipped 0 -> 1.  Instrumented
xfs/xfs_mxfs_dlm.c:41141
- * sess120 (ccloop 4eef1f39, Gemini RULE-5 design — VERIFIED against kernel
+ * sess120 (ccloop 4eef1f39, Gemini design-consult design — VERIFIED against kernel
xfs/xfs_mxfs_dlm.c:41209
-		 * ccloop-4dd7 sess4 (b62r4 ROOT, RULE 4 PROVEN by
+		 * ccloop-4dd7 sess4 (b62r4 ROOT, PROVEN by
xfs/xfs_mxfs_dlm.c:41254
-	 * sess35 run14d (RULE 4, PROVEN via P35A): the former payload-LSN
+	 * sess35 run14d (instrumented, PROVEN via P35A): the former payload-LSN
xfs/xfs_mxfs_dlm.c:41338
- * sess133: dir data-fork BMBT read coherency (RULE 4, PROVEN family:
+ * sess133: dir data-fork BMBT read coherency (instrumented, PROVEN family:
xfs/xfs_mxfs_dlm.c:41465
-		 * sess123 (Gemini RULE-5) — DLM-TENURE AUTHORITY, the structural
+		 * sess123 (Gemini design-consult) — DLM-TENURE AUTHORITY, the structural
xfs/xfs_mxfs_dlm.c:41524
-			 * sess103 REVERT of sess102 (RULE 4 step 2b — PROVEN by the
+			 * sess103 REVERT of sess102 (instrument step 2b — PROVEN by the
xfs/xfs_mxfs_dlm.c:41611
-			 * sess117 FIX (Gemini RULE-5 ROOT): the "in-AIL AG-meta
+			 * sess117 FIX (Gemini design-consult ROOT): the "in-AIL AG-meta
xfs/xfs_mxfs_dlm.c:41696
-			 * GPT RULE-5 layer 2): a PRIOR-tenure AG-meta buffer that is
+			 * Design-consult layer 2): a PRIOR-tenure AG-meta buffer that is
xfs/xfs_mxfs_dlm.c:41989
-		 * P35D (RULE 4, sess35 run14d): localize the divergence.  Set
+		 * P35D (instrumented, sess35 run14d): localize the divergence.  Set
xfs/xfs_mxfs_dlm.c:42014
- * sess79 RULE-4 DIRECTION PROBE.  For a bnobt/cntbt buffer we are about to
+ * sess79 instrumented DIRECTION PROBE.  For a bnobt/cntbt buffer we are about to
xfs/xfs_mxfs_dlm.c:42062
- * sess69 RULE-4 DECISIVE PROBE (splits Theory D vs Theory B).
+ * sess69 instrumented DECISIVE PROBE (splits Theory D vs Theory B).
xfs/xfs_mxfs_dlm.c:42270
-	 * sess10 (ccloop c7ee71c6) RULE-0/RULE-4 FIX — dlm_scaling@32/tcp
+	 * sess10 (ccloop c7ee71c6) the budget rule/instrumented FIX — dlm_scaling@32/tcp
xfs/xfs_mxfs_dlm.c:42381
- * sess24 (ccloop 4eef1f39) RULE-4 INODE-DOUBLE-ALLOC DETECTOR.  Read the
+ * sess24 (ccloop 4eef1f39) instrumented INODE-DOUBLE-ALLOC DETECTOR.  Read the
xfs/xfs_mxfs_dlm.c:42412
- * D-0947 / D-0948 (RULE 4 instrumentation, sess573).
+ * D-0947 / D-0948 (instrumentation, sess573).
xfs/xfs_mxfs_dlm.c:42981
-	/* ccloop 12e0d157 sess4: RULE-4 A/B gate on the per-modify sync flush.
+	/* ccloop 12e0d157 sess4: instrumented A/B gate on the per-modify sync flush.
xfs/xfs_mxfs_dlm.c:43480
-		 * sess432 (D-0353, RULE-5 ruling): a SINGLE_NODE-provenance
+		 * sess432 (D-0353, design-consult ruling): a SINGLE_NODE-provenance
xfs/xfs_mxfs_dlm.c:43506
-		 * sess391 (RULE-5 ruling ccloop-c7ee71c6-sess391-GPT-ruling-
+		 * sess391 (design-consult ruling ccloop-c7ee71c6-sess391-GPT-ruling-
xfs/xfs_mxfs_dlm.c:43915
-		 * P1-AGWAIT attribution (RULE 4): a BLOCKING fresh AG acquire on
+		 * P1-AGWAIT attribution (instrumented): a BLOCKING fresh AG acquire on
xfs/xfs_mxfs_dlm.c:43972
-			 * sess5 (ccloop a16ec5f2) ABBA ROOT FIX (RULE 4 —
+			 * sess5 (ccloop a16ec5f2) ABBA ROOT FIX (instrumented —
xfs/xfs_mxfs_dlm.c:44180
-		 * sess123 (Gemini RULE-5): a genuine fresh CAW grant opens a NEW
+		 * sess123 (Gemini design-consult): a genuine fresh CAW grant opens a NEW
xfs/xfs_mxfs_dlm.c:44275
-		 * sess432 (D-0353, RULE-5 ruling Q2): an open lineage at a
+		 * sess432 (D-0353, design-consult ruling Q2): an open lineage at a
xfs/xfs_mxfs_dlm.c:44338
-		 * sess69 RULE-4 decisive probe: does the medium's bnobt root
+		 * sess69 instrumented decisive probe: does the medium's bnobt root
xfs/xfs_mxfs_dlm.c:44371
- * sess390 (RULE-5 ruling docs/rulings/readopt-close-shape.md):
+ * sess390 (design-consult ruling docs/rulings/readopt-close-shape.md):
xfs/xfs_mxfs_dlm.c:44411
- * sess5 (ccloop a16ec5f2) ABBA EDGE-2 FIX (RULE 4 — stack-PROVEN run36:
+ * sess5 (ccloop a16ec5f2) ABBA EDGE-2 FIX (instrumented — stack-PROVEN run36:
xfs/xfs_mxfs_dlm.c:44660
- * sess385 P85-INODE-DRAIN-SPLIT (RULE 4 instrumentation).
+ * sess385 P85-INODE-DRAIN-SPLIT (instrumentation).
xfs/xfs_mxfs_dlm.c:44686
- * see the sess384 RULE-5 ruling, interpretation "AGI new but dinode old on
+ * see the sess384 design-consult ruling, interpretation "AGI new but dinode old on
xfs/xfs_mxfs_dlm.c:44700
- * sess385 P86-AGI-UNLINKED-PUBLISH (RULE 4).
+ * sess385 P86-AGI-UNLINKED-PUBLISH (instrumented).
xfs/xfs_mxfs_dlm.c:44750
- * complete must never be followed by an on-disk unlock, and the RULE-5 ruling
+ * complete must never be followed by an on-disk unlock, and the design-consult ruling
xfs/xfs_mxfs_dlm.c:44761
- * sess387: aggregate repair budget per AG release, in ms.  Derivation (RULE-5
+ * sess387: aggregate repair budget per AG release, in ms.  Derivation (design-consult
xfs/xfs_mxfs_dlm.c:44799
-	 * sess387 REWRITE (RULE-5 ruling): the old body took the cluster
+	 * sess387 REWRITE (design-consult ruling): the old body took the cluster
xfs/xfs_mxfs_dlm.c:45201
-	 * sess430 (0.39.3, RULE 4 — s436 P-FREEOB-XRELEASE path=bast-inline
+	 * sess430 (0.39.3, instrumented — s436 P-FREEOB-XRELEASE path=bast-inline
xfs/xfs_mxfs_dlm.c:45311
-		 * sess387: one AGGREGATE deadline per audit walk (RULE-5
+		 * sess387: one AGGREGATE deadline per audit walk (design-consult
xfs/xfs_mxfs_dlm.c:45423
-				 * sess465 (D-0524, RULE-5 ruling Q5): a genuine
+				 * sess465 (D-0524, design-consult ruling Q5): a genuine
xfs/xfs_mxfs_dlm.c:46456
- * ccloop 72513a13 sess3 (RULE-4): the P125-AG-DIVERGE assertion in
+ * ccloop 72513a13 sess3 (instrumented): the P125-AG-DIVERGE assertion in
xfs/xfs_mxfs_dlm.c:46766
- * contention never becomes -EIO (sess238 RULE-5 ruling). */
+ * contention never becomes -EIO (sess238 design-consult ruling). */
xfs/xfs_mxfs_dlm.c:46778
-	/* sess236 (sess235 RULE-5 ruling, D-OPEN-PROTECT-DEMOTE-RACE-
+	/* sess236 (sess235 design-consult ruling, D-OPEN-PROTECT-DEMOTE-RACE-
xfs/xfs_mxfs_dlm.c:46962
-		 * contention, not failure.  sess239 (sess238 RULE-5 ruling,
+		 * contention, not failure.  sess239 (sess238 design-consult ruling,
xfs/xfs_mxfs_dlm.c:46963
-		 * RULE-4 proven on 0.11.486: arm=29008 relflush-admit, 5
+		 * proven by instrument on 0.11.486: arm=29008 relflush-admit, 5
xfs/xfs_mxfs_dlm.c:47109
-/* interactive session 2026-07-13 (2nd pass, RULE-4): the need_resched()-gated
+/* interactive session 2026-07-13 (2nd pass, instrumented): the need_resched()-gated
xfs/xfs_mxfs_dlm.c:47140
-	 * ccloop cc87fed3 sess5 (RULE 4 — separate hang, NOT BUG3): P-PRSWEEP-CAP
+	 * ccloop cc87fed3 sess5 (instrumented — separate hang, NOT BUG3): P-PRSWEEP-CAP
xfs/xfs_mxfs_dlm.c:47157
-	 * own RULE-4 pass later; unrelated to BUG3's ihold/igrab fix) — this
+	 * own instrumented pass later; unrelated to BUG3's ihold/igrab fix) — this
xfs/xfs_mxfs_dlm.c:47169
-	 * (RULE 4 — the sweep-pin tripwire proved pr_sweep is NOT the inode
+	 * (instrumented — the sweep-pin tripwire proved pr_sweep is NOT the inode
xfs/xfs_mxfs_dlm.c:47243
-		 * ccloop cc87fed3 sess6: sweep-pin tripwire (RULE 4 direct
+		 * ccloop cc87fed3 sess6: sweep-pin tripwire (instrumented direct
xfs/xfs_mxfs_dlm.c:47309
- * sess42(ccloop) RULE-4 ROOT FIX (default ON): stamp a shared dir-metadata
+ * sess42(ccloop) instrumented ROOT FIX (default ON): stamp a shared dir-metadata
xfs/xfs_mxfs_dlm.c:47605
- * no write is suppressed — RULE-4 instrument phase); 1=enforce (suppress an
+ * no write is suppressed — instrumented instrument phase); 1=enforce (suppress an
xfs/xfs_mxfs_dlm.c:47615
-/* sess2 (ccloop a16ec5f2) RULE-4 block watch — see xfs_buf_submit_bio. */
+/* sess2 (ccloop a16ec5f2) instrumented block watch — see xfs_buf_submit_bio. */
xfs/xfs_mxfs_dlm.c:47645
- * sess3 (ccloop a16ec5f2) RULE-4 inode-lifecycle watch: mxfs.watch_ino=<ino>
+ * sess3 (ccloop a16ec5f2) instrumented inode-lifecycle watch: mxfs.watch_ino=<ino>
xfs/xfs_mxfs_dlm.c:47671
- * sess41: default 0 (OFF).  enforce=2 REFUTED (RULE 4): under the dir_reuse
+ * sess41: default 0 (OFF).  enforce=2 REFUTED (instrumented): under the dir_reuse
xfs/xfs_mxfs_dlm.c:47765
- * (RULE 4, P-WGHOST=0 + P-WMERGE) root of the 8/tcp dir_reuse readdir=799
+ * (instrumented, P-WGHOST=0 + P-WMERGE) root of the 8/tcp dir_reuse readdir=799
xfs/xfs_mxfs_dlm.c:48339
- * write — a RULE-4 diagnostic for the dir_reuse_coherency durable inode-alloc
+ * write — a instrumented diagnostic for the dir_reuse_coherency durable inode-alloc
xfs/xfs_mxfs_dlm.c:48351
- * sess574 RULE-4 INSTRUMENT — does a SOLE SURVIVOR whole-write inode clusters
+ * sess574 instrumented INSTRUMENT — does a SOLE SURVIVOR whole-write inode clusters
xfs/xfs_mxfs_dlm.c:48383
- * sess63 PROVEN (RULE-4 step 2b): default ON.  On the SCST iSCSI target,
+ * sess63 PROVEN (instrument step 2b): default ON.  On the SCST iSCSI target,
xfs/xfs_mxfs_dlm.c:48597
- * as the RULE-5 review predicted.  Re-arming is only half a fix — the skip
+ * as the design-consult review predicted.  Re-arming is only half a fix — the skip
xfs/xfs_mxfs_dlm.c:48720
- * (sess197 RULE-5 ruling, build-order step 1).  The tenure-release invariant
+ * (sess197 design-consult ruling, build-order step 1).  The tenure-release invariant
xfs/xfs_mxfs_dlm.c:48937
-/* sess447 (RULE-5 ruling, 0.54.0): DEFAULT 1.  The shipped default 0 was a
+/* sess447 (design-consult ruling, 0.54.0): DEFAULT 1.  The shipped default 0 was a
xfs/xfs_mxfs_dlm.c:49294
- * at all.  RULE 6 requires testing that EXERCISES THE CAUSE, so the cause needs
+ * at all.  the zero-defect bar requires testing that EXERCISES THE CAUSE, so the cause needs
xfs/xfs_mxfs_dlm.c:49347
- * sess389 FAULT INJECTION (RULE-5 ruling verification item): make xfs_iflush
+ * sess389 FAULT INJECTION (design-consult ruling verification item): make xfs_iflush
xfs/xfs_mxfs_dlm.c:49363
- * (RULE-5 ruling modes 1/2: core-only fossil, core+buffer fossil at an
+ * (design-consult ruling modes 1/2: core-only fossil, core+buffer fossil at an
xfs/xfs_mxfs_dlm.c:49392
-/* sess409: A/B measurement knob ONLY (RULE 4 cost attribution of the
+/* sess409: A/B measurement knob ONLY (instrumented cost attribution of the
xfs/xfs_mxfs_dlm.c:49877
-				 * sess15(a9a03929) FIX-J (RULE-4 ftrace-proven):
+				 * sess15(a9a03929) FIX-J (instrumented ftrace-proven):
xfs/xfs_mxfs_dlm.c:50126
- * -488 third face seam (sess267 RULE-5 ruling): post-roll ILOCK handoff.
+ * -488 third face seam (sess267 design-consult ruling): post-roll ILOCK handoff.
xfs/xfs_mxfs_dlm.c:50459
- * sess286 (D-501, RULE-5 ruled): phase-aware mandatory/optional AG
+ * sess286 (D-501, design-consult ruled): phase-aware mandatory/optional AG
xfs/xfs_mxfs_dlm.c:50611
-	 * sess269 (-488 fourth face, RULE-5 ruled): the acquire pass is
+	 * sess269 (-488 fourth face, design-consult ruled): the acquire pass is
xfs/xfs_mxfs_dlm.c:50732
-	 * ccloop cc87fed3 sess5 (RULE 4 — BUG3 hunt): mxfs_dlm_ilock_end's
+	 * ccloop cc87fed3 sess5 (instrumented — BUG3 hunt): mxfs_dlm_ilock_end's
xfs/xfs_mxfs_dlm.c:50770
-	 * ccloop cc87fed3 sess5 (RULE 4 PROVEN — BUG3 root cause): this can be
+	 * ccloop cc87fed3 sess5 (PROVEN BY INSTRUMENT — BUG3 root cause): this can be
xfs/xfs_mxfs_dlm.c:50821
-		 * sess9 (ccloop 72513a13) SELF-DEADLOCK GUARD — RULE 4 PROVEN
+		 * sess9 (ccloop 72513a13) SELF-DEADLOCK GUARD — PROVEN BY INSTRUMENT
xfs/xfs_mxfs_dlm.c:50964
-		 * ccloop cc87fed3 sess4 (RULE 4 — BUG3 hunt): log i_count/
+		 * ccloop cc87fed3 sess4 (instrumented — BUG3 hunt): log i_count/
xfs/xfs_mxfs_dlm.c:51406
-	 * sess387 RULE-5 ruling (refusal semantics): bounded DEFERRAL
+	 * sess387 design-consult ruling (refusal semantics): bounded DEFERRAL
xfs/xfs_mxfs_dlm.c:51434
-		 * sess465 (D-0524, RULE-5 ruling): a FREE_PENDING entry that is
+		 * sess465 (D-0524, design-consult ruling): a FREE_PENDING entry that is
xfs/xfs_mxfs_dlm.c:51536
-	 * sess391 (RULE-5 ruling ccloop-c7ee71c6-sess391-GPT-ruling-ag-handoff-
+	 * sess391 (design-consult ruling ccloop-c7ee71c6-sess391-GPT-ruling-ag-handoff-
xfs/xfs_mxfs_dlm.c:51898
-				 * sess55 (ccloop) RULE-4 INSTRUMENTATION:
+				 * sess55 (ccloop) INSTRUMENTATION:
xfs/xfs_mxfs_dlm.c:52112
-	 * sess403 (RULE-5 ruling): CLEAN-RELEASE MARKER.  The COMMIT above
+	 * sess403 (design-consult ruling): CLEAN-RELEASE MARKER.  The COMMIT above
xfs/xfs_mxfs_dlm.c:52600
- * sess432 (D-0353, RULE-5 ruling Q2): the fresh-acquire PREFLIGHT.  A dry
+ * sess432 (D-0353, design-consult ruling Q2): the fresh-acquire PREFLIGHT.  A dry
xfs/xfs_mxfs_dlm.c:53070
-			 * sess432 (D-0353, RULE-5 ruling Q2): the locked branch
+			 * sess432 (D-0353, design-consult ruling Q2): the locked branch
xfs/xfs_mxfs_dlm.c:53323
- * sess60 (RULE 4, zero_silent_loss residual) — the bmbt analogue of
+ * sess60 (instrumented, zero_silent_loss residual) — the bmbt analogue of
xfs/xfs_mxfs_dlm.c:53467
-		 * sess4 (ccloop 46efd8b6) LAUNDERING DETECTOR (RULE 4).  Run
+		 * sess4 (ccloop 46efd8b6) LAUNDERING DETECTOR (instrumented).  Run
xfs/xfs_mxfs_dlm.c:53802
-		 * sess40 (ccloop 8ddb16a2, RULE 4 PROVEN by iter3 P29-DATAWRITE
+		 * sess40 (ccloop 8ddb16a2, PROVEN by iter3 P29-DATAWRITE
xfs/xfs_mxfs_dlm.c:53877
-			 * The loss write (PROVEN sess25/32/33, RULE 4) is a
+			 * The loss write (PROVEN sess25/32/33, instrumented) is a
xfs/xfs_mxfs_dlm.c:53921
-			 * sess17 (ccloop, RULE 4): the tenure-mismatch arm is
+			 * sess17 (ccloop, instrumented): the tenure-mismatch arm is
xfs/xfs_mxfs_dlm.c:53936
-		 * sess50 (ccloop) PROVEN cross-node clobber gate (RULE-4: xnode=1
+		 * sess50 (ccloop) PROVEN cross-node clobber gate (instrumented: xnode=1
xfs/xfs_mxfs_dlm.c:54085
-		* sess63 (RULE 4): FUA-pierce BTREE-dir extent-map (bmbt) leaf
+		* sess63 (instrumented): FUA-pierce BTREE-dir extent-map (bmbt) leaf
xfs/xfs_mxfs_dlm.c:54365
-	 * Prove the reclaim fires (RULE 4).  Each line is one AG-meta track
+	 * Prove the reclaim fires (instrumented).  Each line is one AG-meta track
xfs/xfs_mxfs_dlm.c:54963
-		 * sess469 (D-ORPHAN-SCAN-IGET-BUCKETED-ZOMBIE-…-0525, RULE-5
+		 * sess469 (D-ORPHAN-SCAN-IGET-BUCKETED-ZOMBIE-…-0525, design-consult
xfs/xfs_mxfs_dlm.c:55112
- * sess447 (D-TWO-VICTIM-DEATH-SECOND-SLICE-REPLAY-NOT-STARTED-0514) RULE 4
+ * sess447 (D-TWO-VICTIM-DEATH-SECOND-SLICE-REPLAY-NOT-STARTED-0514) instrumented
xfs/xfs_mxfs_dlm.c:55488
- * RULE-5 ruling): fold a terminal recovery-refusal domain into this
+ * Design-consult ruling): fold a terminal recovery-refusal domain into this
xfs/xfs_mxfs_dlm.c:55626
-	 * sess383 (RULE-5 ruling Q5.2): "nonzero" is not a valid AG mask.  A
+	 * sess383 (design-consult ruling Q5.2): "nonzero" is not a valid AG mask.  A
xfs/xfs_mxfs_dlm.c:55706
-	 * sess383 (RULE-5 ruling Q1): this used to import the record with NO
+	 * sess383 (design-consult ruling Q1): this used to import the record with NO
xfs/xfs_mxfs_dlm.c:55721
- * sess330 (RULE-5 ruling): the SHARED terminal classifier — the one
+ * sess330 (design-consult ruling): the SHARED terminal classifier — the one
xfs/xfs_mxfs_dlm.c:56167
-			 * The sess374 RULE-5 review argued for holding the
+			 * The sess374 design-consult review argued for holding the
xfs/xfs_mxfs_dlm.c:56215
-		 * sess335 (RULE-5 review): the local lease is released HERE,
+		 * sess335 (design-consult review): the local lease is released HERE,
xfs/xfs_mxfs_dlm.c:56308
-	 * sess214 (incident474, RULE-5 b2/b3): this whole function runs as
+	 * sess214 (incident474, design-consult b2/b3): this whole function runs as
xfs/xfs_mxfs_dlm.c:56377
-		 * after which an unproven slice becomes replayable (GPT RULE-5
+		 * after which an unproven slice becomes replayable (design-consult
xfs/xfs_mxfs_dlm.c:56381
-		 * sess330 (RULE-5 ruling): classify BEFORE acquire.  No lease
+		 * sess330 (design-consult ruling): classify BEFORE acquire.  No lease
xfs/xfs_mxfs_dlm.c:56400
-		 * sess407 (RULE-5 ruling Q5, D-FSWIDE-TERMINAL-REPLAY-
+		 * sess407 (design-consult ruling Q5, D-FSWIDE-TERMINAL-REPLAY-
xfs/xfs_mxfs_dlm.c:56660
-		 * sess214 (incident474, RULE-5 b2): but NOT here, between
+		 * sess214 (incident474, design-consult b2): but NOT here, between
xfs/xfs_mxfs_dlm.c:56836
- * GPT (RULE-5 ruling sess93, Q2): "Add an explicit durable and observable
+ * GPT (design-consult ruling sess93, Q2): "Add an explicit durable and observable
xfs/xfs_mxfs_dlm.c:56937
- * The population measurement the sess96 RULE-5 ruling asked for, and which
+ * The population measurement the sess96 design-consult ruling asked for, and which
xfs/xfs_mxfs_dlm.c:57101
- * selftest, the D-EX-GRANT-EPOCH verification vehicle (RULE-5 approved
+ * selftest, the D-EX-GRANT-EPOCH verification vehicle (design-consult approved
xfs/xfs_mxfs_dlm.c:57466
-	 * sess383 (RULE-5 ruling Q3): open the admission transaction BEFORE
+	 * sess383 (design-consult ruling Q3): open the admission transaction BEFORE
xfs/xfs_mxfs_dlm.c:57522
- * sess383 (RULE-5 ruling Q3) — THE ADMISSION TRANSITION.
+ * sess383 (design-consult ruling Q3) — THE ADMISSION TRANSITION.
xfs/xfs_mxfs_dlm.c:57592
- * deadlock (sess55, GPT RULE-5 ruling).  xfs_mountfs itself takes blocking
+ * deadlock (sess55, design-consult ruling).  xfs_mountfs itself takes blocking
xfs/xfs_mxfs_dlm.c:57644
- * sess190 (sess189 GPT ruling) — the admission gate's wait bound.  RULE-0
+ * sess190 (sess189 GPT ruling) — the admission gate's wait bound.  The budget rule
xfs/xfs_mxfs_dlm.c:57657
- * sess421 (sess420 RULE-5 barrier ruling, item 4 — window arm): one-shot
+ * sess421 (sess420 design-consult barrier ruling, item 4 — window arm): one-shot
xfs/xfs_mxfs_dlm.c:57672
- * sess330 (D-513 RULE-5 ruling): the admission barrier's terminal
+ * sess330 (D-513 design-consult ruling): the admission barrier's terminal
xfs/xfs_mxfs_dlm.c:57785
-	 * sess383 (RULE-5 ruling Q3, the structural half) — TERMINAL-GUARD
+	 * sess383 (design-consult ruling Q3, the structural half) — TERMINAL-GUARD
xfs/xfs_mxfs_dlm.c:58226
-			 * sess330 (RULE-5 ruling): classify BEFORE acquire —
+			 * sess330 (design-consult ruling): classify BEFORE acquire —
xfs/xfs_mxfs_dlm.c:58418
-							/* sess442 (RULE-5 review): a
+							/* sess442 (design-consult review): a
xfs/xfs_mxfs_dlm.c:58892
-	 * sess448 (RULE-5 ruling ccloop-c7ee71c6-sess448-GPT-ruling-iclus-
+	 * sess448 (design-consult ruling ccloop-c7ee71c6-sess448-GPT-ruling-iclus-
xfs/xfs_mxfs_dlm.c:59530
-	 * sess448 (RULE-5 ruling): the CLUSTER clean-release certificate.
+	 * sess448 (design-consult ruling): the CLUSTER clean-release certificate.
xfs/xfs_mxfs_dlm.c:59766
-	/* sess6 RULE-4 (phantom self-hold hunt): realns-stamped claim record
+	/* sess6 instrumented (phantom self-hold hunt): realns-stamped claim record
xfs/xfs_mxfs_dlm.c:59811
-		 * sess5's 8-board (RULE 4): the caller's P109 single-inode
+		 * sess5's 8-board (instrumented): the caller's P109 single-inode
xfs/xfs_mxfs_dlm.c:60145
-		/* sess7 (ccloop 72513a13, GPT RULE-5): HONEST rc — a peer is
+		/* sess7 (ccloop 72513a13, design-consult): HONEST rc — a peer is
xfs/xfs_relmark_item.h:3
- * MXFS clean-release marker log item (sess403, RULE-5 ruling
+ * MXFS clean-release marker log item (sess403, design-consult ruling
xfs/xfs_relmark_item.c:4
- * untrusted-replay pass-1 table (sess403 RULE-5 ruling, see xfs_relmark_item.h).
+ * untrusted-replay pass-1 table (sess403 design-consult ruling, see xfs_relmark_item.h).
xfs/xfs_relmark_item.c:93
- * ORDERING CONTRACT (RULE-5 ruling): the caller has completed the
+ * ORDERING CONTRACT (design-consult ruling): the caller has completed the
xfs/xfs_bmap_util.c:563
-	 * sess37 (RULE-4 stack capture, 32/caw dir_reuse): this peek reads
+	 * sess37 (instrumented stack capture, 32/caw dir_reuse): this peek reads
xfs/xfs_inode.c:241
-		 * sess1 (ccloop 46efd8b6) iread-PR (RULE-4 PROVEN root of the
+		 * sess1 (ccloop 46efd8b6) iread-PR (PROVEN BY INSTRUMENT root of the
xfs/xfs_inode.c:347
-/* sess73 (RULE 4): forensic dump for a wedged ip->i_lock acquire — names the
+/* sess73 (instrumented): forensic dump for a wedged ip->i_lock acquire — names the
xfs/xfs_inode.c:376
-	 * ccloop-4dd7 sess4 (b61r6 holder-stack proof, RULE 4): the IOLOCK
+	 * ccloop-4dd7 sess4 (b61r6 holder-stack proof, instrumented): the IOLOCK
xfs/xfs_inode.c:491
-/* sess132 (RULE 4): ILOCK last-locker forensics — the 16-node create-storm
+/* sess132 (instrumented): ILOCK last-locker forensics — the 16-node create-storm
xfs/xfs_inode.c:986
-		 * sess58 (ccloop 8ddb16a2) RULE-4 PROVEN FIX (xfs_rename / comm=mv
+		 * sess58 (ccloop 8ddb16a2) PROVEN BY INSTRUMENT FIX (xfs_rename / comm=mv
xfs/xfs_inode.c:1097
-		 * ccloop-4dd7 sess2 ABBA breaker (RULE-4 PROVEN, 184s cycle):
+		 * ccloop-4dd7 sess2 ABBA breaker (PROVEN BY INSTRUMENT, 184s cycle):
xfs/xfs_inode.c:1162
-		 * sess58 (ccloop 8ddb16a2) RULE-4 PROVEN FIX for the tcp_dlm_scaling
+		 * sess58 (ccloop 8ddb16a2) PROVEN BY INSTRUMENT FIX for the tcp_dlm_scaling
xfs/xfs_inode.c:1345
-	 * sess127 (RULE 4, ungated, miss-only): the unlink_visibility loser's
+	 * sess127 (instrumented, ungated, miss-only): the unlink_visibility loser's
xfs/xfs_inode.c:1629
-		 * sess566 (D-0941) ROOT FIX, RULE 4 step 2b -- THE RETIRE LOOP
+		 * sess566 (D-0941) ROOT FIX, instrument step 2b -- THE RETIRE LOOP
xfs/xfs_inode.c:1692
-			 * ccloop c7ee71c6 sess318 (RULE-4 proven, RULE-5 ruling):
+			 * ccloop c7ee71c6 sess318 (proven by instrument, design-consult ruling):
xfs/xfs_inode.c:1709
-			 * sess566 (D-0941, RULE 4 step 2): i_count is a
+			 * sess566 (D-0941, instrument step 2): i_count is a
xfs/xfs_inode.c:1741
-			 * sess426 (D-0346, RULE 4 step 2): name what pins the
+			 * sess426 (D-0346, instrument step 2): name what pins the
xfs/xfs_inode.c:1770
-		 * sess318 FAIL CLOSED (RULE-5: "a retry budget must never become
+		 * sess318 FAIL CLOSED (design-consult: "a retry budget must never become
xfs/xfs_inode.c:1778
-		 * sess566 (D-0941, RULE 4 step 2): DOES THE REFERENCE EVER
+		 * sess566 (D-0941, instrument step 2): DOES THE REFERENCE EVER
xfs/xfs_inode.c:1995
-			 * sess95 FIX (Gemini-confirmed, RULE 4 step 2b): SAME-TYPE
+			 * sess95 FIX (Gemini-confirmed, instrument step 2b): SAME-TYPE
xfs/xfs_inode.c:2154
-	 * sess95 FIX2 (Gemini-confirmed, RULE 4 step 2b): TYPE-FLIP reuse where
+	 * sess95 FIX2 (Gemini-confirmed, instrument step 2b): TYPE-FLIP reuse where
xfs/xfs_inode.c:2194
-		 * ENOTDIR window (RULE 4, two live captures: 233839Z test16
+		 * ENOTDIR window (instrumented, two live captures: 233839Z test16
xfs/xfs_inode.c:2227
-		 * unresolved outcome is not reproducible on demand, and RULE 6
+		 * unresolved outcome is not reproducible on demand, and the zero-defect bar
xfs/xfs_inode.c:2641
-	/* sess132 (RULE 4): per-mkdir phase decomposition for the 16-node
+	/* sess132 (instrumented): per-mkdir phase decomposition for the 16-node
xfs/xfs_inode.c:2847
-	 * sess11(a9a03929) RULE-4 PROVEN cross-node ABBA (fence suite-3
+	 * sess11(a9a03929) PROVEN BY INSTRUMENT cross-node ABBA (fence suite-3
xfs/xfs_inode.c:2897
-	/* sess-tcp (RULE 4): split dialloc vs icreate as the err=1/ino=0 source
+	/* sess-tcp (instrumented): split dialloc vs icreate as the err=1/ino=0 source
xfs/xfs_inode.c:2983
-		 * sess62's RULE-0 perf objection to the unconditional call.
+		 * sess62's the budget rule perf objection to the unconditional call.
xfs/xfs_inode.c:2992
-		/* sess105 (RULE 4): PROVE the directory-inode divergence.  The
+		/* sess105 (instrumented): PROVE the directory-inode divergence.  The
xfs/xfs_inode.c:3063
-			/* sess106 (RULE 4): PROVE the directory-inode divergence.
+			/* sess106 (instrumented): PROVE the directory-inode divergence.
xfs/xfs_inode.c:3085
-				 * sess127 (RULE 4 PROVEN — corrects sess126): do NOT set
+				 * sess127 (PROVEN BY INSTRUMENT — corrects sess126): do NOT set
xfs/xfs_inode.c:3369
-		/* P24 RULE-4 probe (capped): publish state at queue time. */
+		/* P24 instrumented probe (capped): publish state at queue time. */
xfs/xfs_inode.c:3492
-	 * sess115 (ccloop 4eef1f39) — DISABLED.  RULE 4: the P112-IFLUSH-CALLER
+	 * sess115 (ccloop 4eef1f39) — DISABLED.  Instrumented: the P112-IFLUSH-CALLER
xfs/xfs_inode.c:3517
-	/* v0.5.4 (sess23 ccloop 14d31183, RULE 4 ftrace-proven): the per-mkdir
+	/* v0.5.4 (sess23 ccloop 14d31183, instrumented ftrace-proven): the per-mkdir
xfs/xfs_inode.c:3528
-	 * sess49 (ccloop 14d31183, RULE 4 PROVEN — 16-node cross_write_read /
+	 * sess49 (ccloop 14d31183, PROVEN BY INSTRUMENT — 16-node cross_write_read /
xfs/xfs_inode.c:4444
-		 * sess469 (D-0525, RULE-5 ruling): an UNSTAMPED member's
+		 * sess469 (D-0525, design-consult ruling): an UNSTAMPED member's
xfs/xfs_inode.c:4710
-				 * sess401 (D-TMPFILE-CHURN-RULE0-PERF-400, RULE-5
+				 * sess401 (D-TMPFILE-CHURN-the budget rule-PERF-400, design-consult
xfs/xfs_inode.c:5036
-	 * sess129 (RULE 4 PROVEN): this MUST be a targeted per-inode drain,
+	 * sess129 (PROVEN BY INSTRUMENT): this MUST be a targeted per-inode drain,
xfs/xfs_inode.c:5053
-	 * ccloop c7ee71c6 sess2 (RULE 4, P137/P133/ftrace-attributed): this
+	 * ccloop c7ee71c6 sess2 (instrumented, P137/P133/ftrace-attributed): this
xfs/xfs_inode.c:5351
-		 * sess78 TOCTOU CLOSURE (Gemini RULE-5 design; see
+		 * sess78 TOCTOU CLOSURE (Gemini design-consult design; see
xfs/xfs_inode.c:5566
-				 * sess469 (RULE-5 review of fix A, STOP-SHIP 1):
+				 * sess469 (design-consult review of fix A, STOP-SHIP 1):
xfs/xfs_inode.c:5600
-					/* RULE-5 sess469 ruling 2: a refusal
+					/* design-consult sess469 ruling 2: a refusal
xfs/xfs_inode.c:5617
-			 * sess467 (RULE-5 ruling, D-FOREIGN-SLICE-INTENTS-
+			 * sess467 (design-consult ruling, D-FOREIGN-SLICE-INTENTS-
xfs/xfs_inode.c:5646
-			 * sess6 (ccloop 72513a13) RULE-4 PROVEN root of the
+			 * sess6 (ccloop 72513a13) PROVEN BY INSTRUMENT root of the
xfs/xfs_inode.c:5679
-			 * sess10 (ccloop c7ee71c6) RULE-0 FIX — dlm_scaling@32
+			 * sess10 (ccloop c7ee71c6) the budget rule FIX — dlm_scaling@32
xfs/xfs_inode.c:5708
-				 * sess47 finding-B discriminator (RULE 4):
+				 * sess47 finding-B discriminator (instrumented):
xfs/xfs_inode.c:5792
-		 * B3 (sess19 ccloop, Gemini RULE-5): the killer case B1/B2 both
+		 * B3 (sess19 ccloop, Gemini design-consult): the killer case B1/B2 both
xfs/xfs_inode.c:5820
-		 * ccloop cc87fed3 sess4 (RULE 4 — BUG3 hunt, fence_during_write@
+		 * ccloop cc87fed3 sess4 (instrumented — BUG3 hunt, fence_during_write@
xfs/xfs_inode.c:5973
-		 * sess19 (ccloop 4eef1f39) RULE-4 instrumentation: log the B3
+		 * sess19 (ccloop 4eef1f39) instrumentation: log the B3
xfs/xfs_inode.c:6005
-		 * ccloop-4dd7 OWN-FREE BYPASS (RULE-4 proven, ino 680 autopsy):
+		 * ccloop-4dd7 OWN-FREE BYPASS (proven by instrument, ino 680 autopsy):
xfs/xfs_inode.c:6075
-			 * ccloop-4dd7 UNLEAK (RULE-4 proven: the P2L-INACT-LEAK
+			 * ccloop-4dd7 UNLEAK (proven by instrument: the P2L-INACT-LEAK
xfs/xfs_inode.c:6352
-		 * ccloop c7ee71c6 sess3 FIX-1 (RULE 4, PROVEN via
+		 * ccloop c7ee71c6 sess3 FIX-1 (instrumented, PROVEN via
xfs/xfs_inode.c:6426
-		 * Outcomes (RULE-5 review STOP-SHIP 3): REVOKED is the only
+		 * Outcomes (design-consult review STOP-SHIP 3): REVOKED is the only
xfs/xfs_inode.c:6459
-				/* RULE-5 sess469 re-review STOP-SHIP 1: the
+				/* design-consult sess469 re-review STOP-SHIP 1: the
xfs/xfs_inode.c:6505
-		 * sess128 RULE-4 probe: the ON-DISK slot for this ino is now
+		 * sess128 instrumented probe: the ON-DISK slot for this ino is now
xfs/xfs_inode.c:6644
-		 * RULE-5 ruling (ccmemory
+		 * Design-consult ruling (ccmemory
xfs/xfs_inode.c:7077
-	/* P133 (ccloop c7ee71c6 sess2, RULE 4): stage timing for the tcp
+	/* P133 (ccloop c7ee71c6 sess2, instrumented): stage timing for the tcp
xfs/xfs_inode.c:7140
-	 * sess13 FIX-C (RULE-4 PROVEN, ladder-r16 P12-HOLDERTASK/P36-STACK):
+	 * sess13 FIX-C (PROVEN BY INSTRUMENT, ladder-r16 P12-HOLDERTASK/P36-STACK):
xfs/xfs_inode.c:7168
-			 * sess282 (RULE-5 ruled, shape B): -EAGAIN =
+			 * sess282 (design-consult ruled, shape B): -EAGAIN =
xfs/xfs_inode.c:7282
-	 * PROVEN root (sess96, RULE 4): under 4-node concurrent rename/unlink of
+	 * PROVEN root (sess96, instrumented): under 4-node concurrent rename/unlink of
xfs/xfs_inode.c:7384
-	/* sess4(a16ec5f2) UNLINK ledger (RULE 4): run22's dangling-dirent
+	/* sess4(a16ec5f2) UNLINK ledger (instrumented): run22's dangling-dirent
xfs/xfs_inode.c:7747
-	 * sess286 (D-501, RULE-5 ruled): only the AGs this rename WILL
+	 * sess286 (D-501, design-consult ruled): only the AGs this rename WILL
xfs/xfs_inode.c:7777
-		 * sess282 (RULE-5 ruled, shape B): the retry is UNBOUNDED —
+		 * sess282 (design-consult ruled, shape B): the retry is UNBOUNDED —
xfs/xfs_inode.c:7887
-		 * RULE-5 ruling) — FULL target-expectation preflight for the
+		 * Design-consult ruling) — FULL target-expectation preflight for the
xfs/xfs_inode.c:8520
-	 * sess102 RULE-4 DETECTOR (P-IRESURRECT): the bnobt double-free
+	 * sess102 INSTRUMENTED DETECTOR (P-IRESURRECT): the bnobt double-free
xfs/xfs_inode.c:8575
-		 * P20-IFLUSH-FORENSIC (RULE 4): a bad-magic cluster buffer at
+		 * P20-IFLUSH-FORENSIC (instrumented): a bad-magic cluster buffer at
xfs/xfs_inode.c:8671
-		 * sess22 (ccloop 8ddb16a2) TORN-SHORTFORM-DIR FLUSH BARRIER (RULE 4,
+		 * sess22 (ccloop 8ddb16a2) TORN-SHORTFORM-DIR FLUSH BARRIER (instrumented,
xfs/xfs_inode.c:8739
-	 * sess14(a9a03929) SF-DIR SIZE-DESYNC TRIPWIRE (RULE 4 instrument +
+	 * sess14(a9a03929) SF-DIR SIZE-DESYNC TRIPWIRE (instrumented instrument +
xfs/xfs_inode.c:8778
-	 * sess118 RULE-4 GUARD (the sess102 P-IRESURRECT detector, now acting):
+	 * sess118 instrumented GUARD (the sess102 P-IRESURRECT detector, now acting):
xfs/xfs_inode.c:8810
-		 * sess55 (ccloop 8ddb16a2; GPT-5.5 RULE-5 design #4) — CO-RESIDENT
+		 * sess55 (ccloop 8ddb16a2; GPT-5.5 design-consult design #4) — CO-RESIDENT
xfs/xfs_inode.c:8813
-		 * pending legit write" (invariant #1 drains before release).  RULE-4
+		 * pending legit write" (invariant #1 drains before release).  Instrumented
xfs/xfs_inode.c:8850
-		 * sess389 (D-FOSSIL-NEXT-UNLINKED-IGET-LOGSAME-388 root; RULE-5
+		 * sess389 (D-FOSSIL-NEXT-UNLINKED-IGET-LOGSAME-388 root; design-consult
xfs/xfs_inode.c:8949
-		 * sess119 (Gemini RULE-5 validated) — DLM-OWNERSHIP DISCRIMINATOR.
+		 * sess119 (Gemini design-consult validated) — DLM-OWNERSHIP DISCRIMINATOR.
xfs/xfs_inode.c:8980
-	 * ccloop c7ee71c6 sess3 (RULE 4, PROVEN via P-SFDIR-REVERT ino=152
+	 * ccloop c7ee71c6 sess3 (instrumented, PROVEN via P-SFDIR-REVERT ino=152
xfs/xfs_inode.c:8998
-	 * sess427 (D-0351, FREE-PUBLISH invariant; RULE-5 ruling ccmemory
+	 * sess427 (D-0351, FREE-PUBLISH invariant; design-consult ruling ccmemory
xfs/xfs_inode.c:9046
-		 * that is NOT a sufficient detector (RULE-5 review, 0.39.9):
+		 * that is NOT a sufficient detector (design-consult review, 0.39.9):
xfs/xfs_inode.c:9120
-				 * sess430 (D-0351 chain, RULE-5 ruling): a CHAINED
+				 * sess430 (D-0351 chain, design-consult ruling): a CHAINED
xfs/xfs_inode.c:9200
-		/* sess54 RULE-4: one-shot stack dump for a DIR skip so we can name the
+		/* sess54 instrumented: one-shot stack dump for a DIR skip so we can name the
xfs/xfs_inode.c:9263
-	 * sess25 (ccloop) RESURRECTION GUARD (RULE 4, PROVEN dir_reuse_coherency
+	 * sess25 (ccloop) RESURRECTION GUARD (instrumented, PROVEN dir_reuse_coherency
xfs/xfs_inode.c:9322
-	 * sess74 (ccloop 14d31183) WRITE-BOUNDARY TORN-DINODE BARRIER (RULE 4 +
+	 * sess74 (ccloop 14d31183) WRITE-BOUNDARY TORN-DINODE BARRIER (instrumented +
xfs/xfs_inode.c:9373
-	 * (RULE 4; sess77 PROVEN durable on-disk root).  xfs_inode_to_disk()
+	 * (instrumented; sess77 PROVEN durable on-disk root).  xfs_inode_to_disk()
xfs/xfs_inode.c:9442
-	 * sess32 (ccloop 8ddb16a2) RULE-4 (a)-discriminator (GPT-5.5 2nd consult):
+	 * sess32 (ccloop 8ddb16a2) instrumented (a)-discriminator (GPT-5.5 2nd consult):
xfs/xfs_inode.c:9473
-	 * sess16(ccloop) P32F-NXSHRINK-FENCE (RULE 4 ENFORCEMENT of the P32 probe
+	 * sess16(ccloop) P32F-NXSHRINK-FENCE (instrumented ENFORCEMENT of the P32 probe
xfs/xfs_inode.c:9625
-			 * sess382 ROOT FIX (RULE 4, proven on test31 ino
+			 * sess382 ROOT FIX (instrumented, proven on test31 ino
xfs/xfs_inode.c:9758
-	 * sess67 (RULE 4 ENFORCEMENT — the "next step" the prior P-DIRIFLUSH
+	 * sess67 (instrumented ENFORCEMENT — the "next step" the prior P-DIRIFLUSH
xfs/xfs_inode.c:9864
-	 * <ccloop sess49b> DATA-REGION GAP FENCE (RULE 4, PROVEN DISK-TORN root):
+	 * <ccloop sess49b> DATA-REGION GAP FENCE (instrumented, PROVEN DISK-TORN root):
xfs/xfs_inode.c:10385
-	 * shut down healthy nodes).  Classify before enforcing (RULE 4). */
+	 * shut down healthy nodes).  Classify before enforcing (instrumented). */
xfs/xfs_inode.c:10388
-		/* sess35 CLASS DISCRIMINATORS (RULE 4, classifier round 2):
+		/* sess35 CLASS DISCRIMINATORS (instrumented, classifier round 2):
xfs/xfs_inode.c:10422
-	 * sess34 REFINEMENT (RULE 4 — the unconditional first cut was a
+	 * sess34 REFINEMENT (instrumented — the unconditional first cut was a
xfs/xfs_inode.c:10527
-	 * sess113 (Gemini RULE-5, PROVEN ROOT): this merge does a SYNCHRONOUS
+	 * sess113 (Gemini design-consult, PROVEN ROOT): this merge does a SYNCHRONOUS
xfs/xfs_inode.c:10643
-		 * sess431 (D-0351, RULE-5 ruling): a staged FREE image carries an
+		 * sess431 (D-0351, design-consult ruling): a staged FREE image carries an
xfs/xfs_inode.c:10700
-			 * ccloop c7ee71c6 sess3 (RULE 4, PROVEN via
+			 * ccloop c7ee71c6 sess3 (instrumented, PROVEN via
xfs/xfs_inode.c:10832
-			 * ccloop c7ee71c6 sess3 (RULE 4 PROVEN — see the
+			 * ccloop c7ee71c6 sess3 (PROVEN BY INSTRUMENT — see the
xfs/xfs_inode.c:10979
-	 * sess48 install site 5 — PROVEN in-core fossil reverter (RULE 4):
+	 * sess48 install site 5 — PROVEN in-core fossil reverter (instrumented):
xfs/xfs_inode.c:11254
- * sess389 (RULE-5 ruling F2(b)): repair writer for a LAUNDERED publication
+ * sess389 (design-consult ruling F2(b)): repair writer for a LAUNDERED publication
xfs/xfs_inode.c:11328
- * potentially-blocking stage is deadline-aware (RULE-5 ruling: a blocking
+ * potentially-blocking stage is deadline-aware (design-consult ruling: a blocking
xfs/xfs_inode.c:11440
-		 * sess389 (RULE-5 ruling F2): a CLEAN item with the obligation
+		 * sess389 (design-consult ruling F2): a CLEAN item with the obligation
xfs/xfs_buf.h:203
- * sess-pve (AGI umount-wedge, RULE-4 instrumentation): per-buffer HOLD/RELE
+ * sess-pve (AGI umount-wedge, instrumentation): per-buffer HOLD/RELE
xfs/xfs_buf.h:279
-	uint32_t		b_mxfs_coherent_gen;	/* sess28: dp->i_dlm_dir_gen at last PLATTER-VERIFIED addname coherence check (mxfs_dir_addname_coherent_refresh). Distinct from b_mxfs_dir_gen (stamped on EVERY read, incl stale reads) — only the addname guard sets this, so it dedups the per-addname FUA platter check to ONCE per block per dir_gen (cheap, RULE-0). 0 = never verified -> check. */
+	uint32_t		b_mxfs_coherent_gen;	/* sess28: dp->i_dlm_dir_gen at last PLATTER-VERIFIED addname coherence check (mxfs_dir_addname_coherent_refresh). Distinct from b_mxfs_dir_gen (stamped on EVERY read, incl stale reads) — only the addname guard sets this, so it dedups the per-addname FUA platter check to ONCE per block per dir_gen (cheap, the budget rule). 0 = never verified -> check. */
xfs/xfs_buf.h:394
-	 * sess113 lock-holder tracking (Gemini RULE-5 instrumentation): the
+	 * sess113 lock-holder tracking (Gemini design-consult instrumentation): the
xfs/xfs_dir2_readdir.c:51
- * sess10(a9a03929) RULE-4 readdir-tear instrumentation.
+ * sess10(a9a03929) instrumented readdir-tear instrumentation.
xfs/xfs_dir2_readdir.c:383
-		 * ccloop sess31 P31-FACEA disambiguation (RULE 4, failure-only so
+		 * ccloop sess31 P31-FACEA disambiguation (instrumented, failure-only so
xfs/xfs_dir2_readdir.c:886
-		/* P-RDDIAG (RULE 4): observe the dir grant state at readdir time
+		/* P-RDDIAG (instrumented): observe the dir grant state at readdir time
xfs/xfs_dir2_readdir.c:906
-		 * ccloop c7ee71c6 sess7 (GPT RULE-5 tenure-coherence ruling —
+		 * ccloop c7ee71c6 sess7 (design-consult tenure-coherence ruling —
xfs/xfs_dir2_readdir.c:927
-		 * sess60 ROOT FIX (RULE 4, PROVEN root
+		 * sess60 ROOT FIX (instrumented, PROVEN root
xfs/xfs_dir2_readdir.c:953
-		 * sess60 ROOT FIX v2 (RULE 4 — the FUA-di_size check above was
+		 * sess60 ROOT FIX v2 (instrumented — the FUA-di_size check above was
xfs/xfs_dir2_readdir.c:1095
-			/* sess48 (RULE 4): decisive readdir-reload probe (light, gated
+			/* sess48 (instrumented): decisive readdir-reload probe (light, gated
xfs/xfs_dir2_readdir.c:1171
-	 * sess26 (ccloop 8ddb16a2) RULE-4 DETECTOR (P26-RDDIR, capped): the
+	 * sess26 (ccloop 8ddb16a2) INSTRUMENTED DETECTOR (P26-RDDIR, capped): the
xfs/xfs_trans_ail.c:814
-		 * sess128 RULE-4 probe: this wait has no bound; when it wedges
+		 * sess128 instrumented probe: this wait has no bound; when it wedges
xfs/xfs_inode.h:156
-	/* sess236 (D-OPEN-PROTECT-DEMOTE-RACE-SPURIOUS-EIO, sess235 RULE-5
+	/* sess236 (D-OPEN-PROTECT-DEMOTE-RACE-SPURIOUS-EIO, sess235 design-consult
xfs/xfs_inode.h:208
-	/* sess238 #23 forensics (RULE-5 ruling): open_protect marks its task
+	/* sess238 #23 forensics (design-consult ruling): open_protect marks its task
xfs/xfs_inode.h:250
-	u64			i_dlm_bastq_qns; /* sess294 P296-BASTQLAT (D-503 RULE-4): expected-run ktime of the last SUCCESSFUL bast work/dwork arm — queue time for the immediate work, queue time + delay for the dwork (so work-fn-entry minus this = pure queue-to-run EXCESS, not the intended MHT delay).  Stamped under m_mxfs_arm_lock only when queue(_delayed)_work returned true (a false = already pending — the earlier stamp stays authoritative).  Read+cleared locklessly at both work-fn entries; a re-arm racing that read can make the delta negative, which the probe discards. */
+	u64			i_dlm_bastq_qns; /* sess294 P296-BASTQLAT (D-503 instrumented): expected-run ktime of the last SUCCESSFUL bast work/dwork arm — queue time for the immediate work, queue time + delay for the dwork (so work-fn-entry minus this = pure queue-to-run EXCESS, not the intended MHT delay).  Stamped under m_mxfs_arm_lock only when queue(_delayed)_work returned true (a false = already pending — the earlier stamp stays authoritative).  Read+cleared locklessly at both work-fn entries; a re-arm racing that read can make the delta negative, which the probe discards. */
xfs/xfs_inode.h:303
-	 * the object at the moment the image was formatted.  The sess96 RULE-5
+	 * the object at the moment the image was formatted.  The sess96 design-consult
xfs/xfs_inode.h:405
-	 *                            (RULE-5 sess469 ruling: a certificate
+	 *                            (design-consult sess469 ruling: a certificate
xfs/xfs_inode.h:421
- * reap stops retrying and the node fails closed (RULE-5 sess469 ruling 2:
+ * reap stops retrying and the node fails closed (design-consult sess469 ruling 2:
xfs/xfs_inode.h:431
-	 * why the next write grant failed to install.  The sess104 RULE-5
+	 * why the next write grant failed to install.  The sess104 design-consult
xfs/xfs_inode.h:453
-	 * sess403 (clean-release marker, RULE-5 ruling): the {resource,
+	 * sess403 (clean-release marker, design-consult ruling): the {resource,
xfs/xfs_inode.h:648
-	uint64_t		i_mxfs_lastrel_ns;   /* sess10(a9a03929) RULE-4 last-release ledger: ktime_get_real_ns at the end of the most recent release-path __mxfs_dlm_dir_inode_durable decision for this dir (bast_process sd stage or per-op wrapper).  Printed by P-SFDIR-REVERT so a platter-lags-commit event shows when the last durable-at-release ran relative to the stale reload.  0 = never. */
+	uint64_t		i_mxfs_lastrel_ns;   /* sess10(a9a03929) instrumented last-release ledger: ktime_get_real_ns at the end of the most recent release-path __mxfs_dlm_dir_inode_durable decision for this dir (bast_process sd stage or per-op wrapper).  Printed by P-SFDIR-REVERT so a platter-lags-commit event shows when the last durable-at-release ran relative to the stale reload.  0 = never. */
xfs/xfs_inode.h:655
-	 * GPT RULE-5 design (memories sess14-D/H/J), the replacement for four
+	 * Design-consult design (memories sess14-D/H/J), the replacement for four
xfs/xfs_inode.h:694
-	 * every release (RULE 0 hazard) and can re-publish a prior-tenure image.
+	 * every release (budget hazard) and can re-publish a prior-tenure image.
xfs/xfs_inode.h:771
-	 * ccloop c7ee71c6 sess30 — STAGING TENURE STAMP, for GPT RULE-5 review
+	 * ccloop c7ee71c6 sess30 — STAGING TENURE STAMP, for design-consult review
xfs/xfs_inode.h:903
-	 * sess431 (D-0351, RULE-5 ruling): the FREE-publication CLAIM.  A P55C
+	 * sess431 (D-0351, design-consult ruling): the FREE-publication CLAIM.  A P55C
xfs/xfs_inode.h:921
-	/* sess132 (RULE 4): ILOCK last-locker forensics for the 16-node
+	/* sess132 (instrumented): ILOCK last-locker forensics for the 16-node
xfs/xfs_inode.h:1082
-	 * ccloop c7ee71c6 sess27 (D-UNMOUNT-BUSY-INODES), from the RULE-5 GPT
+	 * ccloop c7ee71c6 sess27 (D-UNMOUNT-BUSY-INODES), from the design-consult GPT
xfs/xfs_inode.h:1625
- * local intent is.  (Gemini RULE-5 design.)
+ * local intent is.  (Gemini design-consult design.)
xfs/xfs_inode.h:1643
- * sess324 (D-FOREIGN-REPLAY-REFUSAL-CLUSTERWIDE-SUICIDE-513, sess320 RULE-5
+ * sess324 (D-FOREIGN-REPLAY-REFUSAL-CLUSTERWIDE-SUICIDE-513, sess320 design-consult
xfs/xfs_inode.h:1703
- * ccloop-4dd7 sess5 RULE-4 s_remove_count shadow ledger: set iff THIS
+ * ccloop-4dd7 sess5 instrumented s_remove_count shadow ledger: set iff THIS
xfs/xfs_inode.h:1718
- * ccloop c7ee71c6 sess3 (RULE-5 GPT incarnation-coherence design, Phase A).
+ * ccloop c7ee71c6 sess3 (design-consult GPT incarnation-coherence design, Phase A).
xfs/xfs_inode.h:1736
- * ccloop c7ee71c6 sess318 (RULE-5 ruling, D-INCARN-STALE-SHELL-UNGATED-
+ * ccloop c7ee71c6 sess318 (design-consult ruling, D-INCARN-STALE-SHELL-UNGATED-
xfs/xfs_inode.h:1873
- * from every retry mechanism XFS has.  (RULE-5 GPT review, sess20: "if an
+ * from every retry mechanism XFS has.  (design-consult GPT review, sess20: "if an
xfs/xfs_inode.h:1920
- * sess387 publication obligations (RULE-5 ruling, D-AGI-UNLINKED-CROSSNODE-
+ * sess387 publication obligations (design-consult ruling, D-AGI-UNLINKED-CROSSNODE-
xfs/xfs_inode.h:1961
-	 * sess5 ROOT FIX (RULE-4 proven, b67r2 provenance print
+	 * sess5 ROOT FIX (proven by instrument, b67r2 provenance print
xfs/xfs_log_recover.c:236
- * sess411 (D-FOREIGN-REPLAY-UNSTABLE-SLICE-READ-FALSE-TORN-527, RULE-5
+ * sess411 (D-FOREIGN-REPLAY-UNSTABLE-SLICE-READ-FALSE-TORN-527, design-consult
xfs/xfs_log_recover.c:261
- * sess444 (RULE-5 ruling ccmemory ccloop-c7ee71c6-sess444-GPT-ruling-frstab-
+ * sess444 (design-consult ruling ccmemory ccloop-c7ee71c6-sess444-GPT-ruling-frstab-
xfs/xfs_log_recover.c:2844
- * How to read the counters (sess167 RULE-5 review):
+ * How to read the counters (sess167 design-consult review):
xfs/xfs_log_recover.c:2922
-	 * sess403 (RULE-5 ruling): the token names a tenure the victim
+	 * sess403 (design-consult ruling): the token names a tenure the victim
xfs/xfs_log_recover.c:2993
-	 * sess443 (docs/whole-cluster-restart.md §6.8.5, RULE-5 review ruling):
+	 * sess443 (docs/whole-cluster-restart.md §6.8.5, design-consult review ruling):
xfs/xfs_log_recover.c:3388
-		/* sess82 RULE-5 ruling: v1 is NEVER authority evidence */
+		/* sess82 design-consult ruling: v1 is NEVER authority evidence */
xfs/xfs_log_recover.c:3450
-		 * sess434 (D-0354 lap 2, RULE-5 ruling): the victim durably
+		 * sess434 (D-0354 lap 2, design-consult ruling): the victim durably
xfs/xfs_log_recover.c:3475
-	 * sess403 (RULE-5 ruling precedence): (1) the frozen manifest says
+	 * sess403 (design-consult ruling precedence): (1) the frozen manifest says
xfs/xfs_log_recover.c:3578
- * {class, resource, epoch} match here.  A RULE-5 ruling REFUTED that scope:
+ * {class, resource, epoch} match here.  A design-consult ruling REFUTED that scope:
xfs/xfs_log_recover.c:3830
-			 * sess444 (RULE-5 ruling, D-ICREATE-REPLAY-REINIT-
+			 * sess444 (design-consult ruling, D-ICREATE-REPLAY-REINIT-
xfs/xfs_log_recover.c:4132
-		 * sess467 (RULE-5 ruling, fix shape B pre-code census): for
+		 * sess467 (design-consult ruling, fix shape B pre-code census): for
xfs/xfs_log_recover.c:4697
- * RULE-5 ruling ccmemory ccloop-c7ee71c6-sess476-GPT-ruling-cancel-item-
+ * Design-consult ruling ccmemory ccloop-c7ee71c6-sess476-GPT-ruling-cancel-item-
xfs/xfs_log_recover.c:4951
-	 * sess407 (RULE-5 ruling Q5): a replayer that already holds a lease
+	 * sess407 (design-consult ruling Q5): a replayer that already holds a lease
xfs/xfs_log_recover.c:4983
-	 * sess436 (D-FOREIGN-SLICE-INTENTS-ABANDONED, RULE-4 proven): the
+	 * sess436 (D-FOREIGN-SLICE-INTENTS-ABANDONED, proven by instrument): the
xfs/xfs_log_recover.c:5095
-			 * sess403 (RULE-5 ruling): inside an ADMITTED
+			 * sess403 (design-consult ruling): inside an ADMITTED
xfs/xfs_trans_priv.h:18
- * sess129 RULE-4 probe: set (latched) by the P128-AILSTUCK dump in
+ * sess129 instrumented probe: set (latched) by the P128-AILSTUCK dump in
xfs/xfs_trans.h:214
-	 * sess103 step 5.3 (P1 of the sess102 RULE-5 ruling): globally unique
+	 * sess103 step 5.3 (P1 of the sess102 design-consult ruling): globally unique
xfs/xfs_icache.c:257
-		 * own failure at tenure_grabs=499).  Per the RULE-5 GPT consult
+		 * own failure at tenure_grabs=499).  Per the design-consult GPT consult
xfs/xfs_icache.c:783
-	 * sess9(a9a03929) RULE-4 s_remove_count skew ledger (see P9-NLEDGE in
+	 * sess9(a9a03929) instrumented s_remove_count skew ledger (see P9-NLEDGE in
xfs/xfs_icache.c:864
-	 * ccloop cc87fed3 sess6: RULE 4 direct-evidence probe (Fable-guided,
+	 * ccloop cc87fed3 sess6: direct-evidence probe (Fable-guided,
xfs/xfs_icache.c:898
-		 * ccloop cc87fed3 sess7: RULE 4 -- the P141-SETUP-DOUBLE-ADD
+		 * ccloop cc87fed3 sess7: instrumented -- the P141-SETUP-DOUBLE-ADD
xfs/xfs_icache.c:1040
-			 * sess5 ROOT FIX (b70r1 shutdown, RULE-4 proven): when
+			 * sess5 ROOT FIX (b70r1 shutdown, proven by instrument): when
xfs/xfs_icache.c:1121
-			     * ccloop-4dd7 ORDERING GUARD (RULE-4 proven, ino
+			     * ccloop-4dd7 ORDERING GUARD (proven by instrument, ino
xfs/xfs_icache.c:1154
-					 * sess68 (RULE 4): we just adopted a
+					 * sess68 (instrumented): we just adopted a
xfs/xfs_icache.c:1232
-	 * sess428 (RULE 4, s431 board collapse): a shell this node FREED may
+	 * sess428 (instrumented, s431 board collapse): a shell this node FREED may
xfs/xfs_icache.c:1793
-		 * P-CR63 (RULE 4, pve1 ino-489 dialloc -117 autopsy): dialloc
+		 * P-CR63 (instrumented, pve1 ino-489 dialloc -117 autopsy): dialloc
xfs/xfs_icache.c:1834
-			 * ccloop-4dd7 FIX (RULE-4 proven chain, vmrig ino 139:
+			 * ccloop-4dd7 FIX (proven by instrument chain, vmrig ino 139:
xfs/xfs_icache.c:1886
-		 * sess4(a16ec5f2) FIX B1 (RULE 4, run23 P-IGET-ENOENT proof:
+		 * sess4(a16ec5f2) FIX B1 (instrumented, run23 P-IGET-ENOENT proof:
xfs/xfs_icache.c:2160
- * RULE-5 ruling option A): read the changecount of the FREED core we are
+ * Design-consult ruling option A): read the changecount of the FREED core we are
xfs/xfs_icache.c:2187
- * sess409 PACE (RULE 0): re-reading the cluster on EVERY create cost +18%
+ * sess409 PACE (budget): re-reading the cluster on EVERY create cost +18%
xfs/xfs_icache.c:2265
-	 * D-0527 (sess470 RULE-5 answer D, landed sess472, 0.64.15): an
+	 * D-0527 (sess470 design-consult answer D, landed sess472, 0.64.15): an
xfs/xfs_icache.c:2536
-		 * sess79 RULE-4 DECISIVE PROBE: inode_from_disk failed
+		 * sess79 instrumented DECISIVE PROBE: inode_from_disk failed
xfs/xfs_icache.c:2625
-	 * sess127 ROOT FIX (RULE 4 PROVEN via repro_uv_create_race.sh +
+	 * sess127 ROOT FIX (PROVEN BY INSTRUMENT via repro_uv_create_race.sh +
xfs/xfs_icache.c:3015
-	 * sess474 (D-0532 RULE-5 option (a)): xfs_ilock_nowait takes NO DLM
+	 * sess474 (D-0532 design-consult option (a)): xfs_ilock_nowait takes NO DLM
xfs/xfs_icache.c:3057
-	/* sess387 (d), RULE-5 ruling: reclaim must REFUSE an inode with a live
+	/* sess387 (d), design-consult ruling: reclaim must REFUSE an inode with a live
xfs/xfs_icache.c:3095
-	 * ccloop cc87fed3 sess6: RULE 4 audit trail (Fable-guided, P135
+	 * ccloop cc87fed3 sess6: audit trail (Fable-guided, P135
xfs/xfs_icache.c:4384
-	 * ccloop cc87fed3 sess6: sweep-pin tripwire (RULE 4 direct-evidence
+	 * ccloop cc87fed3 sess6: sweep-pin tripwire (direct-evidence
xfs/xfs_icache.c:4463
-			 * ccloop cc87fed3 sess4: added ip=/pid=/comm= (RULE 4 —
+			 * ccloop cc87fed3 sess4: added ip=/pid=/comm= (instrumented —
xfs/xfs_mxfs_dlm.md:234
-  (v0.11.18). Root (RULE-4 proven, `xfs_inode.c`, not this file, but the
+  (v0.11.18). Root (proven by instrument, `xfs_inode.c`, not this file, but the
xfs/xfs_inode.c.sess129fix:634
-	 * sess127 (RULE 4, ungated, miss-only): the unlink_visibility loser's
+	 * sess127 (instrumented, ungated, miss-only): the unlink_visibility loser's
xfs/xfs_inode.c.sess129fix:796
-			 * sess95 FIX (Gemini-confirmed, RULE 4 step 2b): SAME-TYPE
+			 * sess95 FIX (Gemini-confirmed, instrument step 2b): SAME-TYPE
xfs/xfs_inode.c.sess129fix:923
-	 * sess95 FIX2 (Gemini-confirmed, RULE 4 step 2b): TYPE-FLIP reuse where
+	 * sess95 FIX2 (Gemini-confirmed, instrument step 2b): TYPE-FLIP reuse where
xfs/xfs_inode.c.sess129fix:1270
-		/* sess105 (RULE 4): PROVE the directory-inode divergence.  The
+		/* sess105 (instrumented): PROVE the directory-inode divergence.  The
xfs/xfs_inode.c.sess129fix:1308
-			/* sess106 (RULE 4): PROVE the directory-inode divergence.
+			/* sess106 (instrumented): PROVE the directory-inode divergence.
xfs/xfs_inode.c.sess129fix:1329
-				 * sess127 (RULE 4 PROVEN — corrects sess126): do NOT set
+				 * sess127 (PROVEN BY INSTRUMENT — corrects sess126): do NOT set
xfs/xfs_inode.c.sess129fix:1538
-	 * sess115 (ccloop 4eef1f39) — DISABLED.  RULE 4: the P112-IFLUSH-CALLER
+	 * sess115 (ccloop 4eef1f39) — DISABLED.  Instrumented: the P112-IFLUSH-CALLER
xfs/xfs_inode.c.sess129fix:2199
-	 * sess129 (RULE 4 PROVEN): this MUST be a targeted per-inode drain,
+	 * sess129 (PROVEN BY INSTRUMENT): this MUST be a targeted per-inode drain,
xfs/xfs_inode.c.sess129fix:2384
-		 * sess78 TOCTOU CLOSURE (Gemini RULE-5 design; see
+		 * sess78 TOCTOU CLOSURE (Gemini design-consult design; see
xfs/xfs_inode.c.sess129fix:2446
-		 * B3 (sess19 ccloop, Gemini RULE-5): the killer case B1/B2 both
+		 * B3 (sess19 ccloop, Gemini design-consult): the killer case B1/B2 both
xfs/xfs_inode.c.sess129fix:2485
-		 * sess19 (ccloop 4eef1f39) RULE-4 instrumentation: log the B3
+		 * sess19 (ccloop 4eef1f39) instrumentation: log the B3
xfs/xfs_inode.c.sess129fix:2615
-		 * sess128 RULE-4 probe: the ON-DISK slot for this ino is now
+		 * sess128 instrumented probe: the ON-DISK slot for this ino is now
xfs/xfs_inode.c.sess129fix:3180
-	 * PROVEN root (sess96, RULE 4): under 4-node concurrent rename/unlink of
+	 * PROVEN root (sess96, instrumented): under 4-node concurrent rename/unlink of
xfs/xfs_inode.c.sess129fix:3660
-	 * sess102 RULE-4 DETECTOR (P-IRESURRECT): the bnobt double-free
+	 * sess102 INSTRUMENTED DETECTOR (P-IRESURRECT): the bnobt double-free
xfs/xfs_inode.c.sess129fix:3794
-	 * sess118 RULE-4 GUARD (the sess102 P-IRESURRECT detector, now acting):
+	 * sess118 instrumented GUARD (the sess102 P-IRESURRECT detector, now acting):
xfs/xfs_inode.c.sess129fix:3826
-		 * sess119 (Gemini RULE-5 validated) — DLM-OWNERSHIP DISCRIMINATOR.
+		 * sess119 (Gemini design-consult validated) — DLM-OWNERSHIP DISCRIMINATOR.
xfs/xfs_inode.c.sess129fix:4066
-	 * sess113 (Gemini RULE-5, PROVEN ROOT): this merge does a SYNCHRONOUS
+	 * sess113 (Gemini design-consult, PROVEN ROOT): this merge does a SYNCHRONOUS
xfs/xfs_mxfs_dentry.c:119
-	 * ccloop cc87fed3 sess5 (RULE 4 — BUG3 hunt): this dentry is about to
+	 * ccloop cc87fed3 sess5 (instrumented — BUG3 hunt): this dentry is about to
xfs/xfs_mxfs_icensus.h:4
- * (sess421, D-FOREIGN-SLICE-INTENTS-ABANDONED interim step; sess420 RULE-5
+ * (sess421, D-FOREIGN-SLICE-INTENTS-ABANDONED interim step; sess420 design-consult
xfs/xfs_mxfs_icensus.h:32
- * sess461 (item 5 increment 1, RULE-5 ruling ccloop-c7ee71c6-sess461-GPT-
+ * sess461 (item 5 increment 1, design-consult ruling ccloop-c7ee71c6-sess461-GPT-
xfs/xfs_mxfs_dirshard.c:311
-	 * sess472 (D-0531 item 3, RULE-5 review): this runs inside the
+	 * sess472 (D-0531 item 3, design-consult review): this runs inside the
xfs/xfs_mxfs_dirshard.c:494
- * RULE-5 review of the first cut (sess473) — what this shape now honours:
+ * Design-consult review of the first cut (sess473) — what this shape now honours:
xfs/xfs_mxfs_dirshard.c:651
-	 * Identity.  Two answers, ruled apart by the sess470 RULE-5 review:
+	 * Identity.  Two answers, ruled apart by the sess470 design-consult review:
xfs/xfs_mxfs_dirshard.c:1600
- * sess470 (0.64.11, RULE-5 review item 3): the SAME transaction removes the
+ * sess470 (0.64.11, design-consult review item 3): the SAME transaction removes the
xfs/xfs_mxfs_dirshard.c:1688
-	 * sess472 (D-0531 root, RULE-5 reviewed): the 0.64.12 check below
+	 * sess472 (D-0531 root, design-consult reviewed): the 0.64.12 check below
xfs/libxfs/xfs_bmap.c:1158
-	 * sess34 (ccloop 14d31183) P34B DECISIVE PROBE (RULE 4 step 2):
+	 * sess34 (ccloop 14d31183) P34B DECISIVE PROBE (instrument step 2):
xfs/libxfs/xfs_bmap.c:1173
-	 * sess68 (ccloop 14d31183) zero_silent_loss FIX — RULE 4 step 2b,
+	 * sess68 (ccloop 14d31183) zero_silent_loss FIX — instrument step 2b,
xfs/libxfs/xfs_bmap.c:1213
-				 * sess6 (ccloop 46efd8b6) RULE-4 PROVEN (runs
+				 * sess6 (ccloop 46efd8b6) PROVEN BY INSTRUMENT (runs
xfs/libxfs/xfs_bmap.c:1287
-	 * of the sess68 leaf refresh (RULE 4 step 2b, proven cause).  The sess68
+	 * of the sess68 leaf refresh (instrument step 2b, proven cause).  The sess68
xfs/libxfs/xfs_bmap.c:1411
-		/* sess59 RULE-4: quantify the dinode-vs-bmbt-leaf disk
+		/* sess59 instrumented: quantify the dinode-vs-bmbt-leaf disk
xfs/libxfs/xfs_bmap.c:5331
-			 * (RULE 4): the in-core iext record `got` is NOT in
+			 * (instrumented): the in-core iext record `got` is NOT in
xfs/libxfs/xfs_bmap_btree.c:478
-	 * sess66 (ccloop 14d31183) RULE-4 write-side probe for zero_silent_loss.
+	 * sess66 (ccloop 14d31183) instrumented write-side probe for zero_silent_loss.
xfs/libxfs/xfs_da_btree.c:44
-/* sess44 RULE-4 isolation lever: gate the unpublished-dir FUA-skip fast path.
+/* sess44 isolation lever: gate the unpublished-dir FUA-skip fast path.
xfs/libxfs/xfs_da_btree.c:2399
-	 * sess34 (ccloop 14d31183) P34C DOUBLE-MAP PROBE (RULE 4 step 2):
+	 * sess34 (ccloop 14d31183) P34C DOUBLE-MAP PROBE (instrument step 2):
xfs/libxfs/xfs_da_btree.c:2846
-	 * sess14(ccloop) SMOKING-GUN probe (RULE 4): on a multi-node DIR DATA-fork
+	 * sess14(ccloop) SMOKING-GUN probe (instrumented): on a multi-node DIR DATA-fork
xfs/libxfs/xfs_da_btree.c:2894
-		 * sess15(ccloop) EXTENT-SHAPE probe (RULE 4): the gen-coupling
+		 * sess15(ccloop) EXTENT-SHAPE probe (instrumented): the gen-coupling
xfs/libxfs/xfs_da_btree.c:2975
-	 * sess55 (ccloop) RULE-4 M2 SOURCE DETECTOR.  Face B of
+	 * sess55 (ccloop) instrumented M2 SOURCE DETECTOR.  Face B of
xfs/libxfs/xfs_da_btree.c:3042
- * sess15 (RULE 4 — block-dir concurrent-create durable loss REQUIRES inode/daddr
+ * sess15 (instrumented — block-dir concurrent-create durable loss REQUIRES inode/daddr
xfs/libxfs/xfs_da_btree.c:3109
-	 * sess5 (ccloop 46efd8b6, RULE 4 PROVEN): the pre-read incore
+	 * sess5 (ccloop 46efd8b6, PROVEN BY INSTRUMENT): the pre-read incore
xfs/libxfs/xfs_da_btree.c:3174
-	/* ccloop 12e0d157 sess4 P-DSCAN (RULE-4, dlm_scaling@32): the proven storm
+	/* ccloop 12e0d157 sess4 P-DSCAN (instrumented, dlm_scaling@32): the proven storm
xfs/libxfs/xfs_da_btree.c:3235
-	/* sess38(ccloop) RULE-4: log the logical->physical resolution of every
+	/* sess38(ccloop) instrumented: log the logical->physical resolution of every
xfs/libxfs/xfs_da_btree.c:3365
-				 * RULE 4 PROVEN (dlm_fairness 4/caw run 020719Z,
+				 * PROVEN BY INSTRUMENT (dlm_fairness 4/caw run 020719Z,
xfs/libxfs/xfs_da_btree.c:3561
-	/* sess32 (GPT RULE-5): EX-side prior-tenure revalidation.  owned_ex
+	/* sess32 (design-consult): EX-side prior-tenure revalidation.  owned_ex
xfs/libxfs/xfs_da_btree.c:3594
-		/* P-RDPATH (RULE-4): decisive — for a dir block0 read, log whether
+		/* P-RDPATH (instrumented): decisive — for a dir block0 read, log whether
xfs/libxfs/xfs_da_btree.c:3657
-			 * sess5 (ccloop, RULE 4 PROVEN) — UNDESTAGED COLD-READ
+			 * sess5 (ccloop, PROVEN BY INSTRUMENT) — UNDESTAGED COLD-READ
xfs/libxfs/xfs_da_btree.c:3736
-			 * sess133 (RULE 4, PROVEN true-silent-dirent-loss):
+			 * sess133 (instrumented, PROVEN true-silent-dirent-loss):
xfs/libxfs/xfs_da_btree.c:3850
-			    /* sess5 (ccloop, RULE 4 PROVEN): the `!in_ail` term was
+			    /* sess5 (ccloop, PROVEN BY INSTRUMENT): the `!in_ail` term was
xfs/libxfs/xfs_da_btree.c:4031
-				 * sess60 DECISIVE PROBE (RULE 4): the gen MATCHES
+				 * sess60 DECISIVE PROBE (instrumented): the gen MATCHES
xfs/libxfs/xfs_da_btree.c:4158
-	 * sess61 DECISIVE PROBE (RULE 4): after reading block0 of a multinode
+	 * sess61 DECISIVE PROBE (instrumented): after reading block0 of a multinode
xfs/libxfs/xfs_da_btree.c:4532
-	 * sess50(ccloop) RULE-4 content-history trace: log the dirent count of the
+	 * sess50(ccloop) instrumented content-history trace: log the dirent count of the
xfs/libxfs/xfs_da_btree.c:4575
-	 * sess67 (GPT-5.5 RULE-5 design): UNDER-BUFFER-LOCK dir-block coherency
+	 * sess67 (GPT-5.5 design-consult design): UNDER-BUFFER-LOCK dir-block coherency
xfs/libxfs/xfs_da_btree.c:4596
-		 * sess20(ccloop) PROVEN (RULE 4): the dir_reuse low-mht
+		 * sess20(ccloop) PROVEN (instrumented): the dir_reuse low-mht
xfs/libxfs/xfs_da_btree.c:4705
-	 * sess19 LEAF-HASH stale-read detector (RULE 4).  PROVEN this session:
+	 * sess19 LEAF-HASH stale-read detector (instrumented).  PROVEN this session:
xfs/libxfs/xfs_da_btree.c:4793
-	 * sess19 (RULE 4, GPT Hole B): on a multinode shared dir, a dir-block
+	 * sess19 (instrumented, GPT Hole B): on a multinode shared dir, a dir-block
xfs/libxfs/xfs_dir2_data.c:535
-	/* sess62 (RULE 4) ALWAYS-ON, capped, low-flood (writes only): the durable
+	/* sess62 (instrumented) ALWAYS-ON, capped, low-flood (writes only): the durable
xfs/libxfs/xfs_dir2_data.c:927
-	 * sess54(ccloop) RULE-4: ALWAYS-ON, I/O-FREE dir-block DOUBLE-ALLOCATION
+	 * sess54(ccloop) instrumented: ALWAYS-ON, I/O-FREE dir-block DOUBLE-ALLOCATION
xfs/libxfs/xfs_dir2_data.c:966
-	 * ccloop sess31 P31E-DATAINIT-ABA (GPT-5.5 RULE-5 decisive detector):
+	 * ccloop sess31 P31E-DATAINIT-ABA (GPT-5.5 design-consult decisive detector):
xfs/libxfs/xfs_dir2_data.c:1091
-						/* sess37 RULE-4 DECISIVE: is the on-disk INODE's
+						/* sess37 instrumented DECISIVE: is the on-disk INODE's
xfs/libxfs/xfs_dir2_data.c:1143
-						/* sess32 RULE-4 (a)-vs-(b) discriminator:
+						/* sess32 instrumented (a)-vs-(b) discriminator:
xfs/libxfs/xfs_dir2_data.c:1180
-	/* sess62 (RULE 4) ALWAYS-ON, no-FUA, decisive: log every data_init of a
+	/* sess62 (instrumented) ALWAYS-ON, no-FUA, decisive: log every data_init of a
xfs/libxfs/xfs_dir2_data.c:1213
-		/* P-DBLALLOC-BIRTH (RULE-4): FUA-read the CURRENT on-disk content of
+		/* P-DBLALLOC-BIRTH (instrumented): FUA-read the CURRENT on-disk content of
xfs/libxfs/xfs_dir2_data.c:1246
-						/* P-DBLALLOC-AGF (RULE 4, sess5): read-vs-write
+						/* P-DBLALLOC-AGF (instrumented, sess5): read-vs-write
xfs/libxfs/xfs_dir2_data.c:1294
-	/* sess68 (RULE 4) ALWAYS-ON, NO-IO: log every data_init of a multinode
+	/* sess68 (instrumented) ALWAYS-ON, NO-IO: log every data_init of a multinode
xfs/libxfs/xfs_dir2_data.c:1374
- * sess49 (ccloop, RULE 4 DECISIVE): does a block image contain an active dirent
+ * sess49 (ccloop, instrumented DECISIVE): does a block image contain an active dirent
xfs/libxfs/xfs_dir2_data.c:1494
-	/* sess2(ccloop) ALWAYS-ON cheap epoch-at-placement probe (RULE 4): the
+	/* sess2(ccloop) ALWAYS-ON cheap epoch-at-placement probe (instrumented): the
xfs/libxfs/xfs_dir2_data.c:1525
-		/* sess13 PLACEMENT LEDGER (watch-gated, RULE 4): the drc f1 loss
+		/* sess13 PLACEMENT LEDGER (watch-gated, instrumented): the drc f1 loss
xfs/libxfs/xfs_dir2_data.c:1566
-	/* sess13run (ccloop, RULE 4 DECISIVE): collision detector AT placement.
+	/* sess13run (ccloop, instrumented DECISIVE): collision detector AT placement.
xfs/libxfs/xfs_dir2_data.c:1609
-					/* sess46 (ccloop, RULE 4 DECISIVE): capture the
+					/* sess46 (ccloop, instrumented DECISIVE): capture the
xfs/libxfs/xfs_dir2_data.c:1642
-			/* sess49 (ccloop, RULE 4 DECISIVE): whole-block stale-base
+			/* sess49 (ccloop, instrumented DECISIVE): whole-block stale-base
xfs/libxfs/xfs_dir2_data.c:1830
-	 * sess9(a9a03929) RULE-4 free-side ledger — mirror of P13-LADD.  Every
+	 * sess9(a9a03929) instrumented free-side ledger — mirror of P13-LADD.  Every
xfs/libxfs/xfs_dir2_data.c:2113
-	 * ALSO the natural RULE-0 throttle: once this node adds an entry the block
+	 * ALSO the natural the budget rule throttle: once this node adds an entry the block
xfs/libxfs/xfs_dir2_data.c:2128
-		 * sess54(ccloop) RULE-4 — relax the in-AIL half of the keep-guard.
+		 * sess54(ccloop) instrumented — relax the in-AIL half of the keep-guard.
xfs/libxfs/xfs_dir2_data.c:2147
-		 * sess5(a9a03929) run80 ROOT FIX (RULE 4, test2 r1 @69.966):
+		 * sess5(a9a03929) run80 ROOT FIX (instrumented, test2 r1 @69.966):
xfs/libxfs/xfs_dir2_data.c:2291
-	 * sess54(ccloop) RULE-4 SERIALIZATION-HOLE probe.  The durable dirent loss
+	 * sess54(ccloop) instrumented SERIALIZATION-HOLE probe.  The durable dirent loss
xfs/libxfs/xfs_ialloc.c:333
-	 * sess444 (RULE-5 ruling, D-ICREATE-REPLAY-REINIT-CLOBBERS-PEER-INODES):
+	 * sess444 (design-consult ruling, D-ICREATE-REPLAY-REINIT-CLOBBERS-PEER-INODES):
xfs/libxfs/xfs_ialloc.c:355
-	 * sess45 (ccloop 8ddb16a2) RULE-4 PROBE — the 2/tcp durable wedge is a
+	 * sess45 (ccloop 8ddb16a2) INSTRUMENTED PROBE — the 2/tcp durable wedge is a
xfs/libxfs/xfs_ialloc.c:395
-	 * sess444 (RULE-5 landing review, STOP-SHIP 1/7): the SYNCINIT proof
+	 * sess444 (design-consult landing review, STOP-SHIP 1/7): the SYNCINIT proof
xfs/libxfs/xfs_ialloc.c:1417
- * P150 (RULE-4, ccloop-4dd7 inobt double-free record corruption): record-
+ * P150 (instrumented, ccloop-4dd7 inobt double-free record corruption): record-
xfs/libxfs/xfs_ialloc.c:1466
- * P-AGIFC (sess399, RULE 4): always-on multi-node audit of the AGI free-inode
+ * P-AGIFC (sess399, instrumented): always-on multi-node audit of the AGI free-inode
xfs/libxfs/xfs_ialloc.c:1601
- * 0.23.0 (sess392, RULE-5 ruling ccloop-c7ee71c6-sess392-GPT-ruling-dialloc-
+ * 0.23.0 (sess392, design-consult ruling ccloop-c7ee71c6-sess392-GPT-ruling-dialloc-
xfs/libxfs/xfs_ialloc.c:1647
- * TRIED AND REVERTED (0.75.122, RULE 4): yielding the AG early on a run of
+ * TRIED AND REVERTED (0.75.122, instrumented): yielding the AG early on a run of
xfs/libxfs/xfs_ialloc.c:2866
-	 * released (RULE-5: no platter I/O under the AGI/btree nesting).
+	 * released (design-consult: no platter I/O under the AGI/btree nesting).
xfs/libxfs/xfs_ialloc.c:3052
- * sess430 (D-0351 containment, RULE-5 ruling ccmemory ccloop-c7ee71c6-sess430-
+ * sess430 (D-0351 containment, design-consult ruling ccmemory ccloop-c7ee71c6-sess430-
xfs/libxfs/xfs_ialloc.c:3336
-	 * sess24 (ccloop 4eef1f39) RULE-4 DETECTOR (P-IALLOC-DBLALLOC): we just
+	 * sess24 (ccloop 4eef1f39) INSTRUMENTED DETECTOR (P-IALLOC-DBLALLOC): we just
xfs/libxfs/xfs_ialloc.c:3552
-	 * MXFS sess386 (RULE-5 ruling): bounded jittered re-sweeps before
+	 * MXFS sess386 (design-consult ruling): bounded jittered re-sweeps before
xfs/libxfs/xfs_ialloc.c:3667
-		 * MXFS sess386 / 0.23.0-0.23.1 (sess392 RULE-5 ruling): a full
+		 * MXFS sess386 / 0.23.0-0.23.1 (sess392 design-consult ruling): a full
xfs/libxfs/xfs_ialloc.c:3729
-		/* sess430 (D-0351 containment, RULE-5): every remaining free
+		/* sess430 (D-0351 containment, design-consult): every remaining free
xfs/libxfs/xfs_ialloc.c:4057
-	 * P-DIFREE-DBL (RULE-4 probe): in multi-node mode, detect the inobt
+	 * P-DIFREE-DBL (instrumented probe): in multi-node mode, detect the inobt
xfs/libxfs/xfs_ialloc.c:4137
-		 * sess103 RULE-4 DISCRIMINATOR (P103-CHUNKFREE): a fully-free
+		 * sess103 instrumented DISCRIMINATOR (P103-CHUNKFREE): a fully-free
xfs/libxfs/xfs_inode_buf.c:82
-			 * sess44 (ccloop 8ddb16a2) RULE-4 PROBE — the 2/tcp
+			 * sess44 (ccloop 8ddb16a2) INSTRUMENTED PROBE — the 2/tcp
xfs/libxfs/xfs_inode_buf.c:129
- * sess60 RULE-4 ALWAYS-ON: P-DIRFLUSH.
+ * sess60 instrumented ALWAYS-ON: P-DIRFLUSH.
xfs/libxfs/xfs_inode_buf.c:170
-	 * sess62 RULE-4 H-A vs H-B discriminator: build the mask of slots being
+	 * sess62 instrumented H-A vs H-B discriminator: build the mask of slots being
xfs/libxfs/xfs_inode_buf.c:197
-	 * sess45 (ccloop 8ddb16a2) RULE-4 PROBE — the 2/tcp durable wedge is a
+	 * sess45 (ccloop 8ddb16a2) INSTRUMENTED PROBE — the 2/tcp durable wedge is a
xfs/libxfs/xfs_inode_buf.c:405
-		 * sess9(a9a03929) RULE-4 s_remove_count skew ledger: soak FAILs
+		 * sess9(a9a03929) instrumented s_remove_count skew ledger: soak FAILs
xfs/libxfs/xfs_inode_buf.c:451
-	 * sess33 (ccloop 8ddb16a2) P33-FROMDISK-DIRSHRINK (RULE 4, decisive):
+	 * sess33 (ccloop 8ddb16a2) P33-FROMDISK-DIRSHRINK (instrumented, decisive):
xfs/libxfs/xfs_inode_buf.c:620
-	 * sess33 (ccloop 8ddb16a2) P33-TODISK-DIRSHRINK (RULE 4, decisive):
+	 * sess33 (ccloop 8ddb16a2) P33-TODISK-DIRSHRINK (instrumented, decisive):
xfs/libxfs/xfs_inode_buf.c:653
-	/* P-CCREGRESS (sess10 ccloop 72513a13, RULE-4 drc@32 unanimous-48
+	/* P-CCREGRESS (sess10 ccloop 72513a13, instrumented drc@32 unanimous-48
xfs/libxfs/xfs_inode_util.c:433
-	 * sess48 (393-c3 fatal root, RULE 4): a just-allocated inode CANNOT
+	 * sess48 (393-c3 fatal root, instrumented): a just-allocated inode CANNOT
xfs/libxfs/xfs_inode_util.c:449
-	 * sess396 (RULE-5 ruling ccmemory ccloop-c7ee71c6-sess396-GPT-ruling-
+	 * sess396 (design-consult ruling ccmemory ccloop-c7ee71c6-sess396-GPT-ruling-
xfs/libxfs/xfs_inode_util.c:554
- * sess469 (D-0525, RULE-5 ruling): the bucket LAYOUT is a property of the
+ * sess469 (D-0525, design-consult ruling): the bucket LAYOUT is a property of the
xfs/libxfs/xfs_inode_util.c:683
-	 * AG10 bucket 10, 0.23.1; RULE-5 ruling ccmemory
+	 * AG10 bucket 10, 0.23.1; design-consult ruling ccmemory
xfs/libxfs/xfs_inode_util.c:711
-		 * sess395 fault injection (RULE-5 ruling modes 1/2, TESTING
+		 * sess395 fault injection (design-consult ruling modes 1/2, TESTING
xfs/libxfs/xfs_inode_util.c:731
-		 * sess398: the injector walks the RULE-5 ruling's matrix by
+		 * sess398: the injector walks the design-consult ruling's matrix by
xfs/libxfs/xfs_inode_util.c:936
-		 * sess396 (0.23.5 test25 kill; RULE-5 ruling ccmemory
+		 * sess396 (0.23.5 test25 kill; design-consult ruling ccmemory
xfs/libxfs/xfs_inode_util.c:986
-		 * log (P-IUNL-NUFIX).  sess396 RULE-5 ruling: that is an
+		 * log (P-IUNL-NUFIX).  sess396 design-consult ruling: that is an
xfs/libxfs/xfs_inode_util.c:1201
-	 * RULE-4 discriminator, instr-gated: one FUA dinode read per remove):
+	 * discriminator, instr-gated: one FUA dinode read per remove):
xfs/libxfs/xfs_inode_util.c:1333
-	 * This was the RULE-4 probe that proved D-...-399 (150/350 removes per
+	 * This was the instrumented probe that proved D-...-399 (150/350 removes per
xfs/libxfs/xfs_inode_util.c:1436
-	 * ccloop c7ee71c6 sess19 nlink LEDGER (RULE 4).  tests/sf_mkdir_storm.sh
+	 * ccloop c7ee71c6 sess19 nlink LEDGER (instrumented).  tests/sf_mkdir_storm.sh
xfs/libxfs/xfs_trans_inode.c:175
-		 * sess33 refinement (RULE 4, attributed on the 289 board):
+		 * sess33 refinement (instrumented, attributed on the 289 board):
xfs/libxfs/xfs_trans_inode.c:297
-	 * sess6 (ccloop 46efd8b6) RULE-4 PROVEN (run 112803Z, test12 i!=1):
+	 * sess6 (ccloop 46efd8b6) PROVEN BY INSTRUMENT (run 112803Z, test12 i!=1):
xfs/libxfs/xfs_dir2.c:328
-			 * sess58 (ccloop 14d31183) P58-FMT-DISIZE-CORRUPT — RULE-4
+			 * sess58 (ccloop 14d31183) P58-FMT-DISIZE-CORRUPT — instrumented
xfs/libxfs/xfs_dir2.c:438
-	 * sess59 RULE-4 ALWAYS-ON: format-agnostic create-time detector for the
+	 * sess59 instrumented ALWAYS-ON: format-agnostic create-time detector for the
xfs/libxfs/xfs_dir2.c:460
-	 * sess58 (ccloop 14d31183) P58-STALE-BASE-ADD — RULE-4 ALWAYS-ON
+	 * sess58 (ccloop 14d31183) P58-STALE-BASE-ADD — instrumented ALWAYS-ON
xfs/libxfs/xfs_dir2.c:729
-	 * sess115 PROVEN ROOT FIX (RULE 4, dmesg EFSCORRUPTED trace):
+	 * sess115 PROVEN ROOT FIX (instrumented, dmesg EFSCORRUPTED trace):
xfs/libxfs/xfs_dir2.c:759
-	 * BOUNDARY (RULE-5 GPT prescription, first of its runtime assertions:
+	 * BOUNDARY (design-consult GPT prescription, first of its runtime assertions:
xfs/libxfs/xfs_dir2.c:863
-					 * sess21 RULE-4: P195 asserts we are in
+					 * sess21 instrumented: P195 asserts we are in
xfs/libxfs/xfs_dir2.c:1195
-	 * ccloop sess39 P-GROW0 (RULE 4 DECISIVE, low-perturbation, NO disk I/O):
+	 * ccloop sess39 P-GROW0 (instrumented DECISIVE, low-perturbation, NO disk I/O):
xfs/libxfs/xfs_dir2.c:1702
-		 * ccloop-4dd7 DEAD-CHILD GUARD (RULE-4 proven, ino 2097824
+		 * ccloop-4dd7 DEAD-CHILD GUARD (proven by instrument, ino 2097824
xfs/libxfs/xfs_dir2_node.c:1720
-	 * sess46 (ccloop, RULE 4): NON-PERTURBING dir-grow probe.  Earlier FUA-read
+	 * sess46 (ccloop, instrumented): NON-PERTURBING dir-grow probe.  Earlier FUA-read
xfs/libxfs/xfs_dir2_node.c:2101
-				/* sess54(ccloop) — TWO staleness signals (RULE-4 PROVEN):
+				/* sess54(ccloop) — TWO staleness signals (PROVEN BY INSTRUMENT):
xfs/libxfs/xfs_dir2_node.c:2341
-	/* sess13run (RULE 4): ALWAYS-ON node-addname placement probe for the
+	/* sess13run (instrumented): ALWAYS-ON node-addname placement probe for the
xfs/libxfs/xfs_dir2_leaf.c:248
-		/* sess38(ccloop) RULE-4 decisive probe: a leaf read that fails
+		/* sess38(ccloop) instrumented decisive probe: a leaf read that fails
xfs/libxfs/xfs_dir2_leaf.c:881
-			 * ccloop c7ee71c6 sess2 (RULE 4, cache_coherency rv
+			 * ccloop c7ee71c6 sess2 (instrumented, cache_coherency rv
xfs/libxfs/xfs_dir2_leaf.c:1492
-	 * sess13run (RULE 4): leaf-addname placement trace for the storm dir.
+	 * sess13run (instrumented): leaf-addname placement trace for the storm dir.
xfs/libxfs/xfs_dir2_leaf.c:1504
-		/* sess42(ccloop) RULE-4: ALWAYS-ON leaf-format placement trace,
+		/* sess42(ccloop) instrumented: ALWAYS-ON leaf-format placement trace,
xfs/libxfs/xfs_dir2_leaf.c:1990
-	 * (RULE 4): a leaf-referenced name is in NO data block this node's
+	 * (instrumented): a leaf-referenced name is in NO data block this node's
xfs/libxfs/xfs_dir2_leaf.c:2437
-	 * sess21 (ccloop 8ddb16a2) RULE-4 DETECTOR for the dir_reuse_coherency
+	 * sess21 (ccloop 8ddb16a2) INSTRUMENTED DETECTOR for the dir_reuse_coherency
xfs/libxfs/xfs_alloc.c:4197
-	 * sess55 (ccloop) RULE-4 SOURCE DETECTOR for the dir-data-over-inode-
+	 * sess55 (ccloop) instrumented SOURCE DETECTOR for the dir-data-over-inode-
xfs/libxfs/xfs_alloc.c:4331
-				 * 2-node test (RULE 0) — too heavy for the alloc hot path
+				 * 2-node test (budget) — too heavy for the alloc hot path
xfs/libxfs/xfs_alloc.c:4341
-	 * ccloop P-DBLALLOC (RULE 4, dir_reuse_coherency 2/tcp): the proven
+	 * ccloop P-DBLALLOC (instrumented, dir_reuse_coherency 2/tcp): the proven
xfs/libxfs/xfs_alloc.c:4363
-	 * plain read per DATA alloc; the 40000 cap bounds worst-case perf (RULE 0)
+	 * plain read per DATA alloc; the 40000 cap bounds worst-case perf (budget)
xfs/libxfs/xfs_log_format.h:276
- * MXFS clean-release marker (sess403 RULE-5 ruling, D-FOREIGN-REPLAY-UNGATED-
+ * MXFS clean-release marker (sess403 design-consult ruling, D-FOREIGN-REPLAY-UNGATED-
xfs/libxfs/xfs_log_format.h:562
- * sess95 RULE-5 ruling, release blocker 1 (the epoch NAMESPACE problem): an
+ * sess95 design-consult ruling, release blocker 1 (the epoch NAMESPACE problem): an
xfs/libxfs/xfs_log_format.h:588
- * sess94 step 5.2, RULE-5 ruling item (a): the explicit token STATUS.
+ * sess94 step 5.2, design-consult ruling item (a): the explicit token STATUS.
xfs/libxfs/xfs_log_format.h:621
- * sess95 step 5.3, RULE-5 ruling Q5.  The v2 set cannot express the five ways
+ * sess95 step 5.3, design-consult ruling Q5.  The v2 set cannot express the five ways
xfs/libxfs/xfs_log_format.h:677
- * (sess82 step 5.0, from a RULE-5 ruling.)  Three defects are inherent to
+ * (sess82 step 5.0, from a design-consult ruling.)  Three defects are inherent to
xfs/libxfs/xfs_log_format.h:1341
- * MXFS (sess444, RULE-5 ruling): an ICREATE record's WRITER-TIME protocol,
+ * MXFS (sess444, design-consult ruling): an ICREATE record's WRITER-TIME protocol,
xfs/libxfs/xfs_dir2_block.c:79
-		/* P-BLKRV (RULE-4): CRC fail on read of a dir3 block under
+		/* P-BLKRV (instrumented): CRC fail on read of a dir3 block under
xfs/libxfs/xfs_dir2_block.c:95
-			/* P-BLKRV (RULE-4): structural fail — same instrumentation.
+			/* P-BLKRV (instrumented): structural fail — same instrumentation.
xfs/libxfs/xfs_dir2_block.c:127
-	/* P-BLKWR (RULE-4): log every WRITE-out of a block-format dir block under
+	/* P-BLKWR (instrumented): log every WRITE-out of a block-format dir block under
xfs/libxfs/xfs_dir2_block.c:798
-	/* P-BLKLK (RULE-4): log the reading dir inode's coherence state right
+	/* P-BLKLK (instrumented): log the reading dir inode's coherence state right
xfs/libxfs/xfs_dir2_block.c:1125
-	 * sess69 RULE-4 PROBE (always-on, ratelimited): the leaf->block reshape
+	 * sess69 INSTRUMENTED PROBE (always-on, ratelimited): the leaf->block reshape
xfs/libxfs/xfs_dir2_block.c:1263
-	/* sess62 (RULE 4): UNGATED — is sf->block conversion even being called?
+	/* sess62 (instrumented): UNGATED — is sf->block conversion even being called?
xfs/libxfs/xfs_dir2_block.c:1368
-	 * sess15 (RULE 4, GPT-confirmed #1 hypothesis): dump the FULL in-core
+	 * sess15 (instrumented, GPT-confirmed #1 hypothesis): dump the FULL in-core
xfs/libxfs/xfs_dir2_priv.h:191
-/* sess10(a9a03929) RULE-4 dir-block content probes (xfs_dir2_readdir.c) */
+/* sess10(a9a03929) instrumented dir-block content probes (xfs_dir2_readdir.c) */
xfs/libxfs/xfs_dir2_sf.c:422
-	 * sess58 RULE-4 ALWAYS-ON modify-time detector for the concurrent
+	 * sess58 instrumented ALWAYS-ON modify-time detector for the concurrent
xfs/libxfs/xfs_dir2_sf.c:950
-	 * sess110 DIAGNOSTIC (RULE 4): the cache_coherency NULL-pointer Oops is
+	 * sess110 DIAGNOSTIC (instrumented): the cache_coherency NULL-pointer Oops is
xfs/libxfs/xfs_ag.h:147
-	 * (P-INOCL-COLDREAD) — fence action pending RULE-4 proof.
+	 * (P-INOCL-COLDREAD) — fence action pending instrumented proof.
xfs/libxfs/xfs_ag.h:331
-	 * sess123 (ccloop, Gemini RULE-5 redesign): monotonic counter bumped
+	 * sess123 (ccloop, Gemini design-consult redesign): monotonic counter bumped
xfs/libxfs/xfs_ag.h:398
-	 * sess391 (RULE-5 ruling ccloop-c7ee71c6-sess391-GPT-ruling-ag-handoff-
+	 * sess391 (design-consult ruling ccloop-c7ee71c6-sess391-GPT-ruling-ag-handoff-
xfs/libxfs/xfs_ag.h:434
-	 * 0.23.0 (sess392, RULE-5 ruling ccloop-c7ee71c6-sess392-GPT-ruling-
+	 * 0.23.0 (sess392, design-consult ruling ccloop-c7ee71c6-sess392-GPT-ruling-
xfs/libxfs/xfs_ag.h:462
-	 * sess430 (D-0351 containment, RULE-5 ruling ccloop-c7ee71c6-sess430-
+	 * sess430 (D-0351 containment, design-consult ruling ccloop-c7ee71c6-sess430-
xfs/libxfs/xfs_ag.h:530
-	 * RULE-5 ruling ccloop-c7ee71c6-sess427-GPT-ruling-free-publish-
+	 * Design-consult ruling ccloop-c7ee71c6-sess427-GPT-ruling-free-publish-
dlm/dlm.c:124
- * ROOT (PROVEN, RULE 4): during 8-node cluster FORMATION the membership ramps
+ * ROOT (PROVEN, instrumented): during 8-node cluster FORMATION the membership ramps
dlm/dlm.c:761
-        /* sess1(a9a03929) RULE-4 (run45 120s ino-131 wedge): make EVERY
+        /* sess1(a9a03929) instrumented (run45 120s ino-131 wedge): make EVERY
dlm/dlm.c:777
-/* sess35 (ccloop) ROOT FIX for the dir_reuse 2/tcp ~6s handoff (RULE 4 PROVEN:
+/* sess35 (ccloop) ROOT FIX for the dir_reuse 2/tcp ~6s handoff (PROVEN BY INSTRUMENT:
dlm/dlm.c:974
-             * sess425 (RULE 4 proven, tests/tauth/concurrent_release_test):
+             * sess425 (proven by instrument, tests/tauth/concurrent_release_test):
dlm/dlm.c:1177
-    /* sess50(ccloop) RULE-4: log the dir_epoch this master SENDS on an EX grant
+    /* sess50(ccloop) instrumented: log the dir_epoch this master SENDS on an EX grant
dlm/dlm.c:1342
-     * sess425 (D-0344, RULE 4 proven by dlm_ledger_test 7b): the identity
+     * sess425 (D-0344, proven by dlm_ledger_test 7b): the identity
dlm/dlm.c:2351
-         * sess426 (D-0345, RULE 4 proven in tests/tauth/unowned_page_test):
+         * sess426 (D-0345, proven by instrument in tests/tauth/unowned_page_test):
dlm/dlm.c:2595
-            /* sess425 (D-0345 RULE 4): a DEFER that repeats forever is a
+            /* sess425 (D-0345 instrumented): a DEFER that repeats forever is a
dlm/dlm.c:2619
-             * sess428 (D-0349, RULE-5 ruling option (c)): one durable
+             * sess428 (D-0349, design-consult ruling option (c)): one durable
dlm/dlm.c:3511
- * sess425 (RULE 4 proven, tests/tauth/concurrent_release_test s5): a LOCAL
+ * sess425 (proven by instrument, tests/tauth/concurrent_release_test s5): a LOCAL
dlm/dlm.c:3576
-     * sess424 (RULE-5 ruling ccmemory ccloop-c7ee71c6-sess424-GPT-ruling-
+     * sess424 (design-consult ruling ccmemory ccloop-c7ee71c6-sess424-GPT-ruling-
dlm/dlm.c:4572
- * sess425 (RULE 4 instrumentation, rig test3 'lock request failed after 10
+ * sess425 (instrumentation, rig test3 'lock request failed after 10
dlm/dlm.c:4620
-        /* D7 RULE-4 probe: quantify the settle-gate share of slow EX
+        /* D7 instrumented probe: quantify the settle-gate share of slow EX
dlm/dlm.c:4755
-             * sess-tcp (RULE 4 PROVEN): pend->status carries the master's
+             * sess-tcp (PROVEN BY INSTRUMENT): pend->status carries the master's
dlm/dlm.c:4795
-                /* sess425 (D-0345 RULE 4): which master we asked, under
+                /* sess425 (D-0345 instrumented): which master we asked, under
dlm/dlm.c:4854
-                 * sess425 (RULE 4: formation_test lap 'fails=1', rig test3
+                 * sess425 (instrumented: formation_test lap 'fails=1', rig test3
dlm/dlm.c:4926
-                            /* sess7(a9a03929) FIX-17 (RULE-4 PROVEN, run90
+                            /* sess7(a9a03929) FIX-17 (PROVEN BY INSTRUMENT, run90
dlm/dlm.c:4998
-                         * sess5 (ccloop a16ec5f2) EPOCH-HOLE FIX (RULE 4 —
+                         * sess5 (ccloop a16ec5f2) EPOCH-HOLE FIX (instrumented —
dlm/dlm.c:5184
-        /* sess1(ccloop a9a03929) RULE-4 PROVEN (run44 wedge): this was the
+        /* sess1(ccloop a9a03929) PROVEN BY INSTRUMENT (run44 wedge): this was the
dlm/dlm.c:5419
-                        /* sess4(a16ec5f2) INVARIANT GUARD (RULE 4, proven in
+                        /* sess4(a16ec5f2) INVARIANT GUARD (instrumented, proven in
dlm/dlm.c:5620
-     * 0.75.53 (D-0922, RULE 4 stack-proven on the two-node TCP rig): a
+     * 0.75.53 (D-0922, instrumented stack-proven on the two-node TCP rig): a
dlm/dlm.c:5898
-     * sess4(a16ec5f2) ROOT FIX (RULE 4, proven runs 19+20 by P4L pointer
+     * sess4(a16ec5f2) ROOT FIX (instrumented, proven runs 19+20 by P4L pointer
dlm/dlm.c:6154
- * sess7(a9a03929) FIX-20b (RULE-4 PROVEN, run91/94): send an UNCONDITIONAL
+ * sess7(a9a03929) FIX-20b (PROVEN BY INSTRUMENT, run91/94): send an UNCONDITIONAL
dlm/dlm.c:6597
- * sess10 (ccloop 72513a13, RULE-4 PROVEN 32/tcp fio cascade): a node whose
+ * sess10 (ccloop 72513a13, PROVEN BY INSTRUMENT 32/tcp fio cascade): a node whose
dlm/dlm.c:7611
-                /* sess48 (RULE 4): dump the master's live lock chain for this
+                /* sess48 (instrumented): dump the master's live lock chain for this
dlm/dlm.c:7667
-        /* sess53(ccloop) RULE-4 under-fire probe: the grantee logs
+        /* sess53(ccloop) instrumented under-fire probe: the grantee logs
dlm/dlm.c:7802
-        /* sess425 (D-0345 RULE 4): name the disagreement — the sender's
+        /* sess425 (D-0345 instrumented): name the disagreement — the sender's
dlm/dlm.c:7826
-                /* sess425 (D-0345 RULE 4): we ARE the master under our view
+                /* sess425 (D-0345 instrumented): we ARE the master under our view
dlm/dlm.c:8069
-                 * sess8/sess12 (2/tcp) DOUBLE-GRANT ROOT (RULE 4 PROVEN via
+                 * sess8/sess12 (2/tcp) DOUBLE-GRANT ROOT (PROVEN BY INSTRUMENT via
dlm/dlm.c:8144
-            /* P1-AGCONFLICT (RULE 4, master-side attribution for the
+            /* P1-AGCONFLICT (instrumented, master-side attribution for the
dlm/dlm.c:8369
-     * (RULE 4 PROVEN, run5 r23 node4_f1.md5 clobber): the OLD order
+     * (PROVEN BY INSTRUMENT, run5 r23 node4_f1.md5 clobber): the OLD order
dlm/dlm.h:356
-    /* sess425 RULE-4 instrumentation: which dlm_lock_impl site answered
+    /* sess425 instrumentation: which dlm_lock_impl site answered
dlm/dlm.md:141
-- (uncommitted, ccloop 72513a13 sess10) **`mxfs_dlm_withdraw_release_all()` — wire-release on force-shutdown withdraw.** RULE-4 root cause for a 32-node fio cascade: when a node's filesystem force-shuts down, it can no longer service BASTs for the locks it still holds, but those GRANTED entries (and any queued WAITING entries) remained live in every master's lock table. A peer with a conflicting request against one of those entries then stalled to its terminal 120s lock-wait timeout and force-shut down itself — one root-cause node produced five cascading peer shutdowns in one fio@32 run (peers died with rc=-110 on ino=128 whose PR holder set included the dead node). Fix: `mxfs_dlm_withdraw_release_all(ctx)` snapshots every `mxfs_lock` entry owned by the local node across all buckets (two-pass count-then-fill under `table_rwlock`, released before unlocking) and runs the normal `mxfs_dlm_unlock()` path on each resource — locally-mastered entries get `promote_waiters()` + grant/BAST as usual; remotely-mastered entries send `LOCK_RELEASE`. A dead FS holds no valid cache, so releasing everything on withdrawal is always safe; peers then promote immediately instead of waiting out the full timeout against an unreachable holder. Wired from the withdraw path in `dlm/v5_mount.c` (TCP-DLM ctx only — CAW's disklock-heartbeat-driven purge already handles the equivalent case for the CAW transport). Changes: dlm.c (new function), dlm.h (declaration), v5_mount.c (call site).
+- (uncommitted, ccloop 72513a13 sess10) **`mxfs_dlm_withdraw_release_all()` — wire-release on force-shutdown withdraw.** instrumented root cause for a 32-node fio cascade: when a node's filesystem force-shuts down, it can no longer service BASTs for the locks it still holds, but those GRANTED entries (and any queued WAITING entries) remained live in every master's lock table. A peer with a conflicting request against one of those entries then stalled to its terminal 120s lock-wait timeout and force-shut down itself — one root-cause node produced five cascading peer shutdowns in one fio@32 run (peers died with rc=-110 on ino=128 whose PR holder set included the dead node). Fix: `mxfs_dlm_withdraw_release_all(ctx)` snapshots every `mxfs_lock` entry owned by the local node across all buckets (two-pass count-then-fill under `table_rwlock`, released before unlocking) and runs the normal `mxfs_dlm_unlock()` path on each resource — locally-mastered entries get `promote_waiters()` + grant/BAST as usual; remotely-mastered entries send `LOCK_RELEASE`. A dead FS holds no valid cache, so releasing everything on withdrawal is always safe; peers then promote immediately instead of waiting out the full timeout against an unreachable holder. Wired from the withdraw path in `dlm/v5_mount.c` (TCP-DLM ctx only — CAW's disklock-heartbeat-driven purge already handles the equivalent case for the CAW transport). Changes: dlm.c (new function), dlm.h (declaration), v5_mount.c (call site).
dlm/dlm_caw.c:73
- * sess380 RULE-4 instrumentation for D-HOT-SLOT-CAW-SERIALIZES-LUN-PER-LBA-379.
+ * sess380 instrumentation for D-HOT-SLOT-CAW-SERIALIZES-LUN-PER-LBA-379.
dlm/dlm_caw.c:194
- * The sess380 RULE-5 review approved this, NARROWED, with conditions that are
+ * The sess380 design-consult review approved this, NARROWED, with conditions that are
dlm/dlm_caw.c:486
- * ccloop c7ee71c6 sess24 — FAIR-HANDOFF EPISODE CLOCK (a real bug; RULE-5 GPT
+ * ccloop c7ee71c6 sess24 — FAIR-HANDOFF EPISODE CLOCK (a real bug; design-consult GPT
dlm/dlm_caw.c:635
- * sess470 (D-AGLOCK-ORPHAN-EX-TRACKING-LOSS-LIVELOCK-488, RULE-5 disposition
+ * sess470 (D-AGLOCK-ORPHAN-EX-TRACKING-LOSS-LIVELOCK-488, design-consult disposition
dlm/dlm_caw.c:780
- * sess113 design; RULE-5 review sess449).  One-shot: the armed value is the
+ * sess113 design; design-consult review sess449).  One-shot: the armed value is the
dlm/dlm_caw.c:1050
- * The test is not a field whitelist (the RULE-5 review rejected that shape:
+ * The test is not a field whitelist (the design-consult review rejected that shape:
dlm/dlm_caw.c:1423
- * ccloop 72513a13 sess3 (RULE-4 PROVEN): probe-chain walks dominated per-op
+ * ccloop 72513a13 sess3 (PROVEN BY INSTRUMENT): probe-chain walks dominated per-op
dlm/dlm_caw.c:1552
-	 * sess407 (RULE-5 ruling Q4): nobody may ADD a protected node's EX/PW
+	 * sess407 (design-consult ruling Q4): nobody may ADD a protected node's EX/PW
dlm/dlm_caw.c:1667
-		 * sess407 STRUCTURAL rule (RULE-5 ruling Q3), independent of
+		 * sess407 STRUCTURAL rule (design-consult ruling Q3), independent of
dlm/dlm_caw.c:1792
-		 * sess135 (ccloop 14d31183) P135-SLOTWR — RULE-4 instrumentation
+		 * sess135 (ccloop 14d31183) P135-SLOTWR — instrumentation
dlm/dlm_caw.c:1875
- * sess119 (sess118 RULE-5 ruling item 3 — AMBIGUOUS CAW RESULT).
+ * sess119 (sess118 design-consult ruling item 3 — AMBIGUOUS CAW RESULT).
dlm/dlm_caw.c:1944
- * sess108 (RULE-5 ruling, blocker 7): mint the next write-authority token for
+ * sess108 (design-consult ruling, blocker 7): mint the next write-authority token for
dlm/dlm_caw.c:1982
- * sess169 (D-EX-GRANT-EPOCH-NOT-UNIQUE-TENURE-ID, RULE-5 ruled): the token
+ * sess169 (D-EX-GRANT-EPOCH-NOT-UNIQUE-TENURE-ID, design-consult ruled): the token
dlm/dlm_caw.c:2206
- * sess176 (lineage discriminator, sess175 RULE-5 ruling): mint the random
+ * sess176 (lineage discriminator, sess175 design-consult ruling): mint the random
dlm/dlm_caw.c:3466
- * WHY (sess115 RULE-5 ruling): "a destructive disk transition may occur only
+ * WHY (sess115 design-consult ruling): "a destructive disk transition may occur only
dlm/dlm_caw.c:4307
-     * sess402 (D-TMPFILE-CHURN-KILL-FOREIGN-REPLAY-EFSCORRUPTED-402, RULE 4
+     * sess402 (D-TMPFILE-CHURN-KILL-FOREIGN-REPLAY-EFSCORRUPTED-402, instrumented
dlm/dlm_caw.c:4401
- * sess47: CLAIM-RACE detector (RULE-4 diagnostic for the bnobt stale-pristine
+ * sess47: CLAIM-RACE detector (instrumented diagnostic for the bnobt stale-pristine
dlm/dlm_caw.c:6528
- * sess299 (D-32NODE-SHARED-DIR-CREATE-PACE, RULE-5 sess298 ruling): the
+ * sess299 (D-32NODE-SHARED-DIR-CREATE-PACE, design-consult sess298 ruling): the
dlm/dlm_caw.c:6569
- * sess108 (RULE-5 ruling, ordering item): validate the nominated waiter BEFORE
+ * sess108 (design-consult ruling, ordering item): validate the nominated waiter BEFORE
dlm/dlm_caw.c:6600
- * sess285 (D-DLMFAIRNESS-32CAW-HOTDIR-EX-STARVATION-501) RULE-4 probe: one
+ * sess285 (D-DLMFAIRNESS-32CAW-HOTDIR-EX-STARVATION-501) instrumented probe: one
dlm/dlm_caw.c:6811
-	 * ccloop c7ee71c6 sess24 (RULE-5 GPT consult) — CAW ATTEMPT CENSUS.
+	 * ccloop c7ee71c6 sess24 (design-consult GPT consult) — CAW ATTEMPT CENSUS.
dlm/dlm_caw.c:7006
-		/* v0.10.43 (RULE-4): when an INODE acquire is stuck far past a
+		/* v0.10.43 (instrumented): when an INODE acquire is stuck far past a
dlm/dlm_caw.c:7182
-				 * DIRECT-HANDOFF RACE (sess374 RULE-5 review
+				 * DIRECT-HANDOFF RACE (sess374 design-consult review
dlm/dlm_caw.c:7607
-					/* ccloop c7ee71c6 sess24 RULE-4: the
+					/* ccloop c7ee71c6 sess24 instrumented: the
dlm/dlm_caw.c:7710
-			 * the RULE-5 GPT ruling ("any node may CAS the next
+			 * the design-consult GPT ruling ("any node may CAS the next
dlm/dlm_caw.c:7923
-		 * sess386 (RULE-5 ruling, bounded-reserve fix for the 474
+		 * sess386 (design-consult ruling, bounded-reserve fix for the 474
dlm/dlm_caw.c:8232
- * sess404 telemetry for the bounded-courtesy ticket override (RULE-5 ruling
+ * sess404 telemetry for the bounded-courtesy ticket override (design-consult ruling
dlm/dlm_caw.c:8321
-	/* ccloop RULE4: prove the hot-slot CAS-storm exhaustion mechanism for
+	/* ccloop instrumented: prove the hot-slot CAS-storm exhaustion mechanism for
dlm/dlm_caw.c:8414
-	 * sess433 (D-0354, RULE-5 ruling ccloop-c7ee71c6-sess433-GPT-ruling-
+	 * sess433 (D-0354, design-consult ruling ccloop-c7ee71c6-sess433-GPT-ruling-
dlm/dlm_caw.c:8593
-			/* daf50d34 sess2 ROOT FIX (mkdir-storm dirent loss, RULE-4
+			/* daf50d34 sess2 ROOT FIX (mkdir-storm dirent loss, instrumented
dlm/dlm_caw.c:8722
-				 * sess40 RULE-4: a claim that miscompares
+				 * sess40 instrumented: a claim that miscompares
dlm/dlm_caw.c:9463
-						 * Fix (GPT-reviewed, RULE 5):
+						 * Fix (GPT-reviewed, design-consult):
dlm/dlm_caw.c:9481
-						 * CREATE-ETIMEDOUT-404, RULE-5 ruling
+						 * CREATE-ETIMEDOUT-404, design-consult ruling
dlm/dlm_caw.c:9840
-			 * D-AGLOCK-...-LIVELOCK-488 (sess243 RULE-5 ruling):
+			 * D-AGLOCK-...-LIVELOCK-488 (sess243 design-consult ruling):
dlm/dlm_caw.c:10085
-	/* ccloop RULE4: dump WHICH CAS site exhausted the retry budget so we
+	/* ccloop instrumented: dump WHICH CAS site exhausted the retry budget so we
dlm/dlm_caw.c:10305
- * sess386 (RULE-5 ruling, D-NOINO-RELFENCE-AIL-FREEZE-474 leg A): acquire with
+ * sess386 (design-consult ruling, D-NOINO-RELFENCE-AIL-FREEZE-474 leg A): acquire with
dlm/dlm_caw.c:10870
-				 * sess299 STICKY TICKET (RULE-5 sess298
+				 * sess299 STICKY TICKET (design-consult sess298
dlm/dlm_caw.c:11555
- * ccloop cc87fed3 sess3 (RULE 4 PROVEN — fence_during_write@8/caw D-state
+ * ccloop cc87fed3 sess3 (PROVEN BY INSTRUMENT — fence_during_write@8/caw D-state
dlm/dlm_caw.c:12114
-/* ─── mxfs_dlm_caw_ex_count (sess52 RULE-4 concurrent-EX detector) ─────────
+/* ─── mxfs_dlm_caw_ex_count (sess52 instrumented concurrent-EX detector) ─────────
dlm/dlm_caw.c:12146
-/* ─── mxfs_dlm_caw_self_held_scan (ccloop a864 sess3, RULE-4 orphan probe) ───
+/* ─── mxfs_dlm_caw_self_held_scan (ccloop a864 sess3, instrumented orphan probe) ───
dlm/dlm_caw.c:13240
-	 * amplifier (RULE 4 step 2b, PROVEN by the test1 stack:
+	 * amplifier (instrument step 2b, PROVEN by the test1 stack:
dlm/dlm_caw.c:13269
- * sess374 (sess363 RULE-5 ruling item A: "REUSE the full-purge mutation
+ * sess374 (sess363 design-consult ruling item A: "REUSE the full-purge mutation
dlm/dlm_caw.c:13747
- * sess374 (D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356, sess363 RULE-5 ruling).
+ * sess374 (D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356, sess363 design-consult ruling).
dlm/dlm_caw.c:13802
- * sess376 (RULE-5 review): every strip attempt gets an id.  The injected pause
+ * sess376 (design-consult review): every strip attempt gets an id.  The injected pause
dlm/dlm_caw.c:14282
-	 * RULE-4 step 1).  A demand scrub that strips nothing is SILENT: every
+	 * instrument step 1).  A demand scrub that strips nothing is SILENT: every
dlm/dlm_caw.c:14297
-	 * SKIP-ONLY candidate filter (RULE-5 review items 2+3).  Restrict to
+	 * SKIP-ONLY candidate filter (design-consult review items 2+3).  Restrict to
dlm/dlm_caw.c:15124
-		 * 0.41.1 (sess434, D-0354 candidate A stop-ship 3 — RULE-5
+		 * 0.41.1 (sess434, D-0354 candidate A stop-ship 3 — design-consult
dlm/dlm_caw.c:16812
-/* sess374 (RULE-5 review items 2+3): skip-only candidate hint — see
+/* sess374 (design-consult review items 2+3): skip-only candidate hint — see
dlm/dlm_caw.h:163
-     * D-AGLOCK-...-LIVELOCK-488 (sess243 RULE-5 ruling, Option B'): sticky
+     * D-AGLOCK-...-LIVELOCK-488 (sess243 design-consult ruling, Option B'): sticky
dlm/dlm_caw.h:292
-     * sess176 (D-FOREIGN-REPLAY lineage discriminator, sess175 RULE-5
+     * sess176 (D-FOREIGN-REPLAY lineage discriminator, sess175 design-consult
dlm/dlm_caw.h:372
- * ─── sess128 BAST DISPATCH QUEUE (RULE-4 rooted, GPT RULE-5 reviewed) ───
+ * ─── sess128 BAST DISPATCH QUEUE (instrumented rooted, design-consult reviewed) ───
dlm/dlm_caw.h:650
-    /* sess374 (RULE-5 review items 2+3): SKIP-ONLY candidate hint — bit i set
+    /* sess374 (design-consult review items 2+3): SKIP-ONLY candidate hint — bit i set
dlm/dlm_caw.h:790
-     * sharing grant_meta's mutex would be scheduling-while-atomic.  RULE-4
+     * sharing grant_meta's mutex would be scheduling-while-atomic.  Instrumented
dlm/dlm_caw.h:1235
-/* Teardown drain budget.  Unmount is not a RULE-0 workload path, but this
+/* Teardown drain budget.  Unmount is not a the budget rule workload path, but this
dlm/dlm_caw.h:1299
- * safety deadline, not a RULE-0 performance budget.
+ * safety deadline, not a the budget rule performance budget.
dlm/dlm_caw.h:1834
- * sess374 (D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356, sess363 RULE-5 ruling).
+ * sess374 (D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356, sess363 design-consult ruling).
dlm/dlm_caw.md:519
-     result could mistake a shared hold for exclusive (RULE-4 proven
+     result could mistake a shared hold for exclusive (proven by instrument
dlm/peer.c:856
-    /* ccloop c7ee71c6 sess12 (16/tcp join-storm false death, RULE-4
+    /* ccloop c7ee71c6 sess12 (16/tcp join-storm false death, instrumented
dlm/disklock.c:127
- * (cache_coherency / crash_consistency / dir_reuse verify convoys) — RULE-4
+ * (cache_coherency / crash_consistency / dir_reuse verify convoys) — instrumented
dlm/disklock.c:249
- * Lifecycle (GPT sess83 RULE-5 ruling):
+ * Lifecycle (GPT sess83 design-consult ruling):
dlm/disklock.c:1013
- * sess453 (0.60.0, RULE-5 review #3 condition 8 / hazard H8): every
+ * sess453 (0.60.0, design-consult review #3 condition 8 / hazard H8): every
dlm/disklock.c:1100
- * sess451 (RULE-5 STOP-SHIP blocker 1): after a CAS mismatch the caller may
+ * sess451 (design-consult STOP-SHIP blocker 1): after a CAS mismatch the caller may
dlm/disklock.c:1141
- * incarnation is a protocol violation: UNKNOWN, loudly.  sess452 (RULE-5
+ * incarnation is a protocol violation: UNKNOWN, loudly.  sess452 (design-consult
dlm/disklock.c:1688
- * sess383 (RULE-5 ruling Q2/Q5.1): the STRUCTURAL verdict of one already-read
+ * sess383 (design-consult ruling Q2/Q5.1): the STRUCTURAL verdict of one already-read
dlm/disklock.c:2369
-        /* sess38 (D-RELABORT-...-SELFFENCE RULE-4): time every hb write and
+        /* sess38 (D-RELABORT-...-SELFFENCE instrumented): time every hb write and
dlm/disklock.c:2647
-                             * sess383 (RULE-5 ruling Q1): the old arm fired
+                             * sess383 (design-consult ruling Q1): the old arm fired
dlm/disklock.c:3094
-                         * RULE-4 proof that stale HB reads occur.
+                         * Instrumented proof that stale HB reads occur.
dlm/disklock.c:4177
-        /* sess419 (RULE-5 ruling, D-PURGE-NONATOMIC-PUBLICATION stop-ship
+        /* sess419 (design-consult ruling, D-PURGE-NONATOMIC-PUBLICATION stop-ship
dlm/disklock.c:4295
-     * sess444 (RULE 0, chain 32 on 0.51.0): the record scan read the table
+     * sess444 (budget, chain 32 on 0.51.0): the record scan read the table
dlm/disklock.c:5780
-     * recovery_claim() / recovery_takeover().  GPT ruling (RULE 5, sess93,
+     * recovery_claim() / recovery_takeover().  GPT ruling (design-consult, sess93,
dlm/disklock.c:6740
- * sess374 (sess363 RULE-5 ruling): the shared state+verdict predicate behind
+ * sess374 (sess363 design-consult ruling): the shared state+verdict predicate behind
dlm/disklock.c:6771
-     * sess374 (RULE-5 review items 4+5): the IDENTITY BINDING, and the reason
+     * sess374 (design-consult review items 4+5): the IDENTITY BINDING, and the reason
dlm/disklock.c:6926
-     * FAIL CLOSED on liveness, in TWO independent ways (RULE-5 review item 4:
+     * FAIL CLOSED on liveness, in TWO independent ways (design-consult review item 4:
dlm/disklock.c:6958
- * sess330 (RULE-5 ruling): leaseless backfill of the LEGACY intent-path
+ * sess330 (design-consult ruling): leaseless backfill of the LEGACY intent-path
dlm/disklock.c:7003
-            /* sess383 (RULE-5 ruling Q2): a pre-mkfs ghost is not this
+            /* sess383 (design-consult ruling Q2): a pre-mkfs ghost is not this
dlm/disklock.c:7696
-     * the alternative is not established.  The sess91 RULE-5 ruling's item 3
+     * the alternative is not established.  The sess91 design-consult ruling's item 3
dlm/disklock.c:9130
- * RULE-5 ruling): durably GIVE BACK every recovery lease this incarnation
+ * Design-consult ruling): durably GIVE BACK every recovery lease this incarnation
dlm/disklock.c:10014
- * sess130 (ccloop, RULE 4 PROVEN): the slot claim was a non-atomic
+ * sess130 (ccloop, PROVEN BY INSTRUMENT): the slot claim was a non-atomic
dlm/disklock.c:10050
- * D-QUARANTINED-SLOT-EXHAUSTS-CLUSTER-ADMISSION-376, sess377 RULE-5 ruling
+ * D-QUARANTINED-SLOT-EXHAUSTS-CLUSTER-ADMISSION-376, sess377 design-consult ruling
dlm/disklock.c:10629
-     * sess440 (§6.2, RULE-5 ruling Q2): a whole-cluster bootstrap owner
+     * sess440 (§6.2, design-consult ruling Q2): a whole-cluster bootstrap owner
dlm/disklock.c:11541
-         * sess451 (RULE-5 STOP-SHIP blocker 2): a RETIRE_PENDING stamp is
+         * sess451 (design-consult STOP-SHIP blocker 2): a RETIRE_PENDING stamp is
dlm/disklock.c:11595
- * though it advanced inside the nominal window.  (GPT RULE-5 review, sess54
+ * though it advanced inside the nominal window.  (design-consult review, sess54
dlm/disklock.h:65
- * sess450 (D-0356 / D-377 two-phase departure, sess449 RULE-5 ruling):
+ * sess450 (D-0356 / D-377 two-phase departure, sess449 design-consult ruling):
dlm/disklock.h:139
- * RULE-5 ruling ccmemory ccloop-c7ee71c6-sess464-GPT-ruling-d0523-claim-wait-
+ * Design-consult ruling ccmemory ccloop-c7ee71c6-sess464-GPT-ruling-d0523-claim-wait-
dlm/disklock.h:269
- * sess64 — the DURABLE RECOVERY DESCRIPTOR (GPT RULE-5 ruling, sess63).
+ * sess64 — the DURABLE RECOVERY DESCRIPTOR (design-consult ruling, sess63).
dlm/disklock.h:516
- * sess440 (docs/whole-cluster-restart.md §6.2, RULE-5 ruling Q2): the
+ * sess440 (docs/whole-cluster-restart.md §6.2, design-consult ruling Q2): the
dlm/disklock.h:985
- * RULE-5 ruling): WRITE-TIME provenance for a SINGLE_NODE_LOCAL_LOG
+ * Design-consult ruling): WRITE-TIME provenance for a SINGLE_NODE_LOCAL_LOG
dlm/disklock.h:1003
- * 0.41.1 (sess434, D-0354 lap 2 — RULE-5 ruling): WRITE-TIME provenance that
+ * 0.41.1 (sess434, D-0354 lap 2 — design-consult ruling): WRITE-TIME provenance that
dlm/disklock.h:1020
- * sess441 (docs/whole-cluster-restart.md §6.5 shape B, RULE-5 ruling): this
+ * sess441 (docs/whole-cluster-restart.md §6.5 shape B, design-consult ruling): this
dlm/disklock.h:1108
- * sess438 (docs/whole-cluster-restart.md item 2, RULE-5 ruling ccmemory
+ * sess438 (docs/whole-cluster-restart.md item 2, design-consult ruling ccmemory
dlm/disklock.h:1356
- * flag's comment).  TRI-STATE plus one special case (sess451, RULE-5
+ * flag's comment).  TRI-STATE plus one special case (sess451, design-consult
dlm/disklock.h:1372
- * sess452 (0.59.2, RULE-5 STOP-SHIP #2 blockers 3 and 4): there is no OWN
+ * sess452 (0.59.2, design-consult STOP-SHIP #2 blockers 3 and 4): there is no OWN
dlm/disklock.h:1424
- * sess419 (D-PURGE-NONATOMIC-PUBLICATION verification, RULE-5 ruling):
+ * sess419 (D-PURGE-NONATOMIC-PUBLICATION verification, design-consult ruling):
dlm/disklock.h:1473
- * sess383 (RULE-5 ruling): the callback now RETURNS a disposition instead of
+ * sess383 (design-consult ruling): the callback now RETURNS a disposition instead of
dlm/disklock.h:1833
-    /* sess407 (RULE-5 ruling Q1): gen validation, mask installation and the
+    /* sess407 (design-consult ruling Q1): gen validation, mask installation and the
dlm/disklock.h:1960
- * sess435 (D-0359 step 1, RULE-5 ruling): OPERATIONAL CAW capability of the
+ * sess435 (D-0359 step 1, design-consult ruling): OPERATIONAL CAW capability of the
dlm/disklock.h:2061
- * sess454 (0.61.0, RULE-5 design ruling D1): the ABSENT settlement no longer
+ * sess454 (0.61.0, design-consult design ruling D1): the ABSENT settlement no longer
dlm/disklock.h:2736
- * sess374 (sess363 RULE-5 ruling, D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356):
+ * sess374 (sess363 design-consult ruling, D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356):
dlm/disklock.h:2820
- * sess330 (RULE-5 ruling): leaseless terminalization of the LEGACY
+ * sess330 (design-consult ruling): leaseless terminalization of the LEGACY
dlm/disklock.h:3006
- * sess451 (RULE-5 STOP-SHIP blocker 2): a RETIRE_PENDING stamp — a clean
+ * sess451 (design-consult STOP-SHIP blocker 2): a RETIRE_PENDING stamp — a clean
dlm/disklock.md:272
-`docs/whole-cluster-restart.md` item 2 (RULE-5 ruling: ccmemory
+`docs/whole-cluster-restart.md` item 2 (design-consult ruling: ccmemory
dlm/disklock.md:329
-RULE-5 review of 0.58.0 (re-stamp RELEASED → WITHDRAWN on a failed
+design-consult review of 0.58.0 (re-stamp RELEASED → WITHDRAWN on a failed
dlm/disklock.md:382
-The sess450 RULE-5 review (ccmemory `ccloop-c7ee71c6-sess450-GPT-ruling-
+The sess450 design-consult review (ccmemory `ccloop-c7ee71c6-sess450-GPT-ruling-
dlm/disklock.md:447
-The sess451 RULE-5 review (ccmemory `ccloop-c7ee71c6-sess451-GPT-ruling-0591-
+The sess451 design-consult review (ccmemory `ccloop-c7ee71c6-sess451-GPT-ruling-0591-
dlm/disklock.md:524
-RULE-5 review #3 condition 8 / design ruling D7 and its companion (see
+design-consult review #3 condition 8 / design ruling D7 and its companion (see
dlm/disklock.md:613
-**Rule** (RULE-5 ruling, ccmemory
+**Rule** (design-consult ruling, ccmemory
dlm/scsipr.c:368
-     * sess433 (D-379(B) / D-0355, sess432 RULE-5 ruling): -EEXIST means
+     * sess433 (D-379(B) / D-0355, sess432 design-consult ruling): -EEXIST means
dlm/scsipr.c:1453
-     * sess381 RULE-5 ruling: NEVER issue a PREEMPT whose SARK is our own key.
+     * sess381 design-consult ruling: NEVER issue a PREEMPT whose SARK is our own key.
dlm/scsipr.c:1896
- * that the recovery stops rather than publishing over it.  GPT (RULE-5 ruling
+ * that the recovery stops rather than publishing over it.  GPT (design-consult ruling
dlm/scsipr.h:138
-     * sess452 (0.59.2, RULE-5 STOP-SHIP #2 blockers 1 and 4): the key-state
+     * sess452 (0.59.2, design-consult STOP-SHIP #2 blockers 1 and 4): the key-state
dlm/scsipr.h:188
-     * sess454 (0.61.0, RULE-5 design ruling D6): THE SINGLE-USE ABSENCE
+     * sess454 (0.61.0, design-consult design ruling D6): THE SINGLE-USE ABSENCE
dlm/scsipr.h:449
- * Deriving this from `kind` would be wrong and the sess381 RULE-5 ruling says
+ * Deriving this from `kind` would be wrong and the sess381 design-consult ruling says
dlm/scsipr.h:516
- * sess451 (D-377 / D-0356 RULE-5 STOP-SHIP blocker 3, merge criteria 2, 5,
+ * sess451 (D-377 / D-0356 design-consult STOP-SHIP blocker 3, merge criteria 2, 5,
dlm/scsipr.h:535
- * sess452 (0.59.2, RULE-5 STOP-SHIP #2): the proof is now COHERENT and
+ * sess452 (0.59.2, design-consult STOP-SHIP #2): the proof is now COHERENT and
dlm/scsipr.h:576
- * sess454 (0.61.0, RULE-5 design ruling D1 + D6): SETTLE-ABSENT — the only
+ * sess454 (0.61.0, design-consult design ruling D1 + D6): SETTLE-ABSENT — the only
dlm/scsipr.h:833
- * fenced (GPT RULE-5 ruling, sess93 follow-up).
+ * fenced (design-consult ruling, sess93 follow-up).
dlm/v5_mount.h:108
-     * mount.  The sess93 RULE-5 ruling requires that a rig which cannot
+     * mount.  The sess93 design-consult ruling requires that a rig which cannot
dlm/v5_mount.h:190
- * sess451 (RULE-5 merge criterion 4, "serialize against any old
+ * sess451 (design-consult merge criterion 4, "serialize against any old
dlm/v5_mount.h:310
- * dmesg.  GPT (RULE-5 ruling sess93, Q2): "Add an explicit durable and
+ * dmesg.  GPT (design-consult ruling sess93, Q2): "Add an explicit durable and
dlm/v5_mount.h:391
-/* RULE 0 derivations for the bounded completion ladder (sess420 ruling 5):
+/* the budget rule derivations for the bounded completion ladder (sess420 ruling 5):
dlm/v5_mount.h:576
-/* sess171 (D-EX-GRANT-EPOCH verification vehicle, RULE-5 approved sess170):
+/* sess171 (D-EX-GRANT-EPOCH verification vehicle, design-consult approved sess170):
dlm/v5_mount.h:656
-/* sess383 (RULE-5 ruling Q1): the consumer VALIDATES and returns an
+/* sess383 (design-consult ruling Q1): the consumer VALIDATES and returns an
dlm/v5_mount.h:703
- * sess374 (sess363 RULE-5 ruling, D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356):
+ * sess374 (sess363 design-consult ruling, D-REFUSAL-GRANT-FREEZE-OUT-OF-CLOSURE-356):
dlm/v5_mount.h:758
-/* sess330 (RULE-5 ruling): leaseless terminalization of a LEGACY intent-path
+/* sess330 (design-consult ruling): leaseless terminalization of a LEGACY intent-path
dlm/v5_mount.h:967
-/* 0.23.0 (sess392 RULE-5 ruling): the dialloc candidate reserve — ONE
+/* 0.23.0 (sess392 design-consult ruling): the dialloc candidate reserve — ONE
dlm/v5_mount.c:142
- * PROVEN ROOT (RULE 4, instrumented): under the N-node create storm a peer's
+ * PROVEN ROOT (instrumented, instrumented): under the N-node create storm a peer's
dlm/v5_mount.c:289
- * sess419 — D-PURGE-NONATOMIC-PUBLICATION verification injectors (RULE-5
+ * sess419 — D-PURGE-NONATOMIC-PUBLICATION verification injectors (design-consult
dlm/v5_mount.c:532
-    /* RULE-4 instrumentation (2026-07-14): latches true the first time
+    /* instrumentation (2026-07-14): latches true the first time
dlm/v5_mount.c:551
-     * sess418 (D-TCP-WEDGE-PIN-NOOP-REPORTS-SUCCESS-0286, sess416 RULE-5
+     * sess418 (D-TCP-WEDGE-PIN-NOOP-REPORTS-SUCCESS-0286, sess416 design-consult
dlm/v5_mount.c:965
-     * restart after a whole-cluster crash (RULE 0).  It runs here
+     * restart after a whole-cluster crash (budget).  It runs here
dlm/v5_mount.c:978
-     * sess454 (0.61.0, RULE-5 design ruling D1/D6/D8): THE RETIRE SETTLE
+     * sess454 (0.61.0, design-consult design ruling D1/D6/D8): THE RETIRE SETTLE
dlm/v5_mount.c:1025
-     * GPT ruling (RULE 5, sess93, correction 1): "claim ONCE before replay and
+     * GPT ruling (design-consult, sess93, correction 1): "claim ONCE before replay and
dlm/v5_mount.c:1083
-     * sess374 (RULE-5 review items 2+3): SKIP-ONLY hint, bit i set once this
+     * sess374 (design-consult review items 2+3): SKIP-ONLY hint, bit i set once this
dlm/v5_mount.c:1092
-    /* sess374 (RULE-5 review round 2, item 1): the mask has THREE producers
+    /* sess374 (design-consult review round 2, item 1): the mask has THREE producers
dlm/v5_mount.c:1433
- * sess437 (RULE-5 ruling, ccmemory
+ * sess437 (design-consult ruling, ccmemory
dlm/v5_mount.c:1774
- * sess426 (D-0347, RULE-5 ruling ccloop-c7ee71c6-sess426-GPT-ruling-
+ * sess426 (D-0347, design-consult ruling ccloop-c7ee71c6-sess426-GPT-ruling-
dlm/v5_mount.c:2797
- * sess451 (RULE-5 merge criterion 4 / item 4 — the P305 lone-node remount
+ * sess451 (design-consult merge criterion 4 / item 4 — the P305 lone-node remount
dlm/v5_mount.c:2816
- * sess452 (0.59.2, RULE-5 STOP-SHIP #2 blockers 2 and 3): EVERY same-boot
+ * sess452 (0.59.2, design-consult STOP-SHIP #2 blockers 2 and 3): EVERY same-boot
dlm/v5_mount.c:3093
- * docs/whole-cluster-restart.md §6.1-6.4 (RULE-5 ruling ccmemory
+ * docs/whole-cluster-restart.md §6.1-6.4 (design-consult ruling ccmemory
dlm/v5_mount.c:3166
-     * sess442 (RULE-5 review (a)/S2): the unwind records NOTHING about K.
+     * sess442 (design-consult review (a)/S2): the unwind records NOTHING about K.
dlm/v5_mount.c:3220
-         * sess442 (§6.7, RULE-5 review S2): a term claimed by THIS host's
+         * sess442 (§6.7, design-consult review S2): a term claimed by THIS host's
dlm/v5_mount.c:3348
-    /* RULE-5 review SS-8: one key published by two victim records is a
+    /* design-consult review SS-8: one key published by two victim records is a
dlm/v5_mount.c:3616
- * sess442 (§6.7, RULE-5 review S2/S4): the adoption of K when the term is
+ * sess442 (§6.7, design-consult review S2/S4): the adoption of K when the term is
dlm/v5_mount.c:3855
-                 * the barrier (RULE-5 review S4) */
+                 * the barrier (design-consult review S4) */
dlm/v5_mount.c:3946
- * RULE-5 rulings: ccmemory ccloop-c7ee71c6-sess442-GPT-ruling-item5f-
+ * Design-consult rulings: ccmemory ccloop-c7ee71c6-sess442-GPT-ruling-item5f-
dlm/v5_mount.c:5089
-        /* RULE-5 review SS-9: the exemption holds only when the WHOLE
+        /* design-consult review SS-9: the exemption holds only when the WHOLE
dlm/v5_mount.c:5338
-     * §6.5 shape B (sess441 RULE-5 ruling): adopt ONE certified class-2
+     * §6.5 shape B (sess441 design-consult ruling): adopt ONE certified class-2
dlm/v5_mount.c:5360
-     * sess441 RULE-5 review (SS-1): a record left SEALED/RECOVERING/REFUSED
+     * sess441 design-consult review (SS-1): a record left SEALED/RECOVERING/REFUSED
dlm/v5_mount.c:5437
- * §5.2 + "Item 2 correction"; RULE-5 rulings in ccmemory
+ * §5.2 + "Item 2 correction"; design-consult rulings in ccmemory
dlm/v5_mount.c:5979
- * Per the sess381 RULE-5 ruling, the retry MUST NOT be driven by re-firing the
+ * Per the sess381 design-consult ruling, the retry MUST NOT be driven by re-firing the
dlm/v5_mount.c:6004
- * the sess381 RULE-5 ruling rejects it explicitly.  Instead: ONE elected node
+ * the sess381 design-consult ruling rejects it explicitly.  Instead: ONE elected node
dlm/v5_mount.c:6522
- * Reporting policy, per the sess381 RULE-5 ruling: losing the reservation while
+ * Reporting policy, per the sess381 design-consult ruling: losing the reservation while
dlm/v5_mount.c:7088
- * GPT ruling (RULE 5, sess93, Q1) — ONE DURABLE INTENT, ONE ISSUING PROVER,
+ * GPT ruling (design-consult, sess93, Q1) — ONE DURABLE INTENT, ONE ISSUING PROVER,
dlm/v5_mount.c:8275
-                    /* D8 RULE-4 probe: every tick's verdict, first 20 */
+                    /* D8 instrumented probe: every tick's verdict, first 20 */
dlm/v5_mount.c:9233
- * sess374 (RULE-5 review items 2+3): note that slot now carries a terminal
+ * sess374 (design-consult review items 2+3): note that slot now carries a terminal
dlm/v5_mount.c:9296
-     * sess383 (RULE-5 ruling Q1/Q4): the callback runs FIRST and its
+     * sess383 (design-consult ruling Q1/Q4): the callback runs FIRST and its
dlm/v5_mount.c:9331
- * whole-cluster crash every node would pay it (RULE 0).  Access to
+ * whole-cluster crash every node would pay it (budget).  Access to
dlm/v5_mount.c:9646
-     * sess421 (sess420 RULE-5 barrier ruling, item 4): the pass-1
+     * sess421 (sess420 design-consult barrier ruling, item 4): the pass-1
dlm/v5_mount.c:10038
-     * sess451 (RULE-5 STOP-SHIP blocker 2): RETIRE_PENDING slots hold the
+     * sess451 (design-consult STOP-SHIP blocker 2): RETIRE_PENDING slots hold the
dlm/v5_mount.c:10234
- * GPT ruling (RULE 5, sess93), correction 1: claim ONCE, before the replay,
+ * GPT ruling (design-consult, sess93), correction 1: claim ONCE, before the replay,
dlm/v5_mount.c:11368
-    /* sess444 (RULE 0 attribution of the ~8 s per-slot completion cost):
+    /* sess444 (the budget rule attribution of the ~8 s per-slot completion cost):
dlm/v5_mount.c:11740
-     * sess442 (RULE-5 review S3): the allow-list alone passes an EMPTY
+     * sess442 (design-consult review S3): the allow-list alone passes an EMPTY
dlm/v5_mount.c:11829
- * sess453 (0.60.0, RULE-5 review #3 condition 6 / D5 ruling): the
+ * sess453 (0.60.0, design-consult review #3 condition 6 / D5 ruling): the
dlm/v5_mount.c:12990
-     * sess435 (D-0359 step 1, RULE-5 ruling): the CAW transport is admitted
+     * sess435 (D-0359 step 1, design-consult ruling): the CAW transport is admitted
dlm/v5_mount.c:13515
- * sess433 (D-0357, RULE-5 ruling ccloop-c7ee71c6-sess433-GPT-ruling-d0357):
+ * sess433 (D-0357, design-consult ruling ccloop-c7ee71c6-sess433-GPT-ruling-d0357):
dlm/v5_mount.c:13758
-     * sess442 (§6.7, RULE-5 review S2): a mount that unwinds under an
+     * sess442 (§6.7, design-consult review S2): a mount that unwinds under an
dlm/v5_mount.c:14322
-     * sess3(ccloop a9a03929) ROOT FIX (RULE 4, PROVEN run61 t6 119.7s):
+     * sess3(ccloop a9a03929) ROOT FIX (instrumented, PROVEN run61 t6 119.7s):
dlm/v5_mount.c:14419
-         * sess386 (D-NOINO-RELFENCE-AIL-FREEZE-474 leg A, RULE-5 ruling):
+         * sess386 (D-NOINO-RELFENCE-AIL-FREEZE-474 leg A, design-consult ruling):
dlm/v5_mount.c:14668
- * RULE-5 approved sess170): drives the REAL kernel CAW lock/convert/unlock
+ * Design-consult approved sess170): drives the REAL kernel CAW lock/convert/unlock
dlm/v5_mount.c:14854
- * RULE-6 closure vehicle for D-SAMENODE-WAITER-CANCEL-COLLISION (and the
+ * defect-bar closure vehicle for D-SAMENODE-WAITER-CANCEL-COLLISION (and the
dlm/v5_mount.c:14872
- *                    OTHER attempt.  Proof sequence (sess449 RULE-5 review):
+ *                    OTHER attempt.  Proof sequence (sess449 design-consult review):
dlm/v5_mount.c:15503
-/* sess58 RULE-4 concurrent-EX discriminator for INODE locks (mirrors
+/* sess58 instrumented concurrent-EX discriminator for INODE locks (mirrors
dlm/v5_mount.c:15616
- * ccloop cc87fed3 sess3 (RULE 4 PROVEN): this was TCP-only (`!ctx->dlm` ->
+ * ccloop cc87fed3 sess3 (PROVEN BY INSTRUMENT): this was TCP-only (`!ctx->dlm` ->
dlm/v5_mount.c:16006
- * sess374 (sess363 RULE-5 ruling), replacing the sess361 disklock-record
+ * sess374 (sess363 design-consult ruling), replacing the sess361 disklock-record
dlm/v5_mount.c:16241
- * sess330 (RULE-5 ruling): leaseless terminalization of a LEGACY intent-path
+ * sess330 (design-consult ruling): leaseless terminalization of a LEGACY intent-path
dlm/v5_mount.c:16283
-             * sess383 (RULE-5 ruling Q1/Q4): this scan used to import the
+             * sess383 (design-consult ruling Q1/Q4): this scan used to import the
dlm/v5_mount.c:17161
-/* sess107 RULE-4: cheap on-disk ownership read for an INODE resource.
+/* sess107 instrumented: cheap on-disk ownership read for an INODE resource.
dlm/v5_mount.c:17233
-/* sess52 RULE-4: popcount(holders_ex) for this AG across the whole CAW probe
+/* sess52 instrumented: popcount(holders_ex) for this AG across the whole CAW probe
dlm/tauth_store.h:51
-    /* sess426 (D-0349 RULE 4): per-phase commit cost in ms — totals + max */
+    /* sess426 (D-0349 instrumented): per-phase commit cost in ms — totals + max */
dlm/tauth_ledger.h:4
- * (docs/tcp-authority-ledger.md step 3c, sess422; RULE-5 rulings in ccmemory
+ * (docs/tcp-authority-ledger.md step 3c, sess422; design-consult rulings in ccmemory
dlm/tauth_ledger.h:100
-    /* sess424 (RULE 4 instrumentation): durable page-write latency of the
+    /* sess424 (instrumentation): durable page-write latency of the
dlm/tauth_ledger.h:349
- * durable transition (RULE-5 ruling option (c)). */
+ * durable transition (design-consult ruling option (c)). */
dlm/dlm_user_compat.h:3
- * Usermode shims for the kernel idioms the DLM engine's RULE-4 probe lines
+ * Usermode shims for the kernel idioms the DLM engine's instrumented probe lines
dlm/tauth_ledger.c:292
- * sess426 (D-0348, RULE-5 ruling ccloop-c7ee71c6-sess426-GPT-ruling-tauth-
+ * sess426 (D-0348, design-consult ruling ccloop-c7ee71c6-sess426-GPT-ruling-tauth-
dlm/tauth_ledger.c:775
- * sess428 (D-0349, RULE-5 ruling option (c)): the BOOTSTRAP node prepares a
+ * sess428 (D-0349, design-consult ruling option (c)): the BOOTSTRAP node prepares a
dlm/prledger.h:3
- * item 2; RULE-5 ruling ccmemory
+ * item 2; design-consult ruling ccmemory
dlm/prledger.h:126
- * sess439 (RULE-5 ruling ccmemory
+ * sess439 (design-consult ruling ccmemory
dlm/bootstrap.h:3
- * §5; RULE-5 ruling ccmemory
+ * §5; design-consult ruling ccmemory
dlm/bootstrap.h:78
- * RULE-5 ruling ccmemory ccloop-c7ee71c6-sess441-GPT-ruling-item5d-adopt-one-
+ * Design-consult ruling ccmemory ccloop-c7ee71c6-sess441-GPT-ruling-item5d-adopt-one-
dlm/bootstrap.h:138
- * sess440 (§6.6, RULE-5 ruling Q5): a sealed slice ended TERMINAL (quarantined
+ * sess440 (§6.6, design-consult ruling Q5): a sealed slice ended TERMINAL (quarantined
dlm/recov_obl.h:8
- * RULE-5 rulings: ccmemory ccloop-c7ee71c6-sess461-GPT-ruling-intents-item5-
+ * Design-consult rulings: ccmemory ccloop-c7ee71c6-sess461-GPT-ruling-intents-item5-
dlm/bootstrap.c:1243
- * docs/whole-cluster-restart.md §6.8; RULE-5 review ccmemory
+ * docs/whole-cluster-restart.md §6.8; design-consult review ccmemory
dlm/recov_obl_done.h:7
- * RULE-5 rulings: ccmemory ccloop-c7ee71c6-sess461-GPT-ruling-intents-item5-
+ * Design-consult rulings: ccmemory ccloop-c7ee71c6-sess461-GPT-ruling-intents-item5-
pal/pal.h:761
- * sess433 (D-379(B) / D-0355, sess432 RULE-5 ruling): this is a PLAIN
+ * sess433 (D-379(B) / D-0355, sess432 design-consult ruling): this is a PLAIN
pal/pal.h:792
- * §5.2, RULE-5 ruling ccmemory
+ * §5.2, design-consult ruling ccmemory
pal/pal.h:1082
- * sess379 (D-MASS-UMOUNT-ROOT-EX-SERIALIZE-100S-526B, RULE-5 ruling item 5).
+ * sess379 (D-MASS-UMOUNT-ROOT-EX-SERIALIZE-100S-526B, design-consult ruling item 5).
pal/linux/kern.c:561
- * sess-tcp (RULE 4 PROVEN): the LIO/tcm_loop test backstore REJECTS the
+ * sess-tcp (PROVEN BY INSTRUMENT): the LIO/tcm_loop test backstore REJECTS the
pal/linux/kern.c:763
-	 * sess444 (RULE-5 landing review, STOP-SHIP 2): a PARTITION's LBAs are
+	 * sess444 (design-consult landing review, STOP-SHIP 2): a PARTITION's LBAs are
pal/linux/kern.c:885
- * ─── sess379: PER-TASK ABSOLUTE I/O BUDGET (RULE-5 ruling item 5) ───
+ * ─── sess379: PER-TASK ABSOLUTE I/O BUDGET (design-consult ruling item 5) ───
pal/linux/kern.c:904
- * i.e. a worst case of ~20 minutes for ONE slot probe.  The RULE-5 ruling
+ * i.e. a worst case of ~20 minutes for ONE slot probe.  The design-consult ruling
pal/linux/kern.c:1205
- * sess103 RULE-4 instrumentation helper: COHERENT plain-bio read of an
+ * sess103 instrumentation helper: COHERENT plain-bio read of an
pal/linux/kern.c:3217
- * of tests/retire_pending_admission.sh (RULE-5 STOP-SHIP #2 required
+ * of tests/retire_pending_admission.sh (design-consult STOP-SHIP #2 required
pal/linux/kern.c:4107
- * sess377 RULE-5 ruling (ccmemory ccloop-c7ee71c6-sess377-GPT-ruling3-pr-
+ * sess377 design-consult ruling (ccmemory ccloop-c7ee71c6-sess377-GPT-ruling3-pr-
pal/linux/xfs_super.c:1737
- * sess475 (D-0133, RULE-5 placement ruling — ccmemory ccloop-c7ee71c6-
+ * sess475 (D-0133, design-consult placement ruling — ccmemory ccloop-c7ee71c6-
pal/linux/xfs_super.c:2427
-	 * sess433 (D-379(B) / D-0355, sess432 RULE-5 ruling): the departure
+	 * sess433 (D-379(B) / D-0355, sess432 design-consult ruling): the departure
pal/linux/xfs_super.c:3137
- * sess447 (D-FOREIGN-REPLAY-UNGATED-IMAGES, RULE-5 ruling "scoped
+ * sess447 (D-FOREIGN-REPLAY-UNGATED-IMAGES, design-consult ruling "scoped
pal/linux/xfs_super.c:3167
- * sess448 (RULE-5 ruling ccloop-c7ee71c6-sess448-GPT-ruling-iclus-relmark-
+ * sess448 (design-consult ruling ccloop-c7ee71c6-sess448-GPT-ruling-iclus-relmark-
pal/linux/xfs_super.c:3465
-		 * sess86 Part A (Gemini RULE-5): respect a stale flag already set
+		 * sess86 Part A (Gemini design-consult): respect a stale flag already set
pal/linux/xfs_super.c:3517
-		 * v0.5.1 DLM hold-epoch fast path (RULE 4 proven, sess19 ccloop:
+		 * v0.5.1 DLM hold-epoch fast path (proven by instrument, sess19 ccloop:
pal/linux/xfs_super.c:3550
-		 * sess126 FIX (RULE 4 proven + RULE 5 Gemini): EXCLUDE DIRECTORY
+		 * sess126 FIX (proven by instrument + design-consult Gemini): EXCLUDE DIRECTORY
pal/linux/xfs_super.c:3730
-	 * sess133 PROVEN ROOT FIX (RULE 4, live /proc/pid/stack +
+	 * sess133 PROVEN ROOT FIX (instrumented, live /proc/pid/stack +
pal/linux/xfs_super.c:3761
-		 * sess86 Part C (Gemini RULE-5): reused-inode (SAME number, SAME
+		 * sess86 Part C (Gemini design-consult): reused-inode (SAME number, SAME
pal/linux/xfs_super.c:3815
-			 * sess126 ROOT FIX (RULE 4 step 2b, proven by P-IRESURRECT):
+			 * sess126 ROOT FIX (instrument step 2b, proven by P-IRESURRECT):
pal/linux/xfs_super.c:3860
-	 * sess71 INSTR (RULE 4): Face A = inode-number REUSE type confusion.
+	 * sess71 INSTR (instrumented): Face A = inode-number REUSE type confusion.
pal/linux/xfs_super.c:3876
-		 * sess89 (RULE 4): convert sess71's detect-only TYPEMISS into a
+		 * sess89 (instrumented): convert sess71's detect-only TYPEMISS into a
pal/linux/xfs_super.c:4612
-		/* sess447 (D-FOREIGN-REPLAY-UNGATED-IMAGES default-on, RULE-5
+		/* sess447 (D-FOREIGN-REPLAY-UNGATED-IMAGES default-on, design-consult
pal/linux/xfs_super.c:4694
-			 * sess389 (D-RSYNC-LAP-PACE-AG-SHARING-388, RULE-5 ruling):
+			 * sess389 (D-RSYNC-LAP-PACE-AG-SHARING-388, design-consult ruling):
pal/linux/xfs_file.c:1851
-	 * sess71 INSTR (RULE 4): Face A decisive probe.  xfs_lookup correctly
+	 * sess71 INSTR (instrumented): Face A decisive probe.  xfs_lookup correctly
pal/linux/xfs_file.c:2067
-	 * sess74 (ccloop 14d31183) SELF-DEADLOCK FIX (RULE 4, PROVEN by
+	 * sess74 (ccloop 14d31183) SELF-DEADLOCK FIX (instrumented, PROVEN by
pal/linux/xfs_iops.c:235
-	 * sess126 INSTR (RULE 4): capture EVERY create's PARENT dir inode + the
+	 * sess126 INSTR (instrumented): capture EVERY create's PARENT dir inode + the
pal/linux/xfs_iops.c:445
-	 * sess71 INSTR (RULE 4): confirm whether a cache-cold `cat` of a
+	 * sess71 INSTR (instrumented): confirm whether a cache-cold `cat` of a
pal/linux/xfs_iops.c:1489
-	 * sess-tcp (RULE 4 PROVEN): the peer-AG-only skip is INSUFFICIENT.
+	 * sess-tcp (PROVEN BY INSTRUMENT): the peer-AG-only skip is INSUFFICIENT.
pal/linux/xfs_iops.c:1551
-		 * blocking O_DIRECT write.  RULE-4 proven on pve1/pve2: fio_perf
+		 * blocking O_DIRECT write.  proven by instrument on pve1/pve2: fio_perf
pal/linux/xfs_iops.c:1821
-	 * ccloop cc87fed3 sess7: RULE-4 ROOT FIX for the P135-PRSWEEP-CYCLE
+	 * ccloop cc87fed3 sess7: instrumented ROOT FIX for the P135-PRSWEEP-CYCLE
pal/linux/xfs_iomap.c:737
-			/* P-WU-CLAMP (sess10 ccloop 72513a13, RULE-4 drc size=0):
+			/* P-WU-CLAMP (sess10 ccloop 72513a13, instrumented drc size=0):
pal/linux/xfs_aops.c:71
-	/* P-SFS (sess10 ccloop 72513a13, RULE-4 for the drc size=0 loss):
+	/* P-SFS (sess10 ccloop 72513a13, instrumented for the drc size=0 loss):
pal/linux/xfs_aops.c:364
- * the paired measurement proves the mechanism, per RULE 4.
+ * the paired measurement proves the mechanism, by instrument.
pal/linux/xfs_aops.c:430
- * timeout (RULE 0). This mask reloads only on peer notifications, which is a
+ * timeout (budget). This mask reloads only on peer notifications, which is a
pal/linux/xfs_xattr.c:90
-	 * sess472 (D-0531 item 4, RULE-5 review; docs/dir-sharding.md Model A):
+	 * sess472 (D-0531 item 4, design-consult review; docs/dir-sharding.md Model A):
pal/linux/xfs_buf.c:206
- * sess-pve (AGI umount-wedge, RULE-4): record one b_hold mutation into the
+ * sess-pve (AGI umount-wedge, instrumented): record one b_hold mutation into the
pal/linux/xfs_buf.c:284
-	 * sess5(ccloop 12e0d157) stale-attribution probe (RULE 4): the 32-node
+	 * sess5(ccloop 12e0d157) stale-attribution probe (instrumented): the 32-node
pal/linux/xfs_buf.c:326
-	/* P-DIRSTALE (RULE-4): a multinode dir3 data/block buffer that is
+	/* P-DIRSTALE (instrumented): a multinode dir3 data/block buffer that is
pal/linux/xfs_buf.c:531
-	/* P-DIRFREE (RULE-4): catch the FREE of a multinode dir3 data/block
+	/* P-DIRFREE (instrumented): catch the FREE of a multinode dir3 data/block
pal/linux/xfs_buf.c:1269
- * ccloop c7ee71c6 sess30 — GPT RULE-5 review item 2 on
+ * ccloop c7ee71c6 sess30 — design-consult review item 2 on
pal/linux/xfs_buf.c:1311
- * sess431 (RULE-5 review of 0.39.6, the one measurement it required): a
+ * sess431 (design-consult review of 0.39.6, the one measurement it required): a
pal/linux/xfs_buf.c:1616
-	 * Proven live (RULE 4) on the 16-node dpn=100 zero_silent_loss storm:
+	 * Proven live (instrumented) on the 16-node dpn=100 zero_silent_loss storm:
pal/linux/xfs_buf.c:1769
-		 * sess2 (ccloop a16ec5f2) RULE-4 DETECTOR — P-PINNED-REREAD.
+		 * sess2 (ccloop a16ec5f2) INSTRUMENTED DETECTOR — P-PINNED-REREAD.
pal/linux/xfs_buf.c:1807
-		 * RULE-5 design, "flush-before-hazardous-cold-read").  A COLD
+		 * Design-consult design, "flush-before-hazardous-cold-read").  A COLD
pal/linux/xfs_buf.c:1856
-		 * the RULE-4 proof that flips it to a fence.
+		 * the instrumented proof that flips it to a fence.
pal/linux/xfs_buf.c:2247
- * sess1(e8e920f7) RULE-4 probe — b_sema poisoning detector, acquire side.
+ * sess1(e8e920f7) instrumented probe — b_sema poisoning detector, acquire side.
pal/linux/xfs_buf.c:2329
-	 * ccloop-4dd7 sess3 (b54r1 test1 node-wide convoy, RULE-4 probe):
+	 * ccloop-4dd7 sess3 (b54r1 test1 node-wide convoy, instrumented probe):
pal/linux/xfs_buf.c:2382
-	 * sess1(e8e920f7) RULE-4 probe — b_sema over-release detector.  For a
+	 * sess1(e8e920f7) instrumented probe — b_sema over-release detector.  For a
pal/linux/xfs_buf.c:2830
-		 * sess42(ccloop) RULE-4 ROOT FIX (paired with xfs_buf_submit): a real
+		 * sess42(ccloop) instrumented ROOT FIX (paired with xfs_buf_submit): a real
pal/linux/xfs_buf.c:4056
- * sess115 (Gemini RULE-5 Candidate A) — PARTIAL inode-cluster WRITE.
+ * sess115 (Gemini design-consult Candidate A) — PARTIAL inode-cluster WRITE.
pal/linux/xfs_buf.c:4152
- * cost.  Log-only: RULE 4 step 2, not a fix.
+ * cost.  Log-only: instrument step 2, not a fix.
pal/linux/xfs_buf.c:4524
-			 * NL.  (GPT RULE-5 invariant: never write an inode core
+			 * NL.  (design-consult invariant: never write an inode core
pal/linux/xfs_buf.c:4529
-			 * sess14 REFINEMENT (RULE 4 — the unrefined skip caused
+			 * sess14 REFINEMENT (instrumented — the unrefined skip caused
pal/linux/xfs_buf.c:4551
-			 * Discriminator (sess14, THIRD iteration — GPT RULE-5
+			 * Discriminator (sess14, THIRD iteration — design-consult
pal/linux/xfs_buf.c:4886
-				 * sess431 (D-0351, RULE-5 ruling): a staged FREE
+				 * sess431 (D-0351, design-consult ruling): a staged FREE
pal/linux/xfs_buf.c:5196
-		 * sess56 (ccloop 8ddb16a2) — RULE 4 PROVEN (tcp_dlm_scaling
+		 * sess56 (ccloop 8ddb16a2) — PROVEN BY INSTRUMENT (tcp_dlm_scaling
pal/linux/xfs_buf.c:5515
-		 * sess29, after a GPT review (RULE 5): ALWAYS apply the
+		 * sess29, after a GPT review (design consult): ALWAYS apply the
pal/linux/xfs_buf.c:5565
-	 * review (RULE 5) rather than by the A/B that "passed".
+	 * review (design consult) rather than by the A/B that "passed".
pal/linux/xfs_buf.c:5586
-		 * sess29 (GPT RULE-5 review): split the empty case by whether
+		 * sess29 (design-consult review): split the empty case by whether
pal/linux/xfs_buf.c:6094
- * predicate (dc_stale) to keep the common path free of disk I/O (RULE 0).
+ * predicate (dc_stale) to keep the common path free of disk I/O (budget).
pal/linux/xfs_buf.c:6096
- * ccloop cc87fed3 sess9 (RULE 4): @do_trim extends this from detect-only to
+ * ccloop cc87fed3 sess9 (instrumented): @do_trim extends this from detect-only to
pal/linux/xfs_buf.c:6244
-							 * ccloop cc87fed3 sess9 (RULE 4,
+							 * ccloop cc87fed3 sess9 (instrumented,
pal/linux/xfs_buf.c:6962
-	 * sess2 (ccloop a16ec5f2) RULE-4 block watch: mxfs.watch_daddr=<daddr>
+	 * sess2 (ccloop a16ec5f2) instrumented block watch: mxfs.watch_daddr=<daddr>
pal/linux/xfs_buf.c:7061
-	 * sess5(ccloop 12e0d157) read-attribution probe (RULE 4).  This is the
+	 * sess5(ccloop 12e0d157) read-attribution probe (instrumented).  This is the
pal/linux/xfs_buf.c:7132
-	 * sess4(a16ec5f2) INODE-CLUSTER slot-transition ledger (RULE 4).
+	 * sess4(a16ec5f2) INODE-CLUSTER slot-transition ledger (instrumented).
pal/linux/xfs_buf.c:7265
-	 * the dir DLM lock to a peer.  ROOT (PROVEN, RULE 4 + GPT-5.5): a dir
+	 * the dir DLM lock to a peer.  ROOT (PROVEN, instrumented + GPT-5.5): a dir
pal/linux/xfs_buf.c:7280
-	 * sess9 (ccloop a864) LEAK NEUTRALIZER (RULE 4, run 160833Z r10+):
+	 * sess9 (ccloop a864) LEAK NEUTRALIZER (instrumented, run 160833Z r10+):
pal/linux/xfs_buf.c:7320
-	     /* sess6 (46efd8b6) RULE-4 PROVEN (run 110411Z): bmbt writes are
+	     /* sess6 (46efd8b6) PROVEN BY INSTRUMENT (run 110411Z): bmbt writes are
pal/linux/xfs_buf.c:7385
-	/* sess60 RULE-4: log every bmbt-leaf WRITE (numrecs + owner hold state)
+	/* sess60 instrumented: log every bmbt-leaf WRITE (numrecs + owner hold state)
pal/linux/xfs_buf.c:7504
-	 * (RULE 0 unaffected).
+	 * (the budget rule unaffected).
pal/linux/xfs_buf.c:7505
-	 *   mxfs_dir_reintro_probe=1 : LOG candidates (measure, RULE 4 step 2).
+	 *   mxfs_dir_reintro_probe=1 : LOG candidates (measure, instrument step 2).
pal/linux/xfs_buf.c:7581
-							     * ccloop cc87fed3 sess9 (RULE 4,
+							     * ccloop cc87fed3 sess9 (instrumented,
pal/linux/xfs_buf.c:7711
-	 * sess61 RULE-4 FIX (PROVEN by P60-BMBTWRITE: a node wrote the shared
+	 * sess61 instrumented FIX (PROVEN by P60-BMBTWRITE: a node wrote the shared
pal/linux/xfs_buf.c:7752
-		/* sess6 (46efd8b6) RULE-4 PROVEN (run 122308Z test7
+		/* sess6 (46efd8b6) PROVEN BY INSTRUMENT (run 122308Z test7
pal/linux/xfs_buf.c:7776
-	 * (gated by dirwr/instr) so the suppression is provable (RULE 4);
+	 * (gated by dirwr/instr) so the suppression is provable (instrumented);
pal/linux/xfs_buf.c:7788
-		 * sess3(a9a03929) RULE-4 lineage probe (always-on, capped):
+		 * sess3(a9a03929) instrumented lineage probe (always-on, capped):
pal/linux/xfs_buf.c:7855
-		 * sess50(ccloop) RULE-4 content-history trace (WRITE side): count of the
+		 * sess50(ccloop) instrumented content-history trace (WRITE side): count of the
pal/linux/xfs_buf.c:8011
-		 * rare prior-tenure write, so RULE-0 timing is unaffected.
+		 * rare prior-tenure write, so the budget rule timing is unaffected.
pal/linux/xfs_buf.c:8074
-			/* RULE-4 instrumentation (2026-07-14): local lock-belief
+			/* instrumentation (2026-07-14): local lock-belief
pal/linux/xfs_buf.c:8154
-							 * dcnt) -- REVERTED, RULE-4 proven
+							 * dcnt) -- REVERTED, proven by instrument
pal/linux/xfs_buf.c:8315
-						/* sess38(ccloop) RULE-4: capture the call stack of
+						/* sess38(ccloop) instrumented: capture the call stack of
pal/linux/xfs_buf.c:8543
-			 * sess19 LEAF-HASH write-clobber detector (RULE 4).
+			 * sess19 LEAF-HASH write-clobber detector (instrumented).
pal/linux/xfs_buf.c:9020
-	 * sess90 DECISIVE PROBE (Gemini RULE-5): a FUA read DMA-overwrites
+	 * sess90 DECISIVE PROBE (Gemini design-consult): a FUA read DMA-overwrites
pal/linux/xfs_buf.c:9032
-	 * sess26(ccloop) ROOT FIX (RULE 4): the P91 skip below keeps an in-core
+	 * sess26(ccloop) ROOT FIX (instrumented): the P91 skip below keeps an in-core
pal/linux/xfs_buf.c:9252
-	 * writeback WITHOUT issuing the stale physical write (GPT RULE-5
+	 * writeback WITHOUT issuing the stale physical write (design-consult
pal/linux/xfs_buf.c:9560
-	 * sess42(ccloop) RULE-4 PROVEN ROOT FIX: for a SHARED DIR-METADATA write
+	 * sess42(ccloop) PROVEN BY INSTRUMENT ROOT FIX: for a SHARED DIR-METADATA write
pal/linux/xfs_buf.c:9600
-	 * P35E (RULE 4, sess35 run14d): dir-block write lineage.  The CRC
+	 * P35E (instrumented, sess35 run14d): dir-block write lineage.  The CRC
pal/linux/xfs_buf.c:9733
-	 * sess89 PROBE-A (Gemini RULE-5): SMOKING-GUN for the cross-node bnobt
+	 * sess89 PROBE-A (Gemini design-consult): SMOKING-GUN for the cross-node bnobt
pal/linux/xfs_buf.c:9815
-		 * read; capped stacks; log-only (RULE-4: prove the AG-exclusion
+		 * read; capped stacks; log-only (instrumented: prove the AG-exclusion
pal/linux/xfs_buf.c:9921
-			/* sess52 RULE-4 concurrent-EX measurement: when the
+			/* sess52 instrumented concurrent-EX measurement: when the
pal/linux/xfs_buf.c:9939
-			 * sess53 RULE-4: WHY is this bnobt buffer gen-fresh
+			 * sess53 instrumented: WHY is this bnobt buffer gen-fresh
pal/linux/xfs_buf.c:9956
-			/* sess79 RULE-4 DIRECTION PROBE: dump the on-disk
+			/* sess79 instrumented DIRECTION PROBE: dump the on-disk
pal/linux/xfs_buf.c:9998
-			 * sess93 DECISIVE PROBE (RULE 4): the REVERT-clobber.
+			 * sess93 DECISIVE PROBE (instrumented): the REVERT-clobber.
pal/linux/xfs_buf.c:10056
-					 * RULE-4 + Gemini re-diagnosis: the
+					 * Instrumented + Gemini re-diagnosis: the
pal/linux/xfs_buf.c:10112
-	 * sess22 (ccloop 4eef1f39) P124 — ALLOCATION-REVERT detector (RULE 4).
+	 * sess22 (ccloop 4eef1f39) P124 — ALLOCATION-REVERT detector (instrumented).
pal/linux/xfs_buf.c:10129
-	 * sess22 RULE-4 2b: PROVEN this run — P124 fired on all 4 nodes with
+	 * sess22 instrumented 2b: PROVEN this run — P124 fired on all 4 nodes with
pal/linux/xfs_buf.c:10232
-	 * (RULE 4).  The dpn=100 storm dies cluster-wide on a dabuf-map HOLE
+	 * (instrumented).  The dpn=100 storm dies cluster-wide on a dabuf-map HOLE
pal/linux/xfs_buf.c:10284
-			 * sess36 (RULE 4): UNCAPPED dinode-write lineage for
+			 * sess36 (instrumented): UNCAPPED dinode-write lineage for
pal/linux/xfs_buf.c:10418
-	 * revert detector (RULE 4).  The zsl dpn=100 storm dies cluster-wide
+	 * revert detector (instrumented).  The zsl dpn=100 storm dies cluster-wide
pal/linux/xfs_buf.c:10513
-	 * (RULE 4 — the decisive instrument GPT/sess68 wanted, finally built).
+	 * (instrumented — the decisive instrument GPT/sess68 wanted, finally built).
pal/linux/xfs_buf.c:10721
-	 * sess64 (RULE 4): ALWAYS-ON node1_f1 clobber tracer.  node1_f1 is
+	 * sess64 (instrumented): ALWAYS-ON node1_f1 clobber tracer.  node1_f1 is
pal/linux/xfs_buf.c:10872
-	 * detector (RULE 4, the dlm_scaling/fence SF-loss discriminator).
+	 * detector (instrumented, the dlm_scaling/fence SF-loss discriminator).
pal/linux/xfs_buf.c:11058
-	 * ltbno+ltlen>bno double-free shutdown.  Per GPT RULE-5 design:
+	 * ltbno+ltlen>bno double-free shutdown.  Per design-consult design:
pal/linux/xfs_buf.c:11112
-	 * write fence (GPT RULE-5 blueprint; root proof in sess6-C).  A dir
+	 * write fence (design-consult blueprint; root proof in sess6-C).  A dir
pal/linux/xfs_buf.c:11358
-		 * sess15(ccloop) PERF probe (RULE 4): the 8-node dir_reuse verify
+		 * sess15(ccloop) PERF probe (instrumented): the 8-node dir_reuse verify
pal/linux/xfs_buf.c:11406
-	 * sess110 (RULE 4): bnobt in-core-revert probe+guard for the PLAIN-BIO
+	 * sess110 (instrumented): bnobt in-core-revert probe+guard for the PLAIN-BIO
pal/linux/xfs_buf.c:11432
-		 * sess122 (ccloop) — RULE 4, proven harmful: this read-side
+		 * sess122 (ccloop) — instrumented, proven harmful: this read-side
pal/linux/xfs_buf.c:11448
-		 * ccloop-4dd7 sess4 (b60r2, RULE 4): the blanket disable was
+		 * ccloop-4dd7 sess4 (b60r2, instrumented): the blanket disable was
pal/linux/xfs_buf.c:11503
-	 * sess61 (RULE 4, PROVEN root of zero_silent_loss): the bmbt analogue of
+	 * sess61 (instrumented, PROVEN root of zero_silent_loss): the bmbt analogue of
pal/linux/xfs_buf.c:11528
-	 * very low volume) doubles as the RULE-4 proof.
+	 * very low volume) doubles as the instrumented proof.
pal/linux/xfs_buf.c:11553
-	 * P20-BIO-READ-LOGGED (RULE 4, catch-all probe): a plain-bio READ is
+	 * P20-BIO-READ-LOGGED (instrumented, catch-all probe): a plain-bio READ is
pal/linux/xfs_buf.c:11814
-	 * sess63 (zero_silent_loss WRITER-SIDE ROOT FIX, RULE 4 — proven by direct
+	 * sess63 (zero_silent_loss WRITER-SIDE ROOT FIX, instrumented — proven by direct
pal/linux/xfs_buf.c:11843
-		 * victim-shutdown family (RULE 4, PROVEN run 045621Z).  The
+		 * victim-shutdown family (instrumented, PROVEN run 045621Z).  The
pal/linux/xfs_buf.c:12139
-	 * sess7(a9a03929) FIX-19 (RULE-4 PROVEN, run92 r1): an UNDESTAGED
+	 * sess7(a9a03929) FIX-19 (PROVEN BY INSTRUMENT, run92 r1): an UNDESTAGED
pal/linux/xfs_buf_item.c:105
-	 * sess448 RULE-4 probe for the f4truth GEN-OPEN-NOT-DIRTY residue
+	 * sess448 instrumented probe for the f4truth GEN-OPEN-NOT-DIRTY residue
pal/linux/xfs_buf_item.c:130
-	 * sess564 (D-...-0924) RULE-4 PROBE — DOES NOT CHANGE BEHAVIOUR.
+	 * sess564 (D-...-0924) INSTRUMENTED PROBE — DOES NOT CHANGE BEHAVIOUR.
pal/linux/xfs_buf_item.c:332
- * sess468 (D-FOREIGN-SLICE-INTENTS-ABANDONED, RULE-5 ruling fix shape B —
+ * sess468 (D-FOREIGN-SLICE-INTENTS-ABANDONED, design-consult ruling fix shape B —
pal/linux/xfs_buf_item.c:429
- * The sess95 RULE-5 ruling is explicit about how this may be done:
+ * The sess95 design-consult ruling is explicit about how this may be done:
pal/linux/xfs_buf_item.c:640
- * sess101 RULE-5 ruling rejected that inference: `unpublished_noted` counts
+ * sess101 design-consult ruling rejected that inference: `unpublished_noted` counts
pal/linux/xfs_buf_item.c:733
- * (RULE-5 ruling A′): a BLFT change after the capture used to void the token
+ * (design-consult ruling A′): a BLFT change after the capture used to void the token
pal/linux/xfs_buf_item.c:761
- * P241-AUTHTRY — the decisive instrument the sess104 RULE-5 ruling asked for.
+ * P241-AUTHTRY — the decisive instrument the sess104 design-consult ruling asked for.
pal/linux/xfs_buf_item.c:990
-		 * sess467 (D-FOREIGN-SLICE-INTENTS-ABANDONED, RULE 4): the
+		 * sess467 (D-FOREIGN-SLICE-INTENTS-ABANDONED, instrumented): the
pal/linux/xfs_buf_item.c:1056
- * RULE-5 ruling predicted from code reading alone.  MISLABEL counts buffers
+ * Design-consult ruling predicted from code reading alone.  MISLABEL counts buffers
pal/linux/xfs_buf_item.c:1382
- * sess476 (CANCEL authority tokens, RULE-5 verification bar — the NEGATIVE
+ * sess476 (CANCEL authority tokens, design-consult verification bar — the NEGATIVE
pal/linux/xfs_buf_item.c:2687
-	 * sess60 (RULE 4, zero_silent_loss residual): the bmbt analogue of the
+	 * sess60 (instrumented, zero_silent_loss residual): the bmbt analogue of the
pal/linux/xfs_buf_item_recover.c:1158
-	 * sess456 (D-0517 RULE 4 step 2): on a FOREIGN replay this read goes
+	 * sess456 (D-0517 instrument step 2): on a FOREIGN replay this read goes
pal/linux/xfs_buf_item_recover.c:1230
-	 * sess459 (D-0517, RULE 4 proven on chain 80 laps 2-3 + chain 81; RULE-5
+	 * sess459 (D-0517, proven by instrument on chain 80 laps 2-3 + chain 81; design-consult
include/mxfs/mxfs_dlm.h:41
- * sess105 RULE-5 ruling: one helper everywhere.  The tree previously mixed
+ * sess105 design-consult ruling: one helper everywhere.  The tree previously mixed
include/mxfs/mxfs_dlm.h:70
- * The sess96 RULE-5 ruling rejected reading the grant epoch back out of a
+ * The sess96 design-consult ruling rejected reading the grant epoch back out of a
include/mxfs/mxfs_dlm.h:117
-	 * sess432 (D-0353, RULE-5 ruling): the CAW single-node fast path — the
+	 * sess432 (D-0353, design-consult ruling): the CAW single-node fast path — the
include/mxfs/mxfs_super.h:150
- * bookkeeping.  The sess74 RULE-5 ruling REFUTED the claim that a per-slot
+ * bookkeeping.  The sess74 design-consult ruling REFUTED the claim that a per-slot
include/mxfs/mxfs_dirshard.h:8
- * Design: docs/dir-sharding.md.  RULE-5 rulings: ccmemory
+ * Design: docs/dir-sharding.md.  Design-consult rulings: ccmemory
include/mxfs/mxfs_tauth.h:4
- * region (docs/tcp-authority-ledger.md, build step 2; sess418 RULE-5 ruling
+ * region (docs/tcp-authority-ledger.md, build step 2; sess418 design-consult ruling
include/mxfs/mxfs_tauth.h:91
- * sess427 (D-0348 step 2, RULE-5 ruling ccloop-c7ee71c6-sess426-GPT-ruling-
+ * sess427 (D-0348 step 2, design-consult ruling ccloop-c7ee71c6-sess426-GPT-ruling-
include/mxfs/mxfs_tauth.h:129
- * single-writer WRITE backend (sess421 RULE-5 ruling 2, option (b), ccmemory
+ * single-writer WRITE backend (sess421 design-consult ruling 2, option (b), ccmemory
bench/phase0_fio_multinode.sh:15
-# Persistent script (per RULE 3 / feedback_scripts_in_tree).
+# Persistent script (so it survives a reboot / feedback_scripts_in_tree).
bench/README.md:7
-These scripts live in the source tree (per `CLAUDE.md` RULE 3) so they
+These scripts live in the source tree (per `CLAUDE.md` the source-tree rule) so they
bench/README.md:104
-See `/src/mxfs/CLAUDE.md` RULE 3.  Test cluster reboots regularly
+See `/src/mxfs/CLAUDE.md` the source-tree rule.  Test cluster reboots regularly
