<!-- sess431 RULE-5 review of 0.39.6/0.39.7: PUB_SKIPPED clear sound; chain-kept correct (no eviction drop, last-ifree epoch binds); platter-home needs a… -->
# sess431 GPT review (gpt-5.6-sol) of the post-claim changes

- A (copy-in clears MXFS_IF_PUB_SKIPPED): sound iff copy-in/submit/completion stay serialized per slot (buffer lock does); suggested hardening: handle PUB_SKIPPED before the `!ili_last_fields` early exit in iodone (landed 0.39.9).
- B (P55C home from a platter read when flush!=durable): right source of truth, but (1) the boards never exercised it (P55C-HOME-PLATTER=0) and (2) flush!=durable is NOT a sufficient detector — every drop path rolls flush back to durable while the buffer keeps the staged mode-0 bytes → buffer-image false discharge on re-push. 0.39.9: ALWAYS classify from the raw platter read (private bounce buffer via mxfs_pal_bdev_read_plain_bdev, never the xfs buffer cache); buffer locked ⇒ no in-flight write of that cluster; invalid/failed read ⇒ DENIED+strike.
- C (UNLINK discharge keeps chain → CHAIN_LIVE): correct transition; chain==0 must still drop (it does); NEVER drop CHAIN_LIVE at eviction (eviction does not end the incarnation); chain>0 sound across AG tenure changes because the inobt bit stays allocated to our live life; the CHAIN write binds the LAST ifree's epoch (free_commit replaces it) — correct.
- Closure: the s437→s442 series + per-claim raw proof + 4 reproducers is sufficient for FIXED AND VERIFIED on the tested configuration, PLUS one directed test: force buffer != media (drop a claimed sector), observe P55C-HOME-PLATTER, re-stage, WRITE, durable, discharge, THEN AG unlock. → mxfs.freepub_drop_once knob + tests/freepub_platter_home_inject.sh (chain stage fph).
