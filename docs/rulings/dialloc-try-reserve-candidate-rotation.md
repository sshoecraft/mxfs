<!-- sess392 RULE-5 ruling: lap-2 shared-AG pace root = dialloc 1 s inode-slot reserve under AG/AGI hold on peer-freed (noino-held) inodes; fix S1 = nonqu… -->
# sess392 — rsync lap-2 pace at 25 AGs: root proven + RULE-5 ruling on fix shape

## Chain of this session (0.22.0 -> 0.22.4)
- 0.22.0 lap 1 collapsed (all rows BUDGET_EXHAUSTED): mxfs_ag_handoff_commit set bast_scheduled=true and mxfs_ag_demote_clear never cleared it -> after the first completed handoff every later BAST saw sched=1 and only the 30 s P275 watchdog requeued the worker (b2c_max=30031, P275 requeued=1). FIXED 0.22.1 (clear in demote_clear).
- 0.22.2: stale bast_pending after RELEASED completion (BAST rx during drains) -> cleared at completion; b2c split stamps (r2e/e2a/a2c).
- 0.22.3: q (rx->queued)/wq split + rx holders; unratelimited P12-HANDOFF-TAIL (b2c>1000). Found ALL 265 tail events had holders==0 at rx: multicast BAST hint recorded while we held NOTHING (waiter's late re-BASTs after our release) -> next fresh tenure pre-poisoned (closing true at first unlock, tenure = 1 op).
- 0.22.4: drop pre-acquire hints at fresh grant (rx_ns < acq_t0; P12-STALE-HINT, stat stale_hint). Lap 1: b2c fleet sum 1037 s -> 60 s, b2c_gt500 313 -> 2, max 129 s -> 5 s. Lap 1 all PASS (rsync 18 s). Lap 2 still FAIL rsync_paired: shared pairs 28-55 s, 4 nodes >55 s.
- Lap-2 root (correlated test15 slot2 / test21 slot27 on AG 2): peer unlinks+ifrees file (noino lifecycle keeps the inode DLM slot until its BAST-driven whole-AIL drain fence); our dialloc (AG EX + AGI held) picks that just-freed inode as first-free candidate -> mxfs_dialloc_reserve_ino -> 1 s bounded CAW acquire expires (P-RESV-DEADLINE el_ms=1011 -> P-DIALLOC-RESV-BUSY -> P12-LATCH by=unlock) -> next pass re-picks the SAME ino. Meanwhile the peer's kworkers wait on our AG (P1-AGWAIT ag=2 kworker) so its fence cannot progress: cross-resource wait broken only by the deadline. Excess rsync wall ~= 1 s x RESV-BUSY count (test15: 43). Quantized AG hold tails 1020/2025/3114 ms.

## Ruling (gpt-5.6-sol): S1 conditionally approved, S2 later, S4 rejected
S1 = (1) pure nonqueued CAW try-reserve (one CAS; failed = contended; if any request could outlive the call, grant-wins cancel machinery remains mandatory; classify transport errors separately, not as skips); (2) working-copy masking of contended/cooldown bits (never touch the on-tree record/freecount/AGI until a reserve succeeds; revalidate the bit after reserve; release slot on every unwind); per-AG logical cursor (record startino + bit) retained across AG unlocks, re-looked-up on reentry; (3) bounded probes per AG visit: <=8 probes AND <=3-5 ms, then release AGI/AG and continue later from the cursor (budget exhaustion = retry, NOT ENOSPC); (4) per-inode cooldown 500 ms jittered 250-750 (bounded LRU 64-128/AG; never hide the only free inode forever); after a full failed sweep of the eligible AG set: randomized 5-20 ms backoff, exponential, cap 100-250 ms, OUTSIDE AGI/buffers/AG; (5) every exit through the canonical AG unlock (zero-holder latch); callers classify as RESTARTABLE; no ENOSPC from contention; (6) REMOVE the dirty-trans blocking fallback (no remote blocking under AGI/AG; needs a restartable boundary / pre-reservation); (7) audit every slot-release path for the durability invariant (slot reservable only after the freed dinode's ordering is complete: noino release, live-skip, unmount, eviction/crash, DLM recovery, shutdown). Do not sort records by free count; do not sleep per candidate.
Acceptance: 25 AGs x 32 nodes, rsync laps 2/3, >=10 runs: all <60 s AND shared-AG nodes within 2x same-run exclusive wall (~28 s); zero 1 s reserve waits, no 1020/2025 ms tails; instrumentation list (try latency histogram, skipped-live/cooldown, probes/visit, budget-exhausted visits, full failed sweeps, backoff ms, restarts, AG hold histogram, noino BAST->release latency, slot released on unwind); 64-AG board statistically unchanged (no extra CAWs).
