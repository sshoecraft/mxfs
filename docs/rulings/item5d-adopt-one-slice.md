<!-- sess441 RULE-5 ruling for item 5d: shape B (adopt ONE certified victim slice K via full own-log xfs_log_mount, existing barrier for N-1), escrow K's… -->
# sess441 GPT ruling — 5d replay shape

Facts put to it: foreign engine hard-refuses without mp->m_log (xfs_log.c:938), skips intents by design, barrier runs after xfs_log_mount; own-log path does FULL replay incl. intents; ACTIVE claim replaces the sector (descriptor gone); full occupancy needs a victim slot.

## Ruling: B — adopt one certified victim slice K. Not A (pre-m_log shadow: STOP-SHIP 4 surface) nor C.
- K gets FULL own-log replay: slice_adopted=false, NO XLOG_MXFS_ADOPTED_SLICE. If claim plumbing cannot say "new HB owner but preexisting dirty log requiring FULL replay" ⇒ STOP-SHIP.
- K's record must be an explicit state ACTIVE|BOOTSTRAP_RECOVERY_PENDING: not AG-admitted, not a live member, not proof of completion, not claimable by fresh-claim rules; EVERY scanner/liveness/claimer/admission path must understand it (it is physically visible to all nodes). Normalised to ordinary ACTIVE only at final completion.
- Escrow (durable BEFORE overwriting K's guard sector): version/state, term, owner node/epoch, manifest hash, slot K, selection reason, expected old sector generation + full-sector digest; original victim tuple (node, epoch, key, gen, host, boot, class); the complete recovery descriptor (version, stage, owner kind/node/epoch/term, lease id, crc); fence certificate (method, fenced key, acting key, generation, result, crc); new K claim identity (node/host/boot, pre-drawn epoch, owner key, claim gen, PENDING, requires_full_replay=true, term, manifest hash, escrow id); crc, generation, torn-write-detectable. If it does not fit in ~290 reserved bytes, allocate durable extension space — never hashes-only for evidence about to be destroyed.
- Ordering: seal → phase 3 for ALL incl. K → select K deterministically from the certified replay-eligible set → escrow PREPARED durable (read back) → CAW K's sector from the exact guarded image to PENDING → K_CLAIMED in bootstrap state → xfs_log_mount K full replay → persist K_REPLAY_OK or terminal K_REPLAY_REFUSED → barrier over N-1 → terminal per-slot outcome for every member → grant release evidence → READ FULL STATUS reconcile persisted → escrow completion evidence → zero the N-1 sectors with a durable cleanup bitmap → verify → RECOVERY_COMPLETE → normalise K to ACTIVE, open admission. K is NEVER zeroed. If RECOVERY_COMPLETE opens admission, zeroing precedes it (or split into RECOVERY_DATA_COMPLETE / ADMISSION_OPEN).
- Crash windows: escrow durable + K unclaimed ⇒ successor resumes the same CAW or abandons the term with no evidence lost; K claimed + record not advanced ⇒ K's record carries term/manifest hash/escrow id so a scanner correlates; owner dies during/after K replay ⇒ next term treats K as a FRESH victim (fence the claimant's key anew, new certificate, include in new manifest, replay again — LSN-gated), old escrow certificate never substitutes for the new fence; next term PREFERS a stale PENDING K as its adopted slice (else a different victim is consumed each failed term and old K's intents are stranded in foreign replay).
- Intents: K's applied by full replay; every other slice needs the existing census (against the immutable post-fence snapshot, part of durable per-slot evidence) to prove none undischarged, else durable REFUSED, no zeroing, admission closed. Does NOT close D-FOREIGN-SLICE-INTENTS-ABANDONED.
- 12 STOP-SHIPs: wrong replay mode for K; ordinary-ACTIVE ambiguity; incomplete escrow; unspecified CAW crash windows; reuse of an old certificate to fence a failed owner; fallback to another K after replay began (terminal refusal instead); foreign intent waiver; completion opens admission before cleanup; zeroing K; no successor preference for pending K; escrow space hidden by hashes; K chosen from a non-eligible class.
