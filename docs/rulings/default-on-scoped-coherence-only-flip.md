<!-- sess447 RULE-5 ruling: sess197 replay_gate_enforce (F1/F3/F4) is NOT a prerequisite for flipping foreign_replay_token_enforce default; scoped coheren… -->
# GPT ruling (sess447) — default-on sequencing for D-FOREIGN-REPLAY-UNGATED-IMAGES

Ground truth brought: F4 registry built, f4_gate=0 (ruling items 4+5 open); F3 built AND release_proof_enforce=1 default defers ICLUS release (F3 READY macro stale 0); F1 250 ms 'releasing anyway' loop live (ICLUS only; icluster_dlm=1 refused by the token setter); F2 durable flush only on replay/mount-recovery path; step 8 oracle absent; B2/B3 unbuilt.

## Q1 — replay_gate_enforce is NOT a prerequisite for the token-gate default flip
Different decisions: release gate = may a tenure be released with obligations; token gate = may a fenced foreign image get APPLY. Manifest snapshot + v3 lineage + RELMARK + di_changecount supersede the F1/F3/F4 release predicates for APPLY. Do NOT tie the token default to F1/F3/F4 READY; do NOT arm replay_gate_enforce; do NOT flip f4_gate. F1 not load-bearing (ICLUS refused). F3: truth-up MXFS_RELGATE_F3_COMPLETION_PROOF_READY=1 after confirming every failed-proof/tripwire/failed-CAS exit defers and never reopens admission (cleanup, not a dependency). F4: leave f4_gate=0; PROVISO (code-review/test condition, stop-ship if violated): missing/malformed/stale/lineage-mismatched/changecount-mismatched RELMARK must never yield a MORE permissive APPLY; an unlinearized F4 result must not forge a permissive clean RELMARK.

## Q2 — shipping shape
Defaults: foreign_replay_token_enforce=1, release_proof_enforce=1, target_cache_protected=0. MOUNT-TIME validator (not setter-only), before clustered RW admission / any recovery write: refuse fua_disable=1&&tcp=0; refuse icluster_dlm=1; refuse prohibited mixed version; fua_disable=0 permitted only after stable-media qualification (else refused in the scoped release). Rig prep declares target_cache_protected=1 = production configuration declaration (coherence-only domain), board must label it coherence-only. Two valid release choices: (1) finish stable-media (step 7 durable ordering + epoch-mint durability + B2-B4 + step-8 oracle + boundary crash/flush-fail/CAS-fail/power-loss evidence) before the flip; or (2) scoped coherence-only flip that also refuses unqualified crash-durable (fua_disable=0) mounts. Step 7 + B2/B3 block supporting fua_disable=0 and any unqualified general default-on.

## Q3 — phases
0 declare scope in CODE (truth table enforced). 1 separate gates; F3 READY=1 (+stale text), F1/F4 stay 0, f4_gate=0, keep ICLUS refusal. 2 audit APPLY paths (manifest immutable snapshot, lineage, RELMARK validation, changecount, missing/corrupt evidence, alloc/IO failure) — no live release-time predicate, no widening on missing evidence. 3 mount-time enforcement table (errno+message; no replay/admission/modification on refusal; policy params load-time/read-only preferred). 4 production-default fleet board: no harness enforce args, only tcp=1 declaration; board prints effective values + domain per mount (closes sess404 item 3). 5 rerun coverage with defaults: >=10 consecutive node_death_replay laps, >=10 unlinker_death laps, all 9 rman arms, F2 matrix, vergate matrix, whole-cluster restart 32/32, negative tests for every refusal row; any failure resets the streak. 6 stable-media campaign before allowing fua_disable=0.
Stop-ship for the scoped flip: missing production-default board; not refusing (1,0); silent fallback/runtime disable; permissive APPLY from missing/stale/mutated evidence; ICLUS/mixed reaching RW; failed default-rerun; F4 race forging a RELMARK.
