<!-- sess474 RULE-5 ruling: D-0533/0534/0535 NO-GO until a closing lap shows chk 0 (D-0133 sb counters block); D-0532 = option (a) xfs_iunlock_nodlm per I… -->
# sess474 GPT ruling (2026-09-02 ~17:50Z)

## 1. D-0533/0534/0535 closure: NO-GO on chain 115 s473c
Cause-exercising evidence accepted (988 adoptions/0 stranger; BLK-REFRESH both roles; P-RESET-STALE-AF=19 zapped, no verifier failure) but the frozen acceptance line said `chk 0` and chk returned 2 sb-counter errors (D-0133). A separately-ledgered defect does not turn a failed criterion into a pass. D-0535 does NOT need the generic reproducer (item 4 corroborative) — its VACUOUS results stay recorded. Closing lap must show: selftest PASS, all reuse laps, adoption/refresh/zap exercised with zero anomalies, cc PASS, clean fleet unmount, **post-unmount chk zero errors incl. inobt totals == sb icount/ifree**, no corruption/shutdown/internal error/warning. => D-0133 must be fixed (or legitimately prevented) first; never filtered/waived.

## 2. D-0532: GO option (a)
Pair every ILOCK-only xfs_ilock_nowait success (and every exit) with xfs_iunlock_nodlm; complete call-site audit + a mechanical check so future ILOCK-nowait users cannot call plain xfs_iunlock unnoticed. Mixed IOLOCK+ILOCK acquisitions: split the unlock or add a helper that skips only the unbegun ILOCK component. Rejected: (c) tolerate holders==0 (hides the holders>0 stolen-count hazard); (b) per-inode/per-task flag (racy/nesting). Verification: every site reached (caller probe/coverage counters); nowait-ILOCK acquisitions == nodlm unlocks; no such acquisition issues ilock_end; begin/end balance for real DLM acquisitions; P71-UNDERFLOW=0 incl. the delayed post-free window; a directed concurrency arm (one genuine DLM holder + another task doing nowait/unlock with a BAST pending: holder count not decremented, no DEMOTING/drain/unlock under the live holder, release only at the genuine final-holder transition, BAST then drains); 20 create->rm->create laps + filler-free arm without shutdown/warning/lock leak/stuck BAST.

## 3. D-0133 instrumentation: CONDITIONAL
P-lines at xfs_log_sb alone are NO-GO (per-node slices => LSNs not globally ordered; logging != final durable image; cached SB buffer != LUN). Quiesce alloc/free, controlled recorded unmount order, and at every SB-counter sync emit: P-SB-SYNC-PRE (node, reason=cover/log_sb/unmount, global ordinal, tid + slice LSN, local m_sb icount/ifree/fdblocks, cached SB-buffer values, UNCACHED durable on-disk values), P-SB-SYNC-WRITE (exact counters logged into the whole-sector image, write status), P-SB-SYNC-POST (uncached durable values after the sync completes), P-UNMOUNT-ORDER (node, begin/end, ordinal, result); finish with inobt totals + chk. Decisive signature: a node whose PRE shows a newer durable image while its local m_sb supplies older counters, then POST shows the sector regressed.
