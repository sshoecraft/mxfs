<!-- sess462 RULE-5 ruling on item-5 increment 2: durable obligation list in the rman slot zone ACCEPTED; inc2 = plumbing only, terminal disposition stays… -->
# sess462 GPT ruling — D-FOREIGN-SLICE-INTENTS-ABANDONED item 5 increment 2

Consult input: F1-F8 tree facts (census on the shadow xlog, 40-byte pad in mxfs_recov_body, ~60 KiB unused per rman slot between the 4 KiB header and the 64 KiB entry area, ladder order in recovery_complete2, selective purge primitive, mount-barrier no-txn variant, dbg_efd_hold_ms reproducer, peer CAW-poll waits) and proposal P1-P6 (36-byte record in pad; RECOVER extent list capped 3072 in the rman slot zone at 4 KiB, written+flushed before the IMAGES_REPLAYED CAS; verdict grows with the list; GRANTS_RELEASED/complete2 gates; mount barrier publishes only; no peer import).

## Rulings
- Q1 P2 ACCEPTED (durable list beats census-only re-replay on takeover) IF: the rman slot is formally partitioned (fence prover owns header+entries; recovery owner owns [4 KiB,64 KiB); the manifest crc/seal must not cover the list zone; header growth into it forbidden); the list becomes IMMUTABLE once referenced by IMAGES_REPLAYED (successor only validates+consumes; before that a new valid owner may overwrite an orphaned list, after fencing/abandonment excludes the displaced publisher's late I/O); header carries fs/LUN id + hb slot, victim node+incarnation, slice id+seal+generation, STABLE recovery-case generation (recovery_gen), publication seq/nonce, publisher node/term (diagnostic only), format version/entry size/byte len/count, canonical-list crc, census/slice digest, header crc. The descriptor record binds recovery_gen + publication seq + count + list crc + census digest + mask/fswide; the spare 4 bytes of the 40 = publication_seq u32. Canonical form: fixed endianness, no padding, exact sort, fsblock/agno/agbno/len consistency, cross-AG => split canonically or quarantine. slice_digest must be the FULL defined census digest (no silent truncation).
- Q2 publishing IMAGES_REPLAYED before completion is VALID, BUT IMAGES_REPLAYED must attest replayed metadata HOME-WRITTEN + device-flushed BEFORE the milestone CAS (submit replay buffers, wait, write list, flush, then CAS; the CAS itself must be FUA/flushed). Today's post-purge flush is TOO LATE for the no-re-replay-after-takeover rule. The milestone proves only the ORIGINAL victim's images; a dead recovery OWNER's own slice still needs replay+home-write before AG transfer (earlier ruling).
- Q3 inc2 MUST NOT flip the disposition: a real open-EFI victim stays TERMINAL QUARANTINE until real completion + takeover + prior-owner replay + SPARSE quarantine + durable completion proof + OBLIGATIONS_DONE authorization land in ONE releasable change set (inc3+4). inc2 may land: list construction/canonicalization, serializer/parser, header+descriptor formats, validation + corruption-injection tests, chk_mxfs, home-write/flush ordering, state-machine gates; it may write the list on the rig BEFORE the terminal outcome only if marked TERMINAL/EVIDENCE-ONLY (flag) so it never blocks the refusal/selective-purge path. OBLIGATIONS_DONE must be IMPOSSIBLE to reach without completion evidence (not merely un-called).
- Q4 confirmed: list-without-record = unpublished garbage; successor ignores, re-replays, rewrites; entries first, checksummed header last, flush; stable case identity (recovery_gen + pub seq) survives takeover, owner term is NOT the key.
- Q5 hazards: (1) OBLIGATIONS_DONE hard guard needs durable proof (every extent EMPTY/FULL, none SPARSE, log-forced, home-written, flushed, same victim/slice gen/pub id/count/mask/digest); (2) at/after IMAGES_REPLAYED: valid count=0 => none; valid count>0 => complete before purge; missing/malformed/mismatched => quarantine/hold, NEVER fall through to the old full purge; explicit policy for old descriptors at IMAGES_REPLAYED without a record; (3) >64 AGs: F_FSWIDE for obligations or quarantine, never truncate; mask recomputed from the list must match; (4) reserve the list subregion in the on-disk format now; fence cannot overwrite an active list; no slot reuse before zero; (5) deep copy (kvmalloc) before shadow teardown, free on every path; (6) mixed RECOVER+QUARANTINE => terminal, domain q_mask|obl_mask, selective purge of out-of-closure only; (7) do not promise short completion latency (3072 txns, takeover) — peers' waits are retryable, unbounded by correctness; (8) exact canonical validation before the digest counts as SS6 evidence.

## STOP-SHIP (ranked)
1 IMAGES_REPLAYED durable before home-write+flush. 2 OBLIGATIONS_DONE reachable without proof. 3 disposition flip without completion/takeover. 4 invalid evidence falls through as count 0 / old purge. 5 descriptor lacks stable case + publication identity / truncated digest. 6 no formal rman region ownership + crc separation. 7 takeover races a displaced publisher's late writes. 8 mask truncation / FSWIDE mishandled. 9 evidence-only list blocks the refusal path. 10 canonicalization underspecified.
