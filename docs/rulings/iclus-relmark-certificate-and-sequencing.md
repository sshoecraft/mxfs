<!-- sess448 RULE-5 ruling: ICLUS clean-release certificate (ic->auth_lineage + RELMARK before iclus unlock CAS) approved, land now READY=0, lab compile-t… -->
# sess448 GPT ruling — ICLUS certificate, #1/#2 sequencing

1. Shape SOUND if: marker identity == token identity. class=INODE,resource=cluster_base acceptable ONLY with a written non-alias proof (routed token cannot alias a per-inode tenure whose ino==base; full tuple {class,resource,lineage,grant_epoch,owner_slot,owner_epoch} unique; lineages not independently scoped per DLM namespace; replay never weakens the lookup). Else a distinct ICLUSTER class, changed on BOTH token capture and marker atomically. ic->auth_lineage installed/cleared/snapshotted together with auth_epoch; zero lineage stays fail-closed.
   One marker per cluster tenure is OK: it certifies closure of ONE tenure. Requires the closing barrier (CLOSING/IRREVOCABLE: no new covered write admission, no same-tenure installs) BEFORE the final proof, drain covered users/publishers, keyed icwr proof for the exact tuple, failed proof -> defer/WEDGE (no mark, no unlock), sync RELMARK, then CAS; never reopen the marked/possibly-marked tuple even on ambiguous publish/unlock. Publish failure proceeds counted (replay refuses) but must never be recorded as marked.
2. Sequencing: LAND NOW in parallel with phase-5, production READY=0; verify with a compile-time lab enable (NOT a runtime bypass); drop the validator icluster arm in a LATER reviewed change. Dropping the arm != default-ON (#2 still needs soak, publisher-coverage GPT items, PROTO_GEN bump).
3. Irrevocability: refuse same-tuple reinstall on the ic, mxfs_iclus_lock fast-path admit, CAS-established path, covered-inode authority install {ICLUSTER, base, lineage, epoch}, and re-admission of an already-covered inode after the barrier. Key on the full tuple where practical. A new grant only via a new validated tuple through normal CAS/lineage validation; a failed/stale install must not clear the closing state.
4. F3 INODE-class unproved CAS = D-0516 prerequisite (fua_disable=0 only); READY stays 0; coherence-only domain not blocked.
5. F4 GEN-OPEN-NOT-DIRTY: registry-accounting debt, not a coherence defect; not evidence F4 is exact; acceptable residue unless it lets the ICLUS close proof pass with a real obligation open.
6. Evidence before dropping the arm: static (atomic epoch+lineage, immutable tuple, non-alias proof, full 16 KB publisher coverage, every failed exit defers, fast+CAS irrevocability, no covered write after CLOSING, sync marker before unlock, failure/ambiguity fail-closed); rig positive (clean-release routed tenures -> exact markers -> REDUNDANT_CLEAN; held-at-death -> APPLY; no cross-cluster/epoch/lineage/owner false redundancy; divergence zero under openunlink + stale-passenger workloads); fault injection at 9 points (before barrier, after barrier/before proof, during proof, after proof/before marker, during marker, after marker IO/before commit ack, after commit/before CAS, CAS failure, death after marker/before teardown) with: no unproved unlock, no old-tuple reinstall, absent marker -> refuse/APPLY never false REDUNDANT, same-epoch installs rejected, new-epoch grant possible; counters zero outside injection; mixed-version fail-closed; PROTO_GEN bump before activation.
7. STOP-SHIP (icluster_dlm=1 production): tokens lack lineage/mixed tuple; INODE+base alias without invariant; writes after proof begins; any proof-failure path reaches unlock; fast/CAS reinstall of marked tuple; marker not durably before unlock; weakened replay match; laps not REDUNDANT_CLEAN/APPLY as required; cross-tenure false certification; runtime bypass needed; mixed-version activation possible.
