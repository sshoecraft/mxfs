<!-- sess447 RULE-5 ruling D-0514: H1 (inline tail sweep inside the single foreign-replay work_struct blocks on victim B's grant; B's replay queued behind… -->
# GPT ruling D-0514 (sess447)

Evidence brought: chain 49 lap 1 trail (test1: RECOVERY-COMPLETE slot 29 12:27:23.907 → P97-SWEEP-START slot=29 same ms, no SWEEP-DONE; slot 31 detected .743 s later, elected, silence; test1 could not release mxfs at prep). Lap 2 PASS: both replays before any sweep; sweeps 13 ms / 0 ms.

Ruling:
1. H1 ranked substantially above alternatives (queue_work-returning-false is not it: a running non-pending work can be requeued but the new instance cannot start while the callback is blocked; the source comment "running instance picks the new slot up" is not a sufficient invariant once the dead-slot pass has finished). Absent SWEEP-DONE matters as much as absent lease.
2. Fix = SUBTRACTIVE: replay work fn does claim/replay/publish/purge/mark sweep_pending/kick reap worker/return; NEVER inline maintenance that can wait on grants whose recovery is serialized through the same callback. Lap 2 shows no invariant requires A's sweep before B's replay. Ensure sweep_pending + reap kick cannot lose a wakeup (running reap worker must recheck before exit or mod_delayed_work).
3. Hazard after the fix: separate work items remove the work_struct serialization but NOT a local-lock cycle (sweep holding ILOCK/AG lock while waiting for B's purge, if B's replay/purge needs that lock). Follow-up hardening: dead-generation counter incremented on every new dead-slot notify; sweep-context DLM waiters capture the gen and return -EAGAIN when it changes (unwind locks, keep sweep_pending, retry) — checks before each AG, each inode, before/after each DLM wait; requires the sweep to be restart-idempotent (defer_reap_add dupes, adopted entries, inodegc flush).
4. Audit every other blocking op inside the replay work item (completion publication / purge handoff / lease release / reap_sched must not flush_work) and the replay path's own DLM acquisitions for the same "waits on a newer undetected victim" shape.
5. Instrumentation to prove in one run: notify line (qret, work_busy, inv), per-instance ENTER/PHASE/EXIT, sweep DLM-wait provenance (resource, holder slot, holder state), purge lifecycle, stacks; gated hold knob (park sweep until B's dead bit appears) is more deterministic than a fixed sleep.
Acceptance: before fix — sweep waits on B's resource, B notify queued, no new instance; after fix — reap worker's sweep waits, replay instance enters concurrently, B replays+purges, sweep completes; no lost sweep_pending bit; unmount does not wait 120 s.
