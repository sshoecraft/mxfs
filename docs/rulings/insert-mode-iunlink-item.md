<!-- sess396 RULE-5 ruling: non-empty-bucket fossil kill (F1 reset + buffer fossil -> precommit mismatch) => INSERT-mode iunlink item applied at sorted pr… -->
# sess396 RULE-5 ruling — INSERT-mode iunlink item (GPT, 2026-08-22)

## The kill it answers
0.23.5 lap 5 test25: `P-IUNL-FOSSIL-ENTRY ... fossil_next=0x9dc head=0x163b fix=1 — reset` (NON-empty bucket) then
`P53-IUNLINK-MISMATCH old_ptr=0x9dc old_agino=0xffffffff next_agino=0x163b` (buffer dinode still 0x9dc, nlink=1) → `xfs_iunlink_item_precommit` -EFSCORRUPTED → `Corruption of in-memory data (0x8) at __xfs_trans_commit (xfs_trans.c:917)` → shutdown/withdraw. F1 alone made the non-empty case worse (before F1, old_agino==buffer==fossil and the apply overwrote it).

## Ruling
- Choose (B): a dedicated INSERT operation on the iunlink item (explicit flag/opcode, NOT inferred from old_agino==NULL). Every insert creates an INSERT item, including NULL->NULL for an empty bucket. At sorted precommit: keep buffer/identity/CRC/next validation, require old_agino==NULLAGINO for INSERT, do NOT require buffer==old; if the buffer pointer is non-NULL emit P-IUNL-PRECOMMIT-INSERT-FOSSIL, write next_agino, log the buffer. Strict equality stays for every non-INSERT item. F1 (core reset+probe on entry) stays. F2 (early buffer lock in the empty branch) is REMOVED.
- The early dirty cluster-buffer lock before precommit (F2, option A, and the sess48 P-CREATE-NUFIX in the create transaction) is a REAL ABBA: a buffer acquired before the sorted precommit is an unordered prefix; T1 holds A dirty and later transiently needs C at its inode-item precommit, T3's sorted iunlink precommit holds C dirty and waits for A. P-CREATE-NUFIX must be audited/converted to the same sorted-precommit mechanism (weeks without a detected hang are not proof).
- INSERT mode only where non-membership is PROVEN (droplink fresh nlink->0, O_TMPFILE/EEXIST-loser, orphan scan only after its per-slot membership walk); AG EX alone does not prove it.
- Log recovery needs nothing new (buffer+AGI changes are in the same journal txn), but crash/replay must be tested. The mount-scope iunl store written at precommit is "precommit-applied", not committed (pre-existing apply-path semantics; entries should be pending/removed on cancel or described honestly).
- Verification: three arms (pre-F1, F1+F2 reproducing the mismatch, INSERT item), full matrix for empty AND non-empty buckets: core==buffer fossil; core fossil/buffer NULL; core NULL/buffer fossil; core fossil != buffer fossil; buffer already == new head; clean NULL/NULL. Plus cold-cache/remount on-disk check, crash injection (pre-precommit / post-apply-pre-commit / post-commit+recovery), negative test that a non-INSERT mismatch still -EFSCORRUPTED, multiple INSERT items in one txn (same cluster + reverse cluster order), opposing-cluster-order deadlock stress, trans-cancel + log-reservation pressure, probe counts (every planted fossil reported, none survives), then natural 32-node rename-over-existing laps.
