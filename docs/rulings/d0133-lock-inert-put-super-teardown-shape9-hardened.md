<!-- sess475 RULE-5 ruling: 0.64.28 A+ lock INERT (rc=-19 96/96: put_super tears DLM down before xfs_unmountfs); shape (9) early locked cover GO only hard… -->
# sess475 GPT ruling (2026-09-02 ~18:50Z) — D-0133 fix placement

## Measured fact that triggered it
0.64.28 chain 116 s474e (3 laps x 32): P-SB-SUMMARY-LOCK rc=-19 on every node (96/96). mxfs_sb_summary_lock() returns -ENODEV because xfs_fs_put_super (pal/linux/xfs_super.c ~1883-1999) does iclus purge, AG force-release, `mp->m_mxfs_dlm = NULL` (1954) and mxfs_v5_dlm_shutdown_defer_release (1981) BEFORE xfs_unmountfs (2015) -> xfs_log_clean -> xfs_log_quiesce, where the locked recount lives. Laps still passed (chk 0, 0-byte diff outside counters/crc/lsn) thanks to the uncached recount alone — benign ordering, not serialization.

## Rulings
1. Shape (9) = move the locked section early in put_super (DLM alive) — NO-GO as proposed, GO only with: (a) EVERY potentially log-producing work/cleanup cancelled synchronously BEFORE the locked cover (cancel_work_sync after the cover is suspect: the work may log before it returns); (b) a "sealed" phase after the lock release with assertions/probes at xfs_trans_alloc, every xfs_sync_sb site, SB-sector buffer submission, cover-state change, work using m_log; (c) before destroying the DLM verify !need_covered, zero trans allocs, zero SB writes, no runnable work; (d) later xfs_log_quiesce takes the guarded no-op path only under that invariant. Cleaner: split xfs_unmountfs into logging-capable prep (DLM alive) -> locked final cover -> strictly non-logging tail.
   NO-GO signals: any P-SB-LATE-DIRTY-COVER; any trans alloc / second xfs_sync_sb / SB submission after seal; a late-dirty path that still does an unlocked SB write; work capable of logging cancelled after the cover.
2. "Uncached re-read right before an unavoidable late unlocked write" is NOT acceptable (read/write race undoes B's serialized result). Late-dirty after the DLM is gone => refuse the clean departure (mark XFS_SICK_FS_COUNTERS / withhold unmount record per MXFS recovery semantics), loud report, recovery required. Inject it and prove no clean departure is recorded.
3. Runtime xfs_log_worker cover (xfs_sync_sb with private counters every 30 s, unlocked) is not terminally relevant for the all-nodes-clean-unmount case (each node's LAST write is its locked one after its own worker cancel) but it means the lock is NOT exclusive vs all SB writers (freeze/remount-ro/mixed-departure/crash cases weaker). Do not describe the lock as exclusive; open a FOLLOW-UP DEFECT; long-term all clustered whole-sector SB writers use one protocol (take the lock through home write + flush, or stop publishing private counters at all).
4. Verification additions for closure: (a) adversarial waiter arm (A paused inside the critical section via inject at several points; B's unmount must block — no LOCK/PRE/WRITE/POST until A releases; heartbeat stays live, no fencing; B terminal, values == AG totals; both orderings); (b) late-dirty injection (non-covering log write after the locked cover -> must refuse clean unmount or reacquire; never an unlocked clean write); (c) no-producer instrumentation counts must be zero between unlock and xfs_log_clean completion (unmount record excepted, must not cause another SB sync); (d) lock ORDERING evidence: NOT a test ordinal in the SB — use a monotonically increasing grant/generation number in the CAW lock record (or a DLM trace seq at acquisition) printed with LOCK/WRITE/POST/UNLOCK; the terminal SB writer must be the highest completed grant; per-node timestamps cannot prove non-overlap; (e) runtime-worker interference arm (peer's worker covers while another node is paused in the critical section; document/prove); (f) holder-failure arm (kill/fence the holder at several points: lock recovered, next node gets it only after recovery, fresh uncached recount, final SB == AG headers, no clean departure for the victim); (g) final-write proof: no SB-sector write after the highest-grant writer's flush/POST (buftarg submission trace or blktrace stronger than P-lines).
