<!-- sess463 design-consult ruling on item-5 inc3+4 (EFI completion engine + takeover): core EMPTY/FULL/SPARSE scheme sound; disposition flip STOP-SHIP on 10 rank… -->
# sess463 GPT ruling — D-FOREIGN-SLICE-INTENTS-ABANDONED item 5 increments 3+4

Consult input: tree facts F1-F12 (ladder re-entrant from IMAGES_REPLAYED; inc2 gates; pag_dlm_* local hold state + grant token; CAW slot fields + caw_grant_epoch_update; 120 s wait with liveness extension; purge primitives; verdict/census export; HOMEFLUSH; F9 drain pipeline; rman zone [56,64 KiB) free; xfs_alloc_has_records/__xfs_free_extent; one reap worker) and proposal P1-P14 (verdict flip; phase-A publication IMAGES_REPLAYED+record; freeze-through-purge by scanning 64 sectors; transfer CAS; recov_excl per task; one-extent txns; home-write; proof block at 56 KiB; release; takeover; mount barrier; peer waits; chk/forge; tests).

## Verdict: core completion algorithm sound; disposition flip STOP-SHIP as submitted.

## Ranked STOP-SHIPs
- SS-A Linearizable freeze publication vs EVERY purge: P3 scan alone races (scan sees no OPEN -> publication -> purge strips from stale snapshot -> case sees no holder -> false FREEZE_LOST). Need a CLUSTER-WIDE publication/purge guard (a CAW/DLM resource, not a node-local mutex): OPEN publication holds it across final validation + list durability + the IMAGES_REPLAYED CAS; every purge (purge_node, purge_dead_nodes[_ex], selective, terminal-refusal, mount/bootstrap, new joiner) holds it across the full 64-sector scan + validation + every CAW mutation. Scan fail-closed: unreadable/corrupt sector that could be OPEN => no victim-bit mutation at all (held retry or FSWIDE per matrix); never invalid->empty mask.
- SS-B Continuous freeze across overlapping OPEN cases: two cases covering AG X — A completes and unlocks at OBLIGATIONS_DONE while B still OPEN => unowned interval. Need an AG freeze-CUSTODIAN protocol: an AG covered by any OPEN case always has one valid EX custodian; unlock only if no other OPEN case covers it, else keep as recovery-exclusive or transfer directly to the next case; local exclusion names WHICH case may transact; GRANTS_RELEASED of one case never implies another's freeze released. Needs durable per-AG OPEN accounting or serialized processing under the guard.
- SS-C Generation-bound transfer (SS4 still unmet): bind victim slot+incarnation, fs/recovery gen, lease owner+incarnation+term, descriptor stage+generation, pub_seq, agno in mask, expected holder slot+incarnation, expected CAW generation/EX epoch/lineage, and (non-victim holder) durable proof it is dead/GUARD with slice replayed+home-written. Checked inside a GUARDED transfer op, not only v5-side before an unqualified CAS. Persist per-AG TRANSFER RECEIPTS {case id, agno, old holder/inc/gen/epoch/lineage, new ..., lease term, desc gen, seq, crc}; proof may carry a digest of them. Initial expected ownership from the sealed fence-time manifest.
- SS-D Canonical transfer image: holders_ex == BIT(from) exactly, all other holder maps 0, granted_mode EX, our slot in no holder/waiter map. Clear from_slot from waiters/waiters_ex; keep other waiters; keep sticky revoke; clear/recompute yield_to if == from; apply new-tenure rules to ex_grant_streak; caw_grant_epoch_update (fresh epoch, dir_epoch, last_ex_slot); preserve lineage; generation++ with overflow handling; token from the CAS-WRITTEN image. Idempotent 'our EX present' only if sole canonical EX + our incarnation + matches a receipt + lease/desc still authorize; unexplained our-bit => terminal/held, never reaffirm. Verdicts: dead holder not replayed => DEFER dependency; holder changed with valid receipt => follow chain; no holder / live unrelated / mixed-malformed => terminal FREEZE_LOST/evidence corruption; CAS mismatch => retry only after revalidating lease+descriptor+record+holder chain.
- SS-E Real local exclusivity: current==recov_task is NOT enough (same task may enter reclaim/allocator). Use a recovery operation CREDENTIAL {case id, agno, transfer epoch/lineage/gen, op=obligation completion} passed via a recovery-only free path into the nested AG-lock check; ordinary calls by the same task blocked. Install sequence: under acquire_lock+pag_dlm_lock enter RECOVERY_INSTALLING (blocks fresh/cached/nested), require holders==0, no cached tenure, no transition in flight; then the guarded CAW transfer; then publish token -> RECOVERY_EXCLUSIVE; on failure unwind + wake. Never overwrite holders=1 over nonzero. BAST: INSTALLING/EXCLUSIVE both 'held'; revoke sticky; completion never caches; explicit release runs full F9; failed drain keeps bit + BAST pending; pending BAST serviced before ordinary re-adoption; wake waiters only after release/demote-safe.
- SS-F OBLIGATIONS_DONE cleanup: completer dies after DONE CAS before unlock => successor's purge_node(victim) leaves the completer's adopted bit => GRANTS_RELEASED with a residual EX bit. Required at DONE: live caller owning the grant => P9 idempotently; dead/GUARD adopted holder => replay+home-write its slice first, then generation-bound remove/transfer-and-release; live previous completer => it releases under lease protocol or is fenced first; before GRANTS_RELEASED verify no residual case holder/receipt needs cleanup (subject to other OPEN custodians).
- SS-G Proof binds authority/transfer chain/durability: proof block at [56,60 KiB) adds format length, fs UUID, rman slot, version/endianness, descriptor generation at authorization, lease gen/term, receipts digest, list header+entries digest, explicit committed/uncommitted state, crc over the WHOLE block incl. bitmap; invalidate/sequence-separate old proofs before a new OPEN record in a reused slot; two-phase durable write (body+flush, committed header+flush, read back). advance_obl_done verifies lease still ours at gen, desc gen+stage exact, record OPEN valid unchanged, no terminal transition won, every AG has a valid receipt with dependencies satisfied, every outcome bit <-> exactly one canonical entry, counts agree, home-write authorization produced after successful P7 per AG; CAS atomically updates stage + F_DONE + record crc.
- SS-H Explicit zero census: bare 'no record = zero obligations' rejected. Add a crc-protected descriptor discriminator F_CENSUS_ZERO set in the IMAGES_REPLAYED CAS; states: F_CENSUS_ZERO + empty record area = none; valid OPEN record = pending; valid TERMINAL = terminal; neither = invalid/legacy unless a strict version rule proves pre-obligation build; corrupt bytes never zero.
- SS-I Dependency scheduler: one worker => 'defer and retry A' can starve B (A waits on dead holder B's replay). Requeue A, process B first, or explicit dependency DAG with cycle detection; dependency satisfied only by durable replay/home-write evidence for the EXACT holder incarnation.
- SS-J Complete error/release matrix: read EIO, bnobt corruption, trans alloc fail, commit fail, shutdown, AGFL/reservation failure — transient => retain+retry; corruption => terminal/FSWIDE; never FULL/count-0 fall-through; failed proof/terminal publish/home-write/drain retains the freeze.

## Other rulings
- Owner info: ANY_OWNER only because format gated to !rmapbt; add a hard feature check (RMAPBT + no owner info => refuse/quarantine). tr_itruncate only after verifying worst-case (AGF+bnobt splits+AGFL refill+busy) or use the EFI recovery reservation if larger; assert/fault-test max split depth.
- AGFL refill inside adopted AG OK with the credential, adopted epoch captured, no defer/roll; cross-AG acquisition = protocol error.
- Home-write before release must PROVE completion (bounded AIL push that cannot prove completion => do not unlock).
- SPARSE sequence: cancel txn; durably publish TERMINAL with full domain; P240 sees it on all paths; full drain; unlock only after terminal durable+visible; failure => retain adopted bits. Partial completion before SPARSE acceptable.
- P12: 120 s -ETIMEDOUT is the derived-budget rule (liveness) provided timeout never strips/steals/bypasses and no allocator fallback without the grant. Do NOT lie through holders_alive_fn; add a distinct OBLIGATION_PROTECTED wait predicate that only extends waits.
- Mount barrier (P11) acceptable only after the publication/purge interlock exists; new joiner obeys the OPEN scan before any purge; OPEN AGs blocked until a transactional worker installs custodian state.
- Takeover crash edges to cover: after transfer CAS before install; after install before first extent; after proof body before commit; after proof commit before DONE; after DONE before each unlock; lease loss mid-txn; slot reuse between receipts and takeover; two OPEN cases sharing an AG; overlapping/duplicate extents across cases; alive-but-fenced prior completer; mount proceeds while post-mount worker cannot install; terminal racing proof authorization.
- Same change set must also include: receipt format+validators; proof versioning + zero-census bit; guard resource + lock-order doc; multi-case custodian; INSTALLING + credential plumbing; idempotent DONE cleanup; dependency scheduling; RMAPBT feature gate; chk_mxfs checks (OPEN masks vs custodians, receipt chains vs CAW generations, DONE proof/list/receipt digests, DONE/RELEASED with residual holders, zero-census canonical form); forge cases (stale proof, stale receipt, slot reincarnation, malformed sole-EX image, absent record w/o zero-census, overlapping OPEN masks).
- Mandatory extra tests: OPEN publication racing each purge path; new-joiner purge racing publication; two OPEN cases one AG; kill after transfer CAS before install; kill right after DONE; dead completer holding adopted bits; live fenced completer + lease takeover; dependency A->B + forged cycle; BAST/revoke during completion; recovery task entering ordinary allocator must block/fail; corrupt proof with matching counts; stale proof from prior pub_seq/incarnation; CAW generation ABA + unexplained our-EX bit; AIL/drain timeout proves no unlock.
