<!-- sess455 RULE-5 impl review of the 0.61.0 departure I/O gate (G3 as first landed): STOP-SHIP, 10 required changes — uncapped wait for real tokens, ret… -->
# sess455 GPT ruling on the G3 departure-gate IMPLEMENTATION (2026-09-02 00:40Z): STOP-SHIP

Reviewed: the code as landed 00:30Z (acct struct, token_take/retire/buf_free, freeze_drain 2000 ms bounded, put_super reorder, unwind, synthetic late-token injector). Findings and the disposition taken in the rework (same session):

1. Drain TIMEOUT then teardown continues → a real admitted completion touches bp/mp/m_buf_workqueue after they are freed (acct refcount pins only the acct). REWORK: real tokens are waited for UNCAPPED (P304-RETIRE-DRAIN-STALL every 2 s — a token outstanding after xfs_unmountfs returned is itself a D4 accounting violation, upstream's xfs_buftarg_drain is likewise uncapped); only `corrupt` abandons after one bounded round (DIRTY).
2. Retire BEFORE the error/retry disposition → zero exposed while a resubmit is about to run. REWORK: retire at the TERMINAL points only (end of __xfs_buf_ioend for every completion that gets there; the transient "caller schedules retries" exit in xfs_buf_ioend_handle_error); the `resubmit:` label sets b_mxfs_io_carry so the retry keeps the token (no retire/re-take).
3. A rejected (post-freeze) generation completes through the shared ioend and would retire an admitted generation's token (concurrent submissions on one buffer DO exist in this tree — the sync-credit machinery). REWORK: rejected generations are counted separately (b_mxfs_io_rejected / acct->rejected_pending) and discounted first at retire; aggregate is exact: inflight reaches 0 only after every admitted generation completed.
4. Rejected completions may route async to m_buf_workqueue and are not covered by the drain → REWORK: the drain also waits rejected_pending == 0.
5. Orphan (xfs_buf_free with tokens) dropped the acct ref → REWORK: keep the reference (acct deliberately leaked, never freed with tokens recorded), corrupt, P304-IOCNT-ORPHAN.
6. Account-level underflow unchecked → REWORK: check acct->inflight > 0 (and rejected_pending) before every decrement; corrupt on inconsistency.
7. Q1: xfs_destroy_mount_workqueues under the host-wide departure mutex is a deadlock shape if any worker issues a PR OUT → REWORK: take the departure lock only AFTER xfs_shutdown_devices, around the assertion + release + late unregister (the flush/freeze/drain need no lock; a same-boot remount cannot start before put_super returns — the sb/bdev holder is still held).
8. Singleton `mxfs_depart_late_pending` + exit flush-after-unlock UAF → REWORK: list under a spinlock; exit walks with cancel_delayed_work_sync and runs the retire inline for cancelled items; schedule failure unwinds the token.
9. Synthetic-token test relabelled honestly: with an uncapped real wait the injector proves the drain WAITS (umount wall ≈ N, STALL lines, then QUIESCED + clean release) — arm `latewait`; it is not a post-teardown late-I/O test (which cannot exist: nothing is torn down while a token is outstanding). GPT's requested "real xfs_buf late completion" is structurally impossible on a healthy unmount (every buffer in flight holds b_hold; xfs_buftarg_drain waits) — state this in review #5.
10. Post-CAS flush failure: the CAS may be visible; wording changed to "release durability uncertain; key retained so peers expire the record and fence" (the existing disposition already is that).

Q2 answer: a bounded wait is defensible only as a quarantine trigger; uncapped is safer than continuing. Q4: overflow (255) must reject + corrupt, never saturate-and-admit. Q6: log iclogs / raw flushes / discards audited separately (docs "D4 audit statement").
