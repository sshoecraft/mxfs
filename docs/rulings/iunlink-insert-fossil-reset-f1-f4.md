<!-- sess395 RULE-5 ruling: insert-path fossil i_next_unlinked (empty-bucket case) — F1 reset+F2 buffer NUFIX are the root fix, F3 ingress sanitize only a… -->
# sess395 RULE-5 ruling — fossil in-core i_next_unlinked at xfs_iunlink_insert_inode (D-AGI-UNLINKED-CROSSNODE-RECOVERY-SHUTDOWN, lap-3 test10 AG10 bucket 10, 0.23.1)

## Evidence brought (test10 = slot 10, bucket 10 = its own bucket)
- 14:47:17.796 P82-REM 0x133 head=0x133 next=NULL at_head=1 (freed same txn) → bucket empty.
- .8204-.8256 P70-BP release of inode DLM on 0x134 to a peer, .833 P291-EXWIN EX re-acquire (rsync rename-over) → ADD(0x134) with head NULL.
- .861 P82-REM 0x134 head=0x134 **next=0x133** at_head=1 → bucket head repointed at a FREED (then re-allocated as live "file20", nlink=1) inode. No AGI re-read / DLM handoff / P-PINNED-REREAD / FOSSIL-RESET / LOGSAME in the window (all 0). Every later REM through 14:47:25 carries next=0x133. 14:48:06+ P86-AGI-UNLINKED-BADHEAD ag=10 bucket=10 agino=0x133 disk_nlink=1 core_nlink=-1. 14:50:10 mkdir EEXIST-loser orphan insert → reload_next 0x133 cached=0 nlink=1 → -117 → dirty cancel → shutdown.
- Conclusion: 0x134 ENTERED the insert with in-core i_next_unlinked==0x133 (prior-life fossil); the sess388 P-IUNL-FOSSIL-RESET fires only when fossil==head, so the empty-bucket path kept it and never logged the dinode.

## Ruling (GPT, 2026-08-22)
- Reading is strong; it proves the pre-state but NOT the ingress (DLM reload suspected) — the probes P-IUNL-FOSSIL-ENTRY / P-IUNL-FOSSIL-INGRESS (0.23.2) must close that.
- **F1 (reset in-core next to NULL on insert entry, any head)**: correct containment. Hazard: it weakens the "already an interior member" detector — keep/assert per-caller proof (fresh nlink→0 under locks; new tmpfile/EEXIST orphan; orphan-scan only after the 64-bucket membership check); log old value+gen+head+caller; ensure no outstanding iunlink item for the inode; document that resetting before xfs_iunlink_log_inode deliberately avoids its ==next check because the equality is fossil state.
- **F2 (empty bucket: clear a non-NULL BUFFER di_next_unlinked with the P-CREATE-NUFIX idiom)**: needed or the next reload re-imports the fossil. Lock order AGI→cluster buffer matches iunlink precommit (agree). Verify: txn reservations cover the extra buffer log for all 4 callers; acquire/validate buffer BEFORE modifying the AGI head; gen/incarnation check that the buffer slot is ip's incarnation (passenger-slot hazard); use the exact NUFIX helper (factor one); keep the iunl-store coherent (supersede a same-gen record). Do NOT route it through a NULL→NULL iunlink item (no-op; its third-value precommit check would reject the fossil it must repair). No value in logging an unchanged NULL on every empty insert.
- **F3 (ingress: import NULL instead of a fossil on a LINKED image)**: invariant agreed — no legitimate reader of a linked inode's di_next_unlinked (mount recovery/bucket walks only follow nlink==0 members). BUT must decide on EFFECTIVE linkedness after LIVESKEW/iunl-store overlays, or run before the gen-keyed overlay that restores the committed value, or preserve in-core next for a certified current listed inode — otherwise a stale linked platter image over an in-core unlinked member truncates a valid list. Keep the anomaly loud (health flag), it does not repair the medium.
- **F4 (online clear of an own-bucket LINKED not-cached head)**: detection-only. Provenance is not proven online (linked H → valid unlinked U is possible via stale/incomplete observation, takeover/deferred-reap, tmpfile asymmetry); never follow the linked head's on-disk next; repair belongs to a scrub with AG EX, settled recovery, authoritative gen/linkedness, reap/takeover state, reconstruction of all nlink==0 inodes.
- **Verification**: fault-injection modes — (1) core-only fossil at insert entry, bucket empty; (2) core+platter fossil; (3) reload ingress of a linked inode with non-NULL platter next (force cache miss/reload); (4) stale-image inversion (raw linked, same-gen authority says nlink==0 with real next) → must not truncate; (5) non-empty insert with fossil != head → next=head; (6) crash points around dinode-repair/AGI-update/commit + replay + walk; (7) F4 adversarial chain reports but does not clear. Primary closure postconditions: entry sanitizes core, empty insert persists NULL, removal leaves head NULL, next reload imports NULL, no P86/reload corruption/shutdown.

## Deploy trap hit this session
scripts/module_swap_deploy.sh 32 caw on the aged fs failed the last join (test2): slot 10 held a TERMINAL REFUSAL quarantine from test10's 14:50 shutdown → P300-CLAIM-EXHAUSTED (-28): the volume admits 31 (D-QUARANTINED-SLOT-EXHAUSTS-CLUSTER-ADMISSION-376). Evidence harvested to tests/logs/sess395_slot10_refusal_evidence.txt before re-prep.
